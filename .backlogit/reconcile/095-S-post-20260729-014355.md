---
shipment_id: 095-S
mode: post
merge_commit_sha: 315e538bc362cb7be603986e7c24d642a60a3700
generated_at: 2026-07-29T01:43:55-07:00
generated_by: ship (post-merge closure)
recommendation: PROCEED
---

# Shipment Reconcile Report — 095-S (post-mode)

Archive-presence integrity check run immediately after
`backlogit shipment ship 095-S`.

Ship result:
`shipment_status: shipped`, `archived_ids: [104.001-T, 104.002-T, 095-S,
104-F]`, `returned_ids: []`, `commit_sha: 315e538b`.

## Archive Presence (shipment + 3 manifest items)

| Item | Archive File | Git State | Classification |
|---|---|---|---|
| 095-S (shipment) | `.backlogit/archive/095-S.md` present | renamed (`RM` queue→archive) | `matched` |
| 104-F | `.backlogit/archive/104-F.md` present | modified (`M`) | `matched` |
| 104.001-T | `.backlogit/archive/104.001-T.md` present | modified (`M`) | `matched` |
| 104.002-T | `.backlogit/archive/104.002-T.md` present | modified (`M`) | `matched` |

The feature + two task files were already in `.backlogit/archive/` (moved
during the in-PR bookkeeping commit `856f1db0`); the ship operation re-modified
them to record the shipment merge SHA and terminal status. `095-S.md` was
relocated queue → archive by the ship operation.

> **Correction (closure-PR #298 review):** the `backlogit shipment ship`
> metadata rewrite of `104-F.md` also dropped its frontmatter `links:` block
> (`related_to: 015-D`). Backlog Markdown is the relationship source of truth,
> so the link was **restored** in the closure PR to preserve traceability — the
> `104-F.md` modification is therefore status/commit bookkeeping **plus** the
> re-added `015-D` relationship, not bookkeeping-only.

## Deleted-File Guard (P-007)

`git status --short -- .backlogit/archive/` reports **no deletions** — only
modifications (`M`, from ship re-tracking commit metadata) and one rename
(`095-S.md` queue → archive). No `git restore .backlogit/archive/` required.

## Status Transitions (from item logs)

* `104.001-T`, `104.002-T`: done → archived; commit `315e538b` tracked.
* `104-F`: done → **archived** (shipment released); commit `315e538b` tracked.
* `095-S`: active → **shipped** → archived; commit `315e538b` tracked.

## Merge Provenance

* PR **#297** merged via **merge commit** (P-009 satisfied — merge strategy
  verified `allow_merge_commit=true`, squash/rebase `false`).
* Merge SHA `315e538bc362cb7be603986e7c24d642a60a3700`; confirmed ancestor of
  `origin/main` (Merge Confirmation Gate passed, `git merge-base
  --is-ancestor` exit 0). Two parents: `f136612a` (prior main) ∪ `4575ae25`
  (feature head).
* 4-point Copilot merge gate GREEN at merge time: latest review
  `commit_id == 4575ae25 == HEAD`, Copilot off `requested_reviewers`,
  7/7 review threads resolved, `mergeStateStatus == CLEAN`, CI `build`
  SUCCESS (4m33s).

## Follow-ups Stashed (3)

| Stash ID | Kind | Priority | Summary |
|---|---|---|---|
| `0B5AAAD2` | task | medium | Pending-sync drain producer/consumer handoff race (residual snapshot-loop TOCTOU; needs state-machine handoff; pre-existing) |
| `B7F52777` | bug | medium | Generation-scoped pending-sync queue (clear-all wipe can erase a new-generation request in the cancel race window) |
| `A85DC0E3` | task | low | Stage harvest-provenance doc reconciliation (Stage-owned planning artifact in base diff) |

## Gate Decision

All 4 archive files present; no archive deletions; expected queue → archive
relocation only.

**recommendation: PROCEED** — closure may continue.

Single-writer lock on `.backlogit/queue/095-S.md` released after this report.
