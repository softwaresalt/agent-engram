---
artifact_type: shipment-reconciliation
title: "Shipment 124-S Post-Ship Reconciliation"
shipment_id: 124-S
mode: post
expected_status: archived
timestamp: 2026-08-23T21:31:00-07:00
recommendation: CLOSED
---

## Summary

`backlogit shipment ship 124-S` completed successfully against merge commit
`8f9904a0a55516582e101d7b75b9457adaf9a0be`. All 6 manifest members plus the
associated plan-review gate artifact and the shipment artifact itself were
archived. No member was returned blocked.

## Ship Result

```json
{
  "shipment_id": "124-S",
  "shipment_status": "shipped",
  "archived_ids": [
    "130.001-R", "130.001-T", "130.002-T", "130.003-T",
    "130.004-T", "130.005-T", "124-S", "130-F"
  ],
  "returned_ids": [],
  "commit_sha": "8f9904a0a55516582e101d7b75b9457adaf9a0be"
}
```

## Final Artifact States

| Item | Location | `status` | `archived_status` |
|---|---|---|---|
| `124-S` | `.backlogit/archive/124-S.md` | `archived` | `shipped` |
| `130-F` | `.backlogit/archive/130-F.md` | `archived` | `done` |
| `130.001-T` | `.backlogit/archive/130.001-T.md` | `archived` | `done` |
| `130.002-T` | `.backlogit/archive/130.002-T.md` | `archived` | `done` |
| `130.003-T` | `.backlogit/archive/130.003-T.md` | `archived` | `done` |
| `130.004-T` | `.backlogit/archive/130.004-T.md` | `archived` | `done` |
| `130.005-T` | `.backlogit/archive/130.005-T.md` | `archived` | `done` |
| `130.001-R` | `.backlogit/archive/130.001-R-…md` | `archived` | `done` |

`.backlogit/queue/124-S.md` no longer exists; Git records the transition as a
rename into `.backlogit/archive/124-S.md`.

## Returned / Blocked

None. `returned_ids` is empty.

## Operational Closure Evidence

| Check | Result |
|---|---|
| PR #359 state | `MERGED` 2026-08-24T04:23:51Z by `softwaresalt` |
| Merge commit | `8f9904a0a55516582e101d7b75b9457adaf9a0be` |
| Merge strategy | merge commit (2 parents `06813e3b` + `189a90e0`) |
| Squash / rebase | disabled repo-wide (`allow_squash_merge:false`, `allow_rebase_merge:false`) |
| Ancestry in `origin/main` | confirmed (`merge-base --is-ancestor` exit 0) |
| CI at merged tree | `build` pass, `start-launcher-windows` pass |
| Tree equivalence | `git diff 189a90e0 8f9904a0` empty — merged tree is byte-identical to the CI-verified tree, so the green run applies to `main` without re-running |
| Merged code on `main` | `src/shim/preinit_compat.rs` present; `ENGRAM_MCP_PREINIT_COMPAT` resolves in `preinit_compat.rs` and `transport.rs` |

## Scope Isolation

The independent Cozo cold-start defect remains tracked separately as spike
`002-SP` (`status: queued`, priority high, labels `cozo`/`daemon`/
`performance`/`cold-start`). It was **not** folded into 124-S and was not
archived by this closure. Width isolation is intact.

## Primary Checkout Non-Interference

All closure work ran in `.worktrees/post-merge-124-s-closure-20260823` on
branch `post-merge/124-s-closure`. The primary checkout
`C:\Source\GitHub\engram` was never modified, reset, cleaned, stashed,
checked out, or committed; its operator-staged backlog repairs and the
unresolved `UU .backlogit/archive/stash.jsonl` are preserved byte-for-byte.
The only shared-repository operations were `git fetch origin main` (updates
remote refs only) and `git worktree add` (creates a new worktree without
touching the primary working tree).

## Closure Index Resync — WARN

`backlogit sync` was re-run after all archival mutations completed and
**failed**:

```text
Error: build canonical artifact index: 19 artifact files failed to parse:
[.backlogit/archive/029.004-C.md … .backlogit/archive/029.009-T.md,
 .backlogit/queue/030.005-C.md]
```

Status: `CLOSURE_INDEX_SYNC_WARN — proceeding with potentially stale index.`

This is a **pre-existing defect, not caused by this closure**:

* None of the 19 files appear in `git status --short` for this branch, and
  `git diff HEAD` against them is empty — they are byte-identical to the merge
  commit.
* The cause is missing `title:` frontmatter (confirmed on
  `.backlogit/archive/029.004-C.md`), the same defect the Stage session
  recorded for this shipment.
* The repair for exactly these 19 files is already staged in the primary
  checkout by the operator and is outside this shipment's scope. Per the
  isolation constraint, it was not touched.

The earlier pre-ship `backlogit sync` succeeded (`Indexed 1061 artifacts`),
so the shipment-ship gate evaluation itself ran against a good index. Only the
post-mutation canonical rebuild trips the pre-existing parse failure.

All artifacts created or modified by this closure were independently validated
for well-formed YAML frontmatter, including a present `title:` field.

## Recommendation

**CLOSED.** Shipment 124-S is fully reconciled.
