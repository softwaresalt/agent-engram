---
shipment_id: 097-S
mode: post
merge_commit_sha: df01b498c257aa288c55c9b856c73d488c69b173
generated_at: 2026-07-30T17:35:15-07:00
generated_by: ship (post-merge closure, ride-along in 099-S PR)
recommendation: PROCEED
---

# Shipment Reconcile Report — 097-S (post-mode)

Archive-presence integrity check for the ride-along post-merge closure of
shipment `097-S` (feature `105-F` — daemon sync/index reconciliation). `105-F`
and its three tasks merged in **PR #302** (`df01b498`) but the `097-S` shipment
card was never closed; this closure rides along in the `099-S` PR (same pattern
used to close `098-S` in PR #302).

## Merge Confirmation Gate (NON-NEGOTIABLE) — PASSED

* `gh pr view 302 --json state,mergedAt,mergeCommit` → `state: MERGED`,
  `mergedAt: 2026-07-30T23:44:44Z`,
  `mergeCommit.oid: df01b498c257aa288c55c9b856c73d488c69b173`.
* `git merge-base --is-ancestor df01b498… origin/main` → exit **0** (SHA is in
  `origin/main` history; local `main` == `origin/main` == `df01b498`).

## Ship Result

`backlogit shipment ship 097-S --sha df01b498… --message "Merge pull request
#302 …" --author softwaresalt`:

* `shipment_status: shipped`
* `archived_ids: [105.001-T, 105.002-T, 105.003-T, 097-S, 105-F]`
* `returned_ids: []`
* `commit_sha: df01b498c257aa288c55c9b856c73d488c69b173`

## Archive Presence (shipment + 4 manifest items)

| Item | Archive File | Git State | Classification |
|---|---|---|---|
| 097-S (shipment) | `.backlogit/archive/097-S.md` present | renamed (`RM` queue→archive) | `matched` |
| 105-F (feature) | `.backlogit/archive/105-F.md` present | modified (`M`) | `matched` |
| 105.001-T | `.backlogit/archive/105.001-T.md` present | modified (`M`) | `matched` |
| 105.002-T | `.backlogit/archive/105.002-T.md` present | modified (`M`) | `matched` |
| 105.003-T | `.backlogit/archive/105.003-T.md` present | modified (`M`) | `matched` |

All four manifest items were already in `.backlogit/archive/` (done, moved
during PR #302 bookkeeping); the ship operation re-modified them to record the
merge SHA and the terminal `status: archived`. `097-S.md` was relocated queue →
archive by the ship operation.

## Link-drop Check (095-S / 096-S lesson) — REMEDIATED

Unlike the 096-S manifest (which carried no `links:`), **two** 097-S manifest
items carry a frontmatter `links:` block, and the `backlogit shipment ship`
metadata rewrite **dropped** them (the known 095-S link-drop defect):

* `105-F.md` — dropped `related_to` links to `015-D`, `104-F`, `103-F`.
* `105.003-T.md` — dropped `related_to` link to `015-D`.

Both blocks were **restored** in-place (the `item_links` DB rows still held the
edges: `105-F → {015-D, 104-F, 103-F}`, `105.003-T → 015-D`). Verified via
`git diff` that the net change to each file is only the ship metadata
(`archived_from` / `archived_status` / `commit` / `status: archived` /
`updated_at`) with the `links:` block intact. `105.001-T` / `105.002-T` carry no
links (no restoration required).

## Deleted-File Guard (P-007)

`git status --short -- .backlogit/archive/` reports **no deletions** — only
modifications (`M`, from the ship re-track) and one rename (`097-S.md` queue →
archive). No `git restore .backlogit/archive/` required.

## Status Transitions (from ship result)

* `105.001-T`, `105.002-T`, `105.003-T`: done → **archived**; commit `df01b498`.
* `105-F`: done → **archived** (shipment released); commit `df01b498`.
* `097-S`: active → **shipped** → archived; commit `df01b498`.

## Merge Provenance

* PR **#302** merged via **merge commit** `df01b498c257aa288c55c9b856c73d488c69b173`;
  confirmed ancestor of `origin/main`. Manifest (`105-F` + R1/R2/R3 tasks) shipped.

## Gate Decision

All 5 archive files present; no archive deletions; expected queue → archive
relocation only; dropped `links:` blocks restored.

**recommendation: PROCEED** — closure complete; artifacts ride along in the
099-S PR.
