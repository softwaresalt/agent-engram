---
shipment_id: "136-S"
mode: post
merge_commit_sha: "7632f8c03b23a5b2da064ca027ed584865cfc74b"
timestamp: "2026-09-08T14:21:30-07:00"
recommendation: PROCEED
---

# Shipment reconciliation report — 136-S (post-mode)

Post-close reconciliation invoked immediately after the safe-close sequence
(`backlogit move 136-S --status done` → `backlogit archive 136-S`), per
Ship Step 6 item 1.d.

## Archive presence — shipment record itself

`.backlogit/archive/136-S.md` present. `archived_status: done`.
`archived_from: .backlogit/queue/136-S.md`. No longer present in
`.backlogit/queue/`.

## Per-item archive check

All 9 manifest items were already present in `.backlogit/archive/` prior to
this closure session (see pre-mode report
`136-S-pre-20260908T141854Z.md`); no change in this step, since the
safe-close sequence only touched the shipment record itself (the manifest
items required no mutation).

| Item ID | Archive file present |
|---|---|
| 142.011-T | yes |
| 142.012-T | yes |
| 142.013-T | yes |
| 142.013.001-ST | yes |
| 142.013.002-ST | yes |
| 142.013.003-ST | yes |
| 142.013.004-ST | yes |
| 142.014-T | yes |
| 142.017-T | yes |

## Deleted-file guard (P-007)

`git status -- ".backlogit/archive/"` reports only the new untracked file
`.backlogit/archive/136-S.md`. No archive files reported as deletions. No
`git restore` required.

## Covering feature / cascade-scope guard

`142-F` verified `status: active` (unchanged). No orphan references to
`136-S` found in `.backlogit/queue/` beyond legitimate `dependencies:`
entries in `137-S.md` and `138-S.md` (both list `136-S` as a dependency,
not manifest membership — expected and unaffected). `backlogit shipment
ship` was never invoked; the cascade path remains correctly unused per the
P-015 partial-covering-feature guard (142-F has 59 roster children; this
shipment's manifest covers only 9, no feature member in `items`).

## Gate decision

All archive files present, no unrestored deletions. **`recommendation:
PROCEED`.**
