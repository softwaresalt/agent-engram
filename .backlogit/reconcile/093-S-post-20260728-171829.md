---
shipment_id: 093-S
mode: post
merge_commit_sha: 308c04bcb0a3aa2b127ee62336291891c6d98c53
generated_at: 2026-07-28T17:18:29-07:00
generated_by: ship (post-merge closure)
recommendation: PROCEED
---

# Shipment Reconcile Report — 093-S (post-mode)

Archive-presence integrity check run immediately after
`backlogit shipment ship 093-S`.

Ship result:
`shipment_status: shipped`, `archived_ids: [102.001-T, 093-S, 102-F]`,
`returned_ids: []`, `commit_sha: 308c04bc`.

## Archive Presence (shipment + 2 manifest items)

| Item | Archive File | Git State | Classification |
|---|---|---|---|
| 093-S (shipment) | `.backlogit/archive/093-S.md` present | renamed (`RM` queue→archive) | `matched` |
| 102-F | `.backlogit/archive/102-F.md` present | modified (`M`) | `matched` |
| 102.001-T | `.backlogit/archive/102.001-T.md` present | modified (`M`) | `matched` |

`102-F` and `102.001-T` were already in `.backlogit/archive/` (moved during the
in-PR bookkeeping commit `2fa7a377`, which recorded them `done`); the ship
operation re-modified them to record the shipment merge SHA and terminal
`archived` status. `093-S.md` was tracked in `.backlogit/queue/` and git
recorded a rename to `.backlogit/archive/093-S.md`.

## Deleted-File Guard (P-007)

`git status --short -- .backlogit/archive/` reports **no deletions** — only two
modifications (`M`, from ship re-tracking commit metadata onto `102-F` and
`102.001-T`) and one rename (`093-S.md` queue→archive). No
`git restore .backlogit/archive/` is required.

`.backlogit/queue/093-S.md` no longer exists (relocated to archive by the ship
operation) — expected.

## Status Transitions (from item logs)

* `102.001-T`: done → archived; commit `308c04bc` tracked.
* `102-F`: done → archived (reason: "shipment released"); commit `308c04bc`
  tracked.
* `093-S`: active → **shipped** → archived; commit `308c04bc` tracked.

## Merge Provenance

* PR **#295** merged via **merge commit** (P-009 satisfied — merge strategy
  verified `allow_merge_commit=true`, squash/rebase `false`).
* Merge SHA `308c04bcb0a3aa2b127ee62336291891c6d98c53`; confirmed ancestor of
  `origin/main` (Merge Confirmation Gate passed). Two parents:
  `661a88c8` (prior main, post-#294 closure) ∪ `9362f58c` (feature head — a
  `Merge branch 'main'` commit that had reset the review clock).
* 4-point Copilot merge gate GREEN at merge time: latest review
  `commit_id == 9362f58c == HEAD` ("generated no new comments"), Copilot off
  `requested_reviewers`, 0 unresolved review threads (the sole cycle-1 thread
  resolved), `mergeStateStatus == CLEAN` and CI `build == SUCCESS`.

## Remediation Outcome (feature 102-F / task 102.001-T)

`cargo audit`: **10 → 1** vulnerabilities (−9).

* Remediated 6 via in-range `cargo update` bumps: crossbeam-epoch
  (0.9.18→0.9.20, RUSTSEC-2026-0204), rustls-webpki (0.103.9→0.103.13,
  RUSTSEC-2026-0049/0098/0099/0104), rmcp+rmcp-macros (1.1.0→1.8.0,
  RUSTSEC-2026-0189).
* Remediated 3 via cozo default-feature reduction (`default-features = false`
  + explicit `storage-sqlite`/`storage-sqlite-src`/`graph-algo`), pruning
  `minreq`/`rustls 0.21.12`/`rustls-webpki 0.101.7`
  (RUSTSEC-2026-0098/0099/0104). Dep count 557→552.
* Accepted 1 with rationale: `lz4_flex 0.10.0` (RUSTSEC-2026-0041) via
  non-optional `cozo → swapvec` (pins `^0.10`, cannot be feature-dropped);
  needs a cozo 0.8+ major bump — deferred as stash `99AFF44B`.

## Follow-up Stashed (1)

| Stash ID | Kind | Priority | Summary |
|---|---|---|---|
| `99AFF44B` | task | low | cozo 0.8+ major-version bump to clear the remaining `lz4_flex` RUSTSEC-2026-0041 advisory (the 1 accepted cargo-audit finding; `cozo → swapvec` non-optional path) |

## Gate Decision

All 3 archive files present; no archive deletions; expected queue→archive
relocation and metadata re-tracking only.

**recommendation: PROCEED** — closure may continue.

Single-writer lock on `.backlogit/queue/093-S.md` released after this report.
