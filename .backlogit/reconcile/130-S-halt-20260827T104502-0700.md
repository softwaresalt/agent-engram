---
artifact_type: shipment-reconciliation
title: "Shipment 130-S Ship-Attempt Halt — Covering-Feature Child Gate"
shipment_id: 130-S
mode: post
expected_status: done
timestamp: 2026-08-27T10:45:02-07:00
recommendation: "HALT — operator reconcile required"
---

## Summary

`backlogit shipment ship 130-S --sha 2e1e01cf0405280ecf9a1e3c21402db3ad9af0f0
--message "..." --author "softwaresalt"` was attempted after the pre-mode
reconciliation (`130-S-pre-20260827T104137-0700.md`) returned `PROCEED` for
the declared manifest (`137-F`, `137.001-T`..`137.005-T`, all `done`/archived).

The command refused with exit code 6:

```
ship shipment 130-S: shipment refused: member 137.006-T is queued (not
completed through the gate): gate blocked: 137.006-T remains active
```

`137.006-T` is **not** a declared member of the `130-S` manifest. It is a
queued follow-up task (`parent_id: 137-F`, `status: queued`) opened directly
from a Copilot review comment on PR #364, after `137-F`'s own scope was
already complete. `backlogit shipment ship` derives release/closure scope
from the covering-feature parent/child relationship in addition to the
declared manifest — the same class of tool behavior documented in
`docs/compound/workflow-issues/backlogit-shipment-ship-force-releases-covering-feature-2026-04-22.md`
(there the tool over-expanded scope to *archive* an excluded covering
feature; here it over-expands scope to *block* release of an included one).
No transactional partial state change resulted from the refusal — the
command is fail-closed and made no queue/archive mutation.

## Corrective Action Taken

Per Ship's closure policy for this exact contingency ("if its parent
prevents closing 137-F, leave 137-F accurately active"), `137-F` — which had
been moved to `done` immediately prior to this ship attempt — was reverted:

```
backlogit move 137-F --status active
```

`137-F` is restored to `.backlogit/queue/137-F.md` with `status: active`. Its
`commit: 2e1e01cf0405280ecf9a1e3c21402db3ad9af0f0` field (the PR #364 merge
SHA) is retained for traceability — the verification/merge work is complete
and confirmed, but the feature is not closed while a legitimate child
follow-up remains queued and the tooling conflates the two.

`137.005-T` remains `done`/archived (no children of its own; unaffected by
this gate). `130-S` remains `status: active` and **is not shipped** by this
session.

## Follow-Up Disposition (137.006-T)

`137.006-T` was **not** modified, reparented, or force-completed. Forcing it
through the gate would require `--force-gates --force-reason`, which is
documented as operator-only and was not authorized (the operator's explicit
approval in this session covered only the PR #364 merge commit, not gate
overrides or backlog force-completions). Detaching/reparenting it via
`backlogit adopt` was considered and rejected: it would require inventing a
new placeholder parent feature, which is Stage-owned backlog structuring
work outside Ship's mandate, and risks falsifying the item's actual
relationship to `137-F` purely to satisfy a tool quirk.

`137.006-T` remains `status: queued`, `parent_id: 137-F`, in
`.backlogit/queue/137.006-T.md`, unchanged — handed off through the normal
Ship→Stage backlog queue (no separate transfer action is needed; it is
already visible to Stage's next triage/planning pass as ordinary queued
work). It is explicitly **not** claimed as completed and its source fix is
**not** implemented as part of this closure, per instruction.

## Outstanding Work

* `130-S` cannot be shipped (archived/closed) until either:
  a. `137.006-T` is completed/archived through the normal task gate, or
  b. Stage formally re-scopes `137.006-T` (e.g., adopts it under a new
     follow-up feature) so it is no longer a child of `137-F`, or
  c. An operator explicitly authorizes a force-gate override with a
     documented reason.
* PR #364's merge itself is fully confirmed and unaffected by this backlog
  administrative gate (see `130-S-pre-20260827T104137-0700.md` for merge
  verification evidence). The code change is live on `origin/main`
  regardless of this backlog closure state.

## Recommendation

**HALT** on `backlogit shipment ship 130-S`. Report to operator/Stage:
`137-F` left accurately `active`; `137.006-T` left accurately `queued`;
`130-S` left accurately `active` (unshipped). No forced or fabricated
state transitions were applied.
