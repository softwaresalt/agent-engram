---
shipment_id: 094-S
mode: post
merge_commit_sha: bb968963a522ee5ef438dd7ed73f1f52016d165a
generated_at: 2026-07-28T14:35:56-07:00
generated_by: ship (post-merge closure)
recommendation: PROCEED
---

# Shipment Reconcile Report — 094-S (post-mode)

Archive-presence integrity check run immediately after
`backlogit shipment ship 094-S`.

Ship result:
`shipment_status: shipped`, `archived_ids: [101.001-T, 101.002-T, 101.003-T,
094-S, 101-F]`, `returned_ids: []`,
`commit_sha: bb968963`.

## Archive Presence (shipment + 4 manifest items)

| Item | Archive File | Git State | Classification |
|---|---|---|---|
| 094-S (shipment) | `.backlogit/archive/094-S.md` present | renamed (`R` queue→archive) | `matched` |
| 101-F | `.backlogit/archive/101-F.md` present | untracked (`??`) | `matched` |
| 101.001-T | `.backlogit/archive/101.001-T.md` present | modified (`M`) | `matched` |
| 101.002-T | `.backlogit/archive/101.002-T.md` present | modified (`M`) | `matched` |
| 101.003-T | `.backlogit/archive/101.003-T.md` present | modified (`M`) | `matched` |

The three task files were already in `.backlogit/archive/` (moved during the
in-PR bookkeeping commit `6b3fed0e`); the ship operation re-modified them to
record the shipment merge SHA and terminal status.

## Deleted-File Guard (P-007)

`git status --short -- .backlogit/archive/` reports **no deletions** — only
modifications (`M`, from ship re-tracking commit metadata), one rename
(`094-S.md` queue→archive), and one new untracked file (`101-F.md`). No
`git restore .backlogit/archive/` is required.

`.backlogit/queue/101-F.md` shows as deleted (`D`) — expected: the covering
feature file was relocated from queue → archive by the ship operation.
`.backlogit/queue/094-S.md` was tracked and git recorded it as a rename to
`.backlogit/archive/094-S.md`.

## Status Transitions (from item logs)

* `101.001-T`, `101.002-T`, `101.003-T`: done → archived; commit `bb968963`
  tracked.
* `101-F`: active → **done** (reason: "shipment released") → archived; commit
  `bb968963` tracked.
* `094-S`: active → **shipped** → archived; commit `bb968963` tracked.

## Merge Provenance

* PR **#293** merged via **merge commit** (P-009 satisfied — merge strategy
  verified `allow_merge_commit=true`, squash/rebase `false`).
* Merge SHA `bb968963a522ee5ef438dd7ed73f1f52016d165a`; confirmed ancestor of
  `origin/main` (Merge Confirmation Gate passed). Two parents:
  `c9d19487` (prior main) ∪ `13f07f91` (feature head).
* 4-point Copilot merge gate GREEN at merge time: latest review
  `commit_id == 13f07f91 == HEAD`, Copilot off `requested_reviewers`,
  9/9 review threads resolved, `mergeStateStatus == CLEAN`.

## Follow-ups Stashed (4)

| Stash ID | Kind | Priority | Summary |
|---|---|---|---|
| `685FAA80` | task | medium | Orphan direct-edge sweep (GC `calls_edge` rows lacking `function_meta`, gated into revalidation certify path) |
| `92EE75BB` | task | low | Forced-index deletion reconciliation (index path lacks sync's deleted/excluded reconciliation before marker advance) |
| `BE366218` | bug | medium | Daemon startup pending-sync drain hardening (hydration cancel/DB-fail skips drain; drain-once; sticky gate bits leak) |
| `D2416925` | task | low | Stage harvest-provenance reconciliation (declined base-diff `stash.jsonl` promotion-link lines) |

## Gate Decision

All 5 archive files present; no archive deletions; expected queue→archive
relocations only.

**recommendation: PROCEED** — closure may continue.

Single-writer lock on `.backlogit/queue/094-S.md` released after this report.
