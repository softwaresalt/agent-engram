---
artifact_type: deliberation
created_at: 2026-09-14T20:44:56.1022195Z
id: 027-D
labels:
    - program-lock
    - planning-only
    - non-implementation
    - stage-only
    - no-shipment
    - package-g0
    - next-operation
    - generation-2
links:
    - target_id: 026-D
      link_type: related_to
priority: high
references:
    - docs/decisions/2026-09-14-checkpoint-resolution-decomposed-program-lock.md
status: blocked
title: Package G0 generation 2 — contained test-filesystem seam (deliberation authority)
updated_at: 2026-09-16T22:23:23.0000000Z
---

PUBLICATION GATE SATISFIED. The program lock published to main via PR #397, and PR #398 explicitly transitioned ONLY this item blocked->queued at merge commit f9425943ec6bdb6431389b25d1f2057f51755d3c. HISTORICAL GATE CONTEXT (dated, no longer operative): this item was previously "BLOCKED pending publication" while the lock was staged-pending-publication. AUTHORIZED STAGE OPERATION (one full policy-conformant Stage package operation - deliberate, decision, impl-plan, hardening if triggered, plan-review, harvest ONLY on PLAN_REVIEW_PASS, then assemble exactly one G0 shipment): CONSUMED on 2026-09-15 by session stage-g0-generation-2-2026-09-15. OUTCOME: PLAN_REVIEW FAIL at attempt 1 - 2 P0 and 15 P1 findings (10 architecture-level) from a 6-persona/6-model adversarial panel (5 FAIL / 1 PASS). Terminal finding: the plan's Res-1 residual claimed the compare-to-read race was scoped to the leaf component while the ancestor chain was "covered"; four independent personas established the window spans the entire resolved path because the content read re-traverses it - reproducing the exact generation-1 over-claim pattern inside the residual-disclosure section. Per program lock advancement contract item 8, architecture-level P0/P1 findings block immediately and the correction budget did NOT open; no correction round was performed. NO harvest, NO backlog hierarchy, NO shipment, NO PR. Artifacts: docs/decisions/2026-09-15-package-g0-generation-2-contained-test-filesystem-seam-deliberation.md (accepted), docs/exec-plans/2026-09-15-package-g0-generation-2-contained-test-filesystem-seam-plan.md (blocked, attempt 1), docs/closure/2026-09-15-package-g0-generation-2-plan-review-record.md (terminal record, 10 SETTLED items inheritable by any future authorized generation). OPERATOR DECISION MADE 2026-09-15 - ROUTE 2, recorded as AMENDMENT 1 of docs/decisions/2026-09-14-checkpoint-resolution-decomposed-program-lock.md (see its "## Amendments" section). AMENDED FIXED INPUT (supersedes the prior single-invariant fixed input; both parts fixed, neither alone sufficient; Part A CORRECTED 2026-09-16 by Correction 1 to Amendment 1 - the prior "retained root anchor" wording was withdrawn because a root-relative read still resolves mutable path components beneath the root): PART A SAME-OBJECT BINDING - each corpus read resolves its target by name in EXACTLY ONE RESOLUTION EPISODE producing one filesystem object (one episode counts resolutions, not syscalls: a capability traversal walking each component in sequence to produce a single object is one episode), and BOTH the read-time identity verification AND every byte of the content read are performed against THAT SAME already-opened object; after the verification point the read performs ZERO name-accepting filesystem operations across the transitive call graph (no re-open by pathname, no re-resolution of any component, and no handing of the pathname to a downstream consumer that re-opens it, including via a procfs-style synthetic name); verification must succeed before any content is released, and on mismatch the read fails closed with no pathname-based fallback. Neither a workspace-root reference nor a parent-directory reference satisfies Part A. A retained full-path capability is permitted as a REALIZATION of Part A and as hardening of the open, never as an equivalent substitute, and only if the single object that traversal produces is the same object that is both verified and read. PART B OPAQUE PER-FILE IDENTITY EQUALITY - retained in full and INDEPENDENTLY REQUIRED (Part A binds which object is read consistently, never which object is authorized): read-time identity must EQUAL the opaque stored authorized identity, compared for equality and never reconstructed, parsed, or re-derived, and remaining inside the workspace is not sufficient. PRECISION REQUIRED BY EVIDENCE: the identity must denote the OBJECT, not a name for it - both the stored and the read-time identity must be obtained FROM THE BOUND OBJECT rather than by resolving a pathname, with derivation symmetry required; a canonicalized path string is a name and is invariant under rename-over, hardlink substitution at the authorized path, and mount overlay. This constrains PROVENANCE, not type. CONTAINMENT remains a NECESSARY resolve-time precondition; identity is not a substitute for containment any more than containment is a substitute for identity. ROOT-CAUSE EVIDENCE (preserved and strengthened): an ancestor directory replaced by a symlink redirecting elsewhere inside the workspace must be rejected. DISCLOSED RESIDUAL WINDOWS (Part A closes the verification-to-read window and nothing wider; a generation-3 plan must carry these rather than rediscover them): R1 resolve-to-open substitution is not prevented, only detected by Part B and only where R4 holds; R2 substitution before authorization capture is detected by neither part; R3 in-place mutation of the authorized object is out of scope - identity equality carries no content-integrity claim; R4 identity stability and reuse (inode reuse, volume-serial instability, overlay and network filesystems) must fail closed where identity cannot be trusted; R5 corpus membership is still obtained by name-based enumeration, so the set read need not be the set authorized, detected by neither part; A2 mountpoint substitution is addressed only to the extent identity denotes the object and is NOT declared closed. STILL UNDECIDED: storage, type, API surface, whether a read-path containment check is retained, and which concrete primitive realizes the binding; the minimal API contract must still be settled in the deliberation before impl-plan begins. ACTIVATION GATE - THIS ITEM REMAINS BLOCKED. Amendment 1 as corrected authorizes the MECHANISM and SHAPE of a future generation-3 G0 attempt; it does NOT start one, and neither the amendment session nor the 2026-09-16 correction session performed any deliberation, planning, plan-review, harvest, or shipment. Correction 1 grants nothing and discharges nothing. Generation 3 requires an explicit operator transition of this item blocked->queued, exactly as the publication gate required before generation 2. Generation 3 carries a NEW artifact name and a FRESH attempt counter starting at zero, and must not edit, amend, or extend any generation-1 or generation-2 artifact. The combined Package G, generation-1, and generation-2 circuits all remain OPEN/triggered. RENEWED FAILURE BOUND: if generation 3 also fails plan-review the program halts again, and no generation 4 and no re-sequencing around G0 may occur without a further explicit operator authorization recorded as a NEW lock amendment. Program structure and package boundary authority: docs/decisions/2026-09-14-checkpoint-resolution-decomposed-program-lock.md (026-D is a registry pointer, not current-state authority).