---
artifact_type: task
created_at: 2026-08-25T05:22:00.9711398Z
dependencies:
    - 131.009-T
id: 131.010-T
labels:
    - test-first
    - red
    - compiling-harness
    - otlp
    - daemon-lifecycle
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/bin/engram.rs
status: queued
title: 'RED: prove exactly-once bounded daemon OTLP cleanup'
updated_at: 2026-08-25T05:35:35.3482315Z
---

Add daemon-lifecycle tests against the runner helper created by 131.009-T. The tests must compile with the real lifecycle interface and fail because the runner retains but does not yet flush or shut down. Use fake run results and lifecycle counters; do not reference future cleanup symbols, sleep, or rely on Drop.


<!-- BEGIN:acceptance-criteria -->
1. 131.009-T is complete and remains the explicit dependency. 2. Exact RED command: cargo test --no-default-features --features otlp-export --bin engram otlp_cleanup_red -- --nocapture. The bin test and production target compile before assertions run. 3. otlp_cleanup_red_clean_exit asserts flush_calls == 1, shutdown_calls == 1, and a clean daemon with cleanup failure returns that cleanup failure; expected RED is (0,0) because the current runner performs no cleanup. 4. otlp_cleanup_red_combined_failure injects a daemon error plus cleanup error and asserts the daemon error remains primary while a diagnostic record preserves cleanup phase, source, and the 5-second configured bound; expected RED is no cleanup diagnostic. 5. otlp_cleanup_red_timeout injects the bounded provider failure/cancellation result from 131.006-T and asserts it is surfaced without retry, panic, or hang, while shutdown is still attempted exactly once. 6. No production edit, collector, network, socket, wall-clock sleep, unbounded retry, or Drop-only oracle is included; 131.011-T owns the unchanged harness GREEN.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Daemon cleanup RED-harness skill only; test-only module in exactly 1 existing file, at most 4 test/helper functions, 3 scenarios, and 90 minutes. Atomic milestone: cleanup coordination has a compiling failing contract over the already-real lifecycle interface.
<!-- END:implementation-notes -->
