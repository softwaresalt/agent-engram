---
artifact_type: task
created_at: 2026-08-25T05:22:00.9711398Z
dependencies:
    - 131.009-T
id: 131.010-T
labels:
    - test-first
    - red
    - compiling-harness
    - otlp
    - daemon-lifecycle
    - cleanup-isolation
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/bin/engram.rs
status: queued
title: 'RED: prove isolated OTLP cleanup wait and honest timeout reporting'
updated_at: 2026-08-25T07:56:46.5103029Z
---

Add daemon-lifecycle tests against the runner helper created by 131.009-T. The tests compile with the real lifecycle interface and fail because the runner retains the owner but does not launch an isolated cleanup worker or wait on a completion signal. Fake synchronous cleanup methods use deterministic entered/release barriers; worker-spawn failure is deliberately excluded and owned by 131.011-T through 131.013-T.

<!-- BEGIN:acceptance-criteria -->
1. 131.009-T is complete and remains the explicit dependency. 2. Exact RED command: cargo test --no-default-features --features cozo-backend,otlp-export --bin engram otlp_cleanup_red -- --nocapture. The bin test and production target compile before assertions run. 3. The returned-results scenario asserts one worker launch, one force_flush call, and one shutdown call whenever flush returns Ok or Err; a flush error does not skip shutdown. Current expected RED is zero calls because the runner performs no cleanup. 4. Force-flush-stall and shutdown-stall scenarios signal the exact entered phase, pause Tokio time, prove daemon wait pending at 4,999 ms, advance to 5,000 ms, and expect CleanupWaitTimeout with last_phase, completion unknown, and worker_detached true. The test then releases and reaps its fake worker through test-only coordination. Timeout never asserts cancellation, method return, exporter delivery, or worker termination. 5. The combined-failure scenario keeps the daemon error primary and requires wait limit, last observed phase, completion unknown, and detached-worker residual. A clean daemon returns cleanup failure or timeout. 6. No production edit, collector, network, socket, wall sleep, unbounded retry, spawn-failure simulation, spawn_blocking, runtime-owned join, or Drop-only success is included. 131.011-T owns behavior-neutral spawner scaffolding; 131.012-T owns spawn-failure RED; 131.013-T greens launch and returned results; 131.014-T greens stall, timeout, channel-loss, and precedence assertions.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Daemon cleanup-isolation RED-harness skill only; test-only module in exactly 1 existing file, at most 4 test or helper functions, 3 returned, flush-stall, and shutdown-stall plus precedence scenario groups, and 105 minutes. Atomic milestone: a compiling failing contract distinguishes bounded daemon wait from unbounded synchronous cleanup completion while leaving spawn failure to its separate scaffold, RED, and GREEN tasks.
<!-- END:implementation-notes -->