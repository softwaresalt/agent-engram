---
artifact_type: task
created_at: 2026-08-25T05:22:00.9711398Z
dependencies:
    - 131.009-T
id: 131.010-T
labels:
    - test-first
    - red
    - compiling-harness
    - otlp
    - daemon-lifecycle
    - cleanup-isolation
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/bin/engram.rs
status: blocked
title: 'RED: prove isolated OTLP cleanup wait and honest timeout reporting'
updated_at: 2026-08-25T22:15:51.9220835Z
---

Add daemon-lifecycle tests against the runner helper created by 131.009-T. The tests compile with the real lifecycle interface and fail because the runner retains the owner but does not launch an isolated cleanup worker or wait on a completion signal. Fake synchronous cleanup methods use deterministic entered/release barriers; worker-spawn failure is deliberately excluded and owned by 131.011-T through 131.013-T.

<!-- BEGIN:acceptance-criteria -->
1. 131.009-T is complete and remains the explicit dependency. 2. Exact RED command: cargo test --no-default-features --features cozo-backend,otlp-export --bin engram otlp_cleanup_red -- --nocapture. Bin and production target compile before assertions run. 3. Returned-results asserts one worker launch, one force_flush, and one shutdown whenever flush returns Ok or Err; flush error does not skip shutdown. Current RED is zero calls. 4. Force-flush-stall and shutdown-stall signal exact phase, pause Tokio time, prove production wait pending at 4,999 ms, advance to 5,000 ms, and expect CleanupWaitTimeout with last_phase, completion unknown, and worker_detached true. An independent test-side watchdog at 5,001 ms fails the assertion and releases or reaps test state if production has no deadline, so no intermediate task or RED can hang. 5. Combined failure keeps daemon error primary and requires one structured secondary diagnostic. A clean daemon returns cleanup failure or timeout. Outcome precedence is completed by 131.015-T. 6. No production edit, collector, network, socket, wall sleep, unbounded retry, spawn-failure simulation, spawn_blocking, runtime-owned join, or Drop-only success is included. 131.011-T owns error vocabulary; 131.012-T spawner scaffold; 131.013-T spawn RED; 131.014-T launch GREEN; 131.015-T deadline GREEN.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Daemon cleanup-isolation RED-harness skill only; test-only module in exactly 1 existing file, at most 4 test or helper functions, 3 returned, stall, and precedence scenario groups, and 105 minutes. Atomic milestone: a compiling finite failing contract distinguishes bounded daemon wait from unbounded synchronous cleanup and cannot consume the circuit-breaker stall budget.
<!-- END:implementation-notes -->
