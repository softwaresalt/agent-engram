---
artifact_type: feature
created_at: 2026-07-29T07:17:02.1091285Z
custom_fields:
    harness_status: pending
id: 103-F
labels:
    - code-graph
    - reconciliation
    - 101-F-followup
    - pr-293
references:
    - docs/exec-plans/2026-07-29-forced-index-certify-path-completeness-plan.md
status: queued
title: 'Forced-index / revalidate certify-path completeness: orphan-edge sweep + file-set reconciliation'
updated_at: 2026-07-29T07:17:02.1091285Z
---



<!-- BEGIN:description -->
Close two completeness gaps in the 101-F forced-index / --revalidate-code-graph certify path (found in PR #293 review): (G1/685FAA80) orphaned calls_edge rows with no live function_meta are not swept before the code_graph_extraction_generation marker advances — non-traversable but violate raw-row hygiene (H4); no global calls_edge orphan GC exists (rm_orphan_edges is lineage-only). (G2/92EE75BB) the forced-index route index_workspace walks only discovered files and lacks the sync path's Phase-1 indexed-vs-current file-set reconciliation, so a previously-indexed-now-EXCLUDED (still-on-disk) file keeps its stale same-file direct edge while the marker falsely certifies its generation. Single width: code-graph reconciliation in src/services/code_graph.rs + src/db/cozo_queries.rs.
<!-- END:description -->

<!-- BEGIN:dod -->
- Zero orphaned calls_edge rows remain after any forced-index/revalidate run (count_dangling_calls_edges == 0) before the marker certifies.
- A previously-indexed, now-excluded on-disk file's stale same-file direct edge is evicted before the marker advances.
- Marker stays fail-closed on failure; recall suite green (no regression); ordered gates green (Ship-executed).
<!-- END:dod -->

<!-- BEGIN:goals -->
- Add a retraction-only orphan calls_edge sweep (no live function_meta on from OR to) wired into BOTH marker-advance certify blocks (index_workspace ~1900; --revalidate sync ~2923) before set_code_graph_extraction_generation.
- Reconcile the prior indexed file set against the discovered set on the forced-index/revalidate route (evict indexed-but-not-discovered via handle_deleted_file) before the marker advances.
- Stay fail-closed (013-D/082-F): never remove a live-endpoint edge (no recall loss); keep prior marker on any sweep/reconcile failure; language-agnostic; idempotent.
<!-- END:goals -->