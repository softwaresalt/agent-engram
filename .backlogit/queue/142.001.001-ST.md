---
artifact_type: subtask
created_at: 2026-09-03T00:24:31.3652215Z
id: 142.001.001-ST
parent_id: 142.001-T
status: active
title: Create 19 contract placeholder harness files
updated_at: 2026-09-03T07:33:01.0389255Z
---

Create the 19 new placeholder harness files under `tests/contract/`. Each file is a stub whose only purpose is to make its future `[[test]]` block resolvable, so the registration step in 142.001.004-ST cannot fail at build time.

Contract placeholders cover: `app_state_constructor_migration_test.rs`, `ipc_server_seam_test.rs`, `supervisor_workspace_boundary_test.rs`, `supervisor_release_artifact_test.rs`, `supervisor_install_exclusion_test.rs`, `tool_descriptor_registry_test.rs`, `read_server_dispatch_refusal_test.rs`, `mcp_tool_catalog_parity_test.rs`, `cli_tool_catalog_parity_test.rs`, `read_input_ownership_inventory_test.rs`, `read_path_pinning_enforcement_test.rs`, `ipc_error_envelope_test.rs`, `mcp_envelope_test.rs`, `cli_envelope_test.rs`, `read_response_provenance_test.rs`, `supported_transport_surface_test.rs`, `start_launcher_failure_test.rs`, `start_sh_launcher_test.rs`, `read_server_cli_mcp_parity_test.rs`.

<!-- BEGIN:acceptance-criteria -->
- Exactly 19 new files exist under `tests/contract/`, matching the F00 registration table paths.
- Each file contains a doc comment naming the unit that will own its body, plus a single `#[test] fn placeholder_registered() {}`.
- No file imports anything from the `engram` crate.
- `tests/contract/start_launcher_test.rs` is NOT created; it already exists and is already registered (F51).
- No existing file under `tests/contract/` is modified.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Size: S | size_source: agent | size_ruleset_version: engram-stage-2h-rule-v1. Complexity is task-level metadata; see parent task 142.001-T. Recorded as prose because this workspace subtask type defines no structured size field.

Runs first within F00: files must exist before their `[[test]]` blocks are appended, because rustc - not `cargo metadata` - is what fails on a missing declared target path.
Placeholders must stay import-free so the tree is GREEN at registration and the unit bundles no behavior change.
<!-- END:implementation-notes -->

<!-- BEGIN:verification -->
- `Get-ChildItem tests/contract -Filter *_test.rs` lists the 19 new files.
- `cargo dev-test` remains GREEN (files are inert until registered).
- A search for `use engram` returns no match in the 19 new files.
<!-- END:verification -->