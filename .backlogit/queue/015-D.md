---
artifact_type: deliberation
created_at: 2026-07-28T18:11:20.8282331Z
custom_fields:
    linked_stash_id: 5765BAAB
    linked_stash_kind: bug
    linked_stash_priority: medium
    linked_stash_text: 'Daemon ''engram index'' cross-file singleton not persisted + CLI response hang: on a fresh git workspace, plain ''engram index'' via the daemon leaves cross-file singleton calls edges (e.g. alpha->beta) staged/unresolved and the ''engram index'' CLI hangs on response while the daemon completes server-side. Root cause suspected at src/cli/direct.rs:162 (use_index = full || force) routing plain index to the sync path, which skips the cross-file post-pass (reresolve_calls_edges_with_canonical_context). Pre-existing, NOT caused by 100-F (18/18 recall + cross-file tests GREEN via in-process index_workspace). Investigate index-vs-sync routing + IPC response completion. See docs/compound/workflow-issues/new-extraction-logic-needs-forced-reindex-2026-07-20.md.'
id: 015-D
priority: medium
status: queued
title: Daemon `engram index` cross-file singleton non-persist + CLI response hang — investigate-first spike
updated_at: 2026-07-28T18:11:20.8282331Z
---

# Daemon `engram index` cross-file singleton non-persist + CLI response hang — investigate-first spike

## Problem Frame

<!-- BEGIN:problem-frame -->
On a FRESH git workspace, plain `engram index` via the DAEMON leaves cross-file singleton calls edges (e.g. alpha->beta) staged/unresolved, AND the `engram index` CLI hangs on response while the daemon completes server-side. Static analysis THIS CYCLE CONTRADICTS the stash's suspected root cause (direct.rs:162 use_index=full||force routing index->sync): `engram index` routes to index_workspace on BOTH paths — daemon via run_index -> run_tool_timed("index_workspace") (cli/commands/indexing.rs), and --direct via run_direct_sync(full=true) -> use_index=true -> index_workspace. The index path DOES run the unconditional cross-file post-pass (code_graph.rs:1815). So direct.rs:162 is NOT the cause for `engram index` (full=true). The content-hash skip freshness landmine (compound doc 2026-07-20) explains the ALREADY-indexed no-op case, but NOT the FRESH-workspace non-persist. The real defect is on the DAEMON path (routing/commit) and/or IPC response completion — unproven, needs hands-on daemon reproduction. Two entangled symptoms across CLI + daemon + IPC layers.
<!-- END:problem-frame -->

## Options

<!-- BEGIN:options -->
Hypotheses to test in the spike: || H1 (commit boundary): the daemon index_workspace tool (tools/write.rs:113) commits symbols but the post-pass singleton resolution is not flushed/committed before the IPC response, or runs outside the persisted transaction. || H2 (IPC framing/backpressure): the daemon finishes the long index but the response frame is never sent / the CLI read blocks (ipc_server.rs response completion) -> CLI hang; edges MAY actually be persisted but the client never observes completion, so the operator concludes "not persisted." || H3 (daemon routing divergence): the daemon routes `engram index` through an event-debounce/sync path (daemon/debounce.rs) rather than a true full index, skipping the post-pass on the daemon path specifically (divergence from --direct). || H4 (staging left unresolved): a cross-file alpha->beta call is staged correctly but the post-pass is not invoked on the daemon path, so it stays staged.
<!-- END:options -->

## Chosen Direction

<!-- BEGIN:chosen-direction -->
SPIKE — investigate-first (constitution safety mode). Reproduce on a fresh git workspace via the daemon: (1) run `engram index` through the daemon and capture whether the IPC response returns (and within INDEXING_TIMEOUT_SECS or indefinitely); (2) inspect the DB for the alpha->beta calls_resolved_singleton edge + staged_call rows post-run; (3) compare daemon vs `engram index --direct` vs in-process index_workspace (the GREEN 18/18 recall path); (4) bisect the routing chain run_index -> run_tool_timed -> IPC dispatch (tools/mod.rs:352) -> tools::write::index_workspace -> post-pass -> IPC response (ipc_server.rs). Exit criteria: root cause pinned to a specific layer (routing vs commit boundary vs IPC framing) with a proven minimal repro, THEN a fix plan. DEFER the fix plan/shipment until the spike lands. NOTE: 8DD29746's versioned backfill mitigates the sync-path freshness gap but does NOT address the daemon fresh-workspace non-persist or the IPC hang — this is a separate width.
<!-- END:chosen-direction -->

## Open Questions

<!-- BEGIN:open-questions -->
(1) Is the singleton actually NOT persisted, or persisted-but-invisible because the IPC hang prevents the operator from observing completion? (2) Does the hang reproduce headless/CI, or only interactively? (3) Does the CLI hang exceed INDEXING_TIMEOUT_SECS (run_tool_timed) or hang indefinitely? (4) Is the daemon actually taking a full-index path or an event-debounced sync path for `engram index`?
<!-- END:open-questions -->

## Notes

<!-- BEGIN:notes -->
Static grounding: cli/commands/indexing.rs run_index -> index_workspace (daemon) / run_direct_sync(full=true) (--direct); post-pass code_graph.rs:1815 (index path, unconditional) vs 2752 (sync path, gated on run_python_backfill); daemon dispatch tools/mod.rs:352-353, tools/write.rs:113; IPC ipc_server.rs event batching ~384, code_graph calls ~515/693/928/1150; daemon/debounce.rs sync coalescing. NOT a 100-F regression (18/18 recall + cross-file GREEN via in-process index_workspace). Compound doc: docs/compound/workflow-issues/new-extraction-logic-needs-forced-reindex-2026-07-20.md. Highest operator-assessed value, but root cause is UNPROVEN — a fix plan on the misattributed direct.rs:162 cause would be low quality. Stage did the static investigation; hands-on daemon repro is deferred (runtime execution is outside this cycle's role scope).
<!-- END:notes -->
