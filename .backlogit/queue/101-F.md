---
artifact_type: feature
created_at: 2026-07-28T18:14:05.4197871Z
custom_fields:
    harness_status: pending
id: 101-F
labels:
    - code-graph
    - freshness
    - reindex
    - schema-meta
    - upgrade-backfill
    - tech-debt
links:
    - target_id: 015-D
      link_type: related_to
references:
    - docs/exec-plans/2026-07-28-versioned-codegraph-revalidation-backfill-plan.md
    - docs/compound/workflow-issues/new-extraction-logic-needs-forced-reindex-2026-07-20.md
status: queued
title: Versioned code-graph revalidation / stale-direct-edge backfill (100-F upgrade freshness)
updated_at: 2026-07-28T18:16:07.0348353Z
---



<!-- BEGIN:description -->
Source stash 8DD29746 (Copilot review thread PRRT_kwDORJEduc6UUYBN on PR #291); plan docs/exec-plans/2026-07-28-versioned-codegraph-revalidation-backfill-plan.md (reviewed, hardened). Mirrors 096-F T7 (096.010-T rollout + 096.013-T schema_meta version-marker seam). The content-hash skip keys re-parsing on FILE CONTENT, not EXTRACTOR GENERATION (compound landmine 2026-07-20). After the 100-F same-file fail-closed extractor upgrade, an unchanged file that already has a WRONG-target direct edge persisted (minted before 100-F) is hash-skipped and keeps that stale wrong edge until a manual `engram index --force` — 100-F's guard only applies to freshly re-extracted files. Fix: add a durable schema_meta `code_graph_extraction_generation` marker + a generation-gated revalidation backfill that force re-extracts affected files (so the 100-F guard drops/corrects the stale edge) and re-materializes cross-file singletons, advancing the marker ONLY on full success (C7-3). Governing invariant 013-D/082-F: correctness (drop wrong edges) over recall. DISTINCT from 5765BAAB/015-D (daemon fresh-workspace non-persist + IPC hang) — separate width, separate fix.
<!-- END:description -->

<!-- BEGIN:dod -->
- All child tasks done; ordered gates pass (cargo fmt --all -- --check; cargo clippy --all-targets -- -D warnings -D clippy::pedantic; cargo dev-test; cargo audit).
- Upgrade acceptance: an old-generation persisted WRONG same-file edge is corrected/dropped after revalidation; cross-file singletons re-materialized; NO recall regression on unaffected edges; marker advanced.
- Matching-generation run is a strict no-op (A5); file_node.content_hash staleness detection unchanged (A4); partial failure keeps the old marker and retries (A3/C7-3).
- Generation marker is a separate schema_meta record; additive non-migrating DB change only (A4/A6).
- Docs updated to supersede the 2026-07-20 manual-force guidance; stale-generation hint present.
- Rollback: backfill is opt-in/gated + additive; revert = leave marker unset (falls back to manual --force).
<!-- END:dod -->

<!-- BEGIN:goals -->
- Correct/drop stale WRONG same-file direct edges persisted before the 100-F upgrade WITHOUT a manual --force (013-D/082-F).
- Add a durable, separate schema_meta extractor-generation marker (no content_hash contract change; zero canonical-schema migration) reusing the 096-F T7 get/set machinery.
- Make a matching generation a strict no-op (no re-extraction churn, C12-5); advance the marker only on a fully-successful pass (C7-3 fail-closed retry).
- Surface a stale-generation detection hint so the correctness gap is never silent (supersede the 2026-07-20 manual-force operational guidance).
<!-- END:goals -->