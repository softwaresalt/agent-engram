---
artifact_type: task
created_at: 2026-08-01T04:05:27.1809274Z
dependencies:
    - 109.003-T
id: 109.004-T
labels:
    - daemon
    - startup
    - pending-sync
    - concurrency
    - green
    - pr-302
parent_id: 109-F
priority: medium
references:
    - docs/exec-plans/2026-07-31-post-105-pending-sync-residuals-plan.md
    - src/daemon/ipc_server.rs
    - src/server/state.rs
status: blocked
title: 'GREEN: route startup producer through R2 backstop and exactly-one finisher'
updated_at: 2026-08-01T06:35:34.8668562Z
---

Source-only GREEN unit for 88EB5FB1. In ipc_server.rs, capture the startup producer generation before its initial try_start_indexing attempt. If that attempt fails, pass the captured token to the explicit-generation publish_pending_sync_and_try_reacquire(generation,false,false) backstop; never call set_pending_sync or the unqualified two-argument publisher. A token that became stale is ignored under pending_sync rather than relabeled. Distinguish only the private outcomes needed: initial owner runs the normal startup body; queued-under-holder returns; producer-reacquired releases the flag and drains exactly once, then returns without running the normal startup body twice. Complete all awaits before any pending_sync guard; no synchronous guard crosses await. Preserve bounded drain, startup retry/backoff, registry ingestion, embedding backfill, flush behavior, and no-contention fast path.

<!-- BEGIN:acceptance-criteria -->
AC1: generation is captured before the startup indexing-lock attempt and the failed-attempt path passes exactly that token to the explicit-generation publisher; no unqualified two-argument publisher or set_pending_sync call remains in production startup. AC2: 109.003-T final-peek race passes with one same-generation guaranteed finisher and no external tick; a token made stale is ignored, never relabeled. AC3: no-contention path runs the normal startup body exactly once. AC4: reacquired path drains exactly once without recursion, duplicate startup ingestion/backfill, or stranded pending state. AC5: no synchronous pending_sync guard crosses await; bounded 64-pass drain, retry/backoff, registry ingestion, embedding backfill, flush, and response contracts remain unchanged. AC6: controlled verification shows zero attributable drain-bound warnings and completion within the existing 30-second debug budget.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Source-only width: src/daemon/ipc_server.rs, at most three private functions, target 60–90 minutes. Reuse the explicit-generation publish/reacquire and existing drain; do not add flags, queues, schedulers, tasks, response fields, or unbounded retry. STOP/return blocked at 110 minutes, use of an unqualified publisher, generation capture after the failed attempt, recursion/double-drain, second state machine, any change outside startup coordination, synchronous guard across await, or loss of exactly-one-finisher proof.
<!-- END:implementation-notes -->