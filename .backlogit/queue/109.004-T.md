---
artifact_type: task
created_at: 2026-08-01T04:05:27.1809274Z
dependencies:
    - 109.003-T
id: 109.004-T
labels:
    - daemon
    - startup
    - pending-sync
    - concurrency
    - green
    - pr-302
parent_id: 109-F
priority: medium
references:
    - docs/exec-plans/2026-07-31-post-105-pending-sync-residuals-plan.md
    - src/daemon/ipc_server.rs
    - src/server/state.rs
status: queued
title: 'GREEN: route startup producer through R2 backstop and exactly-one finisher'
updated_at: 2026-08-01T07:15:09.0567665Z
---

Source-only GREEN startup unit for 88EB5FB1. In ipc_server.rs only, capture G before the initial try_start_indexing attempt. If it fails, pass exact G to the explicit-generation publish/reacquire primitive; never call set_pending_sync or an unqualified publisher. Distinguish initial owner, queued-under-holder, and producer-reacquired outcomes privately. A reacquirer releases and drains exactly once, then returns without running the normal startup body twice.

<!-- BEGIN:acceptance-criteria -->
AC1: generation is captured before startup contention and the failed path passes exactly that token to explicit publish/reacquire. AC2: 109.003-T final-peek race passes with exactly one guaranteed finisher; stale G is ignored, never relabeled. AC3: no-contention startup body runs exactly once. AC4: reacquired path drains exactly once without recursion, duplicate startup ingestion/backfill, or stranded pending state. AC5: bounded drain, retry/backoff, registry ingestion, embedding backfill, flush, and response contracts remain unchanged; no synchronous pending guard crosses await.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Production width: src/daemon/ipc_server.rs only, at most three private production functions, target 60-90 minutes. STOP/return blocked for a second production file, fourth function, post-failure capture, unqualified publisher, recursion/double-drain, second state machine, guard across await, or 110 minutes.
<!-- END:implementation-notes -->