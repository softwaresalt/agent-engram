---
artifact_type: task
created_at: 2026-09-13T06:52:47.7148301Z
dependencies:
    - 143.005-T
id: 143.006-T
parent_id: 143-F
priority: medium
status: queued
title: T6 - Reorder checkpoint resolution and anchor the full gate set
updated_at: 2026-09-13T06:52:47.7148301Z
---

FILE: .github/agents/_ship.agent.md (session-end resolution L1055-1058; Step 6.0 L721-753)

Move session-checkpoint resolution out of the post-merge phase and state RESOLUTION_ORDER verbatim. Resolution occurs only after work is complete and the PR is merge-ready - never speculatively. Cross-reference HEAD_EVIDENCE_RULE and LAST_MILE_RECOVERY. Retain the post-merge branch protocol for all genuinely post-merge closure artifacts; ONLY checkpoint resolution moves. NO CHECKPOINT RESOLUTION STEP MAY REMAIN AFTER MERGE (H23, CR-8).

RESOLUTION_ORDER (per plan revision 4, verbatim): implementation complete and PR merge-ready -> write compensating checkpoint FIRST -> resolve the session checkpoint (commit; advances HEAD) -> resolve the compensating checkpoint (commit; advances HEAD; LAST commit on the branch) -> record Reviewed HEAD in the PR BODY at that final HEAD (metadata; does not advance headRefOid) -> re-run P-014 section 1.9, required CI, and P-018 when engaged AT that final HEAD -> obtain merge approval anchored to that same final HEAD -> re-fetch live PR HEAD/threads/CI and merge only if unchanged.

Both resolutions now ride the SAME merge. Resolving the compensating checkpoint after merge would strand its commit on an already-merged branch with no unmerged PR able to carry it to main - the exact 139-S / commit 43e70430 defect repaired only by the extra PR #395 - so the mitigation would recreate the defect one level down, recursively (H23). The PR-body record must PRECEDE the section 1.9 gate because that gate reads the body and requires Reviewed HEAD == headRefOid; running the gate first is unsatisfiable once the resolution commits have advanced HEAD (H25).

ACCEPTANCE CRITERIA
1. RESOLUTION_ORDER appears verbatim, with the compensating checkpoint written BEFORE the resolve mutation. (H3, CR-4)
2. BOTH the session checkpoint and the compensating checkpoint are resolved BEFORE the final HEAD-bound gates, so both resolutions ride the same merge. No post-merge resolution step exists. (H23, CR-8)
3. The PR-body Reviewed HEAD record is written BEFORE the P-014 section 1.9 gate runs, and approval is obtained AFTER that gate. (H25, CR-9)
4. The re-run gate set names P-014, required CI, AND P-018 when engaged. (CR-1)
5. Merge approval is explicitly anchored to the FINAL post-resolution HEAD. (CR-2)
6. The compensating checkpoint's full lifecycle is defined, terminating in a PRE-MERGE resolution, with a resume_hint preventing double resolution.
7. LAST_MILE_RECOVERY is stated for the checkpoint-free residual window between compensating resolution and merge, including the durable shipment-to-PR locator and the live-PR-state reconciliation table (open at expected HEAD, open at different HEAD, merged, closed-unmerged, lookup failed, lost merge response). Failed lookups fail closed; merge status is never inferred; no blind second merge. (H24, CR-10)
8. Step 6.0's post-merge branch rule is unchanged for other closure artifacts.
9. Scenarios S21-S24 and S36-S43 of the verification matrix produce their stated outcomes.
10. markdownlint passes.

POSTURE: documentation-first
Size: M | Complexity: high
PLAN: docs/exec-plans/2026-09-13-checkpoint-lifecycle-continuity-plan.md (T6)