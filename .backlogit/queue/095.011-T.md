---
artifact_type: task
created_at: 2026-07-22T17:50:05.3719441-07:00
dependencies:
    - 095.002-T
id: 095.011-T
parent_id: 095-F
priority: medium
status: queued
title: 'U1b: Notebook lineage authority context — config surface + live propagation into U4 write path'
updated_at: 2026-07-22T17:50:05.3719441-07:00
---

NEW prerequisite unit (cycle-5 F1): notebook lineage AUTHORITY CONTEXT — config surface + live propagation. The U2/U3 extractors take an authority_ctx, but the notebook source model AND engram config carry NO metastore/storage authority (verified: ZERO authority/metastore references in src/**/*.rs), so in the LIVE indexer no table/path identity can ever bind to a trusted authority — only unit tests (which inject one) would produce positive lineage, and v1 would ship table lineage that is UNREACHABLE in production. This closes the impl-time propagation gap F1 flags, split into its own prerequisite task (NOT silently folded into U4) so width stays isolated. Changes: (a) CONFIG SURFACE — add trusted-authority configuration (metastore default catalog/schema + a storage-authority allowlist) to engram's config; (b) CONSTRUCTION — build a LineageAuthorityContext (type defined in U1/095.002-T) from that config at index time; (c) PROPAGATION — thread the context through src/services/notebook_indexer.rs into the U2/U3 extractor calls (which already accept authority_ctx). FAIL CLOSED: an absent/empty authority config yields an EMPTY LineageAuthorityContext, so every table/path identity stays unresolved and NO edge is emitted (never a bare-name guess; 013-D, A5 precision floor). Files: engram config surface + src/services/notebook_indexer.rs propagation. <=3 files, config/indexer-integration domain (width-isolated from the parsers and the schema). Tests: with a configured trusted authority, a live index of a fixture notebook produces the expected authority-bound table edge; with NO authority configured, the same notebook produces ZERO table/path edges (fail-closed parity with the U2/U3 unit-level drop). Blocked by U1 (095.002-T; uses LineageAuthorityContext). U4 (095.006-T) depends on THIS. Plan Unit 1b.