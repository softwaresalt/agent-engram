---
artifact_type: task
created_at: 2026-07-22T15:00:31.66011-07:00
dependencies:
    - 095.002-T
    - 095.003-T
    - 095.004-T
    - 095.005-T
id: 095.006-T
parent_id: 095-F
priority: medium
status: queued
title: 'U4: Notebook cell routing + magic stripping + lineage write path'
updated_at: 2026-07-22T15:31:40.2341888-07:00
---

Wire notebook cell routing + magic handling + the lineage write path in src/services/notebook_extract.rs and src/services/notebook_indexer.rs. Route python code cells to the U2/U2b extractors and %%sql cells to the U3 extractor, preserving chunk_index (notebook_extract.rs:29-30) so evidence carries correct cell ordering (order is metadata only — NEVER an edge). Strip the leading cell magic before parsing: notebook_extract.rs:54 currently stores the full source including the magic line, and magic_language (notebook_extract.rs:111) only peeks without stripping — so %%sql / %%spark must be removed before handing text to the SQL parser. Decide and implement the %sql line-magic policy (parse only that line, or exclude from v1) and hand the choice to U7 to document. Persist results via U1 writers: upsert_dataset_nodes / upsert_lineage_edges / upsert_lineage_edge_evidence — NEVER through ExtractedEdge / the code_graph consumer. On re-index of a CHANGED notebook, call delete_lineage_by_scope(notebook_path) BEFORE re-upsert (mirror index_notebook_source -> delete_content_records_by_scope at notebook_indexer.rs:98,164) so stale evidence and orphaned edges are GC'd while still-evidenced datasets survive. NOTE: the content-hash SKIP path for UNCHANGED notebooks (notebook_indexer.rs:153-156) and whole-notebook DELETION cleanup (sweep_deleted_notebook_files, notebook_indexer.rs:238-263) are handled by U4b (freshness/deletion), not here. Tests: a notebook with a %%sql cell + a PySpark cell routes both, strips the magic, preserves chunk_index; %sql handled per chosen policy; re-indexing a CHANGED notebook with one lineage cell removed drops exactly that cell's edge/evidence. Domain: notebook-path (integration). Blocked by U1, U2, U2b, U3. Plan Unit 4.