---
artifact_type: task
created_at: 2026-07-22T15:00:31.66011-07:00
dependencies:
    - 095.002-T
    - 095.003-T
    - 095.004-T
    - 095.005-T
    - 095.011-T
id: 095.006-T
parent_id: 095-F
priority: medium
status: queued
title: 'U4: Notebook cell routing + magic stripping + lineage write path'
updated_at: 2026-07-22T17:54:01.2448762-07:00
---

Wire notebook cell routing + magic handling + the lineage write path in src/services/notebook_extract.rs and src/services/notebook_indexer.rs. Route python code cells to the U2/U2b extractors and %%sql cells to the U3 extractor, preserving chunk_index (notebook_extract.rs:29-30) so evidence carries correct cell ordering (order is metadata only — NEVER an edge). The extractors MUST receive the RAW cell source, NOT the persisted retrieval-wrapped content: notebook_extract stores each code cell's content as format!(\"Language: {language}. {trimmed}\") (notebook_extract.rs:49-55) — a synthetic 'Language: {lang}. ' prefix PLUS the original source which still carries the %%sql/%sql magic — and magic_language (notebook_extract.rs:111) only peeks. Capture a dedicated raw parse-source at extraction time (from the pre-wrap trimmed at notebook_extract.rs:24) with (a) the 'Language: {lang}. ' retrieval wrapper NEVER applied and (b) the leading cell magic (%%sql/%%spark) stripped, and hand THAT to the U2/U3 extractors — never the retrieval content. FAIL CLOSED: if the raw parse-source can't be recovered for a cell, emit no lineage for it. Decide + implement the %sql line-magic policy (parse only that line, or exclude from v1) and hand the choice to U7. AUTHORITY (cycle-5 F1): the extractors' trusted authority_ctx is BUILT and THREADED by U1b (095.011-T; config surface + propagation) — with no configured authority the router still runs but emits NO table/path edge (fail-closed), so U4 DEPENDS ON U1b. Persist results via U1 writers: U4 COLLECTS LineageEdgeCandidates from U2b (Python path) and U3 (SQL path) and FLATTENS each candidate to one directional lineage_edge per (target, source) pair (a multi-source statement persists multiple edges sharing the target — cycle-5 F3); then assemble edges first and upsert ONLY the dataset_nodes that are endpoints of an emitted lineage_edge (node creation is EDGE-DRIVEN, never endpoint-driven — a standalone read/write endpoint with no counterpart edge writes NOTHING; fail-closed, PR #281 finding D1); write via upsert_dataset_nodes / upsert_lineage_edges / upsert_lineage_edge_evidence (one lineage_edge_evidence row per (edge, cell) keyed on chunk_index so the same edge in two cells is preserved, not overwritten — finding E1) — NEVER through ExtractedEdge / the code_graph consumer. On re-index of a CHANGED notebook, call delete_lineage_by_scope(notebook_path) BEFORE re-upsert (mirror index_notebook_source -> delete_content_records_by_scope at notebook_indexer.rs:98,164) so stale evidence (all cells) and orphaned edges are GC'd while still-evidenced datasets survive. NOTE: content-hash SKIP for UNCHANGED notebooks (notebook_indexer.rs:153-156) and whole-notebook DELETION cleanup (sweep_deleted_notebook_files, notebook_indexer.rs:238-263) are handled by U4b, not here. Tests: a notebook with a %%sql cell + a PySpark cell routes both with the EXACT byte-for-byte parse-source asserted (no 'Language: {lang}. ' wrapper and no leading magic token) and chunk_index preserved; %sql handled per chosen policy; an unrecoverable raw source emits no edge (fail-closed); a cell containing ONLY a bare read (spark.table(\"c.s.t\") with no downstream write) produces ZERO dataset_nodes and ZERO edges (edge-driven, finding D1); a MULTI-SOURCE candidate (a CTAS reading two tables) persists TWO directional lineage_edges sharing the write target, each with its own per-cell evidence (cycle-5 F3); re-indexing a CHANGED notebook with one lineage cell removed drops exactly that cell's edge/evidence. Domain: notebook-path (integration). Blocked by U1, U1b, U2, U2b, U3. Plan Unit 4.