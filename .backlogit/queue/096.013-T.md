---
artifact_type: task
created_at: 2026-07-25T15:27:47.6721614-07:00
id: 096.013-T
labels:
    - python
    - indexing
    - rollout
    - code
parent_id: 096-F
priority: medium
references:
    - src/db/cozo_backend/schema.rs
    - tests/unit/schema_meta_test.rs
status: queued
title: 'T7-seam: version-valued schema_meta extraction-version marker get/set (DB seam)'
updated_at: 2026-07-25T15:27:47.6721614-07:00
---



<!-- BEGIN:acceptance-criteria -->
- R1 seam: generalize the durable `schema_meta { key => value }` get/set pattern (schema_meta_flag_set / set_schema_meta_flag, schema.rs:275/307) to STORE and COMPARE a VERSION VALUE (not just a boolean "true") under key `PYTHON_CANONICAL_EXTRACTION_VERSION`, via the same durable pattern the calls_resolution_rolled_back marker uses (relation created by CREATE_SCHEMA_META, schema.rs:876).
- The seam exposes get / set / compare for the version marker; file_node.content_hash is UNTOUCHED (the version lives in schema_meta, NOT in content_hash -- R1: is_index_stale compares content_hash byte-for-byte against the raw source SHA).
- Compiling-but-failing harness (TDD RED): a unit test asserts set-then-get round-trips the version value and compare detects match vs mismatch. Consistent RED->GREEN with the sibling tasks.
- Single skill DOMAIN: DB / schema-meta layer (schema.rs) ONLY; NO rollout / backfill / re-extraction logic here (that is 096.010-T / T7, which CONSUMES this seam).
- cargo test green (the new marker-seam unit tests); clippy --all-targets -- -D warnings -D clippy::pedantic clean; fmt --all --check clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Version-valued schema_meta marker get/set SEAM split out of T7 (096.010-T) per C14-3 (2-hour rule + single skill domain: T7 bundled the DB schema-meta version-marker seam (schema.rs) with the indexing/rollout backfill logic (code_graph.rs) across 3 files / 2 domains). This task OWNS ONLY the DB-layer primitive: generalize the durable schema_meta get/set (schema_meta_flag_set / set_schema_meta_flag, schema.rs:275/307; relation from CREATE_SCHEMA_META, schema.rs:876) to persist and compare a VERSION value under PYTHON_CANONICAL_EXTRACTION_VERSION -- the analogue of the calls_resolution_rolled_back marker, but version-valued. It does NOT mix the version into file_node.content_hash (R1). 096.010-T (T7) depends_on this seam and CONSUMES it for the versioned re-extraction + one-step resolution backfill + partial-failure gate + rollout opt-in gate. Single skill domain: DB / schema. TDD RED->GREEN.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Split from 096.010-T per C14-3 (2-hour rule + single-domain: T7 spanned code_graph.rs rollout logic + schema.rs marker seam + test = 3 files / 2 domains). This task = the DB schema-meta version-marker seam ONLY. Files (<=2): src/db/cozo_backend/schema.rs (generalize schema_meta_flag_set / set_schema_meta_flag at 275/307 to a version-valued get/set/compare; reuse CREATE_SCHEMA_META at 876; NO canonical-schema migration -- function_meta/calls_edge and the content_hash contract unchanged), tests/unit/schema_meta_test.rs (or the existing DB-layer test harness) for the marker round-trip + compare unit tests. NO deps (a read/write over the existing durable schema_meta relation). Consumed downstream by 096.010-T (T7 rollout), which OWNS the depends_on edge (096.010-T -> 096.013-T). Keep <=4 scenarios; single DB/schema skill domain.
<!-- END:implementation-notes -->