---
artifact_type: task
created_at: 2026-08-25T05:22:30.2121369Z
dependencies:
    - 131.011-T
id: 131.012-T
labels:
    - scaffold
    - behavior-neutral
    - otlp
    - rust
    - daemon-lifecycle
    - cleanup-isolation
    - spawn-failure
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
status: blocked
title: 'SCAFFOLD: add an injectable cleanup-worker spawner without launching work'
updated_at: 2026-08-25T22:16:28.3027213Z
---

Add a behavior-neutral cleanup-worker spawner and residual-sink seam to the daemon lifecycle runner. The production adapter is a thin safe wrapper over std::thread::Builder::spawn and returns its Result; the runner launches no worker in this task. The injected fake and sink make OS spawn failure and ownership retention deterministic for the next RED.

<!-- BEGIN:acceptance-criteria -->
1. 131.011-T is complete and remains the explicit dependency. 2. In src/bin/engram.rs define one object-safe or equivalent CleanupWorkerSpawner abstraction, a NativeThreadSpawner adapter, and an injectable RetainedCleanupOwnerSink used only on failed launch. NativeThreadSpawner uses a stable worker name and returns std::io::Result; it never calls std::thread::spawn. 3. Caller and job hold separate Arc references to one cleanup ownership cell until spawn succeeds. A failed spawn cannot drop the sole explicit provider owner or invoke force_flush, shutdown, or provider Drop as claimed success. 4. The production residual sink retains the owner for process lifetime on the fatal fail-exit path. The test sink records retention without leaking. A clean daemon spawn failure is explicitly fatal and returns nonzero; 131.017-T owns restart-loop monitoring and rollback. 5. The runner accepts injected spawner and sink but deliberately invokes neither, so existing runtime behavior is unchanged. No unwrap, expect, panic, unsafe, spawn_blocking, runtime task, join, retry, environment mutation, manifest, lockfile, config, provider construction, endpoint, or attachment change is included. 6. Record exact bin compile check; 131.013-T owns deterministic forced-failure RED and 131.014-T owns launch and error GREEN.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Daemon cleanup spawner and residual-sink interface skill only; exactly 1 production file, at most 4 functions or trait methods, 2 compile and ownership scenario groups, and 60 minutes. Atomic milestone: a behavior-neutral safe injection point exists before any spawn-failure behavior test or worker launch.
<!-- END:implementation-notes -->
