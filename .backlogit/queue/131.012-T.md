---
artifact_type: task
created_at: 2026-08-25T05:22:30.2121369Z
dependencies:
    - 131.011-T
id: 131.012-T
labels:
    - verification
    - otlp
    - runtime
    - export
    - cleanup-isolation
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
status: queued
title: 'VERIFY: prove OTLP export and isolated cleanup-wait semantics'
updated_at: 2026-08-25T06:18:01.3549237Z
---

Without implementation edits, independently drive the completed daemon CLI/environment -> shared tracing seam -> retained provider -> attachment -> native cleanup-worker path through deterministic in-process fakes. Prove endpoint consumption, one named span, ownership lifetime, per-export cancellation, bounded daemon wait, honest timeout reporting, and failure precedence.

<!-- BEGIN:acceptance-criteria -->
1. 131.011-T is complete and remains the explicit dependency. 2. Re-run the exact RED commands from 131.001-T, 131.005-T, 131.007-T, and 131.010-T unchanged and record all passing assertion names. 3. A configured endpoint produces exactly one `otlp-daemon-red-span` while the owner survives daemon use; absence constructs no exporter. 4. The pending exporter future is dropped at the injected 25 ms per-export limit. Separately, a fake synchronous cleanup call held beyond the five-second application wait yields `completion=unknown` and `worker_detached=true` at the deadline; it is not reported canceled or complete. Test-only release/reap prevents a leaked test thread. 5. Returned cleanup and combined daemon/cleanup failures follow documented return/diagnostic precedence. Verify the production path contains no `spawn_blocking` or join that Tokio runtime teardown could wait on. 6. No socket, collector, network, sleep polling, unbounded retry, source/test broadening, manifest edit, lockfile edit, or config edit occurs; exact focused commands and outcomes are recorded on this task.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Runtime-verification skill only; at most 2 unchanged evidence/test surfaces, 0 changed production functions, 3 endpoint-export/per-export-timeout/cleanup-wait-and-failure scenario groups, and 90 minutes. Atomic milestone: deterministic evidence proves configured export plus the distinction between bounded daemon wait and unknown synchronous cleanup completion.
<!-- END:implementation-notes -->
