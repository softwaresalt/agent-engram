---
artifact_type: task
created_at: 2026-08-25T05:22:30.2121369Z
dependencies:
    - 131.011-T
id: 131.012-T
labels:
    - test-first
    - red
    - compiling-harness
    - otlp
    - rust
    - daemon-lifecycle
    - cleanup-isolation
    - spawn-failure
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
status: queued
title: 'RED: deterministically prove cleanup-worker spawn failure is safe and visible'
updated_at: 2026-08-25T07:46:19.9263985Z
---

Add a deterministic daemon-lifecycle RED against the 131.011-T injectable spawner. A fake returns a forced std::io::Error without consuming operating-system thread resources. The test proves spawn failure is propagated as EngramError with an actionable diagnostic, performs no synchronous cleanup fallback, and preserves the explicit provider owner as an unresolved process-lifetime residual.

<!-- BEGIN:acceptance-criteria -->
1. 131.011-T is complete and remains the explicit dependency. 2. Exact RED command: cargo test --no-default-features --features cozo-backend,otlp-export --bin engram otlp_cleanup_spawn_failure_red -- --nocapture. The production bin and test compile before assertions execute. 3. The fake CleanupWorkerSpawner records exactly one invocation and returns std::io::ErrorKind::Other with marker forced cleanup-worker spawn failure; no OS thread limit, allocation exhaustion, sleep, panic hook, or nondeterministic resource pressure is used. 4. For a clean daemon, expect Err(EngramError::Daemon(DaemonError::SpawnFailed { reason })) or the repository-equivalent typed EngramError. The reason and structured stderr diagnostic contain operation=otlp_cleanup_worker_spawn, cleanup_attempted=false, completion=not_started, worker_detached=false, and provider_residual=retained_for_process_lifetime, plus the safe source error text. 5. Assert force_flush and shutdown are each called zero times, no completion wait starts, and dropping the failed job does not drop the sole provider owner. For an existing daemon error plus spawn failure, the daemon error remains primary and the complete spawn-failure diagnostic is emitted once. 6. Current expected RED is that the injected spawner is never called and no typed spawn error or residual diagnostic exists. 131.013-T owns GREEN. No production edit, unwrap, expect, panic, unsafe, thread::spawn, spawn_blocking, join, retry, network, environment mutation, manifest, lockfile, or config change is included.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Daemon cleanup spawn-failure RED-harness skill only; test-only module in exactly 1 existing file, at most 4 test/helper functions, 3 clean-failure/combined-precedence/ownership scenario groups, and 90 minutes. Atomic milestone: a compile-then-fail deterministic contract names the exact GREEN owner and proves spawn failure cannot panic or silently drop into synchronous cleanup.
<!-- END:implementation-notes -->