---
artifact_type: task
created_at: 2026-07-23T18:45:36.7548937-07:00
dependencies:
    - 096.001-T
id: 096.003-T
labels:
    - python
    - canonical-identity
    - code
parent_id: 096-F
priority: medium
references:
    - src/services/code_graph.rs
    - src/db/cozo_queries.rs
status: queued
title: 'T3: Python canonical_path populator (module.name; "" for package roots)'
updated_at: 2026-07-23T20:08:51.0779983-07:00
---



<!-- BEGIN:acceptance-criteria -->
- A .py module-level def gets canonical_path == "mod.f".
- A def in __init__.py gets canonical_path == "" (package-root fail-closed).
- M5: a def under an undeclared src/ source root or an implicit PEP 420 namespace package gets canonical_path == "" (T1 PackageLayout REJECTs unprovable layouts).
- Two same-file defs of f both persist the identical canonical path (sets up the duplicate the resolver later fails closed on; ties to FF7DE872 non-subsumption).
- No cozo_queries.rs schema change. cargo test --test integration_code_graph green; clippy --all-targets clean; fmt clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Populate function_meta.canonical_path for Python module-level defs. Add a language dispatch to canonical_path_for_function (code_graph.rs:145-169) so Python defs get canonical_path = "<module>.<name>" via T1 ("" when python_module_path_for_file returns None). Build a lightweight Python per-file context at the two populator call sites (code_graph.rs:721 and :1446), parallel to rust_ctx; that context includes the PackageLayout (regular-package markers from the indexed file set + declared source roots) passed to T1's python_module_path_for_file (M5). Reuse upsert_function_with_canonical UNCHANGED (no DB/schema change). TDD RED->GREEN. Domain: code.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on T1 (096.001-T). Dispatch on Language::Python; Rust path unchanged. Build PackageLayout once per index pass from the indexed file set (which dirs contain __init__.py; declared source roots) and thread it into the per-file context (M5). Files (<=2): src/services/code_graph.rs, tests/integration/code_graph_test.rs.
<!-- END:implementation-notes -->