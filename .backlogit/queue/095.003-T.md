---
artifact_type: task
created_at: 2026-07-22T14:59:49.0699874-07:00
dependencies:
    - 095.002-T
id: 095.003-T
parent_id: 095-F
priority: medium
status: queued
title: 'U2: PySpark read/write literal extraction (pure) in python.rs'
updated_at: 2026-07-22T16:00:00.2294523-07:00
---

Add a pure PySpark read/write literal extractor as a new spark_lineage submodule reachable from src/services/parsing/python.rs (parse_python_source at python.rs:19; reuse is_method at python.rs:227,288-289). Expose a PUBLIC entry point spark_lineage::extract_python_lineage(source, authority_ctx) -> Vec<LineageEndpoint> that tests call DIRECTLY — do NOT route lineage through parse_source, which returns ParseResult { symbols, edges } and has NO lineage carrier (PR #281 finding 3). Whitelist Spark read/write method chains: spark.read.<fmt>(...), spark.read.load(...), spark.table(...), df.write.saveAsTable(...), df.write.save(...), df.write.mode(...).saveAsTable/save(...). Read string-literal args only and return structured LineageEndpoints (PURE — no persistence, NOT via ExtractedEdge / code_graph; U4 persists through U1 writers). Emit an endpoint ONLY when the ref satisfies the Q3 predicate AND is bound to a trusted authority in authority_ctx: a table endpoint requires a 3-part catalog.schema.table literal AND a trusted metastore authority; a path endpoint requires an absolute URI AND a storage authority. FAIL CLOSED (013-D, 0 false edges) — drop and emit ZERO for: non-literal / f-string / concatenated args, relative path literals, 1- or 2-part table names, config/widget-derived names, AND a 3-part literal with NO trusted authority present (PR #281 finding 10 — a bare 3-part name is explicitly insufficient). spark.sql(...) is DEFERRED OUT of v1 (PR #281 finding D2): its argument is SQL text, not a table/path identifier, so it is REMOVED from the whitelist and documented as a deferred exclusion — the equivalent CTAS/INSERT lineage is still captured when the statement is written in a %%sql cell (U3); a future extension may delegate the literal to extract_sql_lineage. createOrReplaceTempView(...) is captured as content only — NO v1 edge (temp-view unrepresentable, Fork A). Do NOT link read->write expressions here (that is U2b). Tests: each resolvable form WITH an injected trusted authority yields an endpoint; the SAME 3-part literal with NO authority yields ZERO; each fail-closed case (including spark.sql("CREATE TABLE ... AS SELECT ...") — deferred) yields exactly ZERO endpoints (strong assertion == 0). Domain: python-parser (width-isolated). Blocked by U1. Plan Unit 2.