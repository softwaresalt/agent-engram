---
artifact_type: task
created_at: 2026-07-22T14:59:49.0699874-07:00
dependencies:
    - 095.012-T
id: 095.003-T
parent_id: 095-F
priority: medium
status: queued
title: 'U2: PySpark read/write literal extraction (pure) in python.rs'
updated_at: 2026-07-22T17:53:05.7925967-07:00
---

Add a pure PySpark read/write literal extractor as a new spark_lineage submodule reachable from src/services/parsing/python.rs (parse_python_source at python.rs:19; reuse is_method at python.rs:227,288-289). Expose a PUBLIC entry point spark_lineage::extract_python_lineage(source, authority_ctx) -> Vec<SparkLineageEvent> that tests call DIRECTLY — do NOT route lineage through parse_source, which returns ParseResult { symbols, edges } and has NO lineage carrier (PR #281 finding 3). SparkLineageEvent is the SHARED U2->U2b contract type, OWNED and defined by U2 (in the spark_lineage module) — a TAGGED EVENT with THREE kinds, each carrying source-order index + enclosing scope (top-level cell/module body vs a branch / loop / nested / comprehension / with / except / def / class block): (1) an atomic READ BIND event — a resolved Spark read assignment (df = spark.read...(literal) / spark.table(literal)) is modeled as ONE Bind (resolved LineageEndpoint + the receiver variable name), NOT a read-call PLUS a separate rebind at equal source-order, so it does NOT self-invalidate (AR-02); (2) a WRITE CALL event (resolved LineageEndpoint or unresolved marker + the receiver variable = the base simple-name at the CHAIN ROOT so df.write.mode(...).saveAsTable(...) resolves to df, Option<name> None when the chain root is not a simple name — AR-13); (3) a NON-SPARK (RE)BINDING / INVALIDATION event emitted for ANY assignment/binding form whose left side is a tracked name with a NON-Spark RHS (df = other, df = compute()) — cycle-5 finding F2 — where spark itself is ALSO a tracked name so spark = other invalidates (AR-29). The rebind event lets U2b invalidate a prior df->dataset binding and fail closed at a later df.write: a resolver seeing only whitelisted Spark-call events could NOT detect df = other and would emit a FALSE edge (extends the cycle-4 finding E2 event model). SCOPE RULE (AR-07): new bindings AND writes are honored ONLY for statements that are DIRECT CHILDREN of the module/cell body; a rebind of a tracked name in ANY other scope/form — branch, loop, nested, comprehension, with/except target, del, augmented-assign, walrus, import, def/class — INVALIDATES the binding (never silently dropped); U2's scope enum is tightened to record which form each event came from. Whitelist Spark read/write method chains: spark.read.<fmt>(...), spark.read.load(...), spark.table(...), df.write.saveAsTable(...), df.write.save(...), df.write.mode(...).saveAsTable/save(...). Read string-literal args only (PURE — no persistence, NOT via ExtractedEdge / code_graph; U2b links events into LineageEdgeCandidates, U4 persists through U1a' writers). Resolve an endpoint ONLY when the ref satisfies the Q3 predicate AND is bound to a trusted authority in authority_ctx: table endpoint requires 3-part catalog.schema.table + trusted metastore authority; path endpoint requires absolute URI + storage authority. FAIL CLOSED (013-D, 0 false edges) — zero resolved endpoints for: non-literal / f-string / concatenated args, relative path literals, 1-/2-part table names, config/widget-derived names, AND a 3-part literal with NO trusted authority (finding 10). spark.sql(...) is DEFERRED OUT of v1 (finding D2): REMOVED from the whitelist, documented deferred exclusion (equivalent CTAS/INSERT captured via %%sql cells / U3). createOrReplaceTempView(...) captured as content only — NO v1 edge (temp-view unrepresentable, Fork A). Do NOT link read->write here (that is U2b). Tests: each resolvable form WITH an injected trusted authority yields a Spark-call event whose resolved endpoint is the expected dataset ref AND carries role/receiver-var/source-order/scope for U2b (finding E2); the SAME 3-part literal with NO authority yields ZERO resolved endpoints; a NON-SPARK reassignment (df = other) emits a REBIND/INVALIDATION event (not a Spark-call event) so U2b can drop the df binding (cycle-5 F2); each fail-closed case (including spark.sql(\"CREATE TABLE ... AS SELECT ...\") — deferred) yields exactly ZERO resolved endpoints (strong assertion == 0); an atomic-bind sequence df = spark.read(\"in\"); df.write(\"out\") emits exactly ONE read Bind (receiver df) + ONE write event and NO self-invalidation (AR-02); a mode-chain write df.write.mode(\"overwrite\").saveAsTable(\"c.s.t\") resolves the receiver to the chain-root simple-name df (AR-13); a tracked-name rebind in EACH scope/form — branch, loop, comprehension, with/except target, del, augmented-assign, walrus, import, def/class — invalidates the binding (per-form rejection tests, AR-07); and spark = other invalidates (AR-29). Domain: python-parser (width-isolated). Blocked by U1a' (095.012-T). Plan Unit 2.