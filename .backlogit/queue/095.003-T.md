---
artifact_type: task
created_at: 2026-07-22T14:59:49.0699874-07:00
dependencies:
    - 095.002-T
id: 095.003-T
parent_id: 095-F
priority: medium
status: queued
title: 'U2: PySpark read/write literal extraction (pure) in python.rs'
updated_at: 2026-07-22T16:43:09.7908748-07:00
---

Add a pure PySpark read/write literal extractor as a new spark_lineage submodule reachable from src/services/parsing/python.rs (parse_python_source at python.rs:19; reuse is_method at python.rs:227,288-289). Expose a PUBLIC entry point spark_lineage::extract_python_lineage(source, authority_ctx) -> Vec<SparkLineageEvent> that tests call DIRECTLY — do NOT route lineage through parse_source, which returns ParseResult { symbols, edges } and has NO lineage carrier (PR #281 finding 3). SparkLineageEvent is the SHARED U2->U2b contract type, OWNED and defined by U2 (in the spark_lineage module): it wraps the resolved LineageEndpoint (or an unresolved marker) PLUS the AST metadata U2b needs to fail closed — role (Read/Write), the bound/receiver variable (Option<name>, None when the receiver is not a simple name), the source-order index within the cell, and the enclosing scope (top-level cell body vs a branch/loop block) — PR #281 finding E2. Whitelist Spark read/write method chains: spark.read.<fmt>(...), spark.read.load(...), spark.table(...), df.write.saveAsTable(...), df.write.save(...), df.write.mode(...).saveAsTable/save(...). Read string-literal args only and return structured SparkLineageEvents (PURE — no persistence, NOT via ExtractedEdge / code_graph; U2b links events into edges, U4 persists through U1 writers). Resolve an endpoint ONLY when the ref satisfies the Q3 predicate AND is bound to a trusted authority in authority_ctx: a table endpoint requires a 3-part catalog.schema.table literal AND a trusted metastore authority; a path endpoint requires an absolute URI AND a storage authority. FAIL CLOSED (013-D, 0 false edges) — leave unresolved / emit ZERO resolved endpoints for: non-literal / f-string / concatenated args, relative path literals, 1- or 2-part table names, config/widget-derived names, AND a 3-part literal with NO trusted authority present (PR #281 finding 10 — a bare 3-part name is explicitly insufficient). spark.sql(...) is DEFERRED OUT of v1 (PR #281 finding D2): its argument is SQL text, not a table/path identifier, so it is REMOVED from the whitelist and documented as a deferred exclusion — the equivalent CTAS/INSERT lineage is still captured when the statement is written in a %%sql cell (U3); a future extension may delegate the literal to extract_sql_lineage. createOrReplaceTempView(...) is captured as content only — NO v1 edge (temp-view unrepresentable, Fork A). Do NOT link read->write expressions here (that is U2b). Tests: each resolvable form WITH an injected trusted authority yields an event whose resolved endpoint is the expected dataset ref AND which carries role/receiver-var/source-order/scope for U2b (finding E2); the SAME 3-part literal with NO authority yields ZERO resolved endpoints; each fail-closed case (including spark.sql("CREATE TABLE ... AS SELECT ...") — deferred) yields exactly ZERO resolved endpoints (strong assertion == 0). Domain: python-parser (width-isolated). Blocked by U1. Plan Unit 2.