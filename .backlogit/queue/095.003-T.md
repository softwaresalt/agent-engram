---
artifact_type: task
created_at: 2026-07-22T14:59:49.0699874-07:00
dependencies:
    - 095.002-T
id: 095.003-T
parent_id: 095-F
priority: medium
status: queued
title: 'U2: PySpark read/write literal extraction (pure) in python.rs'
updated_at: 2026-07-22T14:59:49.0699874-07:00
---

Add a pure PySpark read/write literal extractor as a new spark_lineage submodule reachable from src/services/parsing/python.rs (parse_python_source at python.rs:19; reuse is_method at python.rs:227,288-289). Whitelist Spark read/write method chains: spark.read.<fmt>(...), spark.read.load(...), spark.table(...), spark.sql(...), df.write.saveAsTable(...), df.write.save(...), df.write.mode(...).saveAsTable/save(...). Read string-literal args only and return a structured LineageEndpoint list (PURE — no persistence, NOT via ExtractedEdge / code_graph; U4 persists through U1 writers). Emit an endpoint ONLY when the ref satisfies the Q3 predicate: a table endpoint requires a 3-part catalog.schema.table literal; a path endpoint requires an absolute URI. FAIL CLOSED (013-D, 0 false edges) — drop and emit ZERO for: non-literal / f-string / concatenated args, relative path literals, 1- or 2-part table names, config/widget-derived names, spark.sql(variable). createOrReplaceTempView(...) is captured as content only — NO v1 edge (temp-view unrepresentable, Fork A). Do NOT link read->write expressions here (that is U2b). Tests: each resolvable form yields an endpoint; each fail-closed case yields exactly ZERO endpoints (strong assertion == 0). Domain: python-parser (width-isolated). Blocked by U1. Plan Unit 2.