---
artifact_type: task
created_at: 2026-09-03T00:22:54.7269696Z
dependencies:
    - 142.001-T
    - 142.047-T
    - 142.008-T
id: 142.048-T
parent_id: 142-F
priority: medium
status: queued
title: Remove lossy IPC error conversion
updated_at: 2026-09-03T01:48:17.1957829Z
---

Plan unit F40 - remove lossy IPC error conversion. Implement `src/daemon/error_transport.rs` against the F04a seam (`error_transport::to_wire`) and delete the existing lossy conversion that collapses typed domain errors into an opaque string, so the F39 envelope actually survives to the client.

Execution posture: test-first.

<!-- BEGIN:acceptance-criteria -->
- No code path converts a typed `DomainError` into an opaque string before transport.
- `error_transport::to_wire` is the single conversion point.
- Every reserved F38 code round-trips through IPC to the client with its detail fields.
- Refusal and availability errors are distinguishable by the client without string matching.
- `tests/integration/ipc_error_round_trip_test.rs` asserts an end-to-end round trip for each code class.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Size: M | Complexity: medium | size_source: agent | size_ruleset_version: engram-stage-2h-rule-v1. Recorded as prose because this workspace task type defines no structured size/complexity field. Size is effort/volume; complexity is difficulty/uncertainty; the two axes are independent.

Owned files: `src/daemon/error_transport.rs`, `tests/integration/ipc_error_round_trip_test.rs` (registered by F00 as `integration_ipc_error_round_trip`).
After Revision 6, F40 does NOT touch `src/daemon/ipc_server.rs` - it implements the seam F04a declared.
Edges: F04a -> F40; F39 -> F40.
<!-- END:implementation-notes -->

<!-- BEGIN:verification -->
1. `cargo test --test integration_ipc_error_round_trip` is GREEN.
2. Assertion that no `to_string`-style collapse remains on the error path.
3. `cargo dev-test`, `cargo ci`.
<!-- END:verification -->