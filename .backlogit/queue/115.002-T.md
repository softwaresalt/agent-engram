---
artifact_type: task
created_at: 2026-08-08T02:46:01.3289703Z
custom_fields:
    source_stash_id: 98CF66D5
    source_stash_kind: bug
    source_stash_path: stash.jsonl
    source_stash_priority: medium
    source_stash_text: '099-S/097.004-T W1 follow-up: lineage recall gap in resolve_cell_candidates (src/services/parsing/python.rs ~L1311/1333). Two fail-closed false-negatives from U2b invalidation: (1) read;write;read;write reuse (df=read(a);df.write(t1);df=read(b);df.write(t2)) drops the 2nd valid edge because WriteCall does not consume the binding, so the 2nd read looks like a double-read and invalidates; (2) invalidated_sessions is shared between session invalidation and re-read poison, so df=other;df=spark.read(a);df.write(t) drops a valid edge. Fix: dedicated re-read invalidation set (keep invalidated_sessions for session receivers only) + per-var written-since-bind flag so a re-read after a write rebinds instead of invalidating, without breaking single-read->multi-write fan-out. Precision floor unaffected (fail-closed). Add regression fixtures. Deferred from 099-S review gate.'
id: 115.002-T
labels:
    - spark
    - python
    - lineage
    - recall
    - reviewed
owner: stage
parent_id: 115-F
priority: medium
status: active
title: Correct Python lineage reread and session state
updated_at: 2026-08-10T03:00:04.4072628Z
---

Plan U2. Source stash 98CF66D5. Separate Python reread invalidation from session invalidation and permit read-write-read-write reuse. Reviewed plan: docs/exec-plans/2026-08-07-spark-lineage-parser-correctness-plan.md.

<!-- BEGIN:acceptance-criteria -->
RED first: read-write-read-write emits both exact valid edges; a non-Spark invalidation followed by a valid Spark read emits the valid edge; one read followed by multiple writes keeps fan-out. Nested, unresolved, and ambiguous inputs remain fail closed.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
src/services/parsing/python.rs only; <=110 minutes and exactly three scenarios. Separate session invalidation from reread poison and track written-since-bind state.
<!-- END:implementation-notes -->