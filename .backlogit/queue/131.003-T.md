---
artifact_type: task
commit: 4cab736e05ebd832e048a0338949ef62c0089b2d
created_at: 2026-08-24T22:54:24.2295849Z
dependencies:
    - 131.002-T
id: 131.003-T
labels:
    - green
    - otlp
    - rust
    - config-propagation
    - daemon-cli
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
status: queued
title: 'GREEN: propagate the configured OTLP endpoint to daemon tracing'
updated_at: 2026-08-25T04:43:15.7046595Z
---

After the dependency graph is aligned, implement only the real endpoint handoff in src/bin/engram.rs and src/lib.rs. Add daemon-subcommand parsing for --otlp-endpoint with ENGRAM_OTLP_ENDPOINT fallback, then pass the resolved Option<&str> into a daemon-only tracing initialization seam. The shim child already inherits its environment; keep src/shim/mod.rs init_tracing formatting-only and do not use the dead src/config/mod.rs Config::otlp_endpoint or add an endpoint to GlobalFlags.

<!-- BEGIN:acceptance-criteria -->
1. 131.002-T is complete and remains the explicit dependency. 2. src/bin/engram.rs Command::Daemon owns an Option<String> with clap --otlp-endpoint and ENGRAM_OTLP_ENDPOINT resolution; an explicit flag wins over environment according to clap precedence. 3. The daemon branch passes that value as Option<&str> into a dedicated src/lib.rs daemon tracing initialization seam before engram::daemon::run; the value is not reread from hidden global state. 4. The shim path continues to call formatting-only init_tracing, and the shim-spawned daemon obtains ENGRAM_OTLP_ENDPOINT through inherited process environment. 5. The unchanged 131.001-T direct-daemon, inherited-environment, and absent-endpoint assertions pass while provider/export assertions remain RED. 6. No Cargo, provider construction, layer attachment, owner retention, shutdown, PluginConfig, GlobalFlags, or shim lifecycle edit is included.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Daemon CLI/config-propagation skill only; exactly 2 production files: src/bin/engram.rs and src/lib.rs; at most 4 functions and 3 flag/environment/absent scenarios; target 95 minutes. Run the 131.001-T harness unchanged. Atomic milestone: one explicit typed endpoint value reaches the daemon tracing initialization boundary with no exporter construction yet.
<!-- END:implementation-notes -->
