---
artifact_type: subtask
created_at: 2026-09-11T02:14:37.6121706Z
custom_fields:
    blocked_reason: 'Terminal Revision 3 plan-review FAIL after three cycles; do not claim. Requires a fresh operator-authorized Stage cycle.'
dependencies:
    - 143.001-T
id: 143.002.001-ST
labels:
    - daemon
    - ipc
    - ttl
    - liveness
parent_id: 143.002-T
priority: critical
status: blocked
title: Reference-counted RAII useful-work lease in the idle-TTL owner
updated_at: 2026-09-11T03:01:33.351901Z
---

RED-FIRST CORE OF WORKSTREAM A2. Replaces the one-shot ttl.reset() with a reference-counted, RAII-scoped useful-work LEASE owned by the idle-TTL owner. Split out of 143.002-T in Revision 3 because the Revision 2 leaf carried eight acceptance criteria across three distinct concerns and exceeded the 2-hour rule.

AC1 (RED BEFORE GREEN, IN THIS LEAF): probe_only_daemon_reaches_idle_expiry is committed FAILING against the 143.001-T deterministic scaffold, then made to pass by the implementation in this same leaf. No always-red artifact is left in the tree.
AC2 (SINGLE OWNER, REFERENCE COUNTED): the lease lives in the idle-TTL owner, not in the accept loop, so reference-count correctness is enforced at exactly one site. N overlapping acquisitions hold the daemon alive until the LAST release.
AC3 (FULL IDLE TTL RE-ARM FROM THE RELEASE INSTANT, LOAD-BEARING): releasing the LAST outstanding lease re-arms a FULL idle TTL measured from the RELEASE INSTANT. It does NOT resume a partial remainder and does NOT restore the pre-acquisition deadline. A test advances the injected clock and asserts the post-release deadline equals release_instant plus the full configured idle TTL.
AC4 (OVERLAP): two overlapping useful-work leases; the daemon stays alive until the later one releases, and the full-TTL re-arm is measured from THAT release, not the earlier one.
AC5 (RAII): release happens in the guard's Drop. No explicit release call exists on any path, so a path added later cannot forget to release.
AC6 (OBSERVABLE COUNT): the outstanding-lease count is readable in-process for the rollback monitoring signal named in the plan (classifier-regressed-open alarm). Emission to the A-owned sink is owned by 143.004.001-ST and is NOT a dependency of this leaf.

An equivalent proven in-tree scoped-guard/Drop-counter protocol satisfies this requirement. The invariant - reference counted, RAII released, owned by the TTL owner, full-TTL re-arm from last release - is binding; a specific internal type name is not.

Files: src/daemon/ipc_server.rs (idle-TTL owner) plus its tests. Single domain: code with its own tests.
Gates (authoritative, in order): cargo fmt --all -- --check; cargo clippy --all-targets -- -D warnings -D clippy::pedantic; cargo dev-test; cargo audit. cargo build is an optional precheck only, not a gate. Do NOT use cargo ci or cargo lint (--all-features is broken on this baseline, risk H6).
Test-first: RED before GREEN, within this leaf.
Size: M | Complexity: medium.
