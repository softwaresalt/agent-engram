---
artifact_type: task
created_at: 2026-07-22T15:00:55.8599906-07:00
dependencies:
    - 095.007-T
    - 095.010-T
id: 095.008-T
parent_id: 095-F
priority: medium
status: queued
title: 'U7: Document v1 lineage limits + fail-closed boundaries'
updated_at: 2026-07-22T15:31:32.7194612-07:00
---

Document the v1 Spark-lineage feature: what it produces and its fail-closed boundaries. Cover: v1 emits lineage_derives_from edges for TABLES (3-part catalog.schema.table bound to trusted metastore authority) and PATHS (absolute URI bound to storage authority) ONLY; dataset_node.kind = {table, path}; dataset_node carries canonical fields only and per-notebook provenance lives in lineage_edge_evidence. Fail-closed drops (013-D, 0 false edges): 1-/2-part names, relative literals, f-strings / spark.sql(variable), config/widget paths, a 3-part literal with NO trusted authority, unresolvable metastore/storage authority. Deferrals with DISTINCT rationale: temp-view lineage deferred = unrepresentable (Fork A, no durable node); permanent (catalog) view lineage deferred = scope-minimization only (representable/authority-bindable, a clean future extension = re-add the view node kind + CREATE [OR REPLACE] VIEW DDL coverage). Cross-cell DataFrame propagation OUT (single-cell resolver only). Source/cell order (chunk_index) is metadata, NEVER an edge. Record the %sql line-magic policy chosen in U4. State the verdict is feasibility-only: product value is operator-asserted and empirical recall is the Fork A GO/NO-GO checkpoint (not a shipped guarantee). Document the retrieval surface: v1 adds NO new MCP tool — lineage is queryable via query_graph AFTER U8 extends its traversal allowlist + dataset_node resolution (there is no query_sql tool; PR #281 finding 7). Document the RELEASE ROLLBACK TRIGGER: any confirmed false lineage edge disables/reverts lineage indexing (the zero-false-edge invariant is the trigger); name the observation window + owner (PR #281 finding 8). Domain: docs (width-isolated). Blocked by U6 and U8. Plan Unit 7.