---
id: "042.010-T"
title: "Agent fallback documentation"
artifact_type: task
status: queued
parent_id: "042-F"
priority: medium
dependencies: "042.009-T"
description: "Document CLI agent fallback protocol and update architecture docs"
labels: "cli,docs,agent-fallback"
created_at: 2026-05-06T19:50:00Z
updated_at: 2026-05-06T19:50:00Z
---

## Scope

Update documentation:

1. `.github/instructions/agent-engram.instructions.md` — add "Agent Fallback Protocol" section:
   - If MCP tool call fails with transport error, agent MAY retry via CLI
   - Exec `engram <subcommand> [args] --json --id <request-id>`
   - Parse stdout as JSON-RPC 2.0 response
   - Extract `result` or surface `error` identically to MCP path

2. `docs/architecture.md` — add "CLI Architecture" section:
   - Subcommand mapping table
   - IPC routing diagram
   - Exit code semantics
   - Global flags reference

## Acceptance Criteria

- agent-engram.instructions.md contains complete fallback protocol
- docs/architecture.md contains CLI section with command reference
- Documentation is consistent with implemented behavior

## Execution Posture

Direct: documentation update after implementation is verified.

## Dependencies

- 042.009-T (document what's built and tested)
