---
artifact_type: task
created_at: 2026-08-25T07:47:15.7100129Z
dependencies:
    - 131.013-T
id: 131.014-T
labels:
    - green
    - otlp
    - rust
    - daemon-lifecycle
    - cleanup-isolation
    - spawn-failure
    - error-propagation
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/bin/engram.rs
status: blocked
title: 'GREEN: launch cleanup safely and propagate spawn failure without panic'
updated_at: 2026-08-25T22:17:02.4175848Z
---

Make the unchanged 131.013-T spawn-failure RED GREEN and green the returned-results branch of 131.010-T. Launch one detached named native worker only through the injected std::thread::Builder adapter. Propagate Builder::spawn failure through the dedicated EngramError and exact residual diagnostic without panic, synchronous cleanup fallback, retry, or loss of the explicit provider owner.

<!-- BEGIN:acceptance-criteria -->
1. 131.013-T is complete with compile-then-fail evidence and remains the explicit dependency. 2. NativeThreadSpawner calls std::thread::Builder::new().name(...).spawn(job) exactly once and preserves the returned Result. No thread::spawn, unwrap, expect, panic, unsafe, spawn_blocking, runtime task, join, retry, or fallback worker is permitted. 3. Caller and job share an Arc-backed ownership cell until spawn succeeds. On success the caller releases its duplicate and the worker safely takes the owner, calls force_flush once, then shutdown once whenever flush returns Ok or Err, and publishes phase and completion. Lock poisoning or empty-cell transfer is diagnosed through the worker channel without unwrap and is never mislabeled timeout. 4. On Builder::spawn Err, RetainedCleanupOwnerSink receives the caller-held cell exactly once, cleanup calls remain zero, and the runner returns DaemonError::CleanupWorkerSpawnFailed with distinct stable code for a clean daemon. Existing daemon error remains primary with one complete secondary diagnostic. The top-level anyhow boundary preserves the typed source chain and renders the distinct code. 5. The unchanged 131.013-T command passes deterministically and nonstalling returned-result assertions from 131.010-T pass. Test-side watchdogs keep still-RED deadline scenarios finite until 131.015-T; no intermediate test can hang. 6. No provider construction, tracing subscriber, endpoint, attachment, Cargo, lockfile, config, network, environment, or unrelated lifecycle edit is included.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Daemon cleanup launch and error-propagation skill only; exactly 1 production file, at most 4 functions or trait methods, 3 successful-launch, spawn-failure, and combined-precedence scenario groups, and 100 minutes. Atomic milestone: native worker creation is fallible, deterministic, non-panicking, and ownership-safe while bounded wait remains separately owned.
<!-- END:implementation-notes -->
