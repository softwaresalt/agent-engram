---
artifact_type: task
created_at: 2026-04-14T16:28:00-07:00
dependencies:
    - 024.001-T
id: 024.002-T
labels:
    - daemon
    - shipment-a
parent_id: 024-F
priority: medium
references:
    - docs/exec-plans/2026-04-14-atomic-policy-snapshot-plan.md
status: active
title: Wire atomic snapshot into dispatch and record denied metrics
updated_at: 2026-04-23T07:59:09.8722933-07:00
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Replace the separate `policy_config()` and `snapshot_workspace()` calls in
`dispatch()` with the atomic `snapshot_dispatch_context()` snapshot, and add
metrics recording for policy-denied calls.

**Files:**

* `src/tools/mod.rs` — Replace policy check (line 127) with snapshot-based
  evaluation. Replace `snapshot_workspace()` call (line 193) with the
  already-captured snapshot. Add `UsageEvent` recording with `outcome="denied"`
  before the policy error return. Update the TOCTOU comment block to reference
  the snapshot.

* `Cargo.toml` — Add `[[test]]` registration for
  `atomic_policy_snapshot_test`.

**Execution posture:** test-first — contract test harness exists in red phase at
`tests/contract/atomic_policy_snapshot_test.rs` (tests C018-06 through C018-07).

**Granularity:** 2 files, 3 functions modified, 2 test scenarios
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria

<!-- AC:BEGIN -->
- [ ] #1 dispatch() uses snapshot_dispatch_context() instead of separate policy_config() and snapshot_workspace() calls
- [ ] #2 Policy-denied calls record a UsageEvent with outcome="denied" and correct agent_role
- [ ] #3 Contract tests C018-06 and C018-07 pass
- [ ] #4 All existing policy integration tests continue to pass
- [ ] #5 atomic_policy_snapshot_test is registered in Cargo.toml [[test]] section
<!-- AC:END -->
