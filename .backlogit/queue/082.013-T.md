---
artifact_type: task
created_at: 2026-07-11T01:47:01.9386401-07:00
dependencies:
    - 082.010-T
id: 082.013-T
labels:
    - width:cli
    - graph
    - migration
    - rollback
    - slice:1
parent_id: 082-F
priority: medium
references:
    - docs/exec-plans/2026-07-10-callgraph-cross-file-resolution-plan.md
status: queued
title: 'ROLLBACK TRIGGER: engram maintenance CLI subcommand invoking rollback_calls_resolution'
updated_at: 2026-07-11T01:47:01.9386401-07:00
---

ROLLBACK TRIGGER (split from 082.010-T per Copilot #240 cycle-3 finding 2 — 082.010-T delivers the reusable down-migration LOGIC as the named `rollback_calls_resolution` entry point but has no operator-invocable trigger; this task adds the actionable pre-revert entry point so operators can run the cleanup BEFORE deploying reverted code, when the down-migration code still exists).

Files (<=3 src + 1 test): src/cli/commands/migrate.rs (NEW thin-CLI-wrapper following the report.rs/verify.rs precedent — a `run` fn that opens the workspace DB and calls `rollback_calls_resolution` from 082.010-T, printing a summary of retracted-edge count + column-drop status), src/cli/commands/mod.rs (add `pub mod migrate;`), src/bin/engram.rs (add a maintenance `Command` variant e.g. `MigrateDown { target }` or `Rollback { target: calls-resolution }` at the subcommand enum ~:23 and a dispatch arm in main ~:469, mirroring the `Command::Report` wiring). Test: tests/integration/cli_calls_resolution_rollback_test.rs.

Contract: expose an operator-facing maintenance subcommand (local/direct, not a daemon MCP tool — it is a deliberate destructive migration, must NOT auto-run on startup) that invokes the 082.010-T `rollback_calls_resolution` entry point against the workspace DB: retract all `calls_resolved_singleton` edges, then drop/ignore the `resolution` attribute, in that order, idempotently. This makes rec1 revertible operationally (plan §7) rather than leaving the rollback as dead internal code reachable only from a test. Keep the CLI layer thin — all migration logic stays in 082.010-T; this task only wires argument parsing, DB open, invocation, and output.

Width: cli. Scenarios (3): the subcommand parses and dispatches to `rollback_calls_resolution` (exit 0, prints retracted count); running it on a workspace with tagged edges retracts them and drops the column (end-state verified via a fresh open); running it a second time is idempotent (exit 0, no error, zero further retractions).

Depends on 082.010-T (the `rollback_calls_resolution` down-migration entry point it invokes).