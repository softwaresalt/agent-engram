---
artifact_type: task
created_at: 2026-08-01T07:11:37.5981808Z
custom_fields:
    blocked_reason: superseded by reviewed Phase 5E single-authority permit plan; keep blocked/unassigned outside manifest
dependencies:
    - 109.010-T
    - 109.013-T
id: 109.005-T
labels:
    - daemon
    - lifecycle
    - pending-sync
    - concurrency
    - test-first
    - red
    - pr-302
parent_id: 109-F
priority: medium
references:
    - docs/exec-plans/2026-07-31-post-105-pending-sync-residuals-plan.md
    - src/tools/write.rs
    - src/server/state.rs
status: blocked
title: SUPERSEDED — RED token-qualified start-or-publish arbitration
updated_at: 2026-08-03T05:13:50.656152Z
---

Tests-first RED pair for the qualified write/start arbitration established after the public-wrapper floor pair. Exercise one token-qualified state arbitration slot under the queue-floor mutex while public DispatchSnapshot and caller contracts remain unchanged.

<!-- BEGIN:acceptance-criteria -->
AC1: Exactly three deterministic private scenarios. W1 proves a coherent qualified workspace/config/token snapshot and the free-slot `Acquired` outcome while preserving qualified write behavior. W2 advances the queue floor after token capture but before initial CAS and requires `Stale`: older intent is ignored, performs no initial acquisition, queues nothing, and cannot reacquire. W3 covers same-generation live-holder `Queued` and holder-release `Reacquired` outcomes, full-mask OR, and frozen queued status/message. AC2: outcomes are explicitly `Acquired`, `Reacquired`, `Queued`, and `Stale`; no bool collapse in state. AC3: on `Reacquired`, the caller's routine-only startup claim is consumed atomically so unchanged caller completion cannot execute a duplicate routine body. AC4: any surviving same-generation revalidate/backfill companion bits retain an owning pending-sync bit for bounded drain. AC5: barriers/steps only; no sleeps, daemon/IPC, retry, fallback, public seam, response/error change, or release-behavior change. AC6: all RED scenarios are fully GREEN in 109.006-T.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Files: src/server/state.rs and src/tools/write.rs only. Exactly three deterministic scenarios, <=2 files, and <=110 minutes. Private cfg(test) pause/injection only. STOP for a third file, fourth scenario, stale initial acquisition/reacquisition, duplicate routine claim, orphan companion bits, public contract widening, or >110 minutes.
<!-- END:implementation-notes -->