---
artifact_type: task
created_at: 2026-07-22T15:00:43.372761-07:00
dependencies:
    - 095.006-T
    - 095.009-T
id: 095.007-T
parent_id: 095-F
priority: medium
status: queued
title: 'U6: Lineage fixtures + precision/recall metric (precision floor; fixture recall measured)'
updated_at: 2026-07-22T17:53:57.6850679-07:00
---

Build the lineage fixture corpus + a DEDICATED lineage precision/recall metric. Fixture matrix over RESOLVABLE cases (3-part CTAS, INSERT OVERWRITE, absolute-path read/write, single-cell read->df->write — each WITH an injected trusted metastore/storage authority) and DROPPED cases (temp-view same-cell + cross-cell, 1-/2-part table names, relative path literal, f-string SQL, spark.sql(any arg) — deferred (A6/D2), INCLUDING a literal spark.sql(\"CREATE TABLE c.s.t AS SELECT … FROM c.s.src\") that asserts ZERO edges (AR-09), config/widget-derived name, a 3-part literal with NO trusted authority, multi-cell df propagation). A5 PRECISION FLOOR: assert exactly 0 false edges on EVERY dropped case (fixture-based precision gate — the hard v1 acceptance bar). Fold in the U5-deferral assertions: ONLY temp-view refs must fail closed (no edge). PERMANENT (catalog) views are NOT fail-closed in v1 — v1 cannot distinguish a permanent view from a table (both are a 3-part name + authority), so a permanent-view ref DOES emit a kind=table edge (documented v1 limitation, AR-08); do NOT assert permanent-view fail-closed. MEASURE FIXTURE recall over the resolvable set with a DEDICATED lineage metric computed against the hand-labeled fixture ground truth over lineage_edge / lineage_edge_evidence — do NOT call run_retrieval_eval (src/tools/eval.rs:83), which scores semantic FUNCTION self-retrieval + the code CALL graph and never inspects lineage relations (PR #281 finding 6). REPORT the FIXTURE recall number; do NOT assert a recall target. IMPORTANT (cycle-5 F7): U6 supplies FIXTURE recall ONLY — it does NOT and CANNOT measure real-notebook corpus PREVALENCE from a curated fixture; the spike deliberately left prevalence UNMEASURED. Real-corpus prevalence is a SEPARATE future measurement, NOT supplied by U6; fixture recall feeds the Fork A GO/NO-GO checkpoint (product value operator-asserted; verdict feasibility-only). 091-F guardrail: derive recall from index-time-persisted artifacts (lineage_edge_evidence) or freshness-gate any per-file recompute so recall UNDER-reports, never over-reports. Domain: tests/eval (width-isolated). Blocked by U4 and U4b. Plan Units 5 + 6.