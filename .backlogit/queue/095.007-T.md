---
artifact_type: task
created_at: 2026-07-22T15:00:43.372761-07:00
dependencies:
    - 095.006-T
    - 095.009-T
id: 095.007-T
parent_id: 095-F
priority: medium
status: queued
title: 'U6: Lineage fixtures + retrieval-eval (precision floor; recall measured)'
updated_at: 2026-07-22T15:31:38.1320873-07:00
---

Build the lineage fixture corpus + a DEDICATED lineage precision/recall metric. Fixture matrix over RESOLVABLE cases (3-part CTAS, INSERT OVERWRITE, absolute-path read/write, single-cell read->df->write — each WITH an injected trusted metastore/storage authority) and DROPPED cases (temp-view same-cell + cross-cell, 1-/2-part table names, relative path literal, f-string / spark.sql(variable), config/widget-derived name, a 3-part literal with NO trusted authority, multi-cell df propagation). A5 PRECISION FLOOR: assert exactly 0 false edges on EVERY dropped case (fixture-based precision gate — the hard v1 acceptance bar). Fold in the U5-deferral assertions (temp-view and permanent-view refs must fail closed — no edge). MEASURE recall over the resolvable set with a DEDICATED lineage metric computed against the hand-labeled fixture ground truth over lineage_edge / lineage_edge_evidence — do NOT call run_retrieval_eval (src/tools/eval.rs:83), which scores semantic FUNCTION self-retrieval + the code CALL graph and never inspects lineage relations (PR #281 finding 6). REPORT the recall number; do NOT assert a recall target (recall feeds the Fork A GO/NO-GO checkpoint; product value is operator-asserted, verdict feasibility-only). 091-F guardrail: derive recall from index-time-persisted artifacts (lineage_edge_evidence) or freshness-gate any per-file recompute so recall UNDER-reports, never over-reports. Domain: tests/eval (width-isolated). Blocked by U4 and U4b. Plan Units 5 + 6.