---
artifact_type: task
created_at: 2026-07-28T18:14:41.9061563Z
dependencies:
    - 101.001-T
id: 101.002-T
labels:
    - code-graph
    - db-seam
    - freshness
    - green
parent_id: 101-F
priority: medium
status: queued
title: 'U2: code_graph_extraction_generation marker + gated revalidation backfill (GREEN)'
updated_at: 2026-07-28T18:14:41.9061563Z
---



<!-- BEGIN:acceptance-criteria -->
- New durable schema_meta key `code_graph_extraction_generation` + a CODE_GRAPH_EXTRACTION_GENERATION const (>=1 for the 100-F fail-closed semantics); get/set helpers in cozo_queries.rs mirroring python_extraction_version / set_python_extraction_version.
- Generation-gated revalidation wired into the sync path: on a stale stored generation, force re-extract affected files (100-F guard drops/corrects stale wrong edges) -> run the cross-file post-pass to re-materialize singletons -> advance the marker ONLY when there are no affected-file errors (A3/C7-3).
- Opt-in flag (mirror --backfill-python-canonical, e.g. --revalidate-code-graph) + a stale-generation detection hint (A2).
- Matching generation short-circuits: strict no-op, no re-extraction (A5/C12-5).
- Marker is a SEPARATE schema_meta record — no file_node.content_hash contract change (A4); zero canonical-schema migration (A6).
- U1 scenarios pass; fmt + clippy pedantic clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Implement the generation marker + gated revalidation backfill to make U1 (101.001-T) GREEN. Width-isolated to code-graph + db-marker seam. Plan Unit U2. Blocked by U1; blocks U3.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Mirror 096-F T7: run_python_backfill gate at code_graph.rs ~2751 and set_python_extraction_version marker-advance-on-success; C8-1 retract-and-re-derive is the fail-closed precedent. Reuse cozo_backend/schema.rs schema_meta get/set (schema_meta_flag / set_schema_meta_flag, PYTHON_CANONICAL_EXTRACTION_VERSION_KEY). ~4 functions/edits; within the 2-hour gate. Design fork locked in plan: opt-in gated backfill + auto stale-generation detection (NOT auto heavy re-extraction).
<!-- END:implementation-notes -->