---
artifact_type: task
created_at: 2026-08-25T04:04:41.4373867Z
dependencies:
    - 131.003-T
id: 131.004-T
labels:
    - scaffold
    - test-seam
    - otlp
    - rust
    - behavior-preserving
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/server/observability.rs
    - src/lib.rs
status: queued
title: 'SCAFFOLD: expose behavior-neutral OTLP lifecycle injection and control seams'
updated_at: 2026-08-25T06:57:12.0223292Z
---

After the optional feature compiles, add an explicit behavior-neutral injection boundary before any lifecycle RED. Refactor the compiled observability constructor through an internal exporter-factory callback and a pipeline result/control seam that can report lifecycle unavailable without naming a future symbol. Production adapters preserve current behavior: the returned layer transitively retains the SDK provider through its tracer, but no separately accessible application lifecycle handle, source-owned timeout, endpoint wiring, attachment, or cleanup orchestration exists.

<!-- BEGIN:acceptance-criteria -->
1. 131.003-T is complete and remains the explicit dependency. 2. Only src/server/observability.rs and src/lib.rs change, introducing narrow internal exporter-factory and tracing-pipeline result/control interfaces that deterministic tests and production adapters share. The result can carry an attachable layer and report an unavailable application lifecycle handle without a missing symbol or production behavior change. 3. The interfaces use already compiling 0.26 types and current initializer APIs; a controlled local exporter can be injected, and no RED test has to import or name a not-yet-existing symbol. 4. The production endpoint adapter still returns the same layer-held tracer/provider clone and the public initializer remains formatting-only. It reports no separately accessible lifecycle owner and adds no provider policy, endpoint parsing/propagation, layer attachment, daemon ownership, flush, shutdown, retry, or error-precedence behavior. 5. The exact 131.001-T command stays GREEN and cargo check --no-default-features --features cozo-backend,otlp-export --lib stays successful. 6. No test is added here; 131.005-T and 131.007-T own behavior assertions against these explicit seams. No TODO, panic, fake success, network dependency, or process-global environment mutation is permitted.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Testability-seam/refactor skill only; exactly 2 production files, at most 4 interface/adapter functions, 2 behavior-preservation scenarios, and 90 minutes. Atomic milestone: later RED harnesses compile against a real shared seam that models lifecycle unavailability honestly without hiding production implementation inside test tasks.
<!-- END:implementation-notes -->
