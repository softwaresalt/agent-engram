---
artifact_type: task
created_at: 2026-08-25T04:05:04.4167916Z
dependencies:
    - 131.005-T
id: 131.006-T
labels:
    - green
    - otlp
    - rust
    - provider
    - timeout
    - cancellation
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/server/observability.rs
status: queued
title: 'GREEN: retain the OTLP provider and own its export timeout'
updated_at: 2026-08-25T06:18:01.3549237Z
---

Make the unchanged 131.005-T provider RED GREEN in src/server/observability.rs. Return a lifecycle pipeline that strongly owns the OpenTelemetry 0.26 provider and layer, configure the batch processor with an application-owned five-second maximum for each exporter future, and expose the SDK's synchronous flush/shutdown results without labeling either complete call operation-bounded. Do not attach the pipeline to production tracing or coordinate daemon exit here.

<!-- BEGIN:acceptance-criteria -->
1. 131.005-T is complete with compiling RED evidence and remains the explicit dependency. 2. `src/server/observability.rs` owns `OTLP_EXPORT_TIMEOUT = Duration::from_secs(5)` as source policy; its documentation says per exporter future/per batch, never cleanup-phase or whole-call timeout. 3. Provider construction applies `BatchConfigBuilder::default().with_max_export_timeout(OTLP_EXPORT_TIMEOUT)`, so `OTEL_BSP_EXPORT_TIMEOUT` cannot alter that per-export policy. A test seam may inject only a shorter value. 4. The returned pipeline strongly retains `trace::TracerProvider` with its attachable layer and exposes synchronous `force_flush` and `shutdown` results. Their API documentation states that SDK 0.26 waits on untimed response channels and may process multiple exports, so neither call is cancellable or bounded by this constant. 5. A never-ready fake exporter future is dropped at the deterministic 25 ms test limit under paused Tokio time; this proves only the individual export future timeout. The production effective value remains exactly 5 seconds. 6. The unchanged 131.005-T command passes. No `src/lib.rs`, daemon CLI, production attachment, endpoint parsing, cleanup worker, daemon-wait deadline, Cargo, lockfile, config, retry, sleep, or daemon error-precedence edit is included.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Rust observability-provider skill only; exactly 1 production file, at most 4 functions, 3 owner/export/timeout scenarios, and 105 minutes. Atomic milestone: retained construction and bounded individual exporter behavior are GREEN, while synchronous cleanup remains explicitly unbounded and production endpoint/attachment/exit coordination remain absent.
<!-- END:implementation-notes -->
