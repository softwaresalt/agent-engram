---
artifact_type: task
created_at: 2026-08-25T04:05:04.4167916Z
dependencies:
    - 131.005-T
id: 131.006-T
labels:
    - green
    - otlp
    - rust
    - provider
    - timeout
    - cancellation
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/server/observability.rs
status: blocked
title: 'GREEN: expose the OTLP lifecycle owner and own its export timeout'
updated_at: 2026-08-25T22:14:34.1540026Z
---

Make the unchanged 131.005-T lifecycle and timeout RED assertions GREEN in src/server/observability.rs. Return a pipeline whose attachable layer continues to own its tracer/provider clone and whose separate application lifecycle handle exposes the same provider for explicit force-flush/shutdown control. Configure the batch processor with an application-owned five-second maximum for each exporter future. Do not claim the separate handle is required for layer liveness, and do not attach the pipeline to production tracing or coordinate daemon exit here.

<!-- BEGIN:acceptance-criteria -->
1. 131.005-T is complete with a passing layer-retention baseline plus compiling lifecycle/timeout RED evidence and remains the explicit dependency. 2. src/server/observability.rs owns OTLP_EXPORT_TIMEOUT = Duration::from_secs(5) as source policy; its documentation says per exporter future/per batch, never cleanup-phase or whole-call timeout. 3. Provider construction applies BatchConfigBuilder::default().with_max_export_timeout(OTLP_EXPORT_TIMEOUT), so OTEL_BSP_EXPORT_TIMEOUT cannot alter that per-export policy. A test seam may inject only a shorter value. 4. The returned pipeline contains the attachable layer and a separately accessible application lifecycle handle retaining the same trace::TracerProvider. The layer already keeps a provider clone alive through its tracer; the explicit handle exists so application code can invoke and observe synchronous force_flush and shutdown results. SDK 0.26 waits on untimed response channels, so neither complete call is claimed cancellable or bounded by OTLP_EXPORT_TIMEOUT. 5. Through the unchanged controlled local exporter seam, force_flush exports exactly otlp-red-contract-span. A never-ready fake exporter future is dropped at the deterministic 25 ms test limit under paused Tokio time; this proves only the individual export future timeout. The production effective value remains exactly 5 seconds. 6. The unchanged 131.005-T command passes, including the already-GREEN layer-retention baseline. No src/lib.rs, daemon CLI, production attachment, endpoint parsing, cleanup worker, daemon-wait deadline, Cargo, lockfile, config, process-global environment mutation, retry, sleep, or daemon error-precedence edit is included.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Rust observability lifecycle-control skill only; exactly 1 production file, at most 4 functions, 3 layer-retention/lifecycle-flush/export-timeout scenarios, and 105 minutes. Atomic milestone: explicit application lifecycle control and bounded individual exporter behavior are GREEN, while transitive layer ownership is accurately preserved and production endpoint/attachment/exit coordination remain absent.
<!-- END:implementation-notes -->
