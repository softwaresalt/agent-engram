---
artifact_type: task
created_at: 2026-08-25T04:05:04.4167916Z
dependencies:
    - 131.005-T
id: 131.006-T
labels:
    - green
    - otlp
    - rust
    - provider
    - timeout
    - cancellation
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/server/observability.rs
status: queued
title: 'GREEN: retain the OTLP provider and own its export timeout'
updated_at: 2026-08-25T05:20:21.3686596Z
---

Make the unchanged 131.005-T provider RED GREEN in src/server/observability.rs. Return a lifecycle pipeline that strongly owns the OpenTelemetry 0.26 provider and layer, configure the batch processor with an application-owned five-second maximum export timeout, and expose explicit flush/shutdown operations with typed phase-and-limit failures. Do not attach the pipeline to production tracing or coordinate daemon exit here.

<!-- BEGIN:acceptance-criteria -->
1. 131.005-T is complete with compiling RED evidence and remains the explicit dependency. 2. src/server/observability.rs owns OTLP_EXPORT_TIMEOUT = Duration::from_secs(5); this is a source constant, not CLI/environment/workspace config, and is documented as the per-export/cleanup-phase cap. 3. Provider construction uses BatchConfigBuilder::default().with_max_export_timeout(OTLP_EXPORT_TIMEOUT) after environment-derived defaults, so OTEL_BSP_EXPORT_TIMEOUT cannot lengthen or shorten the production bound. The test seam may inject only a shorter test duration. 4. The returned pipeline strongly retains trace::TracerProvider with its attachable layer and exposes explicit force_flush and shutdown results; SDK failure is reported with operation phase and configured limit rather than swallowed or treated as Drop-only success. 5. The never-ready fake export future is cancelled/dropped at the deterministic 25 ms test limit under paused Tokio time; the production effective-timeout assertion remains exactly 5 seconds. 6. The unchanged 131.005-T command passes. No src/lib.rs, daemon CLI, production attachment, endpoint parsing, Cargo, lockfile, config, retry, sleep, or daemon error-precedence edit is included.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Rust observability-provider skill only; exactly 1 production file, at most 4 functions, 3 owner/export/timeout scenarios, and 105 minutes. Atomic milestone: retained construction and bounded exporter behavior are GREEN, while production endpoint/attachment and exit coordination remain absent.
<!-- END:implementation-notes -->
