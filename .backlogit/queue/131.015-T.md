---
artifact_type: task
created_at: 2026-08-25T07:47:36.4959912Z
dependencies:
    - 131.014-T
id: 131.015-T
labels:
    - green
    - otlp
    - rust
    - daemon-lifecycle
    - cleanup-isolation
    - timeout-reporting
    - error-precedence
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/bin/engram.rs
status: blocked
title: 'GREEN: bound cleanup wait and preserve honest lifecycle diagnostics'
updated_at: 2026-08-25T22:17:18.9222189Z
---

Make the remaining unchanged 131.010-T cleanup-stall and precedence RED assertions GREEN. After successful launch, the daemon awaits only worker phase and completion messages under one monotonic five-second deadline, never joins the native worker, and distinguishes timeout, ownership-transfer failure, channel loss, worker panic, cleanup failure, and pre-existing daemon failure.

<!-- BEGIN:acceptance-criteria -->
1. 131.014-T is complete and remains the explicit dependency; safe Builder::spawn error propagation is unchanged. 2. OTLP_CLEANUP_WAIT_TIMEOUT = Duration::from_secs(5) bounds only daemon wait for the complete worker sequence after successful launch. OTLP_EXPORT_TIMEOUT remains a separate per-export-future policy. 3. Force-flush-stall and shutdown-stall use phase signals and a monotonic Tokio deadline. At 5 seconds return CleanupWaitTimeout with last_phase, completion unknown, worker_detached true, and unresolved resource residual; drop the receiver and do not join, retry, or report SDK cancellation or completion. 4. Precedence is explicit. A pre-existing daemon error is always primary. For a clean daemon: launch failure occurs before wait; then worker panic or channel loss outranks ownership-transfer failure; ownership-transfer failure outranks returned flush or shutdown failure; an observed cleanup failure outranks a later timeout; pure timeout is last. Preserve all observed secondary details in one structured diagnostic emitted once. 5. The exact 131.010-T command passes unchanged, including pending at 4,999 ms, timeout at 5,000 ms, test-only release and reap after the production-facing assertion, returned flush error still followed by shutdown, and both stall phases. 6. No thread::spawn, unwrap, expect, panic, unsafe, spawn_blocking, runtime-owned join, environment mutation, provider construction, tracing subscriber, endpoint, attachment, Cargo, lockfile, config, network, or unrelated lifecycle edit is included.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Daemon cleanup deadline and diagnostic skill only; exactly 1 production file, at most 4 functions, 3 flush-stall, shutdown-stall, and failure-precedence scenario groups, and 105 minutes. Atomic milestone: successful worker launch contributes at most five seconds of daemon wait while timeout truthfully leaves SDK completion unknown.
<!-- END:implementation-notes -->
