---
artifact_type: task
created_at: 2026-08-25T07:47:36.4959912Z
dependencies:
    - 131.014-T
id: 131.015-T
labels:
    - verification
    - otlp
    - runtime
    - export
    - cleanup-isolation
    - spawn-failure
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/bin/engram.rs
status: queued
title: 'VERIFY: prove OTLP export and safe isolated cleanup semantics'
updated_at: 2026-08-25T07:54:36.7113988Z
---

Without implementation edits, independently drive the completed daemon endpoint, retained provider, native cleanup-worker, spawn-failure, and bounded-wait paths through deterministic in-process fakes. Prove configured export, ownership lifetime, per-export cancellation, safe Builder::spawn failure propagation, honest timeout semantics, and error precedence.

<!-- BEGIN:acceptance-criteria -->
1. 131.014-T is complete and remains the explicit dependency. 2. Re-run the exact RED commands from 131.001-T, 131.005-T, 131.007-T, 131.010-T, and 131.012-T unchanged and record every passing assertion name. 3. A configured endpoint produces exactly one otlp-daemon-red-span while the owner survives daemon use; absence constructs no exporter. The pending exporter future is dropped only at the injected 25 ms per-export limit. 4. The deterministic fake-spawner failure returns typed EngramError and exact residual diagnostic with zero force_flush and shutdown calls, no panic, no synchronous fallback, and provider ownership retained for process lifetime. The production adapter uses Builder::spawn Result handling and contains no thread::spawn, unwrap, expect, panic, unsafe, spawn_blocking, join, or retry. 5. Separately, a fake cleanup call held beyond the five-second daemon wait yields completion unknown and worker_detached true; test-only release and reap follows the assertion. Returned cleanup, spawn failure, channel loss or panic, and combined daemon and cleanup failures follow documented precedence. 6. No socket, collector, network, wall sleep, source or test broadening, manifest edit, lockfile edit, config edit, environment mutation, or monitoring-system code occurs; exact focused commands and outcomes are recorded on this task.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Runtime-verification skill only; at most 2 unchanged evidence or test surfaces, 0 changed production functions, 3 endpoint-export, spawn-failure, and cleanup-timeout or precedence scenario groups, and 90 minutes. Atomic milestone: deterministic evidence proves configured export plus safe fallible worker launch and the distinction between bounded daemon wait and unknown SDK completion.
<!-- END:implementation-notes -->