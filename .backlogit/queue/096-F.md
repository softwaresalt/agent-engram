---
artifact_type: feature
created_at: 2026-07-23T18:44:40.1017443-07:00
custom_fields:
    harness_status: pending
id: 096-F
labels:
    - code-graph
    - python
    - canonical-identity
    - call-graph
    - fail-closed
references:
    - docs/decisions/2026-07-23-python-namespace-canonical-resolution-spike.md
    - docs/exec-plans/2026-07-23-python-namespace-canonical-resolution-plan.md
status: queued
title: Python module-namespace-qualified call resolution
updated_at: 2026-07-23T18:44:40.1017443-07:00
---

Extend engram's Rust-built canonical-path call resolution (091-F) to Python's module namespace so cross-module same-name calls resolve to the correct target instead of being dropped by name-only ambiguity. Layers on 094-F Python Calls extraction. Stash FE8B3B2D; Option B ratified (product value operator-asserted; feasibility spike = GO).

<!-- BEGIN:description -->
Python modules ARE namespaces (foo/bar.py -> foo.bar). This feature adds a Python analogue of the Rust canonical machinery: (1) Python module-path + import-binding capture, (2) a Python function_meta.canonical_path populator, (3) a Python-aware canonical resolver branch that reuses the existing singleton/duplicate fail-closed core (function_ids_by_canonical_path) with ZERO DB/schema change.

Resolution rule (frozen): bare call defined-in-module M -> M.name; else imported via `from N import name` -> N.name; else `mod.name()` with mod a bound module -> <mod>.name; else (star/relative/package-root/re-export/dynamic/unbound/local-shadow) -> DROP.

Scope FROZEN: IN = module-level functions + module.func() disambiguation. OUT (documented non-goal) = instance-method dispatch (self.method()/obj.method()), which needs type inference. Related bug FF7DE872 (same-file same-name shadowing) is SPLIT OUT and INDEPENDENT — NOT subsumed here (the canonical index fails closed on the duplicate canonical_path rather than applying last-wins).

Independent of shipment 090-S / 095-F (operator-ratified: no blocking dependency). Sequences after 094-F (merged).
<!-- END:description -->

<!-- BEGIN:dod -->
- All child tasks done: T1 (module-path primitive), T2 (import-binding capture), T2b (scope-aware binding isolation), T3 (canonical_path populator), T4 (emit module.func() candidates), T5a (route Python calls into canonical staging), T5b (Python canonical target resolution), T5c (module-receiver + bare-import shadow guard), T6 (docs + v1 non-goals), T7 (versioned re-extraction + one-step resolution backfill). [10 tasks: 096.001-T .. 096.010-T]
- Ordered quality gates pass: cargo fmt --all -- --check; cargo clippy --all-targets -- -D warnings -D clippy::pedantic; cargo dev-test; cargo audit.
- Acceptance: cross-module same-name call resolves to the EXACT target id (target-identity, not row-existence); every fail-closed case (star/duplicate/unbound/module-receiver-shadow/bare-import-shadow/__init__.py/src-root/namespace-package) yields NO edge.
- Upgrade backfill (T7): after a version-bump sync of an unchanged-hash .py file the RESOLVED cross-module edge is present (one operation, no unresolved-edge window).
- Rust singleton/canonical resolution unchanged (regression assertions).
- Docs updated with capability + v1 non-goals (instance-method dispatch, re-exports, source-root/src-layout resolution, FF7DE872 separateness).
- No cozo_queries.rs schema change.
<!-- END:dod -->

<!-- BEGIN:goals -->
- Recover recall on cross-module same-name Python calls (currently dropped by name-only fail-closed) WITHOUT introducing any false edge (013-D no-false-edge; 082-F target-correctness).
- Reuse the language-agnostic canonical DB layer and singleton fail-closed core with zero schema change.
- Fail closed on every ambiguity: star/relative/package-root (__init__.py, PEP 420 namespace packages, src/-style source roots)/re-export/dynamic imports, duplicate canonical paths, and a module receiver OR a bare imported callee shadowed by a local/parameter/any rebind.
- Source-root-aware package resolution (stripping a declared src/ root) is a documented v1 NON-GOAL (fail closed until a real config source exists).
- Keep Rust resolution byte-identical (all new branches guarded on Language::Python).
<!-- END:goals -->