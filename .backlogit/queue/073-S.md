---
artifact_type: shipment
created_at: 2026-07-04T18:54:21.5987959-07:00
custom_fields:
    items:
        - 065-F
        - 065.004-T
id: 073-S
status: active
title: NotReady error points at --direct (065-F)
updated_at: 2026-07-04T23:22:44.0724117-07:00
---

## Description

<!-- BEGIN:description -->
Queued shipment delivering **065.004-T** — augment the `DaemonError::NotReady`
`#[error(...)]` message so that when daemon **startup** times out, the error points
the user at the daemonless escape hatch (`engram index --direct`, or
`ENGRAM_DIRECT=1`). Test-first; low blast radius (single error string + unit tests,
no daemon runtime; the `to_response` mapping is string-independent so the wire
contract is unaffected).

**Deferral gate SATISFIED:** the defer reason (width-isolation from the 065-F docs
shipment — this is compiled Rust bound by Test-First + cargo gates) is resolved by
giving it its **own code shipment** (073-S). 065.004-T is 065-F's only remaining
open child (001/002/003-T archived), so delivering it completes the feature.

Grounded plan: `docs/exec-plans/2026-07-04-065-004-notready-direct-hint-plan.md`.
Reviewed and ACCEPTED via **073.001-R**
(`docs/reviews/2026-07-04-065-004-notready-direct-hint-plan-review.md`).
Awaiting Ship to claim.
<!-- END:description -->

## Manifest

| Item | Title | Status | Origin |
|---|---|---|---|
| 065-F | Surface & document the daemonless --direct indexing mode | active | umbrella (last open child delivered here) |
| 065.004-T | Point daemon-startup-timeout (NotReady) error at --direct (code) | queued | last open child of 065-F |

## Dependency Order

```text
065-F (umbrella)
   └─contains─▶ 065.004-T (NotReady message → --direct hint + test)
```

Feature parent 065-F is first in the manifest; single delivery task, no intra-shipment edges.

## Step 5.5 Scope Guard

**In scope** = exactly the 065.004-T acceptance criteria:
1. Append an actionable `--direct` / `ENGRAM_DIRECT=1` hint to the
   `DaemonError::NotReady` `#[error(...)]` string (`src/errors/mod.rs:161-162`).
2. A test-first unit test asserting the augmented message (`--direct`,
   `ENGRAM_DIRECT=1`, preserved `{timeout_ms}` interpolation, **no** stray `{`),
   plus a wire-contract regression guard (`code`/`name` unchanged).
3. Quality gates green (fmt / clippy pedantic -D warnings / test / audit).

**Out of scope (needs a new plan / new items):** re-wording the IPC timeout
(`IpcError::Timeout`, `src/errors/mod.rs:149`); any change to the `ENGRAM_DIRECT`
`BoolishValueParser`; the optional `src/bin/engram.rs` top-level help enrichment
(**default OFF** — operator opt-in only; if adopted, update `--help`
snapshot/contract tests in the same PR); anything beyond `≤3` files.

## Ship Notes (binding conditions from 073.001-R)

- **C1:** hint text MUST be brace-free — only the existing `{timeout_ms}`
  interpolation is permitted (thiserror parses `{`/`}` as interpolation).
- **C2:** do **not** modify the `ENGRAM_DIRECT` `BoolishValueParser`
  (see `docs/compound/clap-bool-env-var-boolish-value-parser-2026-05-08.md`).
- **C3:** edit `NotReady` only; leave `IpcError::Timeout` wording alone.
- **C4:** default to `src/errors/mod.rs` + its `mod tests` (`:719`) only (≤2
  files); add the `bin/engram.rs` help hint only on operator opt-in (then update
  help snapshots).
- **Precedent:** `IpcError::VersionMismatch` (`src/errors/mod.rs:151-154`) already
  carries a longer actionable message — same pattern.
- **DoD gates:** `cargo fmt --all -- --check`; `cargo clippy --all-targets -- -D
  warnings -D clippy::pedantic`; `cargo test --all-targets`; `cargo audit`.

## Blocked Returns

<!-- BEGIN:blocked-returns -->
None.
<!-- END:blocked-returns -->

