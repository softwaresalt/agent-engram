---
artifact_type: task
created_at: 2026-08-24T00:06:31.3630427Z
id: 130.002-T
labels:
    - mcp
    - shim
    - transport
    - green
parent_id: 130-F
priority: high
references:
    - docs/exec-plans/2026-08-23-copilot-server-discover-compat-plan.md
status: queued
title: 'GREEN: pre-initialize server/discover compatibility filter in run_shim'
updated_at: 2026-08-24T00:06:31.3630427Z
---

Plan unit U2. Minimum change to turn U1 green. Introduce an interposing transport wrapper between rmcp::transport::io::stdio() and rmcp::serve_server in run_shim (src/shim/transport.rs). Armed at session start, disarms permanently on the first initialize. Behavior: server/discover WITH an id -> answer -32601 on stdout echoing the id type-preserving, do not forward, stay armed; server/discover WITHOUT an id -> drop silently, emit no frame; EVERYTHING ELSE -> forward to rmcp unchanged (review F2, narrow allowlist). Gated by ENGRAM_MCP_PREINIT_COMPAT, default enabled, 0 disables and restores strict ordering (review F1). Framing delegates to the underlying stdio transport; only decoded frames are inspected. No new dependency (serde_json already present). Size: <=2h. Width: shim transport only.