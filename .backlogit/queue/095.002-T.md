---
artifact_type: task
created_at: 2026-07-22T14:59:35.1724056-07:00
id: 095.002-T
parent_id: 095-F
priority: medium
status: queued
title: 'U1a: Lineage subgraph schema + shared value types (dataset_node/lineage_edge/lineage_edge_evidence/lineage_index_state)'
updated_at: 2026-07-22T17:53:00.1826356-07:00
---

Add the lineage subgraph SCHEMA + the shared lineage value types (the db writers + delete cascade are split into U1a'/095.012-T per AR-04's 2-hour granularity rule), mirroring the powerbi_node/powerbi_edge domain-subgraph pattern. In src/db/cozo_backend/schema.rs add FOUR relations near the powerbi templates (schema.rs:1029-1057) and register them in the bootstrap scripts array (schema.rs:86-97): (1) CREATE_DATASET_NODE {id => name, kind} — CANONICAL FIELDS ONLY (no notebook_path/source_path/content_hash on the node — PR #281 finding 1), kind in {table, path}, id = authority-bound canonical key that EMBEDS the stable authority (table = 3-part catalog.schema.table + a REQUIRED metastore_authority_id for the metastore/catalog authority; path = absolute URI + storage authority) so two metastores sharing catalog.schema.table yield DISTINCT nodes, and an unmapped catalog => no authority => no node (fail-closed, AR-01); (2) CREATE_LINEAGE_EDGE {from_id, to_id, edge_type => ingested_at}, v1 edge_type = lineage_derives_from ONLY, ORIENTED from_id = written target -> to_id = read source (data flows source->target; the edge encodes derives-from, AR-05); ingested_at is REPLACED on every re-index (:put overwrite; renamed from first_ingested_at to reflect replace semantics, AR-15); NO per-notebook source_path on the edge (finding 2); (3) CREATE_LINEAGE_EDGE_EVIDENCE {from_id, to_id, edge_type, notebook_path, chunk_index => content_hash, ingested_at} — chunk_index is part of the KEY (finding E1) so the same edge in two cells yields TWO rows; notebook-scope delete removes ALL rows by notebook_path; (4) CREATE_LINEAGE_INDEX_STATE {notebook_path => extractor_version, indexed_at} — a FRESHNESS-STATE relation (not identity/provenance) that DURABLY persists the lineage-extractor semantic version per notebook so U4b's version-fingerprint skip check survives process restarts (content_record carries only content_hash — src/models/content.rs:26 — with no version slot; the 'separately specified version-state design' blessed by cycle-5 finding F5). Idempotent :create with migration guard. SHARED VALUE TYPES (foundation, defined once here so no unit invents its own carrier; plain data, no logic): LineageEndpoint (resolved authority-bound dataset ref: kind in {table,path} + canonical id); LineageEdgeCandidate { target: LineageEndpoint, sources: Vec<LineageEndpoint> } — the STATEMENT-GROUPED, DIRECTIONAL edge carrier that U2b (Python) and U3 (SQL) both PRODUCE and U4 CONSUMES: each candidate pairs one write/target endpoint with the set of read/source endpoints for that one statement/dataflow, so a CTAS with multiple sources or multiple statements per cell keeps its target<->source pairing (cycle-5 F3/F4 — a flat Vec<LineageEndpoint> loses which target each source belongs to); LineageAuthorityContext (the trusted-authority handle the U2/U3 extractors consume — carries the metastore_authority_id + catalog->authority mapping; its config surface + live propagation are U1b/095.011-T). SparkLineageEvent stays owned by U2. NOTE: the node/edge/evidence batch-upsert helpers (upsert_dataset_nodes / upsert_lineage_edges / upsert_lineage_edge_evidence) and the delete_lineage_by_scope cascade — plus their round-trip / deletion tests — are split into U1a' (095.012-T, which DEPENDS ON this task) per AR-04; the VERSION-STATE helpers (upsert_lineage_index_state / lineage_index_version) and their durable version-state test are split into U1a" (095.014-T, H3). U1a' (095.012-T) and U1a" (095.014-T) do NOT overlap: 012 owns ONLY the node/edge/evidence writers + the 4-step scope-delete/GC cascade, 014 owns ONLY the version-state read/write helpers. Tests (U1a): idempotent bootstrap re-run (all FOUR relations registered under the migration guard); relation-shape assertions (keys/values as specified — chunk_index in the evidence KEY, edge oriented from_id=target->to_id=source, ingested_at replace-semantics); value-type construction (LineageEndpoint; LineageEdgeCandidate statement-grouping target<->sources; LineageAuthorityContext carries authority id + catalog->authority mapping). The writer round-trip / canonical-identity / shared-edge-deletion / same-edge-two-cells tests live in U1a' (095.012-T); the durable version-state test lives in U1a" (095.014-T). Files: src/db/cozo_backend/schema.rs (+ a small shared lineage value-types module). <=2 files, db domain. This is the FOUNDATION unit; U1a' (095.012-T) adds the writers, and the U2b/U3 extractors return LineageEdgeCandidates that U4 flattens + persists via those writers. Plan Unit 1a.