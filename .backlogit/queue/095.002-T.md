---
artifact_type: task
created_at: 2026-07-22T14:59:35.1724056-07:00
id: 095.002-T
parent_id: 095-F
priority: medium
status: queued
title: 'U1: Lineage subgraph schema + db write helpers (dataset_node/lineage_edge/lineage_evidence)'
updated_at: 2026-07-22T14:59:35.1724056-07:00
---

Add the lineage subgraph schema + db writers, mirroring the powerbi_node/powerbi_edge domain-subgraph pattern. In src/db/cozo_backend/schema.rs add three relations near the powerbi templates (schema.rs:1029-1057) and register them in the bootstrap scripts array (schema.rs:86-97): CREATE_DATASET_NODE {id => name, kind, notebook_path, source_path, content_hash, ingested_at} where kind is constrained to {table, path} and id is the authority-bound canonical key (table = 3-part catalog.schema.table + trusted metastore authority; path = absolute URI + storage authority); CREATE_LINEAGE_EDGE {from_id, to_id, edge_type => source_path} with v1 edge_type = lineage_derives_from ONLY (role edges deferred); CREATE_LINEAGE_EVIDENCE {dataset_id, notebook_path => content_hash, chunk_index, ingested_at} (multi-source evidence separated from canonical identity — two notebooks referencing one dataset must NOT clobber). Idempotent :create with migration guard. In src/db/cozo_queries.rs add batch-upsert helpers upsert_dataset_nodes / upsert_lineage_edges / upsert_lineage_evidence (mirror upsert_powerbi_nodes/edges at cozo_queries.rs:5471-5520, using :put) + delete_lineage_by_scope(notebook_path) (mirror delete_content_records_by_scope). Tests: idempotent bootstrap re-run; node/edge/evidence round-trip; two evidence rows for one dataset from different notebooks coexist; scope-delete removes one notebook's evidence while a still-evidenced dataset survives. Domain: schema/db (width-isolated — no parser work here). This is the FOUNDATION unit; U2 and U3 extractors return endpoints that U4 persists via THESE writers. Plan Unit 1.