---
artifact_type: task
created_at: 2026-07-22T14:59:35.1724056-07:00
id: 095.002-T
parent_id: 095-F
priority: medium
status: queued
title: 'U1: Lineage subgraph schema + db write helpers (dataset_node/lineage_edge/lineage_edge_evidence)'
updated_at: 2026-07-22T17:53:00.1826356-07:00
---

Add the lineage subgraph schema + db writers + the shared lineage value types, mirroring the powerbi_node/powerbi_edge domain-subgraph pattern. In src/db/cozo_backend/schema.rs add FOUR relations near the powerbi templates (schema.rs:1029-1057) and register them in the bootstrap scripts array (schema.rs:86-97): (1) CREATE_DATASET_NODE {id => name, kind} — CANONICAL FIELDS ONLY (no notebook_path/source_path/content_hash on the node — PR #281 finding 1), kind in {table, path}, id = authority-bound canonical key (table = 3-part catalog.schema.table + trusted metastore authority; path = absolute URI + storage authority); (2) CREATE_LINEAGE_EDGE {from_id, to_id, edge_type => first_ingested_at}, v1 edge_type = lineage_derives_from ONLY (NO per-notebook source_path on the edge — finding 2); (3) CREATE_LINEAGE_EDGE_EVIDENCE {from_id, to_id, edge_type, notebook_path, chunk_index => content_hash, ingested_at} — chunk_index is part of the KEY (finding E1) so the same edge in two cells yields TWO rows; notebook-scope delete removes ALL rows by notebook_path; (4) CREATE_LINEAGE_INDEX_STATE {notebook_path => extractor_version, indexed_at} — a FRESHNESS-STATE relation (not identity/provenance) that DURABLY persists the lineage-extractor semantic version per notebook so U4b's version-fingerprint skip check survives process restarts (content_record carries only content_hash — src/models/content.rs:26 — with no version slot; the 'separately specified version-state design' blessed by cycle-5 finding F5). Idempotent :create with migration guard. SHARED VALUE TYPES (foundation, defined once here so no unit invents its own carrier; plain data, no logic): LineageEndpoint (resolved authority-bound dataset ref: kind in {table,path} + canonical id); LineageEdgeCandidate { target: LineageEndpoint, sources: Vec<LineageEndpoint> } — the STATEMENT-GROUPED, DIRECTIONAL edge carrier that U2b (Python) and U3 (SQL) both PRODUCE and U4 CONSUMES: each candidate pairs one write/target endpoint with the set of read/source endpoints for that one statement/dataflow, so a CTAS with multiple sources or multiple statements per cell keeps its target<->source pairing (cycle-5 F3/F4 — a flat Vec<LineageEndpoint> loses which target each source belongs to); LineageAuthorityContext (the trusted-authority handle the U2/U3 extractors consume — its config surface + live propagation are U1b/095.011-T). SparkLineageEvent stays owned by U2. In src/db/cozo_queries.rs add batch-upsert helpers upsert_dataset_nodes / upsert_lineage_edges / upsert_lineage_edge_evidence + upsert_lineage_index_state / lineage_index_version(path) (mirror upsert_powerbi_nodes/edges at cozo_queries.rs:5471-5520, :put) + delete_lineage_by_scope(notebook_path) as a 3-STEP FAIL-CLOSED CASCADE: (1) delete ALL this notebook's evidence rows (every cell, matched by notebook_path); (2) GC any lineage_edge with zero remaining evidence; (3) GC any dataset_node no longer referenced by any surviving edge. Tests: idempotent bootstrap re-run; node/edge/evidence round-trip; CANONICAL-IDENTITY (same dataset from two notebooks keeps ONE node with both evidence rows, no clobber); SHARED-EDGE DELETION (same edge evidenced by two notebooks; scope-delete one leaves the edge, both removes it); SAME-EDGE/TWO-CELLS (same edge from two cells -> two evidence rows distinct chunk_index; scope-delete removes both, finding E1); VERSION-STATE (upsert_lineage_index_state then lineage_index_version round-trips the extractor version and SURVIVES a store re-open — durable, not in-memory, cycle-5 F5). Files: src/db/cozo_backend/schema.rs, src/db/cozo_queries.rs (+ a small shared lineage value-types module). <=3 files, db domain. This is the FOUNDATION unit; the U2b/U3 extractors return LineageEdgeCandidates that U4 flattens + persists via THESE writers. Plan Unit 1.