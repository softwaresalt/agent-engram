---
artifact_type: task
created_at: 2026-07-22T14:59:35.1724056-07:00
id: 095.002-T
parent_id: 095-F
priority: medium
status: queued
title: 'U1: Lineage subgraph schema + db write helpers (dataset_node/lineage_edge/lineage_edge_evidence)'
updated_at: 2026-07-22T15:31:34.4730628-07:00
---

Add the lineage subgraph schema + db writers, mirroring the powerbi_node/powerbi_edge domain-subgraph pattern. In src/db/cozo_backend/schema.rs add three relations near the powerbi templates (schema.rs:1029-1057) and register them in the bootstrap scripts array (schema.rs:86-97): CREATE_DATASET_NODE {id => name, kind} — CANONICAL FIELDS ONLY (no notebook_path/source_path/content_hash on the node; those are per-notebook and MUST live in evidence, else a 2nd notebook clobbers the 1st's provenance — PR #281 finding 1), where kind is constrained to {table, path} and id is the authority-bound canonical key (table = 3-part catalog.schema.table + trusted metastore authority; path = absolute URI + storage authority); CREATE_LINEAGE_EDGE {from_id, to_id, edge_type => first_ingested_at} with v1 edge_type = lineage_derives_from ONLY (role edges deferred; NO per-notebook source_path on the edge — PR #281 finding 2); CREATE_LINEAGE_EDGE_EVIDENCE {from_id, to_id, edge_type, notebook_path => content_hash, chunk_index, ingested_at} — per-notebook EDGE evidence so two notebooks emitting the same edge are independently scope-deletable. Idempotent :create with migration guard. In src/db/cozo_queries.rs add batch-upsert helpers upsert_dataset_nodes / upsert_lineage_edges / upsert_lineage_edge_evidence (mirror upsert_powerbi_nodes/edges at cozo_queries.rs:5471-5520, using :put) + delete_lineage_by_scope(notebook_path) as a 3-STEP FAIL-CLOSED CASCADE: (1) delete this notebook's evidence rows; (2) GC any lineage_edge with zero remaining evidence; (3) GC any dataset_node no longer referenced by any surviving edge. Tests: idempotent bootstrap re-run; node/edge/evidence round-trip; CANONICAL-IDENTITY — same dataset from two notebooks keeps ONE node with both evidence rows (no clobber); SHARED-EDGE DELETION — same edge evidenced by two notebooks, scope-deleting one leaves the edge (still evidenced) and scope-deleting both removes it. Domain: schema/db (width-isolated — no parser work here). This is the FOUNDATION unit; U2/U3 extractors return endpoints that U4 persists via THESE writers. Plan Unit 1.