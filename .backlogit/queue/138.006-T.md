---
artifact_type: task
created_at: 2026-08-27T18:08:07.8398487Z
dependencies:
    - 138.002-T
    - 138.013-T
id: 138.006-T
labels:
    - shim
    - transport
    - concurrency
    - single-flight
    - tests
    - performance
parent_id: 138-F
priority: high
references:
    - src/shim/transport.rs
    - tests/contract/shim_stdio_initialize_test.rs
status: active
title: 'Red harness: concurrency and amplification C1, C2, C3, N2'
updated_at: 2026-08-27T21:09:06.2674988Z
---

PHASE 2 (harness). Test-only; NO production behavior edit. Lives as an in-crate #[cfg(test)] mod in src/shim/transport.rs using the 138.013-T seams, so ShimHandler, StartupOutcome and EndpointResolutionError stay crate-private (no pub API widening). Revision 1 bundled the production seam INTO this task - that width violation is fixed: the seam is now 138.013-T and lands first. C1 single-flight, using the CORRECTED topology (revision 1's barrier-inside-the-probe DEADLOCKS because forwarding_endpoint takes recovery_lock at src/shim/transport.rs:129 BEFORE invoking the probe at :143, so only one caller ever reaches the probe and the other seven park on the mutex): synchronize 8 caller tasks with a start barrier OUTSIDE the handler; the seam probe signals probe_entered on entry then awaits a test-controlled release; while held assert probe_count==1; after release and join assert probe_count==1 and the other 7 returned recoverable payloads. NO sleeps anywhere; the probe NEVER waits on sibling callers. C2 cooldown: #[tokio::test(start_paused = true)] (tokio::time::pause requires a current-thread runtime); after a transient probe at t0, a call at t0+50ms performs 0 probes and returns recoverable==true, and a call after t0+250ms performs exactly 1 probe. Requires the 138.013-T tokio::time::Instant clock seam. C3 terminal latch under concurrency: 8 concurrent calls where the single in-flight probe resolves Terminal -> all 8 return the terminal payload, total probe count 1, and a 9th call afterwards performs 0 probes. N2: a terminal outcome consumes exactly 1 _health round trip, not 2. C1/C3 use REAL time with no sleeps; determinism comes from the entered/release signals. 5x repeat, zero flakes. All NEW and must start RED.