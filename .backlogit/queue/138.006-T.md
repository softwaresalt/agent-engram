---
artifact_type: task
created_at: 2026-08-27T18:08:07.8398487Z
dependencies:
    - 138.004-T
id: 138.006-T
labels:
    - shim
    - transport
    - concurrency
    - single-flight
    - tests
    - performance
parent_id: 138-F
priority: high
references:
    - src/shim/transport.rs
    - tests/contract/shim_stdio_initialize_test.rs
status: queued
title: Probe seam plus concurrency proof for session-wide single-flight, cooldown, and probe amplification
updated_at: 2026-08-27T18:08:07.8398487Z
---

Close the coverage gap the Copilot review named: the existing recovery test is sequential, so the session-wide single-flight Mutex and the 250ms cooldown are asserted by code shape, not by behavior. Add the D6 seam: a probe indirection field on ShimHandler defaulting to the real lifecycle::probe_health, plus a doc(hidden) with_probe constructor for tests. ShimHandler::new keeps its current signature and always installs the real probe, so production and test execute the SAME path - no cfg(test) branching inside forwarding_endpoint. Then prove C1 (single-flight), C2 (cooldown), C3 (terminal latch under concurrency), N1 and N2 (no probe amplification, no extra round trip).