---
artifact_type: task
commit: 4cab736e05ebd832e048a0338949ef62c0089b2d
created_at: 2026-08-24T22:53:58.5918385Z
id: 131.001-T
labels:
    - test-first
    - red
    - compile-neutral
    - otlp
    - cargo
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - Cargo.toml
status: queued
title: 'RED: prove OTLP graph and feature compilation with a compiling meta-harness'
updated_at: 2026-08-25T05:17:57.866345Z
---

Before any manifest or production edit, add one top-level integration meta-harness that is itself built with otlp-export disabled. It must use only currently available std/dev-dependency APIs, must not import build_otlp_layer or name any planned production symbol, and must invoke isolated cargo tree and cargo check subprocesses against otlp-export. The harness therefore reaches a compiling-and-running RED state even though the feature target does not compile.

<!-- BEGIN:acceptance-criteria -->
1. The only change is tests/otlp_feature_compile_contract_test.rs, recorded before Cargo.toml, Cargo.lock, or source edits. 2. Exact RED command: cargo test --no-default-features --features cozo-backend --test otlp_feature_compile_contract_test -- --nocapture. The integration test binary must compile and begin running with otlp-export disabled. 3. Assertion otlp_dependency_graph_uses_only_026 captures cargo tree --no-default-features --features cozo-backend,otlp-export and fails because the output contains opentelemetry v0.25 alongside v0.26; 131.002-T owns that GREEN. 4. Assertion otlp_export_feature_compiles captures an isolated CARGO_TARGET_DIR cargo check --no-default-features --features cozo-backend,otlp-export --lib and fails because status is 101 with the current SdkTracerProvider / SpanExporter::builder unsupported-0.26 diagnostics; 131.003-T owns that GREEN. 5. Subprocess stderr is included in assertion output, while an invocation failure is distinguished from the intended nonzero cargo result. No nested test command, missing-symbol compile probe, external collector, network oracle, socket, sleep, or retry is used. 6. Record both failing assertion names and expected diagnostics on this task; no production symbol is referenced by the test compiler.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Compile-neutral test-contract skill only; exactly 1 test file, at most 3 test/helper functions, 2 scenarios, and 75 minutes. Atomic milestone: the repository has a runnable failing harness whose failures describe the split graph and broken optional-feature compilation rather than a test compilation error.
<!-- END:implementation-notes -->
