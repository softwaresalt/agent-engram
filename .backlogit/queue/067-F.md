---
artifact_type: feature
created_at: 2026-07-02T17:47:27.025032-07:00
custom_fields:
    harness_status: pending
    source_deliberation_id: 012-D
    source_stash_id: 7D8F395B
    source_stash_kind: feature
    source_stash_path: stash.jsonl
    source_stash_priority: high
    source_stash_text: 'Engram query/usage telemetry JSONL OUTPUT (emit, not ingest). PIVOT: autoharness now owns measuring harness effectiveness, so engram''s Phase-2 telemetry INGESTION is cancelled (supersedes/obsoletes deferred tasks 064.005-T ExecutionEpoch CozoDB schema and 064.006-T JSONL ingestion under feature 064-F). INSTEAD, engram must EMIT its own query/usage telemetry to append-only JSONL so autoharness gains visibility into engram queries and usage. Requirement: on each engram tool/query invocation (e.g. unified_search, query_memory, map_code, list_symbols, impact_analysis, query_graph, sync_workspace, set_workspace), append a structured record to a JSONL file. Natural location .engram/metrics/usage.jsonl (already the test-anticipated convention in src/daemon/watcher.rs tests; .engram is correctly excluded from the file-watcher). Each record should carry enough for autoharness to measure usage/effectiveness: ISO-8601 timestamp, workspace + branch, tool/query name, coarse params (e.g. query text or a hash/summary, limits), result counts, latency ms, and token-savings/delivery figures. Build on existing engram infrastructure: metrics service src/services/metrics.rs and the atomic temp-file-then-rename JSONL write pattern in src/services/dehydration.rs (nodes.jsonl/edges.jsonl). Cross-platform path handling (Windows/Linux). Append-only, rotation/size considerations, and a config toggle (opt-in/out, path override) likely needed. Grounding: NO existing usage.jsonl emission today; the reports (token-savings/health/branch-metrics/retry-metrics) and metrics service exist but do not persist per-call usage telemetry to JSONL. ADDENDUM (operator, 2026-07-02): (1) CORRELATION ID — engram MUST accept a caller-supplied correlation id as a parameterized value on its tool/query calls (a new optional parameter on the MCP tool schemas / CLI, threaded through to the telemetry record) so autoharness can pass a correlation id and relate engram queries to other harness activity. The correlation id must be written through to each usage.jsonl record (e.g. field ''correlation_id''); absent when not supplied. This affects the MCP tool input schemas (add an optional correlation_id param across the emitting tools) and the record schema. (2) TIMESTAMPS — each telemetry record MUST include an explicit date/time stamp (ISO-8601, with timezone/UTC) in the output; treat this as a required, pinned field.'
dependencies:
    - 064-F
id: 067-F
priority: high
status: queued
title: Engram usage-telemetry EMIT (per-call usage.jsonl output)
updated_at: 2026-07-02T17:47:55.2724932-07:00
---

Engram EMITS per-call query/usage telemetry to append-only .engram/metrics/{branch}/usage.jsonl so autoharness can measure engram usage/effectiveness. PIVOT from telemetry INGEST/sink (064-F Phase 2c/2d, superseded) to EMIT/source. EXTENDS the existing emitter (src/services/metrics.rs UsageEvent -> usage.jsonl): adds schema_version=2, caller-supplied correlation_id (via _meta.correlation_id), latency_ms, explicit workspace, coarse params_summary(hash/len/limit); adds config path-override + size-cap rotation; closes choke-point coverage for set_workspace/sync_workspace/index_workspace at src/tools/mod.rs::dispatch. Timestamp pinned ISO-8601 UTC. Source: stash 7D8F395B; deliberation 012-D; decision docs/decisions/decision-017; plan docs/exec-plans/2026-07-02-engram-usage-telemetry-emit-plan.md. Related to 064-F (supersedes its Phase 2 ingestion scope).

<!-- BEGIN:dod -->
- usage.jsonl records include schema_version=2, ISO-8601 UTC timestamp, tool_name, workspace, branch, correlation_id (when supplied), latency_ms, outcome, result counts, token figures, and coarse params_summary (hash/len/limit; no raw query text).
- correlation_id extracted at the single choke point src/tools/mod.rs::dispatch via _meta.correlation_id; omitted when absent.
- Rotation bounded by max_file_bytes / max_rotated_files; usage_path_override validated within workspace root.
- Three-tier tests green: contract (record shape) + integration (daemon/IPC correlation-id flow) + unit/proptest (schema + rotation); clippy pedantic clean.
- Back-compat: additive-only fields; no existing field renamed or removed.
- Feature linked to stash 7D8F395B, deliberation 012-D, decision-017; related_to 064-F (supersedes its Phase 2 ingestion scope).
<!-- END:dod -->

<!-- BEGIN:goals -->
- Emit per-call engram usage telemetry to append-only .engram/metrics/{branch}/usage.jsonl for autoharness consumption.
- Add caller-supplied correlation_id (via _meta.correlation_id) threaded to each record.
- Pin ISO-8601 UTC timestamp and schema_version=2 on every record (stable public contract for autoharness).
- Add config path-override + size-cap rotation; keep opt-out default (enabled=true).
- Close choke-point coverage for set_workspace / sync_workspace / index_workspace.
<!-- END:goals -->