---
artifact_type: subtask
created_at: 2026-09-11T01:24:25.5676237Z
custom_fields:
    blocked_reason: 'Terminal Revision 3 plan-review FAIL after three cycles; do not claim. Requires a fresh operator-authorized Stage cycle.'
dependencies:
    - 144.001.002-ST
id: 144.001.005-ST
labels:
    - daemon
    - observability
    - measurement
parent_id: 144.001-T
priority: critical
status: blocked
title: Per-phase peak RSS and the G3 reference environment
updated_at: 2026-09-11T03:17:22.8297139Z
---

PERFORMANCE BASELINE AND THE REFERENCE ENVIRONMENT. Records per-phase peak RSS and the baseline dimensions that make gates G1, G2, and G3 MEASURABLE rather than asserted. Absorbs hardening constraint H15.

REVISION 3 SCOPE: G1, G2, and G3 are recorded here as MEASURED BASELINE DIMENSIONS AND OBSERVED VALUES. They are NOT implementation promises in 144-S. Enforcement of any threshold belongs to a future implementation plan written after this measurement reports.

AC1 (PER-PHASE PEAK RSS): peak resident memory is recorded PER PHASE against the 144.001.002-ST phase set, using the platform-named counter. Windows working set / private bytes and Linux RSS are NOT the same counter and the recorded figure names which one it is, per platform.
AC2 (REFERENCE ENVIRONMENT, ALL SIX ATTRIBUTES): the reference environment is recorded before any G1/G2/G3 figure is used - named reference workspace with its file count and current DB size, platform matrix, cold vs warm OS page-cache state, repetition count n, and the reported statistic (MEDIAN AND WORST CASE, both). Choice of reference workspace is operator decision OD-4.
AC3 (G1/G2/G3 BASELINE DIMENSIONS RECORDED): the report records, as OBSERVED BASELINE VALUES on that environment, (i) G1 - time to ready for a branch created from a synced parent with an identical tree, (ii) G2 - time to ready on a warm reopen, and (iii) G3 - daemon peak startup RSS against the observed 1.84 GB failure value. Each is reported with median AND worst case. No threshold is enforced by this leaf.
AC4 (PID 30528 EXCLUSION, EXPLICIT): the report states explicitly that the incident daemon PID 30528 was NOT running during measurement, and records the evidence for that statement. Timings or RSS figures taken alongside a live CPU-burning daemon are invalid - they look like data and are not.
AC5 (OD-1 PRECHECK IS A HALT CONDITION, NOT A WORKAROUND): operator decision OD-1 (disposition of the still-running incident daemon PID 30528, ActionResult blocked pending stop approval) MUST be resolved before any figure is recorded. If the precheck cannot be satisfied, this leaf HALTS AND RETURNS TO STAGE. It MUST NOT be worked around by measuring anyway, and MUST NOT be satisfied by asserting the daemon is idle without evidence.
AC6 (INSTRUMENTATION OVERHEAD WITHIN NOISE): heartbeat and instrumentation intervals are coarse relative to phase durations, and startup wall-time regression is within noise on the reference environment (risk H8).

Files: startup instrumentation module plus the reference-environment and baseline report documents. Execution posture: measurement-first.
Gates (authoritative, in order): cargo fmt --all -- --check; cargo clippy --all-targets -- -D warnings -D clippy::pedantic; cargo dev-test; cargo audit. cargo build is an optional precheck only, not a gate. Do NOT use cargo ci or cargo lint (--all-features is broken on this baseline, risk H6).
Size: M | Complexity: medium.
