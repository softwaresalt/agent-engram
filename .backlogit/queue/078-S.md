---
artifact_type: shipment
created_at: 2026-07-10T19:23:28.1200479-07:00
custom_fields:
    items:
        - 082.001-T
        - 082.002-T
        - 082.003-T
        - 082.011-T
        - 082.008-T
        - 082.009-T
        - 082.010-T
        - 082.004-T
id: 078-S
status: queued
title: 'SHIP-2: rec1-calledges cross-file & method-call resolution (082-F re-harvested: 001,002,003,011,008,009,010,004) — eval gate (SHIP-1 S1+S3) satisfied on main'
updated_at: 2026-07-11T01:22:00.0000000-07:00
---

Ships the Rust-first rec1-calledges tasks, re-harvested (Copilot reviews #239 &
#240, stash CC5D369E) into single-width, ≤4-scenario executable items:
082.001-T (method-call capture) → 082.002-T (staging capture) →
082.003-T (provenance storage, DB layer) → 082.011-T (CodeEdge.resolution model
propagation) → 082.008-T (post-pass resolution) → 082.009-T (staging lifecycle) →
082.010-T (rollback down-migration) → 082.004-T (acceptance via 081-F eval). Does
NOT include or archive the parent feature 082-F: the deferred peer-language
fan-out tasks (082.005-T Python, 082.006-T TypeScript, 082.007-T Go) remain queued
under 082-F, so 082-F stays active after this shipment and is archived only once
every child completes in a later follow-on shipment. This avoids leaving queued
tasks orphaned under an archived parent.

RE-HARVEST COMPLETE (Copilot reviews #239 & #240): the over-limit tasks were split
into single-width items — 082.002-T (staging capture) + 082.009-T (staging
lifecycle: clear-before-reindex, deletion cleanup, stale calls_resolved_singleton
retraction); 082.003-T (DB-layer provenance storage: calls_edge.resolution
migration + count/enumerate query) + 082.011-T (model/serialization:
CodeEdge.resolution + dehydration + proptest) + 082.008-T (post-pass unambiguous
resolution) — the full staged_call lifecycle + resolution migration + read-query
contract was propagated INTO the executable task bodies (not just the plan), an
explicit rollback down-migration task (082.010-T) was added with its scenarios
reordered so every provenance count runs before the column drop, and 082.004-T was
reduced from 5 to 4 scenarios. Dependencies: storage (003) precedes model (011)
precedes post-pass (008); capture (002) precedes lifecycle (009); 082.008-T depends
on BOTH 003 and 011; 082.004-T and the language fan-out (005/006/007) depend on the
post-pass task (008). Tracked by stash CC5D369E.

Now QUEUED. The eval-subsystem gate (SHIP-1 / 077-S / 081-F, including 081.001-T
(S1 contract/model) and 081.005-T (S3 graph metric)) is ALREADY SHIPPED and
archived on main, so this dependency is satisfied and no upstream work remains to
wait on — 078-S is claimable as soon as this re-harvest merges.

