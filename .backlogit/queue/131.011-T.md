---
artifact_type: task
created_at: 2026-08-25T05:22:16.8397935Z
dependencies:
    - 131.010-T
id: 131.011-T
labels:
    - scaffold
    - behavior-neutral
    - otlp
    - rust
    - daemon-lifecycle
    - cleanup-isolation
    - spawn-failure
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/bin/engram.rs
status: queued
title: 'SCAFFOLD: add an injectable cleanup-worker spawner without launching work'
updated_at: 2026-08-25T07:45:46.9117001Z
---

Add a behavior-neutral cleanup-worker spawner/factory seam to the daemon lifecycle runner. The production adapter is a thin safe wrapper over std::thread::Builder::spawn and returns its Result; the runner still launches no worker in this task. The injectable fake makes operating-system spawn failure deterministic for the next RED without panic-based resource exhaustion.

<!-- BEGIN:acceptance-criteria -->
1. 131.010-T is complete with compiling cleanup-sequencing/deadline RED evidence and remains the explicit dependency. 2. In src/bin/engram.rs, define one object-safe or equivalent injectable CleanupWorkerSpawner abstraction and one production NativeThreadSpawner adapter. The adapter uses std::thread::Builder with a stable worker name and returns std::io::Result or an equivalent typed Result; it never calls std::thread::spawn. 3. The job payload owns only an Arc-backed cleanup ownership cell while the caller retains another Arc until spawn succeeds. A failed spawn therefore cannot drop the sole explicit provider owner or fall back to synchronous force_flush, shutdown, or provider Drop as claimed success. 4. The daemon runner accepts the injected spawner but deliberately does not invoke it yet, so existing production behavior is unchanged and 131.010-T remains RED. 5. No unwrap, expect, panic, unsafe, spawn_blocking, runtime task, join, retry, environment mutation, manifest, lockfile, config, provider construction, endpoint, or attachment change is included. 6. The task records the exact compile check for the bin target; 131.012-T owns the deterministic forced-failure RED and 131.013-T owns launch/error GREEN.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Daemon cleanup-spawner interface skill only; exactly 1 production file, at most 3 functions or trait methods, 2 compile/ownership scenario groups, and 60 minutes. Atomic milestone: a behavior-neutral safe injection point exists before any spawn-failure behavior test or worker launch implementation.
<!-- END:implementation-notes -->