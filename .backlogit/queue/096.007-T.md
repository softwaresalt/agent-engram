---
artifact_type: task
created_at: 2026-07-23T18:46:42.6277749-07:00
dependencies:
    - 096.006-T
    - 096.009-T
id: 096.007-T
labels:
    - python
    - canonical-identity
    - fail-closed
    - code
parent_id: 096-F
priority: medium
references:
    - src/services/code_graph.rs
status: queued
title: 'T5c: Shadow guard -- module receiver + bare import (fail closed on rebind)'
updated_at: 2026-07-23T20:08:53.7825251-07:00
---



<!-- BEGIN:acceptance-criteria -->
- Clean cases resolve: import bar; bar.parse() AND `from bar import parse; parse()` (no rebind).
- M1 module-receiver rebind table -> NO edge: {assignment, module-level `import bar; bar = factory(); def g(): bar.parse()`, for bar in ..., with ... as bar, except ... as bar, (bar := ...), bar += ..., def g(bar): ...}.
- Q1 bare-import ASSIGNMENT shadow: `from bar import parse; parse = factory(); parse()` -> NO edge.
- Q1 bare-import PARAMETER shadow: `from bar import parse` with `def g(parse): parse()` -> NO edge.
- cargo test --test integration_calls_recall_acceptance green; clippy --all-targets clean; fmt clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Fail-closed guard against any rebind shadowing an imported name -- whether a MODULE RECEIVER (import bar; bar = factory(); bar.parse()) OR a BARE CALLEE (from bar import parse; parse = factory(); parse() -- Q1). Static binding-only resolution would still bind to bar.parse -> a false edge (013-D violation). The bare-callee vector was left open by cycle-1 (guard covered only module receivers). R3: the shadow guard lives ENTIRELY in T5c and WRAPS T5b's guard-agnostic resolution (T5b does NOT call the guard); the dependency is one-directional T5c->T5b, keeping both independently completable and the DAG acyclic. In the T5b Python branch of canonical_target_for_staged_call, AFTER T5b computes a candidate canonical target, T5c fails closed when the resolved name is re-bound anywhere in the APPLICABLE scope (caller function AND module level), consuming the T2b (096.009-T) scope model. Applies to BOTH: qualifier_kind=="module" (the receiver name) AND qualifier_kind=="python_bare" (the bare callee name, when T5b resolved it via a FromImportSymbol binding or in-module def -- Q1). M1: the rebind scan covers the FULL target set: plain assignment, augmented assignment (+=), for targets, with ... as, except ... as, walrus (:=), function parameters, and module-level rebinding. Reuse the already-loaded caller/module source (T5b/T2b context); cheap tree-sitter scan. No new DB call. TDD RED->GREEN. Domain: code.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on T5b (096.006-T) and T2b (096.009-T); WRAPS T5b's guard-agnostic resolution (R3) -- dependency is strictly T5c->T5b, never reverse, so both are independently completable and the DAG is acyclic. Refines BOTH the qualifier_kind=="module" branch (receiver) AND the qualifier_kind=="python_bare" branch (bare callee, Q1). Keep <=4 test scenarios by consolidating the module-receiver rebind forms into one parametrized table (scenario 2) and adding two explicit bare-import shadow scenarios (assignment + parameter). Files (<=2): src/services/code_graph.rs, tests/integration/calls_recall_acceptance_test.rs.
<!-- END:implementation-notes -->