---
artifact_type: task
created_at: 2026-07-23T18:46:42.6277749-07:00
dependencies:
    - 096.006-T
    - 096.009-T
id: 096.007-T
labels:
    - python
    - canonical-identity
    - fail-closed
    - code
parent_id: 096-F
priority: medium
references:
    - src/services/code_graph.rs
status: queued
title: 'T5c: Shadow guard -- module receiver + bare import (fail closed on rebind)'
updated_at: 2026-07-23T20:08:53.7825251-07:00
---



<!-- BEGIN:acceptance-criteria -->
- Clean & ordered cases resolve (ALL callers are function-body -- the extractor stages Calls only from function bodies), with the winner-anchor survivals (F1): `import bar; def g(): bar.parse()` AND `from bar import parse; def caller(): parse()` (no rebind); `def parse(); from bar import parse; def caller(): parse()` -> bar.parse (winner = import; def precedes it -- Y2); AND `from bar import parse; def parse(): ...; def caller(): parse()` -> M.parse (winner = def; def-after-import SURVIVES because no rebind follows the winning def -- F1). C7-1 time-axis FAIL-CLOSED: a function-body call whose winning module binding is contested by a LATER module-level rebind (`def parse(): ...; def g(): parse(); from bar import parse`) -> NO edge (invocation order undecidable -- g may run before or after the rebind). Anchoring on "the import" (cycle-5 Y2) would wrongly drop the F1 M.parse edge. Module-level (top-level) call ordering is a v1 NON-GOAL -- top-level calls are not extracted (C9-3).
- M1/X4 module-receiver rebind table, EACH rebind occurring AFTER the WINNING binding -> NO edge: {assignment, module-level `import bar; bar = factory(); def g(): bar.parse()`, for bar in ..., with ... as bar, except ... as bar, (bar := ...), bar += ..., def g(bar): ..., `def bar(): ...`, `class bar: ...`, `del bar`, match/case capture `case bar:` (X4), AND a later `from n import *` (F4) e.g. `from bar import parse; from n import *; def caller(): parse()`}.
- Q1 bare-import ASSIGNMENT shadow (after the winner): `from bar import parse; parse = factory(); def caller(): parse()` -> NO edge.
- Q1 bare-import PARAMETER shadow: `from bar import parse` with `def g(parse): parse()` -> NO edge.
- C12-1/T-a FUNCTION-LOCAL POISONING (UnboundLocalError, Anchor C): the winner-to-call source-position ordering rule applies to MODULE-LEVEL rebinds ONLY. ANY in-function binder of the callee name (plain/augmented/annotated assignment, for/with-as/except-as target, walrus (:=), comprehension target, nested def/class of the name, del) makes the name function-local for the WHOLE body REGARDLESS OF POSITION -> a call to that name ANYWHERE in the function FAILS CLOSED (never resolves to a module-level import/binding). Regression: `from bar import parse; def g(): parse(); parse = factory()` -> NO edge (the earlier `parse()` is UnboundLocalError, not bar.parse).
- cargo test --test integration_calls_recall_acceptance green; clippy --all-targets clean; fmt clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Fail-closed guard against any rebind shadowing an imported name -- whether a MODULE RECEIVER (import bar; bar = factory(); bar.parse()) OR a BARE CALLEE (from bar import parse; parse = factory(); parse() -- Q1). Static binding-only resolution would still bind to bar.parse -> a false edge (013-D violation). The bare-callee vector was left open by cycle-1 (guard covered only module receivers). R3: the shadow guard lives ENTIRELY in T5c and WRAPS T5b's guard-agnostic resolution (T5b does NOT call the guard); the dependency is one-directional T5c->T5b, keeping both independently completable and the DAG acyclic. In the T5b Python branch of canonical_target_for_staged_call, AFTER T5b computes a candidate canonical target, T5c fails closed when the resolved name is re-bound by a LATER binding -- one that occurs AFTER the WINNING binding T5b actually resolved to (the import OR the in-module def -- F1, ORDER-AWARE ANCHOR) in binding order -- in the APPLICABLE scope (caller function AND module level), consuming the T2b (096.009-T) scope model. F1 RE-ANCHOR: the guard is anchored on the WINNER, NOT on "the import". For def-after-import (`from bar import parse; def parse(): ...; parse()`) T5b's last-binding-wins resolves to M.parse (the def is the later binding); since NO rebind follows that winning def, T5c must let the edge SURVIVE -- anchoring on "the import" (cycle-5 Y2) would wrongly treat the winning def as a post-import rebind and drop it. Applies to BOTH: qualifier_kind=="module" (the receiver name) AND qualifier_kind=="python_bare" (the bare callee name, when T5b resolved it via a FromImportSymbol binding or in-module def -- Q1). M1/X4/F4: the rebind scan covers the SHARED REBIND-FORM SET (RFS), defined ONCE in the plan Design-decision and consumed IDENTICALLY by T5a's in-file routing scan (096.005-T, C6) so the two scans can NEVER diverge on which forms count -- the shared artifact is the syntactic FORM SET, not the order logic (T5c applies it ORDER-AWARE below; T5a's routing use is the coarser order-agnostic "any RFS form present -> stage"). The RFS = plain assignment, augmented assignment (+=), for targets, with ... as, except ... as, walrus (:=), function parameters, def/class definitions, del statements, match/case capture patterns, a later module-scope `from N import *` star import (F4), and module-level rebinding. ORDER-AWARE (Y2 + F1 + C7-1): each of these invalidates the resolved edge when it occurs AFTER the WINNING binding (import or def) at module scope; a def/class/del/match-capture/star or any rebind BEFORE the winner does NOT invalidate it (the winner is then the last binding and T5b's resolution stands). C7-1 call-site-effective (FUNCTION-BODY CALLS ONLY -- the extractor stages Calls only from function bodies): a function-body call resolves to its module (or own function-local, T2b) binding ONLY when that binding is stable (no later module-level rebind of the name); a FUNCTION-BODY call whose module binding is contested by ANY later module-level rebind (invocation order unknown -- the function may run before or after it) is not statically provable -> FAIL CLOSED. Module-level (top-level) call ordering is a documented v1 NON-GOAL -- top-level calls are not extracted (C9-3) -- so such cases are neither regressed nor newly handled. Two symmetric cases with FUNCTION-BODY callers both survive: `def parse(); from bar import parse; def caller(): parse()` -> bar.parse (winner = import, no shadowing rebind after the winner; Y2) AND `from bar import parse; def parse(): ...; def caller(): parse()` -> M.parse (winner = def; F1). A rebind after the winner still drops the edge (e.g. `import bar; class bar: ...; def g(): bar.parse()` -> NO edge, X4; `from bar import parse; from n import *; def caller(): parse()` -> NO edge, F4). Effective binding order is module source order at module scope and, within a function, Python's function-local-for-whole-body rule (T2b, Y1). C12-1/T-a (Anchor C): the winner-to-call source-position ordering is MODULE-LEVEL rebinds ONLY -- ANY function-local binder of the name (plain/augmented/annotated assignment, for/with-as/except-as target, walrus, comprehension target, nested def/class, del) poisons the WHOLE function body regardless of position, so a call to that name anywhere FAILS CLOSED (UnboundLocalError), never resolving to a module import/binding (`from bar import parse; def g(): parse(); parse = factory()` -> NO edge). Reuse the already-loaded caller/module source (T5b/T2b context); cheap tree-sitter scan. No new DB call. TDD RED->GREEN. Domain: code.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on T5b (096.006-T) and T2b (096.009-T); WRAPS T5b's guard-agnostic resolution (R3) -- dependency is strictly T5c->T5b, never reverse, so both are independently completable and the DAG is acyclic. SHARED REBIND-FORM SET (RFS, single source of truth): T5c's invalidation scan and T5a's in-file routing scan (096.005-T, C6) consume the ONE RFS definition in the plan Design-decision, so they cannot drift on which forms count -- T5c applies it order-aware (after the winner -> fail closed), T5a order-agnostic (presence -> stage); no behavior change to T5c (it already scanned these forms). Refines BOTH the qualifier_kind=="module" branch (receiver) AND the qualifier_kind=="python_bare" branch (bare callee, Q1). F1 order-aware ANCHOR: the guard invalidates the resolved edge ONLY on a rebind AFTER the WINNING binding T5b resolved to (import OR def), not always "after the import"; an EARLIER def/class/del/match-capture/star does not invalidate. This keeps BOTH last-binding-wins cases consistent: import-after-def (`def parse; from bar import parse; def caller(): parse()` -> bar.parse, Y2) AND def-after-import (`from bar import parse; def parse(); def caller(): parse()` -> M.parse, F1 -- the def is the winner and survives). Cycle-5 Y2 only covered def-BEFORE-import; F1 fixes the symmetric def-AFTER-import case. F4: a later module-scope `from N import *` is an order-aware invalidator (positioned marker recorded by T2/096.002-T). C7-1 (call-position, FUNCTION-BODY CALLS ONLY): a function-body call whose winning module binding is contested by any later module-level rebind FAILS CLOSED (invocation order undecidable); module-level (top-level) call ordering is a v1 non-goal (top-level calls not extracted, C9-3); T5c consumes the T2/T2b positions (F14) to order the winner against later rebinds. C12-1/T-a (Anchor C): the winner-to-call source-position ordering is MODULE-LEVEL rebinds ONLY; ANY function-local binder of the name (assignment/aug/annotated, for/with-as/except-as, walrus, comprehension, nested def/class, del) poisons the WHOLE function body (Python UnboundLocalError) -> a call to that name anywhere FAILS CLOSED regardless of position, never resolving to a module import/binding. Scenario (a) asserts the winner-anchor survivals AND the C7-1 function-body fail-closed on a later module rebind; cross-references T5b (096.006-T). Keep <=4 test scenarios by consolidating the module-receiver rebind forms (incl. the F4 star) into one parametrized table (scenario 2) and adding two explicit bare-import shadow scenarios (assignment + parameter); the C12-1/T-a function-local-poison regression (`def g(): parse(); parse = factory()` -> NO edge, UnboundLocalError) folds into the assignment scenario (parametrized module-level rebind vs function-local poison), keeping <=4 scenarios. Files (<=2): src/services/code_graph.rs, tests/integration/calls_recall_acceptance_test.rs.
<!-- END:implementation-notes -->