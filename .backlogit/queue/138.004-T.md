---
artifact_type: task
created_at: 2026-08-27T18:07:23.4694032Z
dependencies:
    - 138.001-T
id: 138.004-T
labels:
    - shim
    - transport
    - mcp
    - fail-closed
    - reliability
parent_id: 138-F
priority: high
references:
    - src/shim/transport.rs
status: active
title: Latch terminal health outcomes fail-closed in the request-triggered probe and MCP error metadata
updated_at: 2026-08-27T21:08:58.7141688Z
---

PHASE 3 (behavior). Width: src/shim/transport.rs ONLY (production; the #[cfg(test)] concurrency module was added earlier by 138.006-T). Wire the Terminal arm into forwarding_endpoint under the existing recovery_lock (src/shim/transport.rs:129): Ready -> clear cooldown, publish Ready MONOTONICALLY, forward; Transient -> set last_failure=now, Err(Recoverable); Terminal(kind) -> publish Degraded{ProtocolIncompatible, fixed message derived from TerminalKind} MONOTONICALLY, Err(Permanent{class,message}). MONOTONIC PUBLISH IS MANDATORY: use watch::Sender::send_if_modified, whose closure runs under the channel's internal write lock, and REFUSE any transition out of Degraded. Revision 1 wrongly claimed publishing Degraded under recovery_lock latched the session - it does not, because recovery_lock lives on ShimHandler and does not bind the independently spawned monitor (see 138.005-T and C5). Once Degraded is published, every later forwarding_endpoint short-circuits at src/shim/transport.rs:123-127/:130-134 BEFORE reaching the probe, so probe delta is exactly 0. MCP ERROR METADATA - terminal: engram_code 15005, failure_class 'protocol_incompatible', recoverable false, retry_after_ms KEY ABSENT (not null, not 0 - agents branch on key presence). Transient is UNCHANGED: 15002, 'readiness_timeout', recoverable true, retry_after_ms 250. NOTE the existing degraded_call_tool_result already inserts retry_after_ms only on the recoverable branch (src/shim/transport.rs:303-305), so key-absence is a PRESERVATION requirement - do not regress it. Client-facing content text must be fixed/TerminalKind-derived with no path, no env value and no daemon-supplied message. No tools/call may succeed in a terminal session (124-F invariant 3). Turns T1-T4, T7(a), C3 and N2 green.