---
artifact_type: task
created_at: 2026-07-11T01:20:50.8183317-07:00
dependencies:
    - 082.003-T
id: 082.011-T
labels:
    - width:models
    - graph
    - provenance
    - serialization
    - slice:1
parent_id: 082-F
priority: medium
references:
    - docs/exec-plans/2026-07-10-callgraph-cross-file-resolution-plan.md
status: queued
title: 'MODEL/SERIALIZATION: CodeEdge.resolution field + struct-literal + proptest propagation'
updated_at: 2026-07-11T01:20:50.8183317-07:00
---

MODEL / SERIALIZATION PROPAGATION (split from 082.003-T per Copilot #240 finding 1 — the DB-layer `calls_edge.resolution` migration + count/enumerate query is 082.003-T; this task carries the `CodeEdge` model field + serialization/struct-literal propagation, a distinct model width).

Files (<=3): src/models/code_edge.rs (add `resolution: Option<String>` to `CodeEdge` at :36, matching the existing `import_path`/`linked_by` `#[serde(skip_serializing_if = "Option::is_none")]` pattern plus `#[serde(default)]` for backward-compatible JSONL deserialization of pre-field edges), src/services/dehydration.rs (update the two `CodeEdge` struct literals at :595 and :603 in the edge-serialization test to set `resolution`), tests/unit/proptest_models.rs (update the `CodeEdge` prop_map literal at :323 to generate/set `resolution`).

Contract: `CodeEdge` (code_edge.rs:36, fields edge_type/from/to/import_path/linked_by/created_at) gains `resolution: Option<String>` so dehydrated/exported edges carry provenance (`direct` | `calls_resolved_singleton`) end-to-end and the proptest round-trip covers it. Value semantics mirror 082.003-T's DB column; `None` serializes away (skip_serializing_if) so existing consumers/fixtures are unaffected. Depends on 082.003-T (DB column + canonical strings defined first).

Compiler follow-on (grounding note; single-token, no logic, no test): adding the field also makes rustc flag the THREE pre-existing GENERIC edge-read `CodeEdge` constructors in src/db/cozo_queries.rs (get-edges-by-type readers, unrelated to calls provenance) — each needs `resolution: None`. These are mechanical one-line additions carrying no logic; co-locate them with 082.003-T's existing cozo_queries.rs edit surface (or land them here) so this task stays single-width (model/serialization) within the <=3-file cap. Flag noted so Ship is not surprised by the extra constructor sites.

Width: models / serialization. Scenarios (3): `CodeEdge` round-trips `resolution` through serde for both `Some(..)` and `None`; a dehydrated edges JSONL carries the provenance field only when `Some`; the proptest `CodeEdge` strategy exercises `resolution` without a serialization panic.

Depends on 082.003-T.