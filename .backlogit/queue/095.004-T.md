---
artifact_type: task
created_at: 2026-07-22T15:00:02.1061954-07:00
dependencies:
    - 095.003-T
id: 095.004-T
parent_id: 095-F
priority: medium
status: queued
title: 'U2b: Single-cell DataFrame dataflow resolver (Fork E Option b)'
updated_at: 2026-07-22T15:00:02.1061954-07:00
---

Add the single-cell DataFrame dataflow resolver (Fork E Option b) that links U2 read endpoints to write endpoints WITHIN ONE notebook cell. Track df_var -> dataset bindings from assignments of the form df = spark.read...(literal) / spark.table(literal), then when df.write.saveAsTable/save(literal) is seen in the SAME cell, emit lineage_derives_from(write_target, read_source). FAIL CLOSED — drop (no edge) on: df reassignment before the write, control-flow branches/loops around the binding, any non-literal in the chain, or a df whose source was itself dropped by U2. Cross-cell df propagation is OUT of v1 (a df defined in cell N and written in cell M yields NO edge). Consumes U2's LineageEndpoint output; returns derives edges for U4 to persist via U1 writers (pure — no persistence here). Tests: read->df->write in one cell derives an edge; df reassignment -> no edge; branch/loop around binding -> no edge; read in cell A + write in cell B -> no cross-cell edge. Domain: dataflow (width-isolated). Blocked by U2. Plan Unit 2b.