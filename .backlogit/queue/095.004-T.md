---
artifact_type: task
created_at: 2026-07-22T15:00:02.1061954-07:00
dependencies:
    - 095.003-T
id: 095.004-T
parent_id: 095-F
priority: medium
status: queued
title: 'U2b: Single-cell DataFrame dataflow resolver (Fork E Option b)'
updated_at: 2026-07-22T17:53:03.9433728-07:00
---

Add the single-cell DataFrame dataflow resolver (Fork E Option b) that links read events to write events WITHIN ONE notebook cell and emits LineageEdgeCandidates. CONSUMES U2's Vec<SparkLineageEvent> event stream — both SPARK READ/WRITE CALL events AND VARIABLE REBIND/INVALIDATION events (cycle-5 F2) — using each event's receiver variable + source order + enclosing scope; performs NO second AST walk (U2 is the single extraction source of truth). Track df_var -> dataset bindings from read events (df = spark.read...(literal) / spark.table(literal)), then when a write event on the SAME variable (df.write.saveAsTable/save(literal)) appears LATER in source order within the SAME top-level cell scope, emit a LineageEdgeCandidate { target: write_dataset, sources: [read_dataset] } (the shared directional carrier U4 flattens to lineage_derives_from edges). RETURN TYPE: Vec<LineageEdgeCandidate> (cycle-5 F3 — a directional target<->sources carrier, not a flat endpoint list). FAIL CLOSED — drop (no edge) on: df REASSIGNMENT — ANY rebind event for the tracked variable before the write, whether to another Spark read, a NON-SPARK expression (df = other), or an unresolvable RHS, invalidates the binding (cycle-5 F2 — now observable because U2 emits rebind events from its full AST walk, not only whitelisted Spark calls); BRANCH/LOOP scope (a binding or write whose event scope is not the top-level cell body); UNRESOLVED RECEIVER (receiver = None); a read event whose endpoint U2 left unresolved; or any non-literal in the chain. Cross-cell df propagation is OUT of v1. Pure — no persistence here. Tests: read->df->write in one cell -> a LineageEdgeCandidate U4 persists as derives edge; df reassignment (df = other; df.write...) -> no edge (the non-Spark rebind event invalidates the binding — now achievable, cycle-5 F2); branch/loop-scoped binding -> no edge; unresolved receiver (write on a df never bound to a resolved read, or a non-simple-name receiver) -> no edge; read in cell A + write in cell B -> no cross-cell edge; a U2<->U2b CONTRACT TEST asserts U2 emits BOTH event kinds — Spark-call events carrying role/receiver-var/source-order/scope, AND the rebind/invalidation event for a non-Spark reassignment — that U2b consumes (findings E2 + cycle-5 F2). Domain: dataflow (width-isolated). Blocked by U2. Plan Unit 2b.