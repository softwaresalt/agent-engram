---
artifact_type: task
created_at: 2026-07-22T15:00:02.1061954-07:00
dependencies:
    - 095.003-T
id: 095.004-T
parent_id: 095-F
priority: medium
status: queued
title: 'U2b: Single-cell DataFrame dataflow resolver (Fork E Option b)'
updated_at: 2026-07-22T16:43:11.6159753-07:00
---

Add the single-cell DataFrame dataflow resolver (Fork E Option b) that links read events to write events WITHIN ONE notebook cell. CONSUMES U2's Vec<SparkLineageEvent> (the shared contract type U2 owns — PR #281 finding E2) and uses each event's receiver variable + source order + enclosing scope; performs NO second AST walk (U2 is the single extraction source of truth). Track df_var -> dataset bindings from read events (df = spark.read...(literal) / spark.table(literal)), then when a write event on the SAME variable (df.write.saveAsTable/save(literal)) appears LATER in source order within the SAME top-level cell scope, emit lineage_derives_from(write_target, read_source). FAIL CLOSED — drop (no edge) on: df REASSIGNMENT (two events bind the same receiver variable before the write), BRANCH/LOOP scope (a binding or write whose event scope is not the top-level cell body), an UNRESOLVED RECEIVER variable (receiver = None), a read event whose endpoint U2 left unresolved, or any non-literal in the chain. Cross-cell df propagation is OUT of v1 (a df defined in cell N and written in cell M yields NO edge). Returns derives edges for U4 to persist via U1 writers (pure — no persistence here). Tests: read->df->write in one cell derives an edge; df reassignment -> no edge; branch/loop-scoped binding -> no edge; unresolved receiver (write on a df never bound to a resolved read, or a non-simple-name receiver) -> no edge; read in cell A + write in cell B -> no cross-cell edge; a U2<->U2b CONTRACT TEST asserts U2 emits events carrying the role/receiver-var/source-order/scope metadata U2b consumes (finding E2). Domain: dataflow (width-isolated). Blocked by U2. Plan Unit 2b.