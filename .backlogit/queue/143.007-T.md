---
artifact_type: task
created_at: 2026-09-13T06:53:05.2868377Z
dependencies:
    - 143.006-T
id: 143.007-T
parent_id: 143-F
priority: medium
status: queued
title: T7 - Orphan-detection assertions (merge-path and startup-path)
updated_at: 2026-09-13T06:53:05.2868377Z
---

FILE: .github/agents/_ship.agent.md (Merge Confirmation Gate L684-701; startup recovery L977-979)

Two assertions, both applying to every shipment closure, dark or not. RECORDING: when Ship resolves a checkpoint it records the resolution commit SHA and the associated PR number in the session's closure record, so both assertions have a durable input. When several checkpoints are resolved in one session, every recorded resolution commit is asserted independently.

1. MERGE-PATH: in the Merge Confirmation Gate, assert each recorded resolution commit is an ancestor of origin/main (git merge-base --is-ancestor <resolution_commit> origin/main).
2. STARTUP-PATH (CR-4): at session start, for each recorded resolution commit whose associated PR is not merged, assert the commit is an ancestor of origin/main; flag any that is not. This is the backstop for the case where the merge never happens, which the merge-path assertion cannot cover. The check reads the recorded SHA and PR number rather than local checkout state, so it is correct after a branch change or a fresh clone.

Failure surfaces explicitly and halts to the operator.

ACCEPTANCE CRITERIA
1. Both assertions are stated with commands and pass/fail semantics.
2. The resolution commit SHA and PR number are recorded at resolution time, giving both assertions a durable, checkout-independent input.
3. No-op when no resolution commit was recorded (S26); every commit asserted when several were recorded (S27).
4. Failure halts to the operator and is surfaced, not logged silently.
5. Scenario S25 (merge never occurs) is caught by the startup-path assertion.
6. markdownlint passes.

POSTURE: documentation-first
Size: S | Complexity: medium
PLAN: docs/exec-plans/2026-09-13-checkpoint-lifecycle-continuity-plan.md (T7)