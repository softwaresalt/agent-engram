---
artifact_type: task
created_at: 2026-08-27T18:07:44.6627376Z
dependencies:
    - 138.001-T
id: 138.005-T
labels:
    - shim
    - late-readiness
    - monitor
    - teardown
    - reliability
parent_id: 138-F
priority: high
references:
    - src/shim/mod.rs
status: active
title: Monitor terminal exit, monotonic latch, late-terminal record, and teardown comment fix
updated_at: 2026-08-27T21:09:02.417604Z
---

PHASE 3 (behavior). Width: src/shim/mod.rs ONLY. FOUR changes. (1) Terminal arm in spawn_late_readiness_monitor (src/shim/mod.rs:246-269): Ready -> publish Ready and return (unchanged); Transient -> continue the 50ms->1s capped backoff (unchanged); Terminal(kind) -> publish Degraded{ProtocolIncompatible} MONOTONICALLY, tracing::warn!, write the late-terminal record, return. (2) MONOTONIC PUBLISH - this is the fix for the most severe revision-1 defect, a FAIL-OPEN race: the monitor holds its own Arc<watch::Sender> (:247), loops independently, never inspects the current value, and sends StartupOutcome::Ready UNCONDITIONALLY at :263, so a request-path Degraded could be OVERWRITTEN moments later, un-latching a proven-incompatible session. Route every publication through watch::Sender::send_if_modified and make Degraded ABSORBING (refuse any transition out of it). Additionally check outcome_tx.borrow() at the top of each loop iteration and return early once Degraded is observed - an optimisation only; correctness must NOT depend on it. C5 in 138.012-T is the blocking guard. (3) LATE-TERMINAL RECORD - revision 1 asserted a protocol_incompatible record with NO producer: write_startup_failure_record is private (:348), takes workspace_hint, and runs only inside compute_startup_outcome BEFORE the late probe, and the request handler carries no validated workspace. Give spawn_late_readiness_monitor a workspace_hint: String parameter; the value is already in scope at the call site (workspace_path.display().to_string() is used at :219, two lines after the spawn at :217). The MONITOR is the SOLE late-terminal writer and writes EXACTLY ONCE - structural, because it returns immediately after, so no dedup flag is needed. The REQUEST PATH NEVER writes a record; when it latches first the monitor observes Degraded within at most one backoff tick (<=1s) and writes then. Record stays BEST-EFFORT per the existing contract (:272-289): if the client disconnects first, outcome_tx.closed() fires and the monitor may exit without writing. State this; do not hide it. (4) TEARDOWN COMMENT FIX (dropped during the 137->138 re-scope): the comment at src/shim/mod.rs:441-449 says a timed-out startup task exits cleanly, but the branch at :453-458 constructs ShimFailureClass::TransportFailure and exits 13. Correct the comment; comment-only, no behavior change. PRESERVE the tokio::select! on outcome_tx.closed() (:253-256) VERBATIM. Do NOT retune RECOVERY_INITIAL_BACKOFF_MS (50) or RECOVERY_MAX_BACKOFF_MS (1000). Turns T5, T6, T7(b), R2b, C4-new and C5 green.