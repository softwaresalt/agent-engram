---
artifact_type: task
created_at: 2026-07-11T00:59:21.5579942-07:00
dependencies:
    - 082.003-T
    - 082.008-T
id: 082.010-T
labels:
    - width:schema
    - graph
    - migration
    - rollback
    - slice:1
parent_id: 082-F
priority: medium
references:
    - docs/exec-plans/2026-07-10-callgraph-cross-file-resolution-plan.md
status: queued
title: 'ROLLBACK down-migration: retract resolved edges + drop/ignore resolution column'
updated_at: 2026-07-11T00:59:21.5579942-07:00
---

ROLLBACK DOWN-MIGRATION (added by the re-harvest per Copilot #239 §7 rollback). Reverting rec1 is NOT purely additive once `calls_edge.resolution` (082.003-T) lands.

Files (<=3 src + 1 test): src/db/cozo_backend/schema.rs (down-migration path that drops/ignores the `resolution` attribute), src/db/cozo_queries.rs (retract_all_calls_resolved_singleton_edges). Test: tests/integration/calls_resolution_downmigration_test.rs.

Contract: the reindex/delete paths do NOT clear calls_edge rows (code_graph.rs:294-300, :1181-1205), so a plain reindex after reverting the code leaves prior `calls_resolved_singleton` edges behind, and the reverted old-schema writer (`{ from, to => created_at }`) may be incompatible with the migrated relation. This task defines and tests an explicit pre-revert cleanup / down-migration: (1) retract ALL `calls_resolved_singleton` edges (direct edges preserved); (2) drop/ignore the `resolution` attribute so calls_edge reverts to `(from, to) => created_at` and the old-schema writer round-trips. Idempotent; safe to run before or during the reverting reindex. Until this exists, rollback requires a manual edge-cleanup step, not a plain reindex (plan §7).

Width: schema / migration / rollback. Scenarios (4): down-migration retracts all `calls_resolved_singleton` edges (direct edges preserved); after dropping `resolution`, the old-schema calls_edge writer round-trips; idempotent (second run is a no-op, no error); post-migration count_calls_edges_by_resolution reports zero singleton edges.

Depends on 082.003-T (storage), 082.008-T (post-pass edges exist to retract).