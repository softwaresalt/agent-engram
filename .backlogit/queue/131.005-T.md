---
artifact_type: task
created_at: 2026-08-25T04:04:53.2690889Z
dependencies:
    - 131.004-T
id: 131.005-T
labels:
    - test-first
    - red
    - compiling-harness
    - otlp
    - provider
    - timeout
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/server/observability.rs
status: queued
title: 'RED: prove explicit OTLP lifecycle ownership, flush, and export timeout behavior'
updated_at: 2026-08-25T06:57:45.5446435Z
---

Using only the real interfaces established by 131.004-T, add unit tests that compile under otlp-export and then fail for the intended missing application lifecycle handle, explicit flush path, and source-owned export timeout. The deterministic local exporter is injected through the shared constructor path. The RED must allow the attached layer to retain the tracer/provider clone and export; it must never depend on dropping a constructor-local provider, a collector, socket, missing symbol, production compile error, or process-global environment mutation.

<!-- BEGIN:acceptance-criteria -->
1. 131.004-T is complete and remains the explicit dependency; the shared pipeline result/control seam exists before these tests are authored. 2. Exact RED command: cargo test --no-default-features --features cozo-backend,otlp-export --lib server::observability::tests::otlp_provider_red -- --nocapture. The library and tests must compile before any assertion runs. 3. otlp_provider_red_layer_clone_exports_without_local_handle constructs the pinned 0.26 provider/tracer/layer through a controlled local simple exporter, lets the constructor-local provider binding end, installs the layer, emits exactly otlp-layer-owned-baseline-span, and asserts the exporter receives it. This assertion is expected to PASS before 131.006-T and proves the layer-held tracer/provider clone keeps span processing alive. 4. otlp_provider_red_requires_application_owner_and_flush builds the production-shaped pipeline with a controlled local batch exporter, emits exactly otlp-red-contract-span, requests force_flush through the existing control seam, and asserts a separately accessible application lifecycle handle, successful flush result, and exported_span_names == [otlp-red-contract-span]. Expected RED is LifecycleUnavailable/no explicit owner; an empty export attributed to a dropped local provider is not an acceptable oracle. 5. otlp_provider_red_uses_application_export_timeout injects a never-ready exporter with a drop token and a test-only 25 ms limit, pauses Tokio time, advances 24 ms then 1 ms, and asserts not-cancelled then cancelled with the effective per-export limit. Expected RED is the missing source-owned BatchConfig override. No wall-clock sleep, OTEL_BSP_EXPORT_TIMEOUT mutation, set_var, remove_var, or global test lock is allowed. 6. Record the passing SDK-retention baseline and exact failing lifecycle/timeout assertions; tests remain unchanged while 131.006-T owns both GREEN transitions.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Provider lifecycle RED-harness skill only; test-only module in exactly 1 existing source file, at most 4 test/helper functions, 3 scenarios, and 100 minutes. Atomic milestone: a feature-enabled provider harness compiles, confirms actual layer-held SDK retention, and fails only because explicit application lifecycle/flush control and the exact per-export timeout are missing.
<!-- END:implementation-notes -->
