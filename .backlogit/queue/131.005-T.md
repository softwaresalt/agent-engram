---
artifact_type: task
created_at: 2026-08-25T04:04:53.2690889Z
dependencies:
    - 131.004-T
id: 131.005-T
labels:
    - test-first
    - red
    - compiling-harness
    - otlp
    - provider
    - timeout
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/server/observability.rs
status: queued
title: 'RED: prove retained OTLP export and application-owned timeout behavior'
updated_at: 2026-08-25T05:19:58.2502377Z
---

Using only the real interfaces established by 131.004-T, add unit tests that compile under otlp-export and then fail for the intended missing provider ownership, export, and application-timeout behavior. The deterministic in-process exporter is injected through the shared constructor path; the RED must never depend on a collector, socket, missing symbol, or production compile error.

<!-- BEGIN:acceptance-criteria -->
1. 131.004-T is complete and remains the explicit dependency; the shared pipeline/lifecycle seam exists before these tests are authored. 2. Exact RED command: cargo test --no-default-features --features cozo-backend,otlp-export --lib server::observability::tests::otlp_provider_red -- --nocapture. The library and tests must compile before any assertion runs. 3. otlp_provider_red_retains_and_exports installs the returned layer through the seam, emits exactly otlp-red-contract-span, requests flush, and asserts exported_span_names == [otlp-red-contract-span]; expected RED is left [] because current layer-only construction drops its provider. 4. otlp_provider_red_uses_application_timeout asserts the production effective timeout is exactly Duration::from_secs(5), independent of OTEL_BSP_EXPORT_TIMEOUT; expected RED is the SDK/environment default because no application override is wired. 5. otlp_provider_red_cancels_pending_export injects a never-ready exporter with a drop/cancellation token and a test-only 25 ms limit, pauses Tokio time, advances 24 ms then 1 ms, and asserts not-cancelled then cancelled with a timeout/failure report naming the phase and limit; expected RED is no bounded cancellation/report. No wall-clock sleep is allowed. 6. Record the assertion output; tests remain unchanged while 131.006-T owns the GREEN.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Provider lifecycle RED-harness skill only; test-only module in exactly 1 existing source file, at most 4 test/helper functions, 3 scenarios, and 100 minutes. Atomic milestone: a feature-enabled provider harness compiles and fails only because ownership, export, and the exact timeout are missing.
<!-- END:implementation-notes -->
