---
artifact_type: task
created_at: 2026-08-25T04:04:53.2690889Z
dependencies:
    - 131.004-T
id: 131.005-T
labels:
    - test-first
    - red
    - compiling-harness
    - otlp
    - provider
    - timeout
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/server/observability.rs
status: blocked
title: 'RED: prove explicit OTLP lifecycle ownership, flush, and export timeout behavior'
updated_at: 2026-08-25T22:14:13.7349127Z
---

Using only 131.004-T interfaces, add unit tests that compile then fail for missing application lifecycle control and per-export timeout. The local exporter is injected through the shared constructor and an isolated Registry or tracing::subscriber::with_default; no test installs the process-global subscriber. The already-GREEN layer-retention baseline is pinned to the resolved 0.27 bridge over SDK 0.26.

<!-- BEGIN:acceptance-criteria -->
1. 131.004-T is complete and remains the dependency; pipeline and subscriber-injection seams exist first. 2. Exact RED command: cargo test --no-default-features --features cozo-backend,otlp-export --lib server::observability::tests::otlp_provider_red -- --nocapture. Library and tests compile before assertions. 3. The retention baseline constructs the resolved 0.27 bridge layer over SDK 0.26 through a controlled exporter, ends the constructor-local binding, runs under tracing::subscriber::with_default or injected Dispatch, emits otlp-layer-owned-baseline-span, and expects exactly that export. It never calls SubscriberInitExt::init and must PASS before U6. 4. The lifecycle RED emits otlp-red-contract-span, requests force_flush through existing control seam, and expects an explicit owner, successful result, and exact export. RED is LifecycleUnavailable, not provider drop. 5. Never-ready exporter with injected 25 ms per-export limit uses paused Tokio time and asserts cancellation only at 25 ms. No wall sleep or environment mutation. 6. Record passing 0.27-bridge retention baseline and exact failing lifecycle or timeout assertions; tests remain unchanged while 131.006-T owns GREEN.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Provider lifecycle RED-harness skill only; test-only module in exactly 1 source file, at most 4 test or helper functions, 3 retention, lifecycle, and timeout scenarios, and 100 minutes. Atomic milestone: isolated feature-enabled tests confirm actual 0.27 bridge and SDK 0.26 retention then fail only for missing application control and policy.
<!-- END:implementation-notes -->
