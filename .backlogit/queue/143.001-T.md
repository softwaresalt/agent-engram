---
artifact_type: task
created_at: 2026-09-10T23:32:24.4246813Z
custom_fields:
    blocked_reason: 'Terminal Revision 3 plan-review FAIL after three cycles; do not claim. Requires a fresh operator-authorized Stage cycle.'
id: 143.001-T
labels:
    - daemon
    - ttl
    - test-first
parent_id: 143-F
priority: critical
status: blocked
title: Characterize health-probe idle-TTL reset livelock
updated_at: 2026-09-11T02:57:55.9198761Z
---

GREEN CHARACTERIZATION AND TEST SCAFFOLD ONLY. Revision 2 corrects a Revision 1 defect (plan-review finding P1-02): this task previously had to contain an ALWAYS-RED desired-behaviour test while also passing cargo dev-test as a quality gate. Those two requirements are mutually unsatisfiable, so the task could never be completed as specified. The RED desired-behaviour test now lives in 143.002-T, ALONGSIDE the implementation that turns it green. This task is GREEN on completion.

AC1 (GREEN CHARACTERIZATION): a test named probe_currently_resets_idle_ttl_baseline asserts the CURRENT, DEFECTIVE behaviour - a health/readiness probe connection resets TtlTimer via accept_loop (src/daemon/ipc_server.rs, ttl.reset() at the T049 comment), so a non-ready daemon never expires. This test PASSES ON THE UNMODIFIED BASELINE. Its role is to pin the starting state and fail loudly if the behaviour changes outside 143.002-T. Its deletion or inversion by 143.002-T is PRE-AUTHORIZED here so it is not later mistaken for lost coverage.

AC2 (DETERMINISTIC TTL SCAFFOLD, NO WALL-CLOCK SLEEPS): the livelock reproduces deterministically with the TTL clock INJECTED AND ADVANCED, never by sleeping. Per the 134-S learning, wall-clock polling against an idle timer is itself the bug. The scaffold exposes a stable entry point that 143.002-T reuses for its overlap, cancellation, and error cases, so the harness is built once rather than twice.

AC3: the test names the EXACT reset call site, so a future reader can locate it without re-deriving it.

Files: src/daemon/ipc_server.rs (test module) or tests/integration/daemon_ttl_test.rs. Width isolation: TESTS ONLY - no production change in this task. Posture: characterization-first.
Gates (authoritative, in order): cargo fmt --all -- --check; cargo clippy --all-targets -- -D warnings -D clippy::pedantic; cargo dev-test; cargo audit. cargo build is an optional precheck only, not a gate. Do NOT use cargo ci or cargo lint (--all-features is broken on this baseline, risk H6).
Size: S | Complexity: low.
