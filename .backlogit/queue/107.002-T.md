---
artifact_type: task
created_at: 2026-08-01T03:30:48.5070575Z
dependencies:
    - 107.001-T
id: 107.002-T
labels:
    - code-graph
    - python
    - fail-closed
    - target-correctness
    - green
    - tdd
parent_id: 107-F
priority: high
references:
    - docs/exec-plans/2026-07-31-python-qualified-staging-caller-attribution-plan.md
    - docs/decisions/2026-07-31-python-qualified-staging-caller-attribution-decision.md
    - src/services/code_graph.rs
status: queued
title: 'U2: Unique-only caller attribution at both qualified staging producers'
updated_at: 2026-08-01T06:05:34.9424864Z
---

Code-only GREEN milestone for 107-F. Reuse the existing typed `find_unique_function_id` outcome at the full-index and incremental-sync qualified/provenance staging sites. Preserve current staged-call writes for a unique caller, fail closed and count an ambiguous caller, and retain the current no-op for a missing caller. This task is blocked by the U1 RED harness.

<!-- BEGIN:acceptance-criteria -->
- At both current first-match qualified staging sites in src/services/code_graph.rs (full index and incremental sync), match on find_unique_function_id instead of staging from find_function_id.
- Unique(from_id): preserve the existing enclosing-canonical-type calculation and put_staged_call_with_provenance behavior.
- Ambiguous: increment same_file_ambiguous_dropped and do not create a staged row or resolved edge.
- NotFound: retain the current no-op behavior.
- Keep find_function_id/find_unique_function_id implementation semantics, staged-call relation/key, Python target resolution, extraction version, CLI, and schema unchanged.
- Update nearby helper/call-site documentation that currently says ambiguity-aware lookup is direct-edge-only or qualified caller attribution is intentionally first-match.
- U1 becomes GREEN for full index, sync, and unique-caller control; existing same-file ambiguity and canonical-call suites remain green.
- Ship runs ordered fmt, clippy-pedantic, dev-test, and audit gates plus plan runtime verification. Release exposure handling is handoff-only: never automatically mutate a user/deployed workspace; verify a target-specific full reindex only after operator execution or explicit approval, and record no migration/backfill if no affected binary shipped.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Single production file and two mirrored sites. Use an explicit match so Ambiguous can be counted separately from NotFound. Do not implement source-order/last-wins inference or add another resolver. Ship detects whether an affected PR #301 binary shipped. If exposure exists, Ship prepares a target-workspace-specific full-reindex handoff and waits for the operator to execute it or explicitly approve execution before any deployed/user workspace mutation; verification is recorded only afterward. Ship must not run automatic or fleet-wide reindexing. If no affected binary shipped, closure records no migration/backfill.
<!-- END:implementation-notes -->