---
artifact_type: task
created_at: 2026-09-10T23:32:37.2168961Z
custom_fields:
    blocked_reason: 'Terminal Revision 3 plan-review FAIL after three cycles; do not claim. Requires a fresh operator-authorized Stage cycle.'
dependencies:
    - 143.003.005-ST
    - 143.003.006-ST
id: 143.003-T
labels:
    - daemon
    - watchdog
parent_id: 143-F
priority: critical
status: blocked
title: Add maximum-startup watchdog with self-termination
updated_at: 2026-09-11T02:59:53.6272127Z
---

=== TERMINAL NOTICE - BLOCKED - DO NOT CLAIM, DO NOT IMPLEMENT ===

STATUS IS BLOCKED by the terminal Revision 3 plan-review FAIL (three consecutive review cycles; the review-cycle circuit breaker is OPEN).

EVERYTHING BELOW THIS NOTICE IS HISTORICAL REVISION 3 PROPOSAL TEXT. That includes the phrase CLAIMABLE INTEGRATION MILESTONE, the subtask list, the scope statements, every acceptance criterion, and every other execution, gate, and sizing statement. All of it is NON-OPERATIVE, retained for audit history only, and IT AUTHORIZES NOTHING.

DO NOT claim and DO NOT implement this item without a fresh operator-authorized Stage cycle producing a NEW plan-review PASS.

=== END TERMINAL NOTICE ===

CLAIMABLE INTEGRATION MILESTONE (Revision 3). Retained in the 143-S manifest and NO LONGER a prose-only container. Revision 2 left this item in an executable manifest while declaring it non-claimable; Revision 3 gives it a bounded integration milestone with its own acceptance criteria.

Scope of the subtree: a bounded, configurable maximum-startup budget, DISTINCT from and much larger than the readiness probe timeout, with orderly self-termination and a bounded shim response. Observed failure: 72 CPU-minutes, 1.84GB resident, never ready, never self-terminated. This does NOT raise ENGRAM_READY_TIMEOUT_MS.

SUBTASKS (executable, in dependency order):
143.003.001-ST - single outer wall-clock watchdog: arm at daemon entry, disarm through ONE stable read-only readiness-observation seam. Owns gate G9 and hardening constraint H12.
143.003.002-ST - hard deadline sequence: stop accepting, bounded durable terminal-record flush, bounded cooperative drain, exact-process forced exit without awaiting non-cancellable spawn_blocking. Carries risky action PA-2 and operator checkpoint CP-3.
143.003.004-ST - durable, versioned, cross-process retry ledger (failure taxonomy then LEDGER then backoff policy).
143.003.003-ST - bounded shim exponential backoff with terminal stop, CONSUMING the ledger.
143.003.005-ST - retry-ledger state surfaced through shared DaemonStatus and HealthReport with CLI/MCP parity tests.

ORDERING CORRECTION (Revision 3): the durable ledger MUST PRECEDE its backoff consumer. The chain is 143.004.002-ST (failure taxonomy) then 143.003.004-ST (ledger) then 143.003.003-ST (backoff policy) then 143.003.005-ST (status surface). Revision 2 had the backoff policy ordered before the ledger it consumes, which would have let the bound be implemented against a counter that did not yet exist.

INTEGRATION MILESTONE SCOPE (this item only): one end-to-end lifecycle test plus the triage runbook entry.

AC1 (END-TO-END WATCHDOG LIFECYCLE): a single subprocess-based integration test drives one induced never-ready daemon through the full chain - watchdog armed before admission, deadline fires, terminal record durably flushed, forced exit, a FRESH daemon subprocess reacquires the IPC endpoint and fd_lock, the shim increments the durable ledger, and a subsequent run observes the recorded count. In-process assertions are insufficient.
AC2 (TERMINAL STOP OBSERVED END-TO-END): across repeated induced terminations the shim reaches its terminal stop at the configured bound and surfaces a terminal error rather than respawning, observed through actual separate processes.
AC3 (NO WORKSTREAM-B DEPENDENCY): the recorded evidence confirms no leaf in this subtree depends on gate G3, on 144-F, or on any 144-S item.
AC4 (RUNBOOK): the triage runbook records the budget default and margin rationale, the backoff schedule, the terminal-stop threshold, the ledger path and schema version, and the D13 re-validation hook against 144.001.002-ST measured data.
AC5 (PA-2 SCOPE): this milestone executes REAL self-termination, so CP-3 operator approval MUST be recorded before it runs. Simulation-only evidence is not sufficient for this milestone.

Files: tests (integration) plus the triage runbook. Single domain: tests plus operator documentation for the same behaviour.
Gates (authoritative, in order): cargo fmt --all -- --check; cargo clippy --all-targets -- -D warnings -D clippy::pedantic; cargo dev-test; cargo audit. cargo build is an optional precheck only, not a gate. Do NOT use cargo ci or cargo lint (--all-features is broken on this baseline, risk H6).
Size: M | Complexity: high.
