---
artifact_type: task
created_at: 2026-09-03T00:20:13.9787227Z
dependencies:
    - 142.006-T
id: 142.023-T
parent_id: 142-F
priority: high
status: active
title: Remove retired HTTP and SSE server modules and tests
updated_at: 2026-09-06T02:17:03.7135647Z
---

Plan unit F46 - remove retired HTTP and SSE server modules and tests. Delete `src/server/mcp.rs`, `src/server/router.rs` and `src/server/sse.rs`, remove their module declarations from `src/server/mod.rs`, delete `tests/integration/connection_test.rs`, and remove that harness's `integration_connection` `[[test]]` block from root `Cargo.toml` in the SAME change.

Execution posture: characterization-first removal. This is a destructive source deletion and requires explicit operator approval immediately before execution.

<!-- BEGIN:acceptance-criteria -->
- `src/server/mcp.rs`, `src/server/router.rs` and `src/server/sse.rs` are deleted.
- `src/server/mod.rs` no longer declares the removed modules.
- `tests/integration/connection_test.rs` is deleted.
- The `integration_connection` `[[test]]` block is removed from root `Cargo.toml` in the SAME change as the file deletion.
- No other `[[test]]` block is touched.
- `cargo dev-test`, `cargo lint` (`--all-features`) and `cargo ci` (`--all-features`) are ALL GREEN after this unit, with no broken window.
- Operator approval is recorded before execution.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Size: M | Complexity: medium | size_source: agent | size_ruleset_version: engram-stage-2h-rule-v1. Recorded as prose because this workspace task type defines no structured size/complexity field. Size is effort/volume; complexity is difficulty/uncertainty; the two axes are independent.

Owned files: `src/server/mod.rs`, `src/server/mcp.rs`, `src/server/router.rs`, `src/server/sse.rs`, `tests/integration/connection_test.rs`, root `Cargo.toml` (`[[test]]` section - remove the single pre-existing `integration_connection` block).
DEREGISTRATION IS OWNED BY F46, NOT F47. This was the Revision 7 R7-A1 remediation. The block carries `required-features = ["legacy-sse"]`, so deferring its removal to F47 would leave a broken window: `cargo dev-test` (default features) would stay GREEN and hide the breakage while `cargo lint` and `cargo ci` (both `--all-features`) would fail with `couldn't read tests/integration/connection_test.rs` for the entire span between F46 and F47.
Root `Cargo.toml` total order: `F00 -> F12a -> F46 -> F47`. F00 and F46 both touch the `[[test]]` section but over disjoint blocks.
Edges: F12a -> F46 -> F47 -> F48 -> F49; F46 + F47 + F48 + F49 -> F54.
RISK: destructive source deletion - operator approval required immediately before execution.
<!-- END:implementation-notes -->

<!-- BEGIN:verification -->
1. `cargo dev-test` is GREEN.
2. `cargo lint` (`clippy --all-targets --all-features`) is clean - this is the gate that would catch a deregistration miss.
3. `cargo ci` (`test --all-targets --all-features`) is GREEN.
4. `git status` confirms the four source deletions and the manifest block removal land together.
<!-- END:verification -->