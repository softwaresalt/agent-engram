---
artifact_type: task
created_at: 2026-07-23T18:46:11.4081065-07:00
dependencies:
    - 096.004-T
id: 096.005-T
labels:
    - python
    - call-graph
    - code
parent_id: 096-F
priority: medium
references:
    - src/services/code_graph.rs
status: queued
title: 'T5a: Route Python bare + module-qualified calls into canonical staging'
updated_at: 2026-07-23T18:46:11.4081065-07:00
---



<!-- BEGIN:acceptance-criteria -->
- Python cross-file bare call -> staged row with qualifier_kind=="python_bare" on BOTH the full-index arm (851-908) AND the sync arm (1573-1643); the sync arm no longer name-only-stages Python bare calls (X1).
- Python mod.func() -> staged row with qualifier_kind=="module".
- In-file bare Python call -> still a direct calls_edge.
- Rust cross-file bare call -> STILL name-only staged on both arms (regression assertion).
- cargo test --test integration_code_graph green; existing Rust calls tests unaffected; clippy --all-targets clean; fmt clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Route Python canonical-eligible calls into provenance staging (Option A) at BOTH Calls-consumer sites so version-triggered re-extraction (T7) never strands Python calls on the legacy name-only path (X1, same class as Q2/R1): the full-index arm (code_graph.rs:851-908) AND the sync arm (code_graph.rs:1573-1643), + should_stage_provenance_call (188-194), language-dispatched on the caller file's language: (a) accept qualifier_kind=="module" for Python into provenance staging; (b) for Python BARE calls whose callee is unresolved in-file, stage via put_staged_call_with_provenance with qualifier_kind=="python_bare" instead of name-only put_staged_call -- on BOTH arms (the sync arm's bare-call put_staged_call at line 1639 must also route through provenance for Python). In-file bare calls stay direct edges. Rust behavior untouched on both arms (every new branch guarded on Language::Python). TDD RED->GREEN. Domain: code.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on T4 (096.004-T). Two Calls-consumer sites in the SAME file (full-index 851-908 + sync 1573-1643); the sync arm currently name-only-stages bare calls at line 1639 (X1). Y3: T5a still stamps python_bare on both arms (unchanged); recall for callers whose layout T1 rejects (src/-root / PEP 420 namespace / __init__.py) is preserved DOWNSTREAM by T5b's legacy name-only fallback (096.006-T) -- no staging change here, asserted in T5b's tests. No cozo_queries.rs schema change (reuses put_staged_call_with_provenance). Files (<=2): src/services/code_graph.rs (two Calls-consumer sites), tests/integration/code_graph_test.rs.
<!-- END:implementation-notes -->