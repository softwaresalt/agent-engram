---
artifact_type: task
created_at: 2026-07-23T18:46:11.4081065-07:00
dependencies:
    - 096.002-T
    - 096.004-T
    - 096.009-T
id: 096.005-T
labels:
    - python
    - call-graph
    - code
parent_id: 096-F
priority: medium
references:
    - src/services/code_graph.rs
status: queued
title: 'T5a: Route Python bare + module-qualified calls into canonical staging'
updated_at: 2026-07-23T18:46:11.4081065-07:00
---



<!-- BEGIN:acceptance-criteria -->
- Python cross-file bare call -> staged row with qualifier_kind=="python_bare" on BOTH the full-index arm (851-908) AND the sync arm (1573-1643); the sync arm no longer name-only-stages Python bare calls (X1).
- Python mod.func() -> staged row with qualifier_kind=="module".
- F2 (root defect, GENERALIZED cycle-7 Z1/Z2/C7-2): an in-file bare call whose callee has a same-file `def` AND is SHADOW-CONTESTED at ANY scope the resolver models -- a named module import (`def parse(); from bar import parse; parse()`), a positioned star-import invalidator (`def parse(); from n import *; parse()`, Z1), a non-import module rebind (`def parse(); parse = factory(); parse()`, Z2), OR a scoped function-local/enclosing-function import (`def parse(); def g(): from bar import parse; parse()`, C7-2) -- is ROUTED TO python_bare staging (NOT a direct M.parse edge) on both arms, so T5b/T5c adjudicate call-site-effective last-binding-wins; an in-file callee with NO competing binding at ANY modeled scope STILL yields a direct calls_edge (fast path preserved).
- The routing predicate is expressed as an ABSTRACTION -- "route to staging when the same-file def's name is shadow-contested at any scope the resolver models" -- NOT axis-by-axis, so future shadow axes are covered by construction (closes the F4 remainder in the same abstraction).
- Rust cross-file bare call -> STILL name-only staged on both arms (regression assertion).
- cargo test --test integration_code_graph green; existing Rust calls tests unaffected; clippy --all-targets clean; fmt clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Route Python canonical-eligible calls into provenance staging (Option A) at BOTH Calls-consumer sites so version-triggered re-extraction (T7) never strands Python calls on the legacy name-only path (X1, same class as Q2/R1): the full-index arm (code_graph.rs:851-908) AND the sync arm (code_graph.rs:1573-1643), + should_stage_provenance_call (188-194), language-dispatched on the caller file's language: (a) accept qualifier_kind=="module" for Python into provenance staging; (b) for Python BARE calls whose callee is unresolved in-file, stage via put_staged_call_with_provenance with qualifier_kind=="python_bare" instead of name-only put_staged_call -- on BOTH arms (the sync arm's bare-call put_staged_call at line 1639 must also route through provenance for Python). F2 ROOT-DEFECT FIX, GENERALIZED cycle-7 via the PROVE-OR-FAIL-CLOSED invariant (this task OWNS it): the in-file (Some,Some) branch at code_graph.rs:900-902 currently mints a DIRECT M.callee edge whenever the bare callee resolves against the current file's symbols, BEFORE any python_bare staging -- so `def parse(); from bar import parse; parse()` emits a FALSE direct M.parse edge (frozen rule requires bar.parse) and makes T5b's last-binding-wins branches UNREACHABLE. Cycle-6's fix keyed the skip on NAMED module-level imports ONLY, which does NOT compose: a `from n import *` after the def (Z1), a non-import module rebind (`parse = factory()`/class/del/match-case, Z2), and a function-local/enclosing-function import (C7-2) all leave the predicate false and still mint a false direct edge. Modify code_graph.rs:896-908 on BOTH arms so that, for Python, a same-file `def` is routed to python_bare staging WHENEVER its name is SHADOW-CONTESTED at ANY scope the resolver models -- a named module import (T2/096.002-T ImportBindings), a positioned star-import invalidator (T2/F4), a non-import module rebind, OR a scoped function-local/enclosing import (T2b/096.009-T scoped-binding model). Express the predicate as "route to staging when the same-file def's name is shadow-contested at any modeled scope" (NOT axis-by-axis) so future axes are covered by construction; a same-file callee with NO competing binding at any modeled scope keeps the direct-edge fast path unchanged. Rust behavior untouched on both arms (every new branch guarded on Language::Python). TDD RED->GREEN. Domain: code.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on T4 (096.004-T), T2 (096.002-T) AND T2b (096.009-T) -- the T2b edge is NEW this cycle (Z1/Z2/C7-2 generalization): T5a must consult T2b's scoped-binding model (function-local / enclosing-function imports + positions) in addition to T2's module-level ImportBindings (named imports + positioned star markers) to know whether a same-file `def` is shadow-contested at ANY modeled scope for the generalized in-file routing decision. Cycle-6 added T2->T5a (F2, named imports); cycle-7 adds T2b->T5a (scoped imports). Acyclic: T2b depends only on T2 and is already upstream of T5b/T5c, so T5a<-T2b adds no back-edge. Two Calls-consumer sites in the SAME file (full-index 851-908 + sync 1573-1643); the sync arm currently name-only-stages bare calls at line 1639 (X1); the in-file direct-edge shortcut is at (Some,Some)=>create_calls_edge, 900-902 (F2). Y3/F3: T5a still stamps python_bare on both arms; recall for callers whose layout T1 rejects (src/-root / PEP 420 / __init__.py) AND for provable-namespace-but-unbound callees (star/re-export/relative) is preserved DOWNSTREAM by T5b's legacy name-only fallback (096.006-T), which fires whenever T5b derives NO canonical target -- no staging change here, asserted in T5b's tests. No cozo_queries.rs schema change (reuses put_staged_call_with_provenance). Files (<=2): src/services/code_graph.rs (two Calls-consumer sites + the in-file decision), tests/integration/code_graph_test.rs. 4 scenarios (the shadow-contest routing vectors -- named import / star-after-def / non-import rebind / function-local / enclosing import -- are consolidated into ONE parametrized routing-table scenario to stay within the 2-hour rule while covering every axis by construction).
<!-- END:implementation-notes -->