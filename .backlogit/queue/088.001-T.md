---
artifact_type: task
created_at: 2026-07-14T23:40:08.2055022-07:00
custom_fields:
    source_stash_id: 2C949608
    source_stash_kind: feature
    source_stash_path: stash.jsonl
    source_stash_priority: low
    source_stash_text: 'rec1 followup optional: run the cross-file calls post-pass on the incremental sync path as well, not only full and force index. Today incremental sync stages new cross-file calls but does not resolve them until the next full index. Gated on performance; evaluate a scoped post-pass over only changed callers.'
id: 088.001-T
parent_id: 088-F
priority: low
status: blocked
title: '[Deferred/perf-spike] Cross-file post-pass on incremental sync path'
updated_at: 2026-07-14T23:40:08.2055022-07:00
---

[DEFERRED — perf spike required; NOT in any shipment] Run the cross-file calls post-pass on the INCREMENTAL sync path as well, not only full/force index. VERIFIED 2026-07-14: incremental sync stages new cross-file calls (staged_call) but the deferred post-pass (082.008-T, src/db/cozo_backend/cozo_queries.rs ~1671) that resolves them to calls_resolved_singleton edges runs only on full/force index; incremental-synced cross-file calls stay unresolved until the next full index. OPTIONAL optimization gated on PERFORMANCE: a scoped post-pass over only changed callers might recover freshness cheaply, but the cost/benefit is unproven. DISPOSITION (Stage, DARK_MODE): BLOCKED/DEFERRED — resolving this needs a hands-on performance spike (benchmark a scoped incremental post-pass vs full-index latency and verify no correctness/false-edge regression) which Stage cannot execute under its no-build role boundary. UNBLOCK TRIGGER: run a perf spike; if a scoped post-pass over changed callers stays within incremental-sync latency budget without correctness regressions, re-harvest into an executable task and add to a shipment. FUTURE ACCEPTANCE: scoped post-pass resolves changed-caller cross-file calls on incremental sync; incremental p50/p95 latency within budget; no false edges. Single-width: resolution post-pass invocation on the sync path. Source stash 2C949608 (rec1 followup, optional).