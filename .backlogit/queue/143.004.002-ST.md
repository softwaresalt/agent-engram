---
artifact_type: subtask
created_at: 2026-09-11T01:23:00.1668685Z
custom_fields:
    blocked_reason: 'Terminal Revision 3 plan-review FAIL after three cycles; do not claim. Requires a fresh operator-authorized Stage cycle.'
dependencies:
    - 143.003.002-ST
id: 143.004.002-ST
labels:
    - errors
    - cli
    - mcp
    - contract
    - parity
parent_id: 143.004-T
priority: critical
status: blocked
title: Watchdog terminal cause in the live failure taxonomy
updated_at: 2026-09-11T03:11:47.7207706Z
---

Expose the watchdog terminal cause through the EXISTING live failure taxonomy so it is client-visible, not merely on disk.

VERIFIED LIVE CODE FACTS (do not re-derive): src/errors/mod.rs defines enum ShimFailureClass with variants AdmissionFailure, ReadinessTimeout, EndpointDerivationFailure, TransportFailure, ProtocolIncompatible; exit_code() returns 10,11,12,13,14 and wire_code() returns 15001,15002,15003,15004,15005. The new variant takes the next unused values in both spaces.

AC1: a NEW ShimFailureClass variant is added for the watchdog startup-budget terminal cause. It is added to the EXISTING taxonomy - do not create a parallel classification enum.
AC2: the new variant has a DISTINCT CLI exit code, allocated as the next unused value after 14. A contract test asserts it collides with no existing code.
AC3: the new variant has a DISTINCT MCP wire code, allocated as the next unused value after 15005. A contract test asserts it collides with no existing code.
AC4: the new class is DISTINGUISHABLE from ReadinessTimeout at the client. The ratio of shim ReadinessTimeout to daemon watchdog terminal cause is the R3 triage discriminator and is meaningless if the two collapse.
AC5 (PARITY, mandatory at implementation time): docs/cli-mcp-parity.md is UPDATED IN THE SAME CHANGE and the drift guard at tests/contract/tool_descriptor_registry_test.rs stays GREEN. Because that document is a drift-guarded contract surface rather than a separate docs domain, updating it alongside the code does NOT violate width isolation.
AC6 (GATE G7 part 3): the terminal cause is client-visible through both CLI exit code and MCP wire code, not only in the filesystem sink.
AC7: follows .github/instructions/mcp-server.instructions.md error-code and tool-contract rules; no parallel convention is invented.

Files: src/errors/mod.rs, docs/cli-mcp-parity.md, plus contract tests. Single domain: contract surface (code + its drift-guarded contract doc + tests).
Gates (authoritative, in order): cargo fmt --all -- --check; cargo clippy --all-targets -- -D warnings -D clippy::pedantic; cargo dev-test; cargo audit. cargo build is an optional precheck only, not a gate. Do NOT use cargo ci or cargo lint (--all-features is broken on this baseline, risk H6).
Test-first: RED before GREEN.
Size: S | Complexity: medium.
