---
artifact_type: task
created_at: 2026-07-25T14:02:00.0823887-07:00
id: 096.011-T
labels:
    - python
    - canonical-identity
    - call-graph
    - code
parent_id: 096-F
priority: medium
references:
    - src/db/cozo_queries.rs
status: queued
title: 'T5b-seam: public language-scoped name->IDs query helper + typed no-target fallback policy'
updated_at: 2026-07-25T14:02:00.0823887-07:00
---



<!-- BEGIN:acceptance-criteria -->
- F9 helper exposure (R): expose a small PUBLIC read-only LANGUAGE-SCOPED name->IDs query helper in cozo_queries.rs, extracted from the INLINE-PRIVATE name->IDs singleton currently embedded in reresolve_calls_edges (cozo_queries.rs:2191-2205,2263-2270) which FILTERS OUT python_bare rows and so CANNOT be reused as-is. The helper returns candidate function IDs for a given (name, language); a UNIQUE match yields exactly one id, a non-unique/zero match yields none. HELPER EXPOSURE, NOT a schema change (no function_meta/calls_edge/staging migration; reuse function_ids_by_canonical_path at cozo_queries.rs:1046 unchanged).
- Typed no-target fallback POLICY (C9-4, Anchor B -- Edge Terminology): define the typed no-target reason set {NoModuleContext, UnsupportedImportForm, CompetingBindings, Shadowed, DuplicateSameNameImport} and a fallback-eligibility predicate. The LEGACY name-only unique-match fallback is ALLOWED ONLY for the two RECALL-SAFE reasons {NoModuleContext, UnsupportedImportForm} -> "no canonical edge (fallback allowed)" (Anchor B); it is NEVER taken for the three FAIL-CLOSED reasons {CompetingBindings, Shadowed, DuplicateSameNameImport} -> "no edge" (Anchor B), because emitting the sole indexed legacy edge there would leak a WRONG-CALLEE edge.
- Compiling-but-failing harness (TDD RED): a unit test asserts (i) the helper returns the language-scoped candidate IDs for a unique name and NONE for a non-unique/zero name; (ii) the fallback-policy predicate returns fallback-ALLOWED for exactly {NoModuleContext, UnsupportedImportForm} and fallback-SUPPRESSED for {CompetingBindings, Shadowed, DuplicateSameNameImport}. Consistent RED->GREEN with the sibling tasks.
- Single skill DOMAIN: DB/query layer (cozo_queries.rs) + the typed-policy definition ONLY; NO canonical-resolution wiring here (that is 096.006-T/T5b, which CONSUMES this seam).
- cargo test green (the new helper/policy unit tests); clippy --all-targets clean; fmt clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
DB-helper + typed-fallback SEAM split out of T5b (096.006-T) per C13-4, honoring the 2-hour rule (T5b bundled canonical target selection + the F9 DB helper + typed fallback policy + integration across >=3 files -- exceeds <3 files / single-domain). Provides the two DB/query-layer primitives that T5b's Python canonical-resolution integration CONSUMES: (1) F9 -- a PUBLIC read-only language-scoped name->IDs query helper in cozo_queries.rs. Today the name->IDs singleton is INLINE-PRIVATE CozoScript inside reresolve_calls_edges (cozo_queries.rs:2191-2205,2263-2270) and FILTERS OUT python_bare rows, so it cannot be reused as-is; expose it as a small public helper returning candidate function IDs by (name, language) for the legacy name-only unique-match fallback -- a HELPER EXPOSURE, NOT a schema change (reuse function_ids_by_canonical_path at cozo_queries.rs:1046 unchanged; no function_meta/calls_edge/staging migration). (2) The TYPED no-target fallback POLICY (C9-4): define the typed no-target reason set and the fallback-eligibility rule so downstream T5b can gate the legacy fallback on a typed reason -- fallback fires ONLY for the recall-safe reasons {NoModuleContext (T1-rejected src/-root / PEP 420 / __init__.py), UnsupportedImportForm (provable-namespace-but-unbound: star / re-export / relative)} -> "no canonical edge (fallback allowed)" (Anchor B), and is NEVER taken for the fail-closed reasons {CompetingBindings, Shadowed, DuplicateSameNameImport} -> "no edge" (Anchor B), since emitting the sole indexed legacy edge there would leak a WRONG-CALLEE edge (e.g. a.f when Python's effective target is the later b.f). SINGLE skill domain (DB/query layer); no dependency on the binding-analysis tasks -- a foundational primitive. 096.006-T (T5b) depends_on this task and wires the canonical target selection + integration coverage that consumes the helper and applies the policy. TDD RED->GREEN. Domain: code (DB/query).
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Split from 096.006-T per C13-4 (2-hour rule: T5b bundled canonical target selection + the F9 DB helper + typed fallback policy + integration across >=3 files -- exceeds <3 files / single-domain). This task = the DB-helper + fallback-policy seam ONLY. Files (<=2): src/db/cozo_queries.rs (the F9 public helper exposure -- extraction of the inline-private singleton at 2191-2205,2263-2270; reuse function_ids_by_canonical_path at 1046 unchanged), tests/unit/cozo_queries_test.rs (or the existing query-layer test harness) for the helper + policy-predicate unit tests. NO deps: the helper is a read-only query over existing relations and the typed-policy is a self-contained definition, so this seam does not depend on T1/T2b/T3/T5a. Consumed downstream by 096.006-T (T5b integration), which owns the depends_on edge (096.006-T -> 096.011-T). Keep <=4 scenarios; single DB/query skill domain. Anchor B (Edge Terminology) governs the fallback-allowed vs fail-closed mapping.
<!-- END:implementation-notes -->