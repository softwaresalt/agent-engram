---
artifact_type: task
created_at: 2026-07-13T10:51:27.5383809-07:00
dependencies:
    - 085.003-T
    - 085.005-T
id: 085.006-T
labels:
    - powerbi
    - dax
    - lint
    - mcp-tool
    - agent-native-parity
parent_id: 085-F
priority: high
references:
    - docs/exec-plans/2026-07-13-dax-intelligence-plan.md
    - src/tools/mod.rs
    - src/shim/tools_catalog.rs
status: queued
title: P6 — DAX lint Tier 2 + lint_dax MCP tool (dispatch/catalog/manifest/contract lockstep)
updated_at: 2026-07-13T10:52:06.301729-07:00
---



<!-- BEGIN:acceptance-criteria -->
- Tier-2 rules produce correct findings: broken column/measure refs (not in model), unqualified column that resolves to a real column, qualified reference to a name that is actually a measure, and measure->measure cycles.
- Tier-2 broken-ref rules (`dax.broken_column_ref`, `dax.broken_measure_ref`) obtain their input by REPARSING the indexed model expressions (measure DAX + calculated-column DAX via P2) against the resolved in-memory schema at lint time — NOT from `powerbi_node`/`powerbi_edge`, which persist only resolved endpoints + node identity/path/hash and retain no DAX text or unresolved refs (src/db/cozo_backend/schema.rs:770-796). No CozoDB schema change; a fixture with a broken column ref and a broken measure ref asserts both rules fire.
- `dax.measure_cycle` detection is cycle-SAFE (visited-set / bounded depth) — terminates on self- and mutually-referential measure fixtures (no infinite loop).
- `lint_dax` MCP tool returns `{ conformant, findings[] }` over the bound workspace's indexed model(s), accepting an **optional `source_path`** model selector (omitted = all indexed models; supplied = filter to that one indexed model; a path that is not an indexed model in the bound workspace returns an error result). This lets P7's `engram lint-dax <model.tmdl>` target a single model.
- `TOOL_COUNT` bumped to 21; catalog + manifest + dispatch + should_record_metrics updated in lockstep; `tool_count_matches_dispatch` and the lint_dax schema/error-code contract test pass.
- clippy pedantic clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
DAX lint Tier 2 (semantic) + new `lint_dax` MCP tool. Add Tier-2 schema-aware rules over the INDEXED model: dax.broken_column_ref (Error), dax.broken_measure_ref (Error), dax.unqualified_column (Warning), dax.qualified_measure (Warning), dax.measure_cycle (Error). Tier-2 inputs come from REPARSING the indexed model expressions (measure + calculated-column DAX via P2) against the resolved schema at lint time — the persisted graph (powerbi_node/powerbi_edge) retains no DAX text or unresolved refs (src/db/cozo_backend/schema.rs:770-796), so broken-ref detection re-derives them rather than reading dropped P3 edges; this needs no CozoDB schema change. Add a new `lint_dax` MCP tool returning `{ conformant, findings[] }` for the bound workspace (Tier 1 + Tier 2; Tier 2 needs the resolved schema, hence daemon-side per decision D1), with an optional `source_path` selector (default = every indexed model; supplied = one indexed model; unindexed path = error result) so P7's CLI can target a single model. Register in dispatch + `should_record_metrics` (src/tools/mod.rs), bump `TOOL_COUNT` 20 -> 21 and add the catalog entry (src/shim/tools_catalog.rs) + manifest, and add an agent-native parity contract test. Width: lint service + MCP tool. Largest unit — split at documented seams if >2h.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Subtask split points (promote to siblings if >2h): P6.a Tier-2 semantic rules over the resolved schema (incl. cycle-safe measure->measure detection over P3 edges); P6.b lint_dax tool handler + dispatch + should_record_metrics + TOOL_COUNT/catalog/manifest bump; P6.c contract test (lint_dax schema/error-code + tool_count_matches_*). Test-first: the catalog/contract test is the first-authored FAILING harness (R4). CLI parity (`engram lint-dax`) is delivered by sibling task **P7 (`085.007-T`, depends on this task)** per the 2026-07-13 D4 reversal — **this task (P6) stays MCP-only** (tool + agent-native parity contract test); it does NOT add the CLI subcommand. The broader full-surface MCP↔CLI audit remains with stash `30F372C8`. Grounding: should_record_metrics (src/tools/mod.rs:36), impact_analysis dispatch (:324); TOOL_COUNT=20 (src/shim/tools_catalog.rs:21) asserted by tool_count_matches_dispatch (:381). Depends on P3 (resolved edges/schema) and P5 (VerifyFinding.severity + Tier-1 infra).
<!-- END:implementation-notes -->