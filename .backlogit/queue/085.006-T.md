---
artifact_type: task
created_at: 2026-07-13T10:51:27.5383809-07:00
dependencies:
    - 085.003-T
    - 085.005-T
id: 085.006-T
labels:
    - powerbi
    - dax
    - lint
    - mcp-tool
    - agent-native-parity
parent_id: 085-F
priority: high
references:
    - docs/exec-plans/2026-07-13-dax-intelligence-plan.md
    - src/tools/mod.rs
    - src/shim/tools_catalog.rs
status: queued
title: P6 — DAX lint Tier 2 + lint_dax MCP tool (dispatch/catalog/manifest/contract lockstep)
updated_at: 2026-07-13T10:52:06.301729-07:00
---



<!-- BEGIN:acceptance-criteria -->
- Tier-2 rules produce correct findings: broken column/measure refs (not in model), unqualified column that resolves to a real column, qualified reference to a name that is actually a measure, and measure->measure cycles.
- `dax.measure_cycle` detection is cycle-SAFE (visited-set / bounded depth) — terminates on self- and mutually-referential measure fixtures (no infinite loop).
- `lint_dax` MCP tool returns `{ conformant, findings[] }` over the bound workspace's indexed model(s).
- `TOOL_COUNT` bumped to 21; catalog + manifest + dispatch + should_record_metrics updated in lockstep; `tool_count_matches_dispatch` and the lint_dax schema/error-code contract test pass.
- clippy pedantic clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
DAX lint Tier 2 (semantic) + new `lint_dax` MCP tool. Add Tier-2 schema-aware rules over the INDEXED model: dax.broken_column_ref (Error), dax.broken_measure_ref (Error), dax.unqualified_column (Warning), dax.qualified_measure (Warning), dax.measure_cycle (Error). Add a new `lint_dax` MCP tool returning `{ conformant, findings[] }` for the bound workspace (Tier 1 + Tier 2; Tier 2 needs the resolved schema, hence daemon-side per decision D1). Register in dispatch + `should_record_metrics` (src/tools/mod.rs), bump `TOOL_COUNT` 20 -> 21 and add the catalog entry (src/shim/tools_catalog.rs) + manifest, and add an agent-native parity contract test. Width: lint service + MCP tool. Largest unit — split at documented seams if >2h.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Subtask split points (promote to siblings if >2h): P6.a Tier-2 semantic rules over the resolved schema (incl. cycle-safe measure->measure detection over P3 edges); P6.b lint_dax tool handler + dispatch + should_record_metrics + TOOL_COUNT/catalog/manifest bump; P6.c contract test (lint_dax schema/error-code + tool_count_matches_*). Test-first: the catalog/contract test is the first-authored FAILING harness (R4). CLI parity (engram lint-dax) is DEFERRED to the separate CLI<->MCP parity feature (stash 30F372C8) per decision D4 — this task is MCP tool + parity contract test only. Grounding: should_record_metrics (src/tools/mod.rs:36), impact_analysis dispatch (:324); TOOL_COUNT=20 (src/shim/tools_catalog.rs:21) asserted by tool_count_matches_dispatch (:381). Depends on P3 (resolved edges/schema) and P5 (VerifyFinding.severity + Tier-1 infra).
<!-- END:implementation-notes -->