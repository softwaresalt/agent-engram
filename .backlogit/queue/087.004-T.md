---
artifact_type: task
created_at: 2026-07-14T23:39:47.0821776-07:00
custom_fields:
    source_stash_id: B832AC66
    source_stash_kind: task
    source_stash_path: stash.jsonl
    source_stash_priority: medium
    source_stash_text: 'Symlink-safe source traversal: collect_recursive (powerbi_indexer.rs) follows symlinked directories via is_dir(), so a symlink cycle inside an active content source could infinite-recurse during file collection across ALL collectors (powerbi/pbip/notebook/backlog). lint_dax already canonicalizes+skips files resolving outside the workspace root (PR #246 cycle-3 fix R2/C3-1 containment part), but the shared traversal should use symlink_metadata (no-follow) uniformly to prevent cycle recursion. Deferred from 080-S / PR #246 Copilot finding C3-1.'
id: 087.004-T
parent_id: 087-F
priority: medium
status: queued
title: Symlink-cycle-safe recursive source traversal (all collectors)
updated_at: 2026-07-14T23:39:47.0821776-07:00
---

Make shared recursive source traversal symlink-cycle-safe across ALL collectors. VERIFIED 2026-07-14: collect_recursive (src/services/powerbi_indexer.rs L112-119) tests path.is_dir() (L118) which FOLLOWS symlinks, then recurses — a symlink cycle inside an active content source infinite-recurses during file collection for powerbi/pbip/notebook/backlog. lint_dax already canonicalizes + skips files resolving outside the workspace root (PR#246 cycle-3 fix R2/C3-1 containment). CHOSEN DIRECTION (Stage adversarial decision): do NOT blindly no-follow (that would silently drop legitimate in-workspace symlinked source dirs). Instead: (a) use symlink_metadata (no-follow) to CLASSIFY each entry; (b) for symlinked directories, canonicalize and (i) skip if it resolves OUTSIDE the workspace root (matches lint_dax containment) and (ii) guard against revisiting via a visited-set of canonical real paths to break cycles; (c) traverse real directories normally. This preserves legitimate symlinked sources while making cycles and escapes impossible. TEST-FIRST ACCEPTANCE: (1) failing test first: a symlink cycle under a content source terminates (no stack overflow / no unbounded loop) and collects each real file once; (2) a symlink escaping the workspace root is skipped; (3) a legitimate in-workspace symlinked dir is still collected exactly once; (4) non-symlink traversal behavior unchanged. IMPL NOTES: thread a HashSet<PathBuf> of canonicalized visited dirs through collect_recursive; reuse the existing canonicalize+containment helper if present. Single-width: shared traversal in powerbi_indexer.rs (benefits all collectors). BLAST RADIUS: file collection across all source types (runtime-affecting robustness) — monitoring/rollback note required. Deferred from 080-S / PR#246 finding C3-1. Source stash B832AC66.