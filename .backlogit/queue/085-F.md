---
artifact_type: feature
created_at: 2026-07-13T10:49:09.8130185-07:00
custom_fields:
    harness_status: pending
    source_stash_id: B0E2B374
    source_stash_kind: feature
    source_stash_path: stash.jsonl
    source_stash_priority: high
    source_stash_text: 'DAX intelligence for Power BI (design ready: docs/design-docs/dax-intelligence-design.md). Approved scope (operator 2026-07-05): (C1) safe hand-written DAX reference extractor in crates/powerbi-tmdl-parser (extract_dax_references producing columns/bracket_refs/functions; string+comment-aware lexer; stable seam for a future tree-sitter swap); (C2) DAX lint: Tier 1 syntactic via "engram verify <model.tmdl>" gate + Tier 2 semantic (broken/unqualified/qualified refs, measure cycles) via a new lint_dax MCP tool; add VerifyFinding.severity; (C3) extend impact_analysis to span Power BI nodes (resolve powerbi_node by name; traverse existing powerbi_edge BFS for column -> measure -> visual/report blast radius; additive powerbi_neighborhood + root_kind). Data-model: add PowerBiColumn.expression; emit pbi_uses_field measure->column/measure edges; new find_powerbi_nodes_by_name query. 6-phase plan: P1 extractor -> P2 carry column DAX -> P3 ref edges -> {P4 impact span, P5 lint tier1} -> P6 lint tier2+tool. No CozoDB schema change (powerbi_node/edge exist; BFS already traverses powerbi_edge). Supersedes tree-sitter-from-scratch (no grammar exists; see docs/decisions/2026-07-05-dax-parsing-approach-spike.md). Open Qs: lint_dax standalone vs unified; reuse pbi_uses_field vs new pbi_depends_on_measure; code<->PBI bridge deferred.'
id: 085-F
priority: high
status: active
title: DAX intelligence for Power BI — extractor, lint, and cross-domain impact
updated_at: 2026-07-13T19:54:01.0130046-07:00
---

Make DAX (measure bodies and calculated columns) a first-class, queryable knowledge surface in engram: (C1) a safe, dependency-free hand-written DAX reference extractor in crates/powerbi-tmdl-parser; (C2) two-tier DAX lint (Tier-1 syntactic via `engram verify <model.tmdl>` + Tier-2 semantic via a new `lint_dax` MCP tool, with VerifyFinding.severity); (C3) extend impact_analysis to span Power BI nodes for column->measure->visual/report blast radius. Operator-approved 2026-07-05. Grounded in docs/design-docs/dax-intelligence-design.md and docs/decisions/2026-07-05-dax-parsing-approach-spike.md. Reviewed exec-plan: docs/exec-plans/2026-07-13-dax-intelligence-plan.md (gate PASS).

<!-- BEGIN:dod -->
- P1-P7 tasks complete, each test-first (unit/proptest, contract, integration tiers as specified in the exec-plan). CLI parity (P7 — `engram lint-dax` + a bounded CLI↔MCP parity guard) is IN scope per the 2026-07-13 D4 reversal; the feature MUST NOT close on P1-P6 alone.
- `extract_dax_references` extractor is safe (no unsafe), dependency-free, and the stable seam for a future tree-sitter swap.
- `pbi_uses_field` measure->column and measure->measure edges emitted at index time and queryable via the existing BFS; NO CozoDB schema change.
- `impact_analysis` returns an additive `powerbi_neighborhood` + `root_kind`; the code-symbol path is unchanged (back-compat).
- Tier-1 DAX lint runs in `engram verify <model.tmdl>` (exit codes 0/1/2 preserved); Tier-2 semantic lint available via the new `lint_dax` MCP tool with catalog/manifest/contract in lockstep (TOOL_COUNT 20 -> 21).
- `VerifyFinding.severity` and `PowerBiColumn.expression` are additive (serde default / skip-when-none); existing serialized payloads unaffected.
- Quality gates green: fmt, clippy -D warnings -D clippy::pedantic, tests, audit.
- Fixtures committed (inline r"..." or tests/fixtures/); no reliance on the uncommitted tmp/ILSOS model.
- Open questions Q1/Q2/Q3 resolved per docs/decisions/2026-07-13-dax-open-questions-resolution.md.
<!-- END:dod -->

<!-- BEGIN:goals -->
- **G1 References:** know which tables/columns/measures each measure and calculated column references.
- **G2 Lint:** flag DAX issues (broken refs, unqualified column refs, qualified measure refs, risky patterns) as severity-tagged findings.
- **G3 Cross-domain impact:** answer "if I change this column, what breaks?" by traversing column -> dependent measures -> visuals/reports across the Power BI graph.

Non-goals: DAX evaluation/formatting, full-fidelity AST, a tree-sitter grammar (none exists; superseded by the 2026-07-05 approach spike). Code<->Power BI bridge is deferred (out of scope).
<!-- END:goals -->