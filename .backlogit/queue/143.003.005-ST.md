---
artifact_type: subtask
created_at: 2026-09-11T02:15:02.6718386Z
custom_fields:
    blocked_reason: 'Terminal Revision 3 plan-review FAIL after three cycles; do not claim. Requires a fresh operator-authorized Stage cycle.'
dependencies:
    - 143.003.003-ST
id: 143.003.005-ST
labels:
    - shim
    - status
    - parity
    - observability
parent_id: 143.003-T
priority: critical
status: blocked
title: Surface retry-ledger state through DaemonStatus and HealthReport
updated_at: 2026-09-11T03:07:33.4121599Z
---

SURFACE RETRY-LEDGER STATE THROUGH THE SHARED STATUS TYPES. Added in Revision 3: Revision 2 left the respawn bound invisible to operators and agents, so a workspace approaching its terminal stop looked identical to a healthy one until it stopped working.

AC1 (THREE FIELDS, SHARED TYPES): the consecutive watchdog-termination COUNT, the REMAINING attempts before terminal stop, and the REMAINING COOLDOWN are exposed through the SHARED DaemonStatus and HealthReport types - the existing shared status surface, not a new parallel one.
AC2 (CLI AND MCP PARITY, CONTRACT TESTED): the three fields are returned IDENTICALLY through the CLI and the MCP surface, asserted by a contract test in tests/contract/ alongside the existing parity assertions.
AC3 (PARITY DOC IN THE SAME CHANGE): docs/cli-mcp-parity.md is updated in this same change and the drift guard passes. The parity document is a drift-guarded contract surface, so updating it is part of this contract domain, not a separate docs task.
AC4 (TERMINAL-STOP STATE IS DISTINGUISHABLE): a workspace that has reached its terminal stop reports a state distinguishable from a workspace at count zero and from a workspace mid-cooldown, asserted for all three.

Files: shared status/health types, CLI and MCP status surfaces, docs/cli-mcp-parity.md, plus contract tests. Single contract domain.
Gates (authoritative, in order): cargo fmt --all -- --check; cargo clippy --all-targets -- -D warnings -D clippy::pedantic; cargo dev-test; cargo audit. cargo build is an optional precheck only, not a gate. Do NOT use cargo ci or cargo lint (--all-features is broken on this baseline, risk H6).
Test-first: RED before GREEN.
Size: S | Complexity: medium.
