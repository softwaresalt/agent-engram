---
artifact_type: task
created_at: 2026-08-27T20:50:45.6055519Z
dependencies:
    - 138.002-T
id: 138.009-T
labels:
    - shim
    - tests
    - harness
    - tdd
    - hygiene
    - red
parent_id: 138-F
priority: high
references:
    - tests/contract/shim_stdio_initialize_test.rs
    - src/shim/mod.rs
status: queued
title: 'Red harness: terminal side-effects T5 durable record and T7 message hygiene'
updated_at: 2026-08-27T20:50:45.6055519Z
---

PHASE 2 (harness). Test-only; NO src/ edit. T5 (monitor-driven, NO tools/call issued, so the write is deterministic): assert exactly ONE ADDITIONAL protocol_incompatible startup-failure record beyond the pre-existing readiness_timeout record that is ALREADY written on this path at src/shim/mod.rs:218-221; the pre-existing readiness_timeout record must be present and byte-unchanged; the new record's failure_class=='protocol_incompatible', message equals the fixed record_message() string, key set unchanged vs a golden record, and no filesystem path or environment-variable value appears anywhere in the line; further monitor iterations append no second protocol_incompatible line. T7 message hygiene, TWO cases because there are two daemon-controlled text sources: (a) _health JSON-RPC error whose message embeds a path and an env value; (b) undecodable result payload embedding a path (serde error text was formatted into the reason at src/shim/lifecycle.rs:113-117 and HealthCheckResult.workspace at src/daemon/ipc_server.rs:374 legitimately carries a workspace path). In BOTH cases assert neither string appears in the tools/call content text, structuredContent, tracing warn fields, or the durable record; only fixed TerminalKind-derived text is emitted. NEW assertions must start RED.