---
artifact_type: task
created_at: 2026-07-13T10:50:38.0831563-07:00
dependencies:
    - 085.001-T
    - 085.002-T
id: 085.003-T
labels:
    - powerbi
    - dax
    - indexer
    - graph
parent_id: 085-F
priority: high
references:
    - docs/exec-plans/2026-07-13-dax-intelligence-plan.md
    - src/services/powerbi_indexer.rs
    - src/models/powerbi_graph.rs
status: queued
title: P3 — Reference edges in the indexer (resolve refs -> pbi_uses_field measure->column/measure)
updated_at: 2026-07-13T10:52:05.6894837-07:00
---



<!-- BEGIN:acceptance-criteria -->
- Measures referencing a column produce `pbi_uses_field` measure->column edges; measures referencing another measure produce measure->measure edges.
- Calculated columns referencing a column/measure produce `pbi_uses_field` calculated_column->column and calculated_column->measure edges (the calculated column's `pbi_column` node is the edge SOURCE), so G1 covers what each calculated column references; the contract fixture includes at least one calculated-column reference edge.
- Bracket refs [Name] are disambiguated column-vs-measure against the model schema; refs with no schema match are recorded as unresolved, not fabricated.
- Contract test: indexing a committed inline `.tmdl` fixture yields the expected `pbi_uses_field` edges (queryable via query_graph_neighborhood / pbi_* filters).
- No CozoDB relation schema change; existing PBIP visual->measure edges unaffected.
- clippy pedantic clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Emit DAX reference edges in the indexer. In `build_powerbi_graph_data_from_model()` (src/services/powerbi_indexer.rs:533), for each measure (and calculated column via P2's carried expression) call `extract_dax_references`, resolve each ref against the in-model schema (known tables/columns/measures) to an existing `pbi_` node id via `make_node_id` (:489), and emit `PowerBiEdgeType::UsesField` (pbi_uses_field) edges from BOTH sources: measure->column, measure->measure, calculated_column->column, and calculated_column->measure (the calculated column's `pbi_column` node = source). This makes a `pbi_column` a valid UsesField SOURCE (not only a target); update the UsesField doc-comment (src/models/powerbi_graph.rs:124-131) to state it covers a measure OR a calculated column referencing a column/measure. Reuse the existing UsesField edge type per decision D2 — NO new edge type, NO CozoDB schema change. Unresolved refs are recorded/dropped, never guessed (R1/R2). Width: indexer. This is one of the two largest units — split at the documented seams if it exceeds ~2h.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Subtask split points (promote to sibling tasks only if >2h): P3.a ref->node-id resolution helper over the model schema (bracket disambiguation + unresolved bucket); P3.b emit pbi_uses_field measure->column/measure edges from resolved refs. Keep the resolution helper INSIDE the indexer module so the parser crate stays pure. Test-first: author the contract test asserting the edges before emission logic. Grounding: make_node_id (:489), build_powerbi_graph_data_from_model (:533), PowerBiEdgeType::UsesField -> pbi_uses_field (src/models/powerbi_graph.rs:131,149). Analogous prior art for candidate-list resolution: SQL quoted-identifier resolution. Depends on P1 (extractor) and P2 (calculated-column expression).
<!-- END:implementation-notes -->