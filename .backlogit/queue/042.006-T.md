---
id: "042.006-T"
title: "Search and query subcommands (6 commands)"
artifact_type: task
status: queued
parent_id: "042-F"
priority: medium
dependencies:
  - 042.002-T
description: "Implement 6 search/query CLI subcommands with full parameter mapping"
labels: "cli,search,query"
created_at: 2026-05-06T19:50:00Z
updated_at: 2026-05-06T19:50:00Z
---

## Scope

Create `src/cli/commands/search.rs` with 6 handlers:

| CLI Command | MCP Method | Key Params |
|---|---|---|
| `engram search <q> [--region R] [--limit N] [--content-type T] [--scope-to S]` | `unified_search` | query, region, limit, content_type, scope_to_symbol |
| `engram query-memory <q> [--limit N] [--content-type T]` | `query_memory` | query, limit, content_type |
| `engram symbols [--file F] [--type T] [--prefix P] [--limit N] [--offset O]` | `list_symbols` | file_path, node_type, name_prefix, limit, offset |
| `engram map-code <name> [--depth D] [--max-nodes M]` | `map_code` | symbol_name, depth, max_nodes |
| `engram impact <name> [--depth D] [--max-nodes M] [--concept C]` | `impact_analysis` | symbol_name, depth, max_nodes, concept |
| `engram query-graph <q>` | `query_graph` | query |

Parameter names MUST match daemon handler struct field names exactly.

## Acceptance Criteria

- All 6 commands construct correct IpcRequest with proper method and exact param names
- Optional params are omitted from JSON when not provided (serde skip_serializing_if)
- `query-graph` surfaces graceful stub error (exit 1 with JSON-RPC error, not panic)
- Contract tests verify exact params JSON for each command variant

## Execution Posture

Test-first: write contract tests for parameter mapping, then implement.

## Dependencies

- 042.002-T (IPC runner)
