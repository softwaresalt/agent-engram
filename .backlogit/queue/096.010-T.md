---
artifact_type: task
created_at: 2026-07-23T20:10:07.9385364-07:00
dependencies:
    - 096.003-T
    - 096.006-T
id: 096.010-T
labels:
    - python
    - indexing
    - rollout
    - code
parent_id: 096-F
priority: medium
references:
    - src/services/code_graph.rs
status: queued
title: 'T7: Rollout -- versioned re-extraction + one-step resolution backfill (upgrade)'
updated_at: 2026-07-23T20:10:07.9385364-07:00
---



<!-- BEGIN:acceptance-criteria -->
- Q2 upgrade regression: after a version-bump SYNC of an unchanged-hash .py file, the cross-module RESOLVED EDGE is present (assert the resolved edge, NOT merely the def's canonical_path) -- restored in one operation, no unresolved-edge window.
- A file already at the current extraction version is still skipped (content-hash fast-path preserved; no perf regression).
- Rust files are unaffected (regression).
- cargo test --test integration_code_graph green; clippy --all-targets clean; fmt clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
M4+Q2: existing indexes will NOT acquire Python canonical EDGES through normal incremental indexing. Full-index and sync skip files whose content hash is unchanged (code_graph.rs:590-599 and :1252-1263), force defaults to false, and the canonical call post-pass runs on the full-index path ONLY (code_graph.rs:985-992). Q2: a versioned-hash bump also fires on startup auto-SYNC, but sync re-extracts + clears prior resolved edges/staging then EXITS WITHOUT running the post-pass -- leaving staged calls UNRESOLVED until a later full index (a regression window). Re-extraction alone is insufficient. Fold a versioned extraction constant into the Python file content-hash comparison (versioned-extraction-hash option -- NO new schema column): a bump invalidates the cached hash for .py files so they re-extract. Crucially (Q2), when the extraction version changes the operation must ALSO run a full canonical resolution pass in the SAME step -- either escalate that run to the full-index path (which runs the post-pass) or invoke the canonical post-pass from the sync path -- so resolved edges are restored in ONE operation, never left staged-but-unresolved. Documented fallback: a one-shot forced index --force. Replace the earlier 'no migration' claim with this real, single-step backfill trigger. TDD RED->GREEN. Domain: code.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
New rollout task added under PR #285 M4 (split from T3 to keep a single concern per task); Q2 hardens it to resolve edges in one operation. Depends on T3 (096.003-T populator) and T5b (096.006-T resolver) so the re-extraction + resolution pass produces real canonical paths + edges. The upgrade regression test asserts the RESOLVED cross-module edge after the version-bump sync, not merely canonical_path. Keeps the zero-schema-change invariant (hash input change only). Files (<=2): src/services/code_graph.rs, tests/integration/code_graph_test.rs.
<!-- END:implementation-notes -->