---
artifact_type: task
created_at: 2026-07-23T20:10:07.9385364-07:00
dependencies:
    - 096.003-T
    - 096.006-T
id: 096.010-T
labels:
    - python
    - indexing
    - rollout
    - code
parent_id: 096-F
priority: medium
references:
    - src/services/code_graph.rs
status: queued
title: 'T7: Rollout -- versioned re-extraction + backfill trigger (upgrade)'
updated_at: 2026-07-23T20:10:07.9385364-07:00
---



<!-- BEGIN:acceptance-criteria -->
- M4: an already-indexed .py file with an UNCHANGED content hash but an OLDER extraction version is re-extracted and acquires its Python canonical_path (upgrade regression).
- A file already at the current extraction version is still skipped (content-hash fast-path preserved; no perf regression).
- Rust files are unaffected (regression).
- cargo test --test integration_code_graph green; clippy --all-targets clean; fmt clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
M4: existing indexes will NOT acquire Python canonical paths through normal incremental indexing. Both full-index and sync skip files whose content hash is unchanged (code_graph.rs:590-599 and :1252-1263), force defaults to false, and the canonical call post-pass runs on the full-index path ONLY (code_graph.rs:985-992). After upgrade, unchanged .py files keep EMPTY canonical paths and stale staging unless a rebuild is forced. Fold a versioned extraction constant into the Python file content-hash comparison (the versioned-extraction-hash option -- NO new schema column): bumping the constant invalidates the cached hash for .py files, so the full-index path re-extracts them AND the canonical post-pass re-runs. Documented fallback: a one-shot forced backfill (index --force). Replace the plan's earlier 'no migration' claim with this real backfill trigger. TDD RED->GREEN. Domain: code.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
New rollout task added under PR #285 M4 (split from T3 to keep a single concern per task). Depends on T3 (096.003-T populator) and T5b (096.006-T resolver) so the re-extraction produces real canonical paths + edges. Keeps the zero-schema-change invariant (hash input change only). Files (<=2): src/services/code_graph.rs, tests/integration/code_graph_test.rs.
<!-- END:implementation-notes -->