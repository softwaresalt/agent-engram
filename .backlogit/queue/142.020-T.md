---
artifact_type: task
created_at: 2026-09-03T00:20:00.8365918Z
dependencies:
    - 142.013-T
    - 142.014-T
    - 142.015-T
    - 142.006-T
id: 142.020-T
parent_id: 142-F
priority: high
status: queued
title: Build supervisor crate foundation and boundary harness
updated_at: 2026-09-03T01:37:49.4532751Z
---

Plan unit F12 - supervisor crate foundation. Build out `crates/engram-indexer` as the separately distributed non-agent supervisor. Add `engram = { path = "../.." }` plus the dev-dependencies its boundary harness needs to the crate manifest F12a created, and implement `main.rs` / `lib.rs` so both the build and publish entry points live in the supervisor crate and consume only the minimal public facade from F06-F10.

The agent `engram` package defines no supervisor binary. The supervisor never appears in the agent CLI or MCP catalogs.

Execution posture: test-first.

<!-- BEGIN:acceptance-criteria -->
- `crates/engram-indexer/Cargo.toml` gains `engram = { path = "../.." }` and the dev-dependencies the boundary harness requires - dependency sections only.
- `crates/engram-indexer/src/main.rs` and `src/lib.rs` implement the build and publish entry points.
- The crate declares `#![forbid(unsafe_code)]`.
- Build and publish go through F07-F10 only; the supervisor mints no `IndexTarget` of its own.
- `crates/engram-indexer/tests/supervisor_boundary_test.rs` builds and is GREEN.
- The agent package declares no supervisor binary.
- The supervisor appears in neither the CLI nor the MCP catalog.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Size: M | Complexity: medium | size_source: agent | size_ruleset_version: engram-stage-2h-rule-v1. Recorded as prose because this workspace task type defines no structured size/complexity field. Size is effort/volume; complexity is difficulty/uncertainty; the two axes are independent.

Owned files: `crates/engram-indexer/Cargo.toml` (dependency sections ONLY; after F12a), `crates/engram-indexer/src/main.rs`, `crates/engram-indexer/src/lib.rs`, `crates/engram-indexer/tests/supervisor_boundary_test.rs`.
`crates/engram-indexer/Cargo.toml` has exactly two owners in declared order `F12a -> F12` over disjoint sections.
The boundary harness is auto-discovered inside the supervisor crate's own `tests/` directory, so root `Cargo.toml` never registers it and F12 is excluded from the F00 registration table.
Revision 7 P1-1: without the `engram` path dependency the RED harness cannot resolve the public facade and does not build. The workspace-stub-before-RED order (F12a then F12) is preserved.
Edges: F07 + F08 + F09 + F10 -> F12; F12a -> F12 -> F13 -> F14; F12 -> F15.
Governed by amended R39: candidate build and publication belong to a separately distributed supervisor executable.
<!-- END:implementation-notes -->

<!-- BEGIN:verification -->
1. `cargo test -p engram-indexer` is GREEN with the boundary harness present.
2. `cargo dev-test`, `cargo ci`.
3. `cargo lint` confirms `#![forbid(unsafe_code)]` holds.
<!-- END:verification -->