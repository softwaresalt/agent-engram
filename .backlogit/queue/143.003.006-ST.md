---
artifact_type: subtask
created_at: 2026-09-11T02:18:42.7775419Z
custom_fields:
    blocked_reason: 'Terminal Revision 3 plan-review FAIL after three cycles; do not claim. Requires a fresh operator-authorized Stage cycle.'
dependencies:
    - 143.003.002-ST
id: 143.003.006-ST
labels:
    - daemon
    - watchdog
    - lifecycle
    - termination
parent_id: 143.003-T
priority: critical
status: blocked
title: Exact-process forced exit and endpoint/fd_lock reacquisition proof
updated_at: 2026-09-11T03:08:11.4520016Z
---

SECOND HALF OF THE WATCHDOG HARD DEADLINE: exact-process forced exit and the proof that the next process can actually start. Split from 143.003.002-ST in Revision 3 to keep both leaves inside the 2-hour rule. Carries risky action PA-2.

Lock and endpoint release is only meaningful if the NEXT process can take them, so reacquisition must be PROVEN, not assumed.

AC1 (NEVER AWAIT NON-CANCELLABLE WORK, NON-NEGOTIABLE): the bootstrap runs inside spawn_blocking; a blocking thread stuck inside Cozo cannot be cancelled by dropping its handle, and awaiting it would make the watchdog hang on the very stall it exists to break. A test asserts the forced-exit path DOES NOT AWAIT the stuck task, and that a bootstrap blocked inside a non-cancellable spawn_blocking closure still exits within budget.
AC2 (EXACT-PROCESS IDENTITY ONLY): forced termination targets THIS EXACT PROCESS by its own identity, NEVER a name-matched or pattern-matched process set. A test proves no sibling workspace daemon and no unrelated process is signalled.
AC3 (SUBPROCESS REACQUISITION PROOF, NON-NEGOTIABLE): after a watchdog forced exit, a FRESHLY SPAWNED DAEMON SUBPROCESS successfully binds the IPC endpoint AND acquires fd_lock. An in-process assertion is INSUFFICIENT - it cannot observe an OS-held handle that survives only in the terminated process. The test must spawn a real child process.
AC4 (DISTINCT EXIT CODE): the process exits with a distinct non-zero exit code reserved for watchdog self-termination, asserted by the same subprocess test and agreeing with the 143.004.002-ST class allocation.
AC5 (GATE G7, JOINT): contributes to G7 jointly with 143.003.002-ST, 143.004.001-ST, and 143.004.002-ST.
AC6 (PA-2 APPROVAL BOUNDARY, PRECISE): CP-3 operator approval MUST be recorded BEFORE THE FIRST REAL TERMINATION EXECUTION. Simulation-safe tests against an injected exit seam MAY run before approval. AC3 requires a real child-process exit and therefore REQUIRES CP-3 approval before it is executed. If approval is withheld, this leaf returns blocked rather than shipping the forced-exit path unexercised.
AC7 (NO WORKSTREAM-B DEPENDENCY): no dependency on gate G3, on 144-F, or on any 144-S item.

Files: daemon lifecycle/supervisor path plus a subprocess test harness. Single domain: code with its own tests.
Gates (authoritative, in order): cargo fmt --all -- --check; cargo clippy --all-targets -- -D warnings -D clippy::pedantic; cargo dev-test; cargo audit. cargo build is an optional precheck only, not a gate. Do NOT use cargo ci or cargo lint (--all-features is broken on this baseline, risk H6).
Test-first: RED before GREEN.
Size: M | Complexity: high.
