---
artifact_type: feature
created_at: 2026-07-14T23:36:03.6148995-07:00
custom_fields:
    harness_status: pending
    source_stash_id: 30F372C8
    source_stash_kind: feature
    source_stash_path: stash.jsonl
    source_stash_priority: medium
    source_stash_text: 'Full CLI <-> MCP parity: the MCP tool surface (src/tools/mod.rs dispatch) and the CLI command service (src/cli/commands/, src/bin/engram.rs) MUST mirror each other in functionality AND documentation, and ship a complete MCP-to-CLI command mapping doc. Current state (2026-07-05): substantial parity already exists and CLI --help annotates each subcommand with the MCP tool it mirrors (bind=set_workspace, daemon-status=get_daemon_status, workspace-status=get_workspace_status, flush=flush_state, sync=sync_workspace, index=index_workspace, search=unified_search, query-memory=query_memory, symbols=list_symbols, map-code=map_code, impact=impact_analysis, query-graph=query_graph, stats=get_workspace_statistics, health=get_health_report, branch-metrics=get_branch_metrics, report token-savings/eval/retry-metrics=get_token_savings_report/get_evaluation_report/get_mutable_script_retry_metrics). GAPS to close (MCP tools with no CLI subcommand): query_graph_neighborhood, create_task, update_task, query_changes (git-graph), index_git_history (git-graph), and the planned lint_dax (B0E2B374). Also verify reverse direction (every CLI subcommand maps to an MCP tool) and that verify/manifest local-only commands are documented as such. Deliverables: (1) close functional gaps so every agent-facing MCP tool has an equivalent engram <cmd> subcommand and vice versa (respecting daemon-vs-direct/local distinctions); (2) a canonical, maintained mapping doc (table: MCP tool | CLI command | daemon/local | notes) under docs/ and referenced from both surfaces; (3) doc parity: CLI subcommand help references its MCP tool (already a convention) AND MCP tool docs/catalog reference the CLI command; (4) a parity guard test/contract that fails when a new MCP tool is added without a CLI mapping (and vice versa) to prevent drift. Grounding: src/tools/mod.rs (dispatch + should_record_metrics), src/shim/tools_catalog.rs (compile-time catalog / manifest), src/cli/commands/*.rs, src/bin/engram.rs (subcommand help already annotates MCP names).'
id: 090-F
priority: medium
status: queued
title: 'CLI <-> MCP full parity: audit, canonical mapping doc & drift guard'
updated_at: 2026-07-14T23:36:03.6148995-07:00
---

Full CLI<->MCP parity. NOTE: the original stash gap list (dated 2026-07-05) is now PARTIALLY STALE — the lint-dax CLI subcommand and the query-graph 'neighborhood' operation now exist (verified 2026-07-14 against src/bin/engram.rs). This feature therefore BEGINS with a fresh parity audit that re-derives the actual current gaps, then delivers: (1) a canonical, maintained MCP-tool<->CLI-command mapping doc under docs/ (table: MCP tool | CLI command | daemon/local | notes) referenced from both surfaces; (2) bidirectional doc parity (CLI --help already annotates its MCP tool; the MCP tool catalog/docs must reference the CLI command); (3) a parity guard test/contract that fails when a new MCP tool is added without a CLI mapping and vice versa. Functional gap-closing (e.g. MCP tools absent from tools_catalog.rs such as create_task/update_task/query_changes/index_git_history, plus any CLI-only or local-only commands) is DEFERRED to a post-audit re-harvest because its concrete scope depends on the audit findings. Source stash 30F372C8. Grounding: src/tools/mod.rs (dispatch + should_record_metrics), src/shim/tools_catalog.rs (compile-time catalog/manifest), src/cli/commands/*.rs, src/bin/engram.rs.