---
artifact_type: task
created_at: 2026-04-28T17:41:19.2128959-07:00
id: 033.004-T
labels:
    - sql-parser
    - db-schema
parent_id: 033-F
priority: medium
references:
    - src/db/schema.rs
    - src/db/queries.rs
    - src/db/cozo_queries.rs
status: queued
title: Add references relation table and DB edge operations
updated_at: 2026-04-28T17:41:19.2128959-07:00
---

Add the `references` relation table to the SurrealDB schema and create/delete operations in both DB backends. This is the data-layer prerequisite for wiring SQL reference resolution in the code graph service.

Implementation (from plan Unit 1):
- `src/db/schema.rs`: Add `DEFINE TABLE IF NOT EXISTS \`references\` SCHEMALESS TYPE RELATION;` with `created_at` datetime and optional `qualified_name` string field. Use backtick escaping (P2-3 from plan review).
- `src/db/queries.rs`: Add `create_references_edge(source_id, target_id, qualified_name)`. Source is a `code_file` node; target is a `class` node (resolved) or `code_file` (unresolved). Add `delete_edges_from_file("references", ...)` call in cleanup paths.
- `src/db/cozo_queries.rs`: Add matching stub method (CozoDB edge methods are all stubs returning `Err(backend_err())`).

Tests: Contract test verifying edge creation round-trip against SurrealDB.
Execution posture: Test-first.
Plan reference: docs/exec-plans/2026-04-28-033-F-sql-parser-enhancements-plan.md, Unit 1