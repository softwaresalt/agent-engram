---
artifact_type: task
created_at: 2026-07-23T18:45:22.3168835-07:00
id: 096.002-T
labels:
    - python
    - canonical-identity
    - code
parent_id: 096-F
priority: medium
references:
    - src/services/parsing/canonical/use_graph.rs
    - src/services/parsing/python_canonical/bindings.rs
status: queued
title: 'T2: Python import-binding capture (symbol-level, fail-closed)'
updated_at: 2026-07-23T20:08:00.3956717-07:00
---



<!-- BEGIN:acceptance-criteria -->
- from p import f -> {f: p.f}; from p import f as g -> {g: p.f}; import a.b as c -> {c: a.b}.
- from p import * and from . import x produce NO binding.
- M1: competing/duplicate binding for the SAME local name (e.g. `import p` + `from q import p`) -> NO binding (fail closed; flat last-writer-wins map forbidden).
- cargo test --test unit_python_canonical green (target registered by T1/M3); clippy --all-targets -D warnings -D pedantic clean; fmt clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
New src/services/parsing/python_canonical/bindings.rs. extract_python_import_bindings(source) -> ImportBindings mapping a LOCAL name to its canonical origin, by walking tree-sitter import_statement / import_from_statement nodes. from N import name -> name=N.name; from N import name as p -> p=N.name; import a.b as c -> c=a.b; import a.b -> a=a. Fail closed (NO binding) for star (from N import *), relative imports (from . import x / leading-dot), and importlib/__import__/dynamic. M1: a competing/duplicate binding for the same local name within a scope is marked ambiguous -> NO binding; a flat last-writer-wins HashMap is FORBIDDEN. Function-vs-module scope isolation is handled by T2b (096.009-T). The Python analogue of Rust's UseGraph. TDD RED->GREEN. Domain: code.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Grammar pre-check FIRST: confirm tree-sitter-python 0.23 node/field names via a debug tree-walk (import_from_statement, dotted_name, aliased_import, wildcard_import, relative_import) before coding. Same-crate unit tests exercise the fn (no dead_code). Verification uses the unit_python_canonical target registered by T1 (096.001-T, M3). ImportBindings is the base type extended into a scoped model by T2b (096.009-T). Files (<=3): python_canonical/bindings.rs, python_canonical/mod.rs (re-export), tests/unit/python_canonical_test.rs.
<!-- END:implementation-notes -->