---
artifact_type: task
created_at: 2026-05-08T22:38:48.6681388-07:00
id: 046.003-T
labels:
    - cli
    - installer
    - feature
parent_id: 046-F
priority: medium
status: queued
title: Add .backlogit/ to AUTO_DETECT_DIRS for registry scaffold
updated_at: 2026-05-08T22:38:48.6681388-07:00
---

Add `(".backlogit", "backlog", Some("markdown"))` to the `AUTO_DETECT_DIRS` constant in `src/installer/mod.rs` so that `generate_default_registry()` includes a backlog source entry when `.backlogit/` exists.

## Scope

- `src/installer/mod.rs` — add 1 entry to `AUTO_DETECT_DIRS`
- `tests/integration/installer_test.rs` — add test for `.backlogit/` detection

## Acceptance Criteria

- Test-first: write test that creates workspace with `.backlogit/`, runs `install()`, verifies `registry.yaml` contains `path: .backlogit`
- Test fails before the change, passes after
- Existing `.backlog` entry is preserved (both coexist)
- `cargo test` passes