---
artifact_type: subtask
created_at: 2026-09-11T01:22:55.0204646Z
custom_fields:
    blocked_reason: 'Terminal Revision 3 plan-review FAIL after three cycles; do not claim. Requires a fresh operator-authorized Stage cycle.'
id: 143.004.001-ST
labels:
    - daemon
    - observability
    - diagnostics
parent_id: 143.004-T
priority: critical
status: blocked
title: Daemon-owned diagnostics sink (A-owned, single-writer)
updated_at: 2026-09-11T03:10:58.6451228Z
---

Establish a diagnostics sink OWNED BY WORKSTREAM A. Resolves risk H11 and hardening constraint H14, and closes operator decision OD-3 structurally.

CRITICAL ORDERING CONSTRAINT: this sink MUST NOT depend on 144.001-T or any Workstream B leaf. Workstream A ships first and independently; a dependency on the B startup sink would make 143-S unshippable as a recovery shipment. Workstream A monitoring signals read THIS sink, not the B sink.

AC1: a NEW, SEPARATE daemon-owned sink is created. The shim-owned .engram/diagnostics/shim-startup-failures.jsonl is NOT appended to by the daemon. The shared-file option from Revision 1 is removed entirely, which is why no per-platform atomic-append proof is needed.
AC2 (OWNER-ONLY, INTERLEAVING-SAFE): the sink has exactly ONE writing process by construction. Within that process all writes are SERIALIZED so records cannot interleave. A test writes concurrently from multiple tasks and asserts every record parses whole.
AC3: records are line-oriented and carry an explicit schema version.
AC4 (H14): the per-platform durability mechanism is NAMED in code and in a doc comment, for both Windows and POSIX.
AC5 (constitution III/IV): the sink path resolves under the workspace root and is capability-relative, no-follow, and owner-only.
AC6 (H14, DURABLE FLUSH BEFORE EXIT): every record is explicitly FLUSHED before process exit and before any rename or rotation. A test kills the process immediately after a write and asserts the record is present and complete. An unflushed record is indistinguishable from no record; this exact defect already cost this repository one shipment (tokio-fs-file-flush-before-rename-2026-07-03).
AC7 (GATE G7 part 2): the terminal cause is DURABLY recorded before exit.

Files: src/daemon/ (diagnostics sink module), plus tests. Single domain: code with its own tests.
Gates (authoritative, in order): cargo fmt --all -- --check; cargo clippy --all-targets -- -D warnings -D clippy::pedantic; cargo dev-test; cargo audit. cargo build is an optional precheck only, not a gate. Do NOT use cargo ci or cargo lint (--all-features is broken on this baseline, risk H6).
Test-first: RED before GREEN.
Size: M | Complexity: medium.
