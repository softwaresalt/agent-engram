---
artifact_type: task
created_at: 2026-08-25T05:22:50.9341495Z
dependencies:
    - 131.012-T
id: 131.013-T
labels:
    - green
    - otlp
    - rust
    - daemon-lifecycle
    - cleanup-isolation
    - spawn-failure
    - error-propagation
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
status: queued
title: 'GREEN: launch cleanup safely and propagate spawn failure without panic'
updated_at: 2026-08-25T07:46:57.5942581Z
---

Make the unchanged 131.012-T spawn-failure RED GREEN and green the returned-results branch of 131.010-T. Launch one detached named native worker only through the injected std::thread::Builder adapter. Propagate Builder::spawn failure through EngramError and an exact diagnostic without unwrap, expect, panic, unsafe, synchronous cleanup fallback, retry, or loss of the explicit provider owner.

<!-- BEGIN:acceptance-criteria -->
1. 131.012-T is complete with compile-then-fail evidence and remains the explicit dependency. 2. The production adapter calls std::thread::Builder::new().name(...).spawn(job) exactly once and preserves the returned Result. No std::thread::spawn, unwrap, expect, panic, unsafe, spawn_blocking, runtime task, join, retry, or fallback worker is permitted. 3. The caller and job share an Arc-backed ownership cell until spawn succeeds. On success the caller releases its duplicate and the worker safely takes the explicit provider owner, calls force_flush once, then calls shutdown once whenever flush returns Ok or Err, and publishes phase/completion. Lock poisoning or ownership transfer failure is propagated and diagnosed, never unwrapped. 4. On Builder::spawn Err, the caller retains the ownership cell for process lifetime so provider Drop is not misreported or used as synchronous cleanup. Return the typed EngramError spawn failure for a clean daemon; when a daemon error already exists, preserve it as primary and emit the full cleanup spawn diagnostic once. The diagnostic states cleanup_attempted=false, completion=not_started, worker_detached=false, and provider_residual=retained_for_process_lifetime. 5. The unchanged 131.012-T command passes deterministically, and the nonstalling returned-results assertions from 131.010-T pass. Force-flush-stall, shutdown-stall, deadline, channel-loss/panic, and combined timeout precedence remain explicitly RED for 131.014-T; this intermediate helper is not shipment-executable. 6. No provider construction, endpoint, attachment, Cargo, lockfile, config, network, environment, or unrelated lifecycle edit is included.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Daemon cleanup launch/error-propagation skill only; exactly 1 production file, at most 4 functions or trait methods, 3 successful-launch/spawn-failure/combined-precedence scenario groups, and 100 minutes. Atomic milestone: native worker creation is fallible, deterministic, non-panicking, and ownership-safe while timeout behavior remains separately owned.
<!-- END:implementation-notes -->