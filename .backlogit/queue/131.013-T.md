---
artifact_type: task
created_at: 2026-08-25T05:22:50.9341495Z
dependencies:
    - 131.012-T
id: 131.013-T
labels:
    - test-first
    - red
    - compiling-harness
    - otlp
    - rust
    - daemon-lifecycle
    - cleanup-isolation
    - spawn-failure
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
status: queued
title: 'RED: deterministically prove cleanup-worker spawn failure is safe and visible'
updated_at: 2026-08-25T08:42:58.972441Z
---

Add a deterministic daemon-lifecycle RED against the 131.012-T injectable spawner and residual sink. Fakes return realistic std::io::Error kinds without consuming OS thread resources. The test proves spawn failure becomes the exact dedicated EngramError, performs no synchronous cleanup fallback, and preserves the explicit provider owner as a fatal fail-exit residual.

<!-- BEGIN:acceptance-criteria -->
1. 131.012-T is complete and remains the explicit dependency. 2. Exact RED command: cargo test --no-default-features --features cozo-backend,otlp-export --bin engram otlp_cleanup_spawn_failure_red -- --nocapture. Production bin and tests compile before assertions execute. 3. The fake spawner records one invocation and returns WouldBlock and OutOfMemory in a parameterized kind-agnostic scenario with marker forced cleanup-worker spawn failure; no OS limits, allocation exhaustion, sleep, or panic hook is used. 4. For a clean daemon expect Err(EngramError::Daemon(DaemonError::CleanupWorkerSpawnFailed { reason })) with the distinct stable code. Diagnostic fields are operation=otlp_cleanup_worker_spawn, cleanup_attempted=false, completion=not_started, worker_detached=false, and provider_residual=retained_for_process_lifetime; endpoint values and secrets are absent or redacted. 5. Assert force_flush and shutdown are each zero, no completion wait starts, the injected residual sink receives the caller-retained owner exactly once, and existing daemon error plus spawn failure preserves daemon error primary while emitting the spawn diagnostic once. Current RED is that spawner and sink are not invoked. 6. 131.014-T owns GREEN. No production edit, generic daemon SpawnFailed reuse, unwrap, expect, panic, unsafe, thread::spawn, spawn_blocking, join, retry, network, environment mutation, manifest, lockfile, or config change is included.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Daemon cleanup spawn-failure RED-harness skill only; test-only module in exactly 1 existing file, at most 4 test or helper functions, 3 clean-failure, combined-precedence, and ownership-retention scenario groups, and 90 minutes. Atomic milestone: a compile-then-fail deterministic contract names the exact GREEN owner and proves spawn failure cannot panic or silently enter synchronous cleanup.
<!-- END:implementation-notes -->
