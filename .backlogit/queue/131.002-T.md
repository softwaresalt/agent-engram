---
artifact_type: task
commit: 4cab736e05ebd832e048a0338949ef62c0089b2d
created_at: 2026-08-24T22:54:13.9033059Z
dependencies:
    - 131.001-T
id: 131.002-T
labels:
    - green
    - otlp
    - rust
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/server/observability.rs
    - src/lib.rs
    - src/bin/engram.rs
    - src/config/mod.rs
status: queued
title: 'GREEN: attach and retain OTLP through the daemon lifecycle'
updated_at: 2026-08-25T03:47:41.6982021Z
---

Own the complete OTLP GREEN implementation: align tracing-opentelemetry 0.26 to 0.27, migrate to the pinned 0.26 exporter API, replace the layer-only return with an explicit lifecycle owner, attach the configured layer through production daemon tracing initialization, retain its TracerProvider across daemon execution, and implement coordinated bounded exit-time flush/shutdown with propagated errors. Turn 131.001-T GREEN.

<!-- BEGIN:acceptance-criteria -->
1. The recorded RED task 131.001-T remains the explicit dependency and its lifecycle tests precede implementation. 2. Only tracing-opentelemetry changes from 0.26 to 0.27; Cargo.lock reconciles to the direct OpenTelemetry 0.26 stack with no unrelated dependency drift. 3. The 0.26 TracerProvider, new_exporter().tonic().with_endpoint(...).build_span_exporter(), and runtime::Tokio APIs are used. 4. build_otlp_layer no longer returns an unowned or uncalled layer: production daemon tracing initialization consumes the configured endpoint, attaches OTLP beside the stderr formatting layer, and returns a runtime/guard owning the provider and attachment contract. 5. The daemon command retains that guard across the complete `engram::daemon::run` future and invokes explicit exactly-once bounded flush/shutdown on every exit path; a daemon error remains primary if both operations fail, while shutdown failure after a clean run is returned. 6. The unconfigured/default build and shim remain formatting-only. 7. A private/test-only exporter seam exercises the same provider/layer ownership and production-initialization path without changing endpoint behavior or requiring network. 8. All 131.001-T production attachment, emitted-span, flush, and bounded-failure contracts pass. 9. No unrelated observability redesign.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Cargo.toml, Cargo.lock, src/server/observability.rs, src/lib.rs, the daemon branch in src/bin/engram.rs, endpoint plumbing in src/config/mod.rs if needed, and focused tests; target <=130 minutes. This task explicitly owns production attachment, provider retention, and shutdown/flush semantics. Preserve the non-feature no-op behavior, but do not preserve an uncalled feature-enabled builder or the invalid layer-only return contract.
<!-- END:implementation-notes -->
