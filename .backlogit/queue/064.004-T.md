---
artifact_type: task
commit: c38b85519fae1585f8e7a4e399b3b0fad709894c
created_at: 2026-06-30T22:27:19.6801136-07:00
dependencies:
    - 064.001-T
id: 064.004-T
labels:
    - engram
    - phase-1b
    - daemon
    - watcher
parent_id: 064-F
priority: medium
references:
    - src/daemon/debounce.rs
    - src/daemon/watcher.rs
    - src/daemon/mod.rs
    - src/services/ingestion.rs
    - src/services/verify.rs
status: queued
title: Phase 1b — reactive sync gated on verify conformance
updated_at: 2026-07-04T18:52:13.7476007-07:00
---

<!-- BEGIN:description -->
Phase 1b (design item b). Reactive sync: gate the daemon's markdown re-ingestion on verify conformance so only CONFORMANT mutated markdown syncs into CozoDB content nodes; non-conformant mutations are skipped and logged (not ingested). Width domain: daemon event loop. Enhancement on the existing watcher -> debounce -> ServiceAction pipeline (adapt_event / ServiceAction::ReingestContent + the live run_with_shutdown_v2 consumer). Deferral gate SATISFIED — Phase 1a shipped via PR #188; queued-and-planned (plan + 072.001-R + shipment 072-S).
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
- Markdown ReingestContent actions are gated on services::verify::verify_markdown; non-conformant mutations are skipped and logged (not ingested).
- No regression to the existing code-file reindex path.
- Tests cover: valid md → ingested; invalid md → skipped+logged.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Gate SATISFIED: the deferral reason ("sequence after Phase 1a lands") is resolved — Phase 1a (064.001-T verify linter + 064.002/003-T) shipped and MERGED via PR #188 (merge c38b855). The PR #188 HARDENING SUBSET (Finding B body.empty coverage test, Finding D containment normalize-both-sides, CRLF regression guard) ALSO shipped there and is OUT OF SCOPE here — do NOT re-plan them. Only the PRIMARY reactive-sync gating remains.

Scope-honest note: markdown currently SKIPS in adapt_event (src/daemon/debounce.rs:89) and ServiceAction::ReingestContent (:50) is defined but never produced/consumed, so ReingestContent must first be PRODUCED (adapt_event) before it can be GATED (consumer loop). Gate the LIVE run_with_shutdown_v2 consumer (src/daemon/ipc_server.rs:1081, dispatched from src/daemon/mod.rs:202) ONLY; comment the legacy v1 loop (:638) and treat v1 parity as a separate item. Reuse the watcher -> debounce -> ServiceAction pipeline (enhancement, not greenfield). Width domain: daemon event loop ONLY; do NOT bundle with CLI or schema work. Elevated blast radius — freeze-scope + pure injectable gate helper so tests do not spin the daemon (avoids the Windows run_with_shutdown_v2 SQLite flake).

Planning artifacts: plan docs/exec-plans/2026-07-04-064-004-reactive-sync-verify-gate-plan.md; plan-review 072.001-R (docs/reviews/2026-07-04-064-004-reactive-sync-verify-gate-plan-review.md, ACCEPTED); shipment 072-S.
<!-- END:implementation-notes -->