---
artifact_type: task
commit: c38b85519fae1585f8e7a4e399b3b0fad709894c
created_at: 2026-06-30T22:27:19.6801136-07:00
dependencies:
    - 064.001-T
id: 064.004-T
labels:
    - engram
    - phase-1b
    - daemon
    - watcher
    - deferred
parent_id: 064-F
priority: medium
references:
    - src/daemon/debounce.rs
    - src/daemon/watcher.rs
    - src/daemon/mod.rs
    - src/services/ingestion.rs
    - src/services/verify.rs
status: queued
title: Phase 1b — reactive sync gated on verify conformance (DEFERRED)
updated_at: 2026-07-02T23:01:22.0525226-07:00
---

Phase 1b (design item b) — DEFERRED (not in first shipment). Reactive sync: gate the daemon's markdown re-ingestion on verify conformance so only VALID mutated markdown syncs into CozoDB nodes. Width domain: daemon event loop. Enhancement on existing src/daemon/debounce.rs::adapt_event (ServiceAction::ReingestContent) + watcher.rs.

<!-- BEGIN:acceptance-criteria -->
- Markdown ReingestContent actions are gated on services::verify::verify_markdown; non-conformant mutations are skipped and logged (not ingested).
- No regression to existing code-file reindex path.
- Tests cover: valid md → ingested; invalid md → skipped+logged.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
DEFERRED follow-on. Depends on 064.001-T (linter). Reuse the existing watcher→debounce→ServiceAction pipeline; this is an enhancement, not a greenfield watcher. Keep within the daemon width domain; do NOT bundle with CLI or schema work. Sequence after the Phase 1a shipment lands.
<!-- END:implementation-notes -->

<!-- BEGIN:hardening-followups (from PR #185 re-review, 2026-07-01) -->
Phase-1a advisory hardening items folded into this Phase-1b family (authoritative record: `docs/closure/2026-07-01-052-S-engram-verify-cli-closure.md`, "Follow-on Backlog"):

- **Finding B — `body.empty` rule lacks a test.** Add unit/contract coverage in `src/services/verify.rs` for empty-body markdown → `body.empty` finding with `conformant == false`. Copilot thread `PRRT_kwDORJEduc6NrAdB`; coverage gap on the 064.001-T service, out of the operator-authorized PR #185 nit scope.
- **Finding D — Windows verbatim-prefix (`\\?\`) containment.** Normalize both sides of `contain_path` via `crate::db::workspace::normalize_canonical` before the `starts_with` containment check. Copilot thread `PRRT_kwDORJEduc6NrQsy`; robustness only — does not change the pinned exit-code contract (missing paths already exit `2`).
<!-- END:hardening-followups -->

<!-- BEGIN:shipped-status -->
PR #188 (merge c38b855, merged 2026-07-03T05:57:07Z) shipped the review-driven HARDENING SUBSET only: Finding B (body.empty coverage test), Finding D (containment normalize-both-sides via normalize_canonical), plus a CRLF regression-guard test. The PRIMARY Phase-1b reactive-sync / Workspace-Monitoring scope (acceptance criteria: gate daemon ReingestContent on verify conformance; tests for valid->ingested / invalid->skipped+logged) remains UNIMPLEMENTED. Task stays QUEUED.
<!-- END:shipped-status -->