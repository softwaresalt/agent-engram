---
id: "042.003-T"
title: "Manifest subcommand (no daemon required)"
artifact_type: task
status: queued
parent_id: "042-F"
priority: high
dependencies: "042.001-T"
description: "Implement engram manifest command reading tools_catalog::all_tools() directly without daemon"
labels: "cli,manifest,discovery"
created_at: 2026-05-06T19:50:00Z
updated_at: 2026-05-06T19:50:00Z
---

## Scope

Create `src/cli/commands/manifest.rs`:
- Read `tools_catalog::all_tools()` (compile-time catalog)
- Wrap in JSON-RPC 2.0 response: `{"jsonrpc":"2.0","id":<id>,"result":{"tools":[...]}}`
- Output via `OutputFormatter`
- Ensure `tools_catalog::all_tools()` is pub-accessible from cli module
  (adjust visibility in `src/shim/mod.rs` if needed)

No daemon connection required — this is a local-only command for tool discovery.

## Acceptance Criteria

- `engram manifest --json` outputs valid JSON-RPC 2.0 with exactly TOOL_COUNT tools
- Each tool entry has `name`, `description`, and `inputSchema` fields
- Output shape matches the MCP `tools/list` response exactly
- Works without daemon running
- Contract test validates tool count and field presence

## Execution Posture

Test-first: write contract test for output shape, then implement.

## Dependencies

- 042.001-T (output formatter)
