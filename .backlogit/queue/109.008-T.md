---
artifact_type: task
created_at: 2026-08-01T07:12:08.5487037Z
dependencies:
    - 109.007-T
id: 109.008-T
labels:
    - daemon
    - lifecycle
    - pending-sync
    - concurrency
    - green
    - pr-302
parent_id: 109-F
priority: medium
references:
    - docs/exec-plans/2026-07-31-post-105-pending-sync-residuals-plan.md
    - src/server/state.rs
    - src/tools/lifecycle.rs
status: queued
title: 'GREEN: atomically claim and generation-qualify lifecycle re-arm'
updated_at: 2026-08-01T07:14:51.8463658Z
---

Source-only GREEN unit for the lifecycle blocker. Across state.rs and lifecycle.rs, atomically claim the complete pending request as an opaque private token carrying generation plus pending/revalidate/backfill bits. If the indexing lock is lost, republish exactly that claim through explicit-generation semantics: older ignored, equal full-mask coalesced, newer ownership never overwritten. If acquired, drain claimed companions exactly once. Preserve bounded drain and every finish_indexing drain site.

<!-- BEGIN:acceptance-criteria -->
AC1: L1a passes: a consumed stale G claim is ignored after G+1 and is never stamped as current. AC2: L1b passes: the full G mask is removed atomically and no heavy companion survives or leaks into G+1. AC3: L1c passes: equal-G republish restores the full mask and remains drainable. AC4: acquired-lock path consumes claimed companions exactly once; lost-lock path republishes exactly once without recursion. AC5: bounded 64-pass drain, all finish_indexing drain-site coverage, no-contention behavior, and public contracts remain unchanged. AC6: no synchronous pending guard crosses await.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Production width: src/server/state.rs plus src/tools/lifecycle.rs, at most four production functions total, target 75-105 minutes. This two-file seam is the smallest coherent buildable unit because state owns the opaque claim and lifecycle consumes/republishes it. STOP/return blocked for a third production file, fifth function, public token, second queue/state machine, unbounded drain, recursion, synchronous guard across await, or 110 minutes.
<!-- END:implementation-notes -->