---
artifact_type: task
created_at: 2026-07-13T10:51:08.4484786-07:00
dependencies:
    - 085.001-T
id: 085.005-T
labels:
    - powerbi
    - dax
    - lint
    - verify
    - cli
parent_id: 085-F
priority: high
references:
    - docs/exec-plans/2026-07-13-dax-intelligence-plan.md
    - src/services/verify.rs
    - src/cli/commands/verify.rs
status: queued
title: P5 — DAX lint Tier 1 + VerifyFinding.severity + engram verify <model.tmdl> gate
updated_at: 2026-07-13T10:52:04.3368757-07:00
---



<!-- BEGIN:acceptance-criteria -->
- `VerifyFinding.severity` added as additive `#[serde(default)]` = Error; existing findings deserialize unchanged.
- Tier-1 rules (dax.empty_expression, dax.divide_operator, dax.deprecated_function, and dax.malformed_ref sourced from P1's extractor `diagnostics` seam — unterminated string/quoted-identifier/bracket/comment, e.g. `'Sales[Amount]`, not re-lexed) each covered by a positive + negative unit fixture.
- `engram verify <model.tmdl>` returns exit 0 when clean, 1 when findings exist, 2 on error; markdown verify behavior unchanged.
- clippy pedantic clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
DAX lint Tier 1 (syntactic) + finding severity. Add `severity: Severity` (enum Severity { Error, Warning, Info }) to `VerifyFinding` (src/services/verify.rs:20) with `#[serde(default)]` = Error for additive back-compat. Add a DAX Tier-1 rule set producing VerifyFindings: `dax.empty_expression` (Warning), `dax.divide_operator` (Info), `dax.deprecated_function` (Warning), plus `dax.malformed_ref` checks driven by P1's extractor **`diagnostics` seam** (unterminated string/quoted-identifier/bracket/comment surfaced by the lexer; the rule consumes those diagnostics rather than re-lexing). Rules namespaced `dax.*`. Extend `engram verify <path>` (src/cli/commands/verify.rs) so a `.tmdl` target runs Tier-1 DAX lint (today non-markdown short-circuits to conformant). Exit-code contract UNCHANGED (0 conformant / 1 findings / 2 error). This makes DAX lint usable as a pre-commit / autoharness gate. Width: verify service + CLI.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Test-first: author rule fixtures and CLI exit-code tests before rules. Start Warning/Info (not Error) for precision-sensitive rules to limit false positives (R3). Additive severity default must be Error for back-compat (serde default). Rust 2024, no unwrap/expect. Grounding: VerifyFinding (src/services/verify.rs:20, currently no severity), verify CLI exit codes EXIT_CONFORMANT=0 etc. (src/cli/commands/verify.rs). Consider a `dax` lint sub-module under src/services/. Depends on P1 (extractor); NOT on P3.
<!-- END:implementation-notes -->