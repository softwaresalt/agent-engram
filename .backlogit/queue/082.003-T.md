---
artifact_type: task
created_at: 2026-07-10T19:21:57.87093-07:00
dependencies:
    - 082.002-T
id: 082.003-T
labels:
    - width:schema
    - graph
    - model
    - provenance
    - slice:1
parent_id: 082-F
priority: medium
references:
    - docs/exec-plans/2026-07-10-callgraph-cross-file-resolution-plan.md
status: queued
title: 'PROVENANCE STORAGE: calls_edge.resolution migration + CodeEdge.resolution + read query'
updated_at: 2026-07-10T19:21:57.87093-07:00
---

PROVENANCE STORAGE (split from the original 082.003-T per Copilot #239 — this half is schema/model only; the post-pass resolution is the separate task 082.008-T).

Files (<=3 src + 1 test): src/db/cozo_backend/schema.rs (extend `calls_edge` with a `resolution` attribute + migration), src/models/code_edge.rs (add `CodeEdge.resolution`), src/db/cozo_queries.rs (persist provenance on create_calls_edge + read/enumerate query). Test: tests/integration/calls_edge_resolution_storage_test.rs.

Contract: today calls_edge stores only `(from, to) => created_at` (schema.rs:319; cozo_queries.rs:1206-1214 `:put calls_edge { from, to => created_at }`) and CodeEdge has no provenance field (code_edge.rs:36). Add a `resolution: String` attribute to calls_edge with a migration that DEFAULTS pre-existing rows to the exact string `direct`; add `CodeEdge.resolution: Option<String>`. Update create_calls_edge to persist `resolution="direct"` for direct in-file edges. The post-pass (082.008-T) writes the EXACT canonical value `calls_resolved_singleton` (never a shortened `resolved_singleton`) — this is the single source-of-truth string consumed by 082.004-T's target-correctness checks and by 082.010-T's retraction. Add `count_calls_edges_by_resolution` and an enumerate-`calls_resolved_singleton`-edges read query so 082.004-T can list the tagged edges it validates.

Width: schema / model (provenance storage). Scenarios (4): migration adds `resolution` and defaults existing rows to `direct`; CodeEdge round-trips `resolution`; a direct in-file edge persists `resolution="direct"`; the enumerate / count-by-resolution query returns `calls_resolved_singleton` edges by provenance.

Depends on 082.002-T.