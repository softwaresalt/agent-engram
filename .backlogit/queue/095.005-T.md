---
artifact_type: task
created_at: 2026-07-22T15:00:17.6757317-07:00
dependencies:
    - 095.001-T
    - 095.002-T
id: 095.005-T
parent_id: 095-F
priority: medium
status: queued
title: 'U3: Spark-SQL table-lineage extraction in sql.rs (sized by U0)'
updated_at: 2026-07-22T17:53:02.1009896-07:00
---

Add pure Spark-SQL table-lineage extraction in src/services/parsing/sql.rs (tree-sitter-sequel 0.3; extract_sql_top_level at sql.rs:57-100; Defines arm handles create_table/create_view/create_function at sql.rs:72-87; CREATE PROCEDURE -> ERROR at sql.rs:11-13). Expose a PUBLIC entry point extract_sql_lineage(source, authority_ctx) -> Vec<LineageEdgeCandidate> that tests call DIRECTLY — NOT parse_source (no lineage carrier; PR #281 finding 3). Returns STATEMENT-GROUPED EDGE CANDIDATES — one LineageEdgeCandidate { target, sources } per CTAS/INSERT statement — so a statement with multiple source tables, or multiple statements in one cell, keeps its target<->source pairing and U4 persists the correct directional edges (cycle-5 findings F3/F4; a flat Vec<LineageEndpoint> could not carry which write target each read belongs to). Emit directional read->write links for: (a) CTAS — CREATE TABLE cat.sch.t AS SELECT ... FROM cat.sch.src, descending into the from nested inside create_table (today only a top-level from is descended); (b) INSERT [OVERWRITE] INTO cat.sch.t SELECT ... FROM cat.sch.src. TABLE lineage ONLY — NO CREATE VIEW / temp-view DDL (permanent-view deferred = scope-min; temp-view deferred = unrepresentable). Pure; persisted by U4 via U1 writers. Resolve ONLY a 3-part catalog.schema.table ALSO bound to a trusted metastore authority in authority_ctx; 1-/2-part names, ERROR statements, AND 3-part names WITHOUT a trusted authority drop (0 false edges; PR #281 finding 10). ***SIZING IS CONTINGENT ON U0 (095.001-T)*** grammar-probe checkpoint: option (A) enhance sql.rs vs option (B) analyzer/grammar swap — do NOT start until U0 resolves; residual U3 sizing risk (R1) flagged in the plan. Tests: CTAS + INSERT OVERWRITE with 3-part names AND injected authority -> derives edge; a MULTI-SOURCE CTAS (CREATE TABLE c.s.t AS SELECT ... FROM c.s.a JOIN c.s.b) -> ONE candidate {target: c.s.t, sources: [c.s.a, c.s.b]} (two derives edges), and two statements in one cell keep DISTINCT targets (cycle-5 F3/F4); the same statements with NO authority -> ZERO; 2-part name + CREATE PROCEDURE/ERROR -> ZERO edges. Domain: sql-parser (width-isolated). Blocked by U0 and U1. Plan Unit 3.