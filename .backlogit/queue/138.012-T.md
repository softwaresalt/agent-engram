---
artifact_type: task
created_at: 2026-08-27T20:51:35.1414366Z
dependencies:
    - 138.002-T
    - 138.014-T
id: 138.012-T
labels:
    - shim
    - tests
    - harness
    - tdd
    - concurrency
    - race
    - red
parent_id: 138-F
priority: critical
references:
    - src/shim/mod.rs
status: active
title: 'Red harness: monitor behavior T6, R2b, and C5 request-terminal vs monitor race'
updated_at: 2026-08-27T21:09:39.2926599Z
---

PHASE 2 (harness). Test-only; NO production behavior edit. Lives as an in-crate #[cfg(test)] mod in src/shim/mod.rs using the 138.014-T monitor probe seam and counter, so StartupOutcome and the monitor stay crate-private (no pub API widening). T6 monitor stops on terminal: with NO tools/call at all, a mismatched protocol_version causes the monitor to publish Degraded and return; the monitor probe counter reaches a fixed value and stays constant for >= 2s (> RECOVERY_MAX_BACKOFF_MS). R2b monitor keeps probing on transient: a bound counting probe returns a version-compatible non-ready status; the monitor probe counter strictly increases across a 1s window and NO Degraded is ever published. C5 is the BLOCKING guard for the revision-1 fail-open race: the request path latches Degraded while the monitor is mid-backoff and the monitor's next probe returns Ready; the monitor MUST NOT publish Ready - final published state stays Degraded, subsequent tools/call stay terminal, and the monitor exits. Drive the monitor seam deterministically; NO sleeps to establish ordering. 5x repeat, zero flakes. All three are NEW and must start RED.