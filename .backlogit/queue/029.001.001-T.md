---
artifact_type: task
created_at: 2026-04-21T11:30:00.0000000-07:00
id: 029.001.001-T
labels: [reliability, daemon, shipment-b1, harness-ready]
parent_id: 029.001-C
priority: high
status: done
title: Scaffold failing version-handshake harness (red phase)
updated_at: 2026-04-21T11:30:00.0000000-07:00
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
**Red-phase harness task.** This task establishes the failing test surface for Unit 1 BEFORE any implementation lands. It scaffolds the absolute minimum constants/types required for tests to compile, then leaves the tests failing. Implementation tasks `029.001.002-T` and `029.001.003-T` make them pass.

Scaffold:
1. `tests/contract/lifecycle_test.rs` — add `version_handshake_returns_typed_mismatch_error` test (asserts `IpcError::VersionMismatch` is returned when protocol versions differ). Test compiles but fails because the error variant and handshake exchange don't yet exist.
2. `tests/integration/version_mismatch_test.rs` — add `shim_respawns_on_stale_daemon` test (end-to-end: spawn old daemon, connect new shim, expect one respawn event then success). Test compiles but fails.
3. Add the **minimum** scaffolding for compilation: stub `IpcError::VersionMismatch { expected, actual }` variant in `src/errors/mod.rs` and stub `pub const ENGRAM_PROTOCOL_VERSION: u32 = 0;` placeholder in `src/shim/version.rs`. Real values land in `029.001.002-T`.

**Files:** `tests/contract/lifecycle_test.rs`, `tests/integration/version_mismatch_test.rs`, `src/errors/mod.rs`, `src/shim/version.rs` (new, placeholder).

**Execution posture:** test-first (red phase).

**Granularity:** 4 files, 0 production functions (scaffolding only), 2 failing test scenarios.

**Dependency:** `029.001.002-T` and `029.001.003-T` depend on this task; they cannot start until `cargo test version_handshake` and `cargo test shim_respawns_on_stale_daemon` both compile and fail with the expected stub-driven failure mode.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Note

- Harness command: `cargo test version_handshake_returns_typed_mismatch_error && cargo test shim_respawns_on_stale_daemon`

## Acceptance Criteria

<!-- AC:BEGIN -->
- [x] #1 `tests/contract/lifecycle_test.rs::version_handshake_returns_typed_mismatch_error` compiles and FAILS with stub-driven failure mode
- [x] #2 `tests/integration/version_mismatch_test.rs::shim_respawns_on_stale_daemon` compiles and FAILS with stub-driven failure mode
- [x] #3 `IpcError::VersionMismatch { expected, actual }` variant exists as stub in `src/errors/mod.rs` and is wrapped by `EngramError`
- [x] #4 `cargo build --tests` succeeds; no production code path is wired up yet
<!-- AC:END -->
