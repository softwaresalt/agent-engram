---
artifact_type: task
created_at: 2026-08-01T04:05:07.6426318Z
dependencies:
    - 109.001-T
id: 109.002-T
labels:
    - daemon
    - lifecycle
    - pending-sync
    - concurrency
    - green
    - pr-302
parent_id: 109-F
priority: medium
references:
    - docs/exec-plans/2026-07-31-post-105-pending-sync-residuals-plan.md
    - src/server/state.rs
    - src/tools/lifecycle.rs
status: queued
title: 'GREEN: linearize binding/generation transition and reject stale pending publishes'
updated_at: 2026-08-01T04:05:07.6426318Z
---

Source-only GREEN unit for FF55E51A plus folded same-width comment provenance 1E70A289. Add one crate-private AppState transition seam replacing the lifecycle sequence set_workspace_and_config then begin_scan_generation. Acquire workspace, config, and scan-cancel write guards in that order, complete all awaits, then briefly lock pending_sync to advance sync_generation and install the binding/config/fresh cancel state before readers can observe the new binding. At pending publish linearization, acquire pending_sync before selecting the generation. Tighten owner semantics: newer replaces, equal OR-coalesces, older is ignored. Correct the stale same-lock claim and restate the R2 lost-wakeup proof through pending-sync mutex critical-section order. No public API/model/response change.

<!-- BEGIN:acceptance-criteria -->
AC1: both RED scenarios in 109.001-T pass: zero dropped new-generation requests and zero stale heavy-bit leaks. AC2: same-generation companions remain sticky; newer generation still replaces stale older bits; old-generation clear still preserves newer owner. AC3: lock order is workspace -> config -> scan_cancel -> pending_sync, and no std::sync::Mutex guard crosses await. AC4: comments accurately state generation selection and the R2 mutex-CS proof. AC5: no schema, serialized model, CLI/MCP result, queue bit, retry bound, hydration-readiness, or unsafe-code change.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Source-only width: src/server/state.rs and src/tools/lifecycle.rs; at most four production functions plus nearby comments; target 80–110 minutes. Refactor begin-generation channel/cancel logic only as needed to avoid duplication. STOP/return blocked at 110 minutes, a third production file, more than four production functions, public contract change, second queue, unbounded retry, unresolved lock order, or any synchronous guard across await. Source stash FF55E51A + 1E70A289.
<!-- END:implementation-notes -->