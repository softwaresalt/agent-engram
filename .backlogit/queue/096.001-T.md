---
artifact_type: task
created_at: 2026-07-23T18:45:07.4200593-07:00
id: 096.001-T
labels:
    - python
    - canonical-identity
    - code
parent_id: 096-F
priority: medium
references:
    - src/services/parsing/canonical/module_path.rs
    - src/services/parsing/python_canonical/module_path.rs
status: queued
title: 'T1: Python module-path primitive (regular-package predicate, fail-closed)'
updated_at: 2026-07-23T20:07:57.8769926-07:00
---



<!-- BEGIN:acceptance-criteria -->
- Regular nested package p/q/r.py (proven __init__.py chain) -> p.q.r; top-level mod.py -> mod.
- Q3/M5 fail-closed table -> None: __init__.py; a src/-style root (src/ lacks __init__.py); an implicit PEP 420 namespace package (ancestor dir without __init__.py); non-identifier segment.
- notes.txt (non-.py) -> None.
- Q3 v1 NON-GOAL: source-root-aware resolution (src/pkg/mod.py -> pkg.mod) is OUT of scope; those layouts fail closed. No PackageLayout/source-root machinery (CodeGraphConfig has no source-root field, config.rs:98-120).
- M3: Cargo.toml registers [[test]] name="unit_python_canonical" path="tests/unit/python_canonical_test.rs" so `cargo test --test unit_python_canonical` runs (repo registers every nested tests/unit/*.rs; this target did not exist).
- No unwrap/expect/panic; returns Option. clippy --all-targets -- -D warnings -D clippy::pedantic clean; fmt --all --check clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
New src/services/parsing/python_canonical/ module (module_path.rs + mod.rs). python_module_path_for_file(rel_path, is_regular_package: &impl Fn(&Path) -> bool) -> Option<String>. Resolve foo/bar.py -> Some("foo.bar") ONLY when EVERY ancestor dir in the path is a provable regular package (contains __init__.py); the is_regular_package predicate is DERIVED FROM THE ALREADY-INDEXED FILE SET by the caller (T3) -- no config, no I/O in this fn. A top-level module (mod.py, no ancestor dirs) -> Some("mod"). Q3/M5 (NARROWED): a pure rel_path transform is unsafe because src/pkg/mod.py and namespace/pkg/mod.py contain only valid identifiers; instead of inventing speculative source-root config (Constitution VI; CodeGraphConfig has no source-root field, config.rs:98-120), v1 FAILS CLOSED (None) on: __init__.py; any non-identifier segment; ANY ancestor dir lacking __init__.py -- which conservatively rejects BOTH implicit PEP 420 namespace packages AND src/-style source-root layouts; and non-.py paths. Source-root-aware resolution (stripping a declared src/ root -> pkg.mod) is an explicit v1 NON-GOAL. None => the caller writes "" (never a match target, D4). TDD RED->GREEN. Single skill domain: code.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Mirror the Rust src/services/parsing/canonical/module_path.rs boundary. Keep python_module_path_for_file pure (no I/O) -- the caller (T3/T5b) supplies is_regular_package, computed once from the already-indexed file set (dirs containing __init__.py). Q3: NO PackageLayout / source-root machinery -- removed per the cycle-2 NARROW decision; src/-roots and namespace packages fail closed. Same-crate unit tests exercise the public fn so it is not dead_code before T3 wires production call sites. M3: add the [[test]] entry to Cargo.toml (unit_parsing exists ~231-237; unit_python_canonical did not). Files: python_canonical/module_path.rs, python_canonical/mod.rs, Cargo.toml (2-line test registration, trivial), tests/unit/python_canonical_test.rs.
<!-- END:implementation-notes -->