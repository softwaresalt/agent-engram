---
artifact_type: task
created_at: 2026-05-05T10:00:00-07:00
id: 041.001-T
labels:
    - cozodb
    - migration
    - blocked-upstream
parent_id: 041-F
priority: medium
status: blocked
title: Upgrade CozoDB dependency to 0.8 (pinned)
updated_at: 2026-05-05T10:00:00-07:00
---

## Description

Bump the `cozo` dependency in `Cargo.toml` to a pinned exact version (e.g.,
`=0.8.0`) once the upstream release is available. Do NOT use open-ended `>= 0.8`.
Verify API compatibility and that all existing tests pass with the new version.

## Scope

- `Cargo.toml` (bump cozo version)
- `Cargo.lock` (regenerated)

## Implementation

- Change cozo version constraint to a pinned exact version (e.g., `=0.8.0`) once available — do NOT use open-ended `>= 0.8` to avoid silently absorbing unrelated upstream API changes
- Run `cargo build` to verify API compatibility
- Run `cargo test` to verify all tests pass
- If API breaks exist, adapt call sites

## Blocker

Blocked on: CozoDB >= 0.8 release on crates.io

## Acceptance Criteria

- `cargo build` succeeds with new CozoDB version
- `cargo test` passes
- No SQLITE_BUSY panics observed during concurrent test scenarios

## References

Plan: `docs/exec-plans/2026-05-05-cozodb-upgrade-plan.md` Unit 1
