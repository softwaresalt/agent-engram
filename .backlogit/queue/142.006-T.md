---
artifact_type: task
created_at: 2026-09-03T00:17:17.4688864Z
dependencies:
    - 142.001-T
id: 142.006-T
parent_id: 142-F
priority: critical
status: active
title: Create supervisor crate stub and workspace membership
updated_at: 2026-09-03T07:33:16.4033165Z
---

Plan unit F12a - supervisor crate skeleton and workspace membership. Create a minimal buildable `crates/engram-indexer` stub crate with `#![forbid(unsafe_code)]`, and add it to the root `Cargo.toml` `[workspace] members` list alongside the existing `["." , "crates/powerbi-tmdl-parser"]`.

This unit exists purely so the workspace gate can run F12's RED harness at the moment that harness is declared. It contains no supervisor logic and does not depend on F07-F10.

Execution posture: structural enabler, build-verified.

<!-- BEGIN:acceptance-criteria -->
- `crates/engram-indexer/Cargo.toml` and `crates/engram-indexer/src/main.rs` exist as a minimal buildable stub.
- The crate declares `#![forbid(unsafe_code)]`.
- Root `Cargo.toml` `[workspace] members` includes `crates/engram-indexer`; no other root section is touched.
- `cargo metadata` resolves the new workspace member.
- `cargo test -p engram-indexer` executes under the workspace gate with zero tests.
- The crate has NO dependency on the `engram` crate at this point (that arrives with F12).
- No supervisor build/seal/publish logic is present.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Size: S | Complexity: low | size_source: agent | size_ruleset_version: engram-stage-2h-rule-v1. Recorded as prose because this workspace task type defines no structured size/complexity field. Size is effort/volume; complexity is difficulty/uncertainty; the two axes are independent.

Owned files: root `Cargo.toml` (`[workspace] members` only), `crates/engram-indexer/Cargo.toml`, `crates/engram-indexer/src/main.rs`.
F12a owns no test file and is excluded from the F00 registration table, but still carries an `F00 -> F12a` edge for root `Cargo.toml` section ordering.
Root `Cargo.toml` total order: `F00 -> F12a -> F46 -> F47` over disjoint sections.
`crates/engram-indexer/Cargo.toml` has exactly two owners in declared order `F12a -> F12` over disjoint sections: F12a creates the manifest, F12 later edits only its dependency sections.
<!-- END:implementation-notes -->

<!-- BEGIN:verification -->
1. `cargo metadata --format-version 1` lists `engram-indexer` as a workspace member.
2. `cargo test -p engram-indexer` runs and reports zero tests.
3. `cargo dev-test` and `cargo ci` stay GREEN.
4. `git diff Cargo.toml` shows a `[workspace] members` change only.
<!-- END:verification -->