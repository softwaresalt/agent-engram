---
artifact_type: task
created_at: 2026-08-25T04:05:16.138006Z
dependencies:
    - 131.006-T
id: 131.007-T
labels:
    - test-first
    - red
    - compiling-harness
    - otlp
    - daemon
    - attachment
    - source-stash-44E573BC
parent_id: 131-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-44e573bc-otlp-api-drift-plan.md
    - src/lib.rs
    - src/bin/engram.rs
status: queued
title: 'RED: prove daemon endpoint propagation and retained OTLP attachment'
updated_at: 2026-08-25T05:21:02.7629949Z
---

After provider behavior is GREEN, add feature-enabled daemon tests against the existing CLI parser and 131.004-T tracing-pipeline seam. The tests must compile before they run, then fail because the daemon does not yet accept/forward an endpoint and production tracing does not yet attach or retain the 131.006-T pipeline.

<!-- BEGIN:acceptance-criteria -->
1. 131.006-T is complete and remains the explicit dependency. 2. Exact RED command: cargo test --no-default-features --features otlp-export otlp_daemon_red -- --nocapture. Both lib and bin test modules compile before assertions run. 3. otlp_daemon_red_flag_beats_environment calls Cli::try_parse_from with ENGRAM_OTLP_ENDPOINT=http://env.invalid and daemon --otlp-endpoint http://flag.invalid, then uses the existing Debug representation rather than a not-yet-existing field access; expected RED is a runtime clap unexpected-argument error. 4. otlp_daemon_red_forwards_exact_endpoint invokes the pre-existing tracing-pipeline seam with a recording factory and asserts recorded_endpoints == [http://flag.invalid]; expected RED is [] because the endpoint is ignored/not wired. 5. otlp_daemon_red_attaches_and_retains emits otlp-daemon-red-span through the same fake pipeline, holds a pending daemon future at a deterministic barrier, and asserts the span exports and the owner drop token is false until the future completes; expected RED is no export or early drop. 6. Absence remains formatting-only and constructs no factory. No missing symbol reference, collector, socket, network, wall-clock sleep, unbounded retry, cleanup assertion, or production edit is included; 131.008-T and 131.009-T own the sequential GREEN states.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Daemon endpoint/attachment RED-harness skill only; test-only modules in exactly 2 existing files, at most 4 test/helper functions, 3 scenarios, and 105 minutes. Atomic milestone: daemon tests compile and fail only for missing runtime endpoint propagation and attachment/retention behavior.
<!-- END:implementation-notes -->
