---
artifact_type: task
created_at: 2026-07-17T01:01:20.7300612-07:00
id: 092.003-T
labels:
    - concurrency
    - atomicity
    - follow-up
    - adversarial
    - 092-F
parent_id: 092-F
priority: medium
status: queued
title: 'Daemon-reader workspace+config atomicity: migrate ipc_server run_with_shutdown paths to snapshot_workspace_and_config'
updated_at: 2026-07-17T01:01:20.7300612-07:00
---

Deferred adversarial P2 (GPT-5.6 Sol + Gemini concurrency lens) from 092.002-T. The auto-sync / background closures in src/daemon/ipc_server.rs (run_with_shutdown and run_with_shutdown_v2) still read the (workspace, config) pair non-atomically via separate state.snapshot_workspace().await + state.workspace_config().await reads, leaving the same (workspace_i, config_j) tear window that 092.002-T closed in lifecycle.rs. Out of scope for 092.002-T, which was constrained to background_db_hydration + drain_pending_sync in lifecycle.rs plus the new reader in state.rs and one integration test. Fix: migrate those daemon readers to AppState::snapshot_workspace_and_config() (None-gating atomic reader added in 092.002-T), preserving each site's existing skip-if-either-None behavior. Add a reader-side non-vacuous test for the daemon paths mirroring integration_get_workspace_status_atomicity. Fail-closed / self-healing: the observable risk is a single background pass acting on a mismatched config during a concurrent bind, not data loss. Source: 092.002-T pre-Copilot adversarial review.