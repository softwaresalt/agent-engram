---
artifact_type: subtask
created_at: 2026-09-11T02:15:10.6310882Z
custom_fields:
    blocked_reason: 'Terminal Revision 3 plan-review FAIL after three cycles; do not claim. Requires a fresh operator-authorized Stage cycle.'
dependencies:
    - 144.001.002-ST
id: 144.002.001-ST
labels:
    - cozo
    - schema
    - measurement
parent_id: 144.002-T
priority: critical
status: blocked
title: Per-script and per-migration sub-phase attribution in run_scripts
updated_at: 2026-09-11T03:17:43.9443861Z
---

PER-SCRIPT AND PER-MIGRATION SUB-PHASE ATTRIBUTION INSIDE run_scripts. Split from 144.002-T in Revision 3 so no measurement leaf carries more than four scenarios.

schema_bootstrap is the ONLY connect_db sub-phase with no timeout, no deadline, and no cancellation. Attributing time inside it is the whole point of Workstream B.

AC1 (PER-SCHEMA-SCRIPT ATTRIBUTION): src/db/cozo_backend/schema.rs::run_scripts attributes elapsed time SEPARATELY to EACH of the roughly 30 :create schema scripts, each identified by a stable name.
AC2 (PER-MIGRATION ATTRIBUTION): each of the three migrations - migrate_calls_edge_resolution, migrate_function_meta_canonical_path, migrate_staged_call_provenance - is attributed separately by name.
AC3 (SUMS TO THE PARENT PHASE): the emitted sub-phase breakdown SUMS to the schema_bootstrap phase total recorded by 144.001.002-ST, within a stated tolerance. A test asserts the sum property so unattributed time cannot hide.
AC4 (EMITTED THROUGH THE EXISTING SINK): sub-phase records are emitted through the 144.001.001-ST serializing sink carrying the 144.001.002-ST attempt identity and monotonic sequence. No second sink and no second identity scheme is introduced.

MEASUREMENT ONLY, NON-NEGOTIABLE: this leaf adds timing observation. It MUST NOT reorder scripts, MUST NOT skip work, MUST NOT change error handling, and MUST NOT move anything off the readiness path.

Files: src/db/cozo_backend/schema.rs plus tests. Single domain: code with its own tests.
Gates (authoritative, in order): cargo fmt --all -- --check; cargo clippy --all-targets -- -D warnings -D clippy::pedantic; cargo dev-test; cargo audit. cargo build is an optional precheck only, not a gate. Do NOT use cargo ci or cargo lint (--all-features is broken on this baseline, risk H6).
Test-first: RED before GREEN.
Size: M | Complexity: medium.
