---
artifact_type: subtask
created_at: 2026-09-11T02:14:54.4807248Z
custom_fields:
    blocked_reason: 'Terminal Revision 3 plan-review FAIL after three cycles; do not claim. Requires a fresh operator-authorized Stage cycle.'
dependencies:
    - 143.002.002-ST
id: 143.002.003-ST
labels:
    - daemon
    - ipc
    - contract
    - docs
parent_id: 143.002-T
priority: critical
status: blocked
title: Supersede the S046/T049 idle-TTL contract text in place
updated_at: 2026-09-11T03:03:39.5966688Z
---

Supersede the stale idle-TTL contract text in place. Split out of 143.002-T in Revision 3 so the contract edit is not buried inside the implementation leaf.

The existing S046 contract ("on each accepted connection the idle TTL is reset") and its T049 implementation comment are SUPERSEDED by 143.002.001-ST and 143.002.002-ST. Leaving the old contract text in the tree next to contradicting behaviour is how the 134-S misdiagnosis happened twice.

AC1 (EDIT IN PLACE, NOT ADD ALONGSIDE): the S046 contract text and the T049 implementation comment are rewritten in place. The superseded wording no longer exists anywhere in the tree; a repository check asserts its absence.
AC2 (REPLACEMENT CONTENT): the replacement text states all four binding properties - reference counted, RAII released, owned by the idle-TTL owner, and a FULL idle TTL re-armed from the LAST lease release instant.
AC3 (BASELINE TEST DISPOSITION): 143.001-T's GREEN baseline characterization test probe_currently_resets_idle_ttl_baseline is inverted or removed in this same change, with the justification recorded in the commit message. Its inversion was pre-authorized by 143.001-T so it is not later mistaken for lost coverage.
AC4 (TEXT MATCHES BEHAVIOUR): a test pins the stated contract rule against the implemented behaviour, so the two cannot drift again.

Files: src/daemon/ipc_server.rs contract/comment text plus its test. Single contract domain.
Gates (authoritative, in order): cargo fmt --all -- --check; cargo clippy --all-targets -- -D warnings -D clippy::pedantic; cargo dev-test; cargo audit. cargo build is an optional precheck only, not a gate. Do NOT use cargo ci or cargo lint (--all-features is broken on this baseline, risk H6).
Size: S | Complexity: low.
