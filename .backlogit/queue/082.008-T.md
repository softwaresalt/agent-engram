---
artifact_type: task
created_at: 2026-07-11T00:58:53.3481543-07:00
dependencies:
    - 082.003-T
id: 082.008-T
labels:
    - width:indexing
    - graph
    - core
    - postpass
    - slice:1
parent_id: 082-F
priority: medium
references:
    - docs/exec-plans/2026-07-10-callgraph-cross-file-resolution-plan.md
status: queued
title: 'POST-PASS: unambiguous cross-file calls resolution (full/--force only)'
updated_at: 2026-07-11T00:58:53.3481543-07:00
---

POST-PASS RESOLUTION (split from the original 082.003-T per Copilot #239 — this half is the core-indexing post-pass; provenance storage is 082.003-T).

Files (<=3 src + 1 test): src/db/cozo_queries.rs (new `reresolve_calls_edges`, modeled on reresolve_references_edges at :1357), src/services/code_graph.rs (invoke in the full/--force index path only). Test: tests/integration/calls_postpass_resolution_test.rs.

Contract: `reresolve_calls_edges` reads the staged_call rows (082.002-T), resolves each callee against a workspace-global `name -> [function_id]` index, and creates a calls_edge ONLY when EXACTLY ONE function has that name (unambiguous-name-only guard), writing provenance `resolution="calls_resolved_singleton"` — the exact canonical string defined by 082.003-T (never a shortened `resolved_singleton`). Ambiguous names (>=2 defs) and names with no matching def are skipped, bounding false edges. Invoke the post-pass in the FULL / `--force` index path ONLY, alongside reresolve_references_edges at code_graph.rs:543; do NOT invoke it in the incremental sync path at :1152 (performance gate — deliberation Q3). CALL_BLOCKLIST is already applied at extraction (082.001-T).

Width: core-indexing / post-pass. Scenarios (4): unique cross-file name -> one edge tagged `calls_resolved_singleton`; ambiguous name (2 defs) -> skipped (no edge); name with no matching def -> no edge (no false edge); incremental sync path -> post-pass NOT invoked (no singleton edges created on sync).

Depends on 082.003-T (storage precedes post-pass; transitively 082.002-T capture, 082.001-T extraction).