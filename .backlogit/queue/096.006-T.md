---
artifact_type: task
created_at: 2026-07-23T18:46:28.4851217-07:00
dependencies:
    - 096.001-T
    - 096.003-T
    - 096.005-T
    - 096.009-T
id: 096.006-T
labels:
    - python
    - canonical-identity
    - call-graph
    - code
parent_id: 096-F
priority: medium
references:
    - src/services/code_graph.rs
    - src/db/cozo_queries.rs
status: queued
title: 'T5b: Python canonical target resolution in the canonical post-pass'
updated_at: 2026-07-23T20:09:14.9214675-07:00
---



<!-- BEGIN:acceptance-criteria -->
- Guard-AGNOSTIC (R3): T5b resolution does NOT invoke the T5c shadow guard; shadow/rebind cases are owned by T5c (096.007-T), which wraps T5b downstream (order-aware, Y2/F1) -- so T5b is independently completable.
- import bar; two modules define parse; caller bar.parse() -> edge resolves to BAR's exact parse id (target-identity, not row-existence; 082-F gate) [ModuleImport receiver, R2].
- Bare parse() with `from bar import parse` -> resolves to bar.parse [FromImportSymbol bare callee, R2]. CALL-SITE-EFFECTIVE ordering (X2/X3 + C7-1), FUNCTION-BODY CALLS ONLY (the extractor stages calls only from function bodies; module-level calls are never staged -- C9-3): with BOTH a local `def parse` AND a LATER `from bar import parse` where the import is the last STABLE module binding, the import rebind WINS -> bar.parse, NOT M.parse (X2/X3); a function-body call with a local `def parse` and NO later rebind binds the def -> M.parse (C7-1 stable binding); and a FUNCTION-BODY call whose module binding is contested by a LATER module-level rebind (invocation order unknown) FAILS CLOSED -> no edge (C7-1). Module-level (top-level) call ordering is a documented v1 NON-GOAL -- top-level calls are not extracted, so such cases neither regress nor are newly handled. [Reachable only because T5a/F2 routes the shadow-contested same-file call to staging.]
- Fail-closed vectors: `from pkg import parse; parse.tokenize()` (FromImportSymbol receiver) -> NO module edge (R2, attribute-on-object); `from bar import *` bare parse() with 2+ parse defs / two same-file parse defs (duplicate canonical path) -> NO canonical edge (FF7DE872 stays unfixed here); AND duplicate same-name imports `from a import f; from b import f; f()` -> NO canonical edge (C8-1: T2 yields no binding per M1, so T5b derives no target with the typed DuplicateSameNameImport no-target reason -- C9-4 -- which is NEVER eligible for the F3 legacy fallback; the name would also be non-unique). All folded into the one ambiguity fail-closed scenario (no 5th scenario; 2-hour rule intact).
- Y3+F3 no-target legacy recall -- GATED ON A TYPED NO-TARGET REASON (C9-4): T5b's outcome carries a typed no-target reason; the LEGACY name-only unique-match fallback fires ONLY for {NoModuleContext (T1-rejected `src/`-root / PEP 420 / __init__.py) and UnsupportedImportForm (provable-namespace-but-unbound: star / re-export / relative with no binding -- F3)} -- a UNIQUE cross-file bare call still gets its legacy name-only edge in both; a non-unique one still fails closed (legacy parity). The fallback is NEVER taken for {CompetingBindings, Shadowed, DuplicateSameNameImport} -- those stay fully fail-closed (NO edge), since emitting the sole indexed legacy edge there would leak a WRONG-CALLEE edge (e.g. a.f when Python's effective target is the later b.f). This makes C8-1 genuinely complete.
- cargo test --test integration_calls_recall_acceptance --test integration_code_graph green; clippy --all-targets clean; fmt clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Python-aware canonical target resolution in reresolve_calls_edges_with_canonical_context (code_graph.rs:264-368). Add python_ctx_for_staged_file (module path via T1 using the regular-package predicate + scope-aware bindings via T2b, read from source like rust_ctx_for_staged_file, using the existing context_cache) and a Python branch dispatched by the CALLER file's language that computes the target canonical path using the T2 binding KIND (R2). This stage is SHADOW-GUARD-AGNOSTIC (R3): it does NOT invoke the T5c guard -- shadow-rebind handling is added downstream by T5c (order-aware, Y2/F1), keeping T5b independently completable. qualifier_kind=="python_bare" -> CALL-SITE-EFFECTIVE last-binding-wins (X2/X3 + C7-1), prove-or-fail-closed, DEFINED OVER FUNCTION-BODY CALLS ONLY (the extractor stages calls only from function bodies -- python.rs:44-79,233-277; module-level calls are never staged, C9-3): a function body runs when the function is CALLED, so its module binding is provable ONLY when it is not contested by a later module-level rebind of callee (the function may run before or after such a rebind). Using the T2/T2b source POSITIONS (F14): when stable, pick the last module-level binding -- a FromImportSymbol (no later rebind) -> N.callee (import WINS); a module-local def (no later rebind) -> M.callee; an enclosing-function binding shadows the module binding per T2b (F5). A later module-level rebind after the winning binding makes the invocation order undecidable -> fail closed (C7-1). Module-level (top-level) call ordering is a v1 NON-GOAL -- top-level calls are not extracted. A ModuleImport-kind name used as a bare callee is not a function -> fail closed; when execution order can't establish a stable call-site binding -> fail closed. C8-1 (duplicate same-name imports): the call-site last-binding-wins ordering resolves def-vs-SINGLE-import + rebinds only -- it does NOT disambiguate two or more competing imports of one name (`from a import f; from b import f; f()`), which T2 fails closed on (M1, no binding); T5b then derives no canonical target and drops (carrying the DuplicateSameNameImport typed reason -> no F3 fallback, C9-4). Consistent with the frozen C7-1 rule; ordered-import-history is a v1 non-goal. (A local-def-first rule would mint M.parse for the X2/X3 counterexample; a whole-module last-binding-wins ignoring call-time stability would mint an edge for a function-body call contested by a later rebind -- both false edges.) qualifier_kind=="module" -> the receiver must resolve to a ModuleImport binding (import pkg / import a.b as c) -> <module>.callee; a FromImportSymbol receiver (from pkg import parse; parse.tokenize()) is attribute-on-object, NOT a module -> fail closed (R2); else fail closed. Y3+F3 no-target fallback -- GATED ON A TYPED NO-TARGET REASON (C9-4): T5b's resolution outcome carries a typed no-target reason, and the LEGACY name-only unique-match fallback fires ONLY for the two recall-safe reasons -- NoModuleContext (python_ctx_for_staged_file yields NO module path: T1 rejected the src/-root / PEP 420 / __init__.py layout) and UnsupportedImportForm (a module path exists but the callee has no import binding and no in-module def: star / re-export / relative -- F3); for these, do NOT drop the bare call -- apply the legacy name-only unique-match, preserving today's recall with no false module edge; a non-unique name still fails closed. The fallback is NEVER taken for the fail-closed reasons {CompetingBindings, Shadowed, DuplicateSameNameImport} -- those stay fully fail-closed (NO edge), because emitting the sole indexed legacy edge there would leak a WRONG-CALLEE edge (e.g. a.f for `from a import f; from b import f` when Python's effective target is the later b.f); the C8-1 duplicate-import case carries DuplicateSameNameImport and gets no fallback, making C8-1 genuinely complete. Gating the fallback on T1==None alone would DROP the provable-namespace-but-unbound calls (the python_bare stamp excludes them from reresolve_calls_edges via the qualifier_kind.is_empty() filter at cozo_queries.rs:2222-2227) -- the F3 recall regression. F9: the pre-existing name->IDs singleton is INLINE-PRIVATE CozoScript inside reresolve_calls_edges (cozo_queries.rs:2191-2205,2263-2270) and filters out python_bare rows, so it CANNOT be reused as-is; T5b EXPOSES it as a small PUBLIC read-only language-scoped name->IDs helper in cozo_queries.rs and calls it for the no-target fallback -- a helper exposure, NOT a schema change; the earlier "NO cozo_queries.rs change" claim is REMOVED. Canonical (binding-kind) resolution + the legacy name-only edge are outside the T5c shadow-guard scope (unchanged legacy semantics). Then REUSE the existing canonical_index.get(&target) singleton match (ids.len()==1), dropping on zero/ambiguous/duplicate. TDD RED->GREEN. Domain: code.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on T1, T2b (096.009-T), T3, T5a. Language-dispatch on caller file language; reuse function_ids_by_canonical_path (cozo_queries.rs:1046) unchanged. Bindings come from the T2b scope model (function-local first, then module-level) and carry the T2 kind (R2). X2/X3 + C7-1: bare-callee resolution is CALL-SITE-EFFECTIVE last-binding-wins over FUNCTION-BODY calls only (module-level calls are never staged, C9-3): a later FromImportSymbol import rebind (last STABLE module binding) beats a local def (`def parse(); from bar import parse; def caller(): parse()` -> bar.parse), a function-body call with a local def and no later rebind binds the def (`def parse(); def caller(): parse()` -> M.parse, C7-1 stable), and a function-body call contested by a later module rebind fails closed (C7-1); module-level (top-level) call ordering is a v1 non-goal (not extracted); local-def-first and whole-module last-binding-wins are both forbidden; when execution order can't establish a stable call-site binding, fail closed. Positions come from T2/T2b (F14). This branch is only REACHABLE because T5a's F2 fix routes shadowed same-file calls to python_bare staging instead of a direct edge. Y3+F3: the legacy name-only unique-match fires whenever T5b derives NO canonical target for the TYPED reasons {NoModuleContext, UnsupportedImportForm} (NEVER for {CompetingBindings, Shadowed, DuplicateSameNameImport} -- C9-4), not only when T1==None -- placed HERE (T5b already depends on T1) so NO new DAG edge is required for the fallback and the legacy pass stays untouched; T5a stamps python_bare unchanged. F9 (option a): expose a public language-scoped name->IDs singleton helper in cozo_queries.rs (extraction of the inline-private singleton at 2191-2205) and ADD cozo_queries.rs to this task's scope; the "No cozo_queries.rs change" claim is deleted. Y2/F1: T5c wraps this resolution with an ORDER-AWARE guard anchored on the WINNING binding (import OR def) -- T5b stays guard-agnostic and consistent with T5c. R3: T5b is guard-agnostic -- it does NOT call the T5c shadow guard; T5c wraps T5b downstream. Dependency stays T5c->T5b (never reverse), so T5b is independently completable and the DAG is acyclic. Files (3): src/services/code_graph.rs, src/db/cozo_queries.rs (the F9 helper exposure), tests/integration/calls_recall_acceptance_test.rs -- three files for a single-domain code task (mirrors T1's four-file precedent); scenarios kept at <=4. F16 (split T5b) declined per operator minimal-DAG directive.
<!-- END:implementation-notes -->