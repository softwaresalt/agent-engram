---
artifact_type: task
created_at: 2026-07-23T18:46:28.4851217-07:00
dependencies:
    - 096.001-T
    - 096.003-T
    - 096.005-T
    - 096.009-T
id: 096.006-T
labels:
    - python
    - canonical-identity
    - call-graph
    - code
parent_id: 096-F
priority: medium
references:
    - src/services/code_graph.rs
    - src/db/cozo_queries.rs
status: queued
title: 'T5b: Python canonical target resolution in the canonical post-pass'
updated_at: 2026-07-23T20:09:14.9214675-07:00
---



<!-- BEGIN:acceptance-criteria -->
- Guard-AGNOSTIC (R3): T5b resolution does NOT invoke the T5c shadow guard; shadow/rebind cases are owned by T5c (096.007-T), which wraps T5b downstream -- so T5b is independently completable.
- import bar; two modules define parse; caller bar.parse() -> edge resolves to BAR's exact parse id (target-identity, not row-existence; 082-F gate) [ModuleImport receiver, R2].
- Bare parse() with `from bar import parse` -> resolves to bar.parse [FromImportSymbol bare callee, R2].
- R2: `from pkg import parse; parse.tokenize()` (FromImportSymbol receiver) -> NO module edge (attribute-on-object, out of scope; NOT mis-resolved as module pkg).
- Fail-closed ambiguity: `from bar import *` bare parse() with 2+ parse defs, AND two same-file parse defs (duplicate canonical path) -> NO canonical edge (FF7DE872 stays unfixed here).
- cargo test --test integration_calls_recall_acceptance --test integration_code_graph green; clippy --all-targets clean; fmt clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Python-aware canonical target resolution in reresolve_calls_edges_with_canonical_context (code_graph.rs:264-368). Add python_ctx_for_staged_file (module path via T1 using the regular-package predicate + scope-aware bindings via T2b, read from source like rust_ctx_for_staged_file, using the existing context_cache) and a Python branch dispatched by the CALLER file's language that computes the target canonical path using the T2 binding KIND (R2). This stage is SHADOW-GUARD-AGNOSTIC (R3): it does NOT invoke the T5c guard -- shadow-rebind handling is added downstream by T5c, keeping T5b independently completable. qualifier_kind=="python_bare" -> M.callee if callee defined in M, else a FromImportSymbol binding for callee (N.callee), else fail closed (a ModuleImport-kind name used as a bare callee is not a function -> fail closed). qualifier_kind=="module" -> the receiver must resolve to a ModuleImport binding (import pkg / import a.b as c) -> <module>.callee; a FromImportSymbol receiver (from pkg import parse; parse.tokenize()) is attribute-on-object, NOT a module -> fail closed (R2); else fail closed. Then REUSE the existing canonical_index.get(&target) singleton match (ids.len()==1), dropping on zero/ambiguous/duplicate. NO cozo_queries.rs change. TDD RED->GREEN. Domain: code.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on T1, T2b (096.009-T), T3, T5a. Language-dispatch on caller file language; reuse function_ids_by_canonical_path (cozo_queries.rs:1046) unchanged. Bindings come from the T2b scope model (function-local first, then module-level) and carry the T2 kind (R2). R3: T5b is guard-agnostic -- it does NOT call the T5c shadow guard; T5c wraps T5b downstream. Dependency stays T5c->T5b (never reverse), so T5b is independently completable and the DAG is acyclic. Files (<=2): src/services/code_graph.rs, tests/integration/calls_recall_acceptance_test.rs.
<!-- END:implementation-notes -->