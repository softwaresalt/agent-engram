---
artifact_type: task
created_at: 2026-07-23T18:46:28.4851217-07:00
dependencies:
    - 096.001-T
    - 096.003-T
    - 096.005-T
    - 096.009-T
id: 096.006-T
labels:
    - python
    - canonical-identity
    - call-graph
    - code
parent_id: 096-F
priority: medium
references:
    - src/services/code_graph.rs
    - src/db/cozo_queries.rs
status: queued
title: 'T5b: Python canonical target resolution in the canonical post-pass'
updated_at: 2026-07-23T20:09:14.9214675-07:00
---



<!-- BEGIN:acceptance-criteria -->
- Two modules define parse; caller bar.parse() -> edge resolves to BAR's exact parse id (target-identity, not row-existence; 082-F gate).
- Bare parse() with `from bar import parse` -> resolves to bar.parse.
- Q1: python_bare resolution invokes the T5c shadow guard on the callee name (fails closed if the bare callee is re-bound in scope) -- shadow cases asserted in T5c (096.007-T).
- from bar import * then bare parse() with 2+ parse defs -> NO edge (fail closed).
- Two same-file parse defs (duplicate canonical path) -> NO canonical edge (fail closed; FF7DE872 stays unfixed here).
- cargo test --test integration_calls_recall_acceptance --test integration_code_graph green; clippy --all-targets clean; fmt clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Python-aware canonical target resolution in reresolve_calls_edges_with_canonical_context (code_graph.rs:264-368). Add python_ctx_for_staged_file (module path via T1 using the regular-package predicate + scope-aware bindings via T2b, read from source like rust_ctx_for_staged_file, using the existing context_cache) and a Python branch dispatched by the CALLER file's language: qualifier_kind=="python_bare" -> M.callee if callee defined in M else the caller-scope binding N.callee (function-local first, then module-level) else fail closed, THEN apply the T5c shadow guard to the bare callee name (Q1: fail closed if callee is re-bound in the applicable scope); qualifier_kind=="module" -> resolve raw_qualifier to a bound module then <module>.callee else fail closed (T5c guard applies to the receiver). Then REUSE the existing canonical_index.get(&target) singleton match (ids.len()==1), dropping on zero/ambiguous/duplicate. NO cozo_queries.rs change. TDD RED->GREEN. Domain: code.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on T1, T2b (096.009-T), T3, T5a. Language-dispatch on caller file language; reuse function_ids_by_canonical_path (cozo_queries.rs:1046) unchanged. Bindings come from the T2b scope model (function-local first, then module-level). Q1: python_bare resolution must invoke the T5c shadow guard on the resolved callee name (not just the module receiver). Files (<=2): src/services/code_graph.rs, tests/integration/calls_recall_acceptance_test.rs.
<!-- END:implementation-notes -->