---
artifact_type: task
created_at: 2026-08-01T07:11:47.4764422Z
dependencies:
    - 109.005-T
id: 109.006-T
labels:
    - daemon
    - lifecycle
    - pending-sync
    - concurrency
    - green
    - pr-302
parent_id: 109-F
priority: medium
references:
    - docs/exec-plans/2026-07-31-post-105-pending-sync-residuals-plan.md
    - src/tools/write.rs
    - src/server/state.rs
status: queued
title: 'GREEN: expose and adopt coherent dispatch generation token'
updated_at: 2026-08-01T07:38:31.3333011Z
---

Source-only GREEN write unit. In state.rs and write.rs, expose a pub(crate) opaque generation token with private fields inside DispatchSnapshot (or the existing equivalent result), captured in the same coherent read window as the exact workspace/config pair. sync_workspace adopts that token directly and carries it through failed-lock explicit publication/reacquire. There is no generation retry or new observable error.

<!-- BEGIN:acceptance-criteria -->
AC1: W1a passes with the exact snapshot token carried through contention; stale G after G+1 is ignored without relabel/heavy leakage. AC2: W1b passes with unchanged queued status/message and gate flags. AC3: no before/after retry, retry budget, exhaustion error, later-generation fallback, response change, or lock before snapshot is introduced. AC4: token type/API is pub(crate), fields private, and unrelated DispatchSnapshot consumers remain source-compatible. AC5: the intermediate tree builds before lifecycle migration.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Production width: src/server/state.rs plus src/tools/write.rs, at most three touched production functions total (snapshot construction, write producer, and only if needed one existing adapter), target 65-95 minutes. Every touched production function counts. STOP for a fourth function, third file, public token field, retry/error path, response change, or 105 minutes.
<!-- END:implementation-notes -->