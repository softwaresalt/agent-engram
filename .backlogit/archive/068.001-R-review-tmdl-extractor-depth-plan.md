---
artifact_type: review
created_at: 2026-07-03T19:32:51.72148-07:00
id: 068.001-R
parent_id: 068-F
status: accepted
title: Review TMDL extractor depth plan
updated_at: 2026-07-03T19:32:51.72148-07:00
---

Accepted plan-review gate for the TMDL extractor depth plan
(docs/exec-plans/2026-07-03-tmdl-extractor-depth-plan.md), covering re-parented
tasks 068.001-T / 068.002-T / 068.003-T under feature 068-F, assembled into
queued shipment 068-S.

## Summary

<!-- BEGIN:summary -->
Reviewed the impl-plan that re-parents the three deferred TMDL depth tasks
(origin 066.005/006/007-T under archived 066-F) into new feature 068-F and a
queued shipment 068-S. Plan is grounded in verified code (crate parser, adapter,
models, indexer, tests), width-isolated to the TMDL/Power BI extraction
subsystem, test-first, and dependency-ordered. **Disposition: ACCEPTED for
harvest** with the noted contingency and corrections applied.
<!-- END:summary -->

## Findings

<!-- BEGIN:findings -->
- **F1 (scope/validity, resolved):** All three tasks are real, non-duplicate gaps
  confirmed against `crates/powerbi-tmdl-parser/src/lib.rs` — `partition` is only a
  boundary keyword (not extracted), `TmdlDataSource` carries name only, and
  `ref`/`annotation`/`lineageTag`/`culture` are dropped. No partition entity exists
  anywhere (grep-confirmed). Not already-done, not duplicate.
- **F2 (missing summary, informs 068.002-T):** There is currently NO
  `powerbi_data_source` summary record emitted by `extract_model_summaries_from_model`
  (only model/table/measure/expression). 068.002-T must ADD that record kind, not
  just enrich fields — captured in the plan and feature DoD.
- **F3 (fixture correction, P2):** 068.001-T's referenced fixture
  `tmp/ILSOS-VehicleServices.SemanticModel/...` is NOT committed. Tests must use
  inline `r"..."` fixtures per the existing `S-PTM-0x` pattern. Noted inline on
  068.001-T and in the plan.
- **F4 (sizing risk, P2):** 068.001-T (partitions + fenced-M capture + new model
  entity + new `PowerBiNodeKind::Partition` + indexer wiring + summary + tests)
  sits at the upper edge of the 2-Hour Rule. Pre-authorized split path recorded so
  Ship is not blocked mid-execution.
- **F5 (blast radius, accepted):** New `Partition` node kind is an additive string
  in the existing `powerbi_node` relation — no CozoDB relation/schema migration.
  New model fields must be `#[serde(default)]` additive. Judged MODERATE; folded-in
  hardening is proportional, no separate plan-harden pass required.
- **F6 (serialization dependencies, accepted):** 001→002→003 are merge-safe
  serialization edges (shared files), not logical data dependencies — documented as
  such so Ship understands they may be parallelized only with conflict care.
- **F7 (test-first, accepted):** `S-PBI-05` kind enumeration and new
  `S-PTM-1x`/`S-PBI-0x` fixtures required before implementation code.
<!-- END:findings -->

## Decisions

<!-- BEGIN:decisions -->
- **ACCEPT** the plan for harvest. Zero gate-blocking (P0/P1) findings; F2/F3/F4
  addressed in the plan, feature DoD, and task notes.
- Ship the three tasks in shipment 068-S in dependency order 068.001-T →
  068.002-T → 068.003-T.
- 066.008-T (tree-sitter grammar eval) remains BLOCKED (constitution decision on
  a grammar-backed FFI/`unsafe` boundary), excluded from 068-S, linked as
  follow-on (depends_on 068.003-T). Source spike:
  docs/decisions/2026-06-12-tmdl-tree-sitter-spike.md.
- Pre-authorize the 068.001-T split contingency if Ship measures it > 2h.
- No new deliberation required: decision space was closed at the original 066-F
  harvest; this is re-parenting + resequencing of pre-decided specs.
<!-- END:decisions -->

