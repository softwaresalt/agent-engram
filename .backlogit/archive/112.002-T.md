---
artifact_type: task
assigned_to: ship
commit: ca398bd6
created_at: 2026-08-06T03:41:20.5792616Z
dependencies:
    - 112.001-T
id: 112.002-T
labels:
    - rust
    - cli
    - ipc
    - windows
    - observability
    - runtime-verification
parent_id: 112-F
priority: high
references:
    - docs/exec-plans/2026-08-05-cold-cli-request-frame-correlation-plan.md
    - src/shim/lifecycle.rs
    - src/daemon/ipc_server.rs
    - docs/decisions/2026-08-04-daemon-index-runtime-root-cause-follow-up.md
status: done
title: Correlate cold CLI request with terminal response frame
updated_at: 2026-08-06T04:48:01.5458578Z
---

U2 from the reviewed 62046B37 plan. Satisfy the U1 RED contract with the minimum contained observability at the existing auto-spawn and response-frame boundaries, then perform exactly one post-seam Windows run. In debug builds only, a boolean test-named switch may capture daemon stdout/stderr to fixed files below the owned workspace `.engram`; no arbitrary path and no release behavior. Emit one terminal frame event carrying connection ID, JSON-RPC response ID, and serialize/write/flush outcome. Do not change wire bytes, startup, request deadlines, shutdown ordering, protocol, or persistence.

<!-- BEGIN:acceptance-criteria -->
- Modify only `src/shim/lifecycle.rs` and `src/daemon/ipc_server.rs`, fewer than four affected functions, with U1 as the prior RED contract.
- Debug-only capture uses a boolean test-named switch and fixed workspace-local stdout/stderr files; it cannot select an arbitrary host path, inherit `ENGRAM_DATA_DIR`, or alter release stdio behavior.
- The terminal frame event carries `connection_id`, exact JSON-RPC `response_id`, and `flushed|serialize_error|write_error|flush_error` after the attempt.
- Wire bytes, response ID echo, timeout semantics, startup ordering, shutdown ordering, protocol, and persistence remain unchanged.
- Run the ignored Windows live scenario exactly once after the seam; this is attempt two and no third run is allowed.
- Record CLI start/finish, user timeout, daemon PID/endpoint, dispatch/correlation record, frame event, client disposition, and aggregate elapsed time; classify `CORRELATED-TIMEOUT`, `CORRELATED-COMPLETION`, `NON-REPRODUCING`, or `BLOCKED`.
- Gracefully shut down only the temp-workspace daemon, use inherited idle timeout as fallback, and prove exact PID dead plus named pipe unreachable. If still live, preserve evidence and block; never force-kill without explicit approval.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Single CLI/IPC observability concern, two source files, budget <=2h. Test-first via 112.001-T. No production timeout fix or broad daemon redesign.
<!-- END:implementation-notes -->