---
artifact_type: task
created_at: 2026-07-23T18:46:11.4081065-07:00
dependencies:
    - 096.002-T
    - 096.004-T
    - 096.009-T
id: 096.005-T
labels:
    - python
    - call-graph
    - code
parent_id: 096-F
priority: medium
references:
    - src/services/code_graph.rs
status: done
title: 'T5a: Route Python bare + module-qualified calls into canonical staging'
updated_at: 2026-07-27T03:46:21.3234862Z
---



<!-- BEGIN:acceptance-criteria -->
- Python cross-file bare call -> staged row with qualifier_kind=="python_bare" on BOTH the full-index arm (851-908) AND the sync arm (1573-1643); the sync arm no longer name-only-stages Python bare calls (X1).
- Python mod.func() -> staged row with qualifier_kind=="module".
- F2 (root defect, GENERALIZED cycle-7 Z1/Z2/C7-2; non-import-rebind axis CLOSED cycle-8 C6): an in-file bare call whose callee has a same-file `def` AND is SHADOW-CONTESTED -- over BOTH scopes AND binding forms -- is ROUTED TO python_bare staging (NOT a direct M.parse edge) on both arms, so T5b/T5c adjudicate call-site-effective last-binding-wins. The contest is EITHER (i) a producer-backed import signal: a named module import (`def parse(); from bar import parse; parse()`), a positioned star-import invalidator (`def parse(); from n import *; parse()`, Z1), OR a scoped function-local import (`def parse(); def g(): from bar import parse; parse()`, C7-2; reachable caller scope = {own function-local, module} ONLY per Anchor A -- enclosing/nested-function scopes are UNREACHABLE as callers, represented only to ISOLATE nested locals from leaking up, C13-2); OR (ii) ANY non-import rebind form in the Shared rebind-form set (RFS, defined once in the plan Design-decision) that T2/T2b do NOT produce -- so T5a detects it with its OWN in-file tree-sitter scan, the SAME module/scope rebind-form scan T5c specifies -- e.g. `def parse(); parse = factory(); parse()` (Z2/C6), also annotated-assignment (`parse: T = ...`)/`class parse`/`del parse`/`match`-case/for/with-as/except-as/walrus/parameter targets. An in-file callee whose matched `def` is PROVABLY the sole APPLICABLE binding STILL yields a direct calls_edge (C9-1 self-exclusion: the scan EXCLUDES the def under consideration -- it is the candidate target, not a self-competitor -- and counts only OTHER bindings of the name applicable to the caller's lexical scope, so no OTHER import/star/scoped signal AND no OTHER RFS form is found; fast path preserved). Without self-exclusion `def f; f()` would self-contest via its own `def` RFS form and the direct fast path would be unreachable dead code.
- Explicit C6 routing-table assertion: `def f; f = factory(); f()` -> STAGED as python_bare with NO direct M.f edge on BOTH arms (also covers `class f`/`del f`/`match`-case capture). This is the non-import-rebind vector cycle-7 missed.
- C12-2/T-b unsupported/relative/duplicate/conditional import CONTEST (Anchor C): a same-file `def f` whose name ALSO has a relative import (`from .other import f`), a duplicate same-name import, or a control-flow-conditional/`try`-guarded import -- all recorded by T2 as POSITIONED AMBIGUITY MARKERS (T-b/T-c) -- is ROUTED TO python_bare staging (a CONTEST), NEVER a direct M.f edge; the guard must NOT fall through while such a marker exists (this is what makes T5b's typed no-target reason reachable). Regression: `def f(): ...; from .other import f; def caller(): f()` -> STAGED (no direct M.f).
- The routing predicate is expressed as a PROVE-THE-NEGATIVE ABSTRACTION -- "keep the direct edge ONLY when the same-file def is provably the sole APPLICABLE binding (C9-1: excluding the matched def itself, within the caller's lexical scope) across every modeled scope AND with no OTHER RFS rebind form present; otherwise route to staging" -- NOT axis-by-axis, so future shadow SCOPES *and* binding FORMS are covered by construction (closes the F4 star remainder AND the C6 non-import-rebind axis in the same abstraction).
- C9-1 no over-routing (sole-binding fast path is live, not dead code): `def f(): ...; def caller(): f()` with NO other binding of `f` applicable to caller's scope -> a DIRECT M.f calls_edge (the matched `def f` is SELF-EXCLUDED from the RFS scan, so it does not self-contest); assert the direct fast path is exercised.
- C9-2 re-parse mechanism: T5a computes the file's Rebind-Form-Set map by RE-PARSING the in-scope `source: String` in-memory (parse_source retains NO tree-sitter Tree -- ParseResult{symbols,edges} only, parsing.rs:247-252; the source is already cloned at the seam ~code_graph.rs:609 full-index / ~1290 sync), ONCE PER FILE and CACHED for reuse across every call in that file (NOT per call-site) -- one extra amortized in-memory parse per Python file; assert no new producer, DB field, task, or DAG edge.
- Rust cross-file bare call -> STILL name-only staged on both arms (regression assertion).
- cargo test --test integration_code_graph green; existing Rust calls tests unaffected; clippy --all-targets clean; fmt clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Route Python canonical-eligible calls into provenance staging (Option A) at BOTH Calls-consumer sites so version-triggered re-extraction (T7) never strands Python calls on the legacy name-only path (X1, same class as Q2/R1): the full-index arm (code_graph.rs:851-908) AND the sync arm (code_graph.rs:1573-1643), + should_stage_provenance_call (188-194), language-dispatched on the caller file's language: (a) accept qualifier_kind=="module" for Python into provenance staging; (b) for Python BARE calls whose callee is unresolved in-file, stage via put_staged_call_with_provenance with qualifier_kind=="python_bare" instead of name-only put_staged_call -- on BOTH arms (the sync arm's bare-call put_staged_call at line 1639 must also route through provenance for Python). F2 ROOT-DEFECT FIX, GENERALIZED cycle-7 and CLOSED over binding FORMS cycle-8 (C6) via the PROVE-OR-FAIL-CLOSED invariant (this task OWNS it): the in-file (Some,Some) branch at code_graph.rs:900-902 currently mints a DIRECT M.callee edge whenever the bare callee resolves against the current file's symbols, BEFORE any python_bare staging -- so `def parse(); from bar import parse; parse()` emits a FALSE direct M.parse edge (frozen rule requires bar.parse) and makes T5b's last-binding-wins branches UNREACHABLE. Cycle-6's fix keyed the skip on NAMED module-level imports ONLY; cycle-7 extended it to star/scoped imports -- but BOTH keyed only on PRODUCER-BACKED import signals, so the NON-IMPORT module/scope rebind axis (C6) -- `parse = factory()`/class/del/match-case/for/with-as/except-as/walrus/parameter targets -- had NO PRODUCER (T2/096.002-T and T2b/096.009-T walk only import nodes) and still minted a false direct edge. Modify code_graph.rs:896-908 on BOTH arms so that, for Python, a same-file `def` is routed to python_bare staging WHENEVER its name is SHADOW-CONTESTED by EITHER (i) a producer-backed import signal -- a named module import (T2 ImportBindings), a positioned star-import invalidator (T2/F4), a scoped function-local import (T2b scoped-binding model), OR a positioned ambiguity marker for a relative/duplicate/conditional-`try` import (T2, Anchor C T-b/T-c) -- so the guard NEVER falls through to a direct M.f edge while such a marker exists -- OR (ii) ANY non-import rebind form in the SHARED REBIND-FORM SET (RFS, defined once in the plan Design-decision; incl. plain/annotated/augmented assignment, class, del, match-case, for/with-as/except-as/walrus/parameter targets), which T5a detects with its OWN in-file rebind-form scan (the SAME module/scope rebind-form scan T5c specifies). Because parse_source retains no tree-sitter Tree (it returns ParseResult{symbols,edges} only, parsing.rs:247-252), T5a RE-PARSES the in-scope file `source: String` (already cloned at the seam ~code_graph.rs:609 full-index / ~1290 sync) in-memory, computing the Rebind-Form-Set map ONCE PER FILE and CACHING it for reuse across every call in the file (NOT per call-site) -- one extra amortized parse per Python file, so NO new producer, DB read, task, or DAG edge is needed. C9-1 SELF-EXCLUSION: the scan EXCLUDES the `def` under consideration (the definition find_function_id matched is the candidate target, never a self-competitor) and counts only OTHER bindings of the name applicable to the caller's lexical scope (module + the caller's own function-local scope -- not enclosing/nested-function or sibling-scope locals/params, unreachable per Anchor A). Express the predicate as the PROVE-THE-NEGATIVE default -- "keep the direct edge ONLY when the matched def is provably the sole APPLICABLE binding (no OTHER import/star/scoped signal AND no OTHER RFS form) across every modeled scope; otherwise route to staging" -- so future SCOPES *and* FORMS are covered by construction. Rust behavior untouched on both arms (every new branch guarded on Language::Python). TDD RED->GREEN. Domain: code.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on T4 (096.004-T), T2 (096.002-T) AND T2b (096.009-T) -- the T2b edge was added cycle-7 (Z1/Z2/C7-2 generalization): T5a consults T2b's scoped-binding model (function-local imports + positions) in addition to T2's module-level ImportBindings (named imports + positioned star markers) to know whether a same-file `def` is IMPORT-shadow-contested at ANY modeled scope. Acyclic: T2b depends only on T2 and is already upstream of T5b/T5c, so T5a<-T2b adds no back-edge. CYCLE-8 (C6) adds NO new dependency: the non-import rebind FORMS (RFS -- plain/annotated/augmented assignment, class, del, match-case capture, for/with-as/except-as/walrus/parameter targets, plus a module-scope `from N import *` star marker) have NO PRODUCER (T2/T2b walk only import nodes), so T5a detects them with its OWN in-file rebind-form scan -- the SAME scan T5c runs (plan Design-decision defines the RFS ONCE; T5a routing and T5c invalidation consume the identical set so they cannot drift; T5a's use is order-agnostic "any RFS form present -> stage", T5c's is order-aware (winner = last module-level binding with NO later module-level rebind; ANY later module-level rebind after the winner fail-closes as undecidable)). C9-2: parse_source retains no tree-sitter Tree (ParseResult{symbols,edges} only, parsing.rs:247-252), so T5a RE-PARSES the in-scope file `source: String` (already cloned at the seam ~code_graph.rs:609 full-index / ~1290 sync) in-memory, building the Rebind-Form-Set map ONCE PER FILE and caching it (reused across all calls in the file, not per call-site); option B thus needs NO new producer, task, or DAG edge. C9-1 self-exclusion: the scan EXCLUDES the matched `def` under consideration (candidate target, not self-competitor) and counts only OTHER bindings of the name applicable to the caller's lexical scope, so the `def f; f()` sole-binding fast path stays live (not dead code). Two Calls-consumer sites in the SAME file (full-index 851-908 + sync 1573-1643); the sync arm currently name-only-stages bare calls at line 1639 (X1); the in-file direct-edge shortcut is at (Some,Some)=>create_calls_edge, 900-902 (F2) full-index and ~1635-1636 sync. Y3/F3: T5a still stamps python_bare on both arms; recall for callers whose layout T1 rejects (src/-root / PEP 420 / __init__.py) AND for provable-namespace-but-unbound callees (star/re-export/relative) is preserved DOWNSTREAM by T5b's legacy name-only fallback (096.006-T), which fires whenever T5b derives NO canonical target -- no staging change here, asserted in T5b's tests. No cozo_queries.rs schema change (reuses put_staged_call_with_provenance). Files (<=2): src/services/code_graph.rs (two Calls-consumer sites + the in-file decision + the shared RFS scan), tests/integration/code_graph_test.rs. 4 scenarios (the shadow-contest routing vectors -- named import / star-after-def / non-import rebind (C6) / function-local import / relative-duplicate-conditional ambiguity marker (T-b/T-c) -- are consolidated into ONE parametrized routing-table scenario, incl. `def f; f = factory(); f()` -> STAGED no direct M.f AND `def f(): ...; from .other import f; def caller(): f()` -> STAGED no direct M.f, to stay within the 2-hour rule while covering every scope and form axis by construction).
<!-- END:implementation-notes -->