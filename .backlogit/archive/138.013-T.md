---
artifact_type: task
commit: e7f80093
created_at: 2026-08-27T20:51:52.2581993Z
id: 138.013-T
labels:
    - shim
    - transport
    - seam
    - behavior-neutral
    - phase1
parent_id: 138-F
priority: critical
references:
    - src/shim/transport.rs
status: done
title: 'Phase 1 seam: behavior-neutral transport probe indirection and tokio clock seam'
updated_at: 2026-08-27T21:23:05.5581297Z
---

PHASE 1 (behavior-neutral seams). Width: src/shim/transport.rs ONLY. Must land BEFORE the concurrency harness 138.006-T, which cannot compile without it - this ordering is the fix for the revision-1 defect where the harness was required to compile against a with_probe API that did not yet exist. TWO changes, both behavior-neutral: (1) Probe indirection - ShimHandler holds a probe fn field DEFAULT-INSTALLED to lifecycle::probe_health (or check_health until 138.001-T lands), plus a test-only constructor. It MUST be a default-installed production indirection, NOT a #[cfg(test)] branch inside forwarding_endpoint, so the tested path is the shipped path (hardening H-7). (2) Clock seam - change RecoveryProbeState.last_failure from std::time::Instant to tokio::time::Instant (src/shim/transport.rs:11,31,144 and .elapsed() at :138). This is a REAL production change, not a test affordance: tokio::time::pause/advance CANNOT move a std::time::Instant, so the C2 cooldown assertion was unprovable in revision 1. tokio::time::Instant is wall-clock-identical when the runtime is not paused. BEHAVIOR-NEUTRALITY GATE (blocking): the entire pre-existing suite is green before and after, NO new behavioral assertion turns green, and the diff introduces NO conditional on health outcome. Keep ShimHandler crate-private; do not widen pub API.