---
artifact_type: task
created_at: 2026-07-22T15:32:10.1631805-07:00
dependencies:
    - 095.012-T
id: 095.010-T
parent_id: 095-F
priority: medium
status: done
title: 'U8: Lineage read surface — extend query_graph traversal + dataset_node resolution'
updated_at: 2026-07-24T00:43:49.7656408Z
---

Make the v1 lineage subgraph retrievable by agents WITHOUT adding a new MCP tool (PR #281 finding 7). Today query_graph/find_path traverse a hard-coded edge allowlist (CODE_EDGE_TABLES / BACKLOG_EDGE_TYPES / POWERBI_EDGE_TYPES_FP at src/db/cozo_queries.rs:4921-4940) and resolve only those node types — lineage_edge is not in any allowlist and dataset_node is not resolvable, so v1 would persist data no agent can read. Extend the EXISTING query_graph path (mirror the PowerBI precedent: query_graph_neighborhood at cozo_queries.rs:4859; powerbi_graph_node_to_json at src/tools/read.rs:1015): add a DEDICATED lineage traversal branch — a lineage traversal loop + a dataset_node resolver in bfs_directed_impl/find_path (cozo_queries.rs:4485/4497/4531-4536), NOT merely a tuple appended to the edge allowlist (AR-16) — plus dataset_node resolution/JSON projection so a query rooted at (or reaching) a dataset returns its lineage neighborhood. TRAVERSAL DIRECTION (AR-06): outgoing / find_path from a TARGET reaches its SOURCES (upstream lineage); discovering downstream CONSUMERS ('what derives from source Y') requires an INCOMING neighborhood (direction=Incoming; query_graph_neighborhood already takes a direction). REFUTE a reciprocal lineage_flows_to edge — it is scope creep (violates A6 scope-min); use DIRECTION semantics only. NO new MCP tool; NO query_sql (it does not exist). READ-SURFACE ADVERTISEMENT (cycle-5 F6): update EVERY surface that advertises edge types — the MCP tool DESCRIPTION in src/shim/tools_catalog.rs:405-445 (today enumerates only code + backlog edge types at :408 and omits lineage, so agents cannot discover it) AND the query_graph tool-doc comment at src/tools/read.rs:1391-1393 — both to advertise lineage_derives_from and to correct the enumeration to the full traversable set code/backlog/powerbi/lineage (Power BI is ALREADY traversable but currently unadvertised) OR to mark the enumeration explicitly non-exhaustive (AR-26). The CLI edge_types arg help (src/bin/engram.rs:270) is generic ('all types', no enumeration) so it needs no per-type change but is confirmed consistent. Tests: query_graph rooted at a dataset_node returns lineage_derives_from neighbors; a query_graph INCOMING neighborhood from a SOURCE dataset returns its downstream CONSUMER targets (AR-06); a non-lineage root is unaffected (no regression); a CATALOG CONTRACT TEST asserting the query_graph MCP tool description (tools_catalog.rs) advertises the lineage_derives_from edge type (and the corrected code/backlog/powerbi/lineage enumeration) so lineage is discoverable (cycle-5 F6 / AR-26). Files: src/db/cozo_queries.rs, src/tools/read.rs, src/shim/tools_catalog.rs (<=3 files). Domain: db-traversal + MCP read (width-isolated from parsers/indexer). Blocked by U1a' (095.012-T). Plan Unit 8.