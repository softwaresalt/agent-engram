---
archived_from: .backlogit/queue/029.002.001-T.md
artifact_type: task
commit: 091a164a405e42d55bc0345f35ce09f39e7d5500
created_at: 2026-04-21T11:30:00-07:00
id: 029.002.001-T
labels:
    - reliability
    - daemon
    - shipment-b1
    - harness-ready
parent_id: 029.002-C
priority: high
status: archived
title: Scaffold failing PID self-healing harness (red phase)
updated_at: 2026-04-22T07:47:42.4775622-07:00
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
**Red-phase harness task.** Establishes failing test surface for Unit 2 BEFORE implementation. Tasks `029.002.002-T` and `029.002.003-T` make these pass.

Scaffold:
1. Unit tests in `src/shim/pidfile.rs` (new, stub only): `verify_alive_returns_false_when_pid_reused`, `verify_alive_returns_true_for_live_process`, `atomic_write_persists_in_pid_dir_not_temp_dir` (Windows runner).
2. `tests/integration/stale_pid_recovery_test.rs` -- `shim_recovers_from_stale_pid_file` end-to-end test.
3. Stub `PidFile { pid: u32, start_time_unix: u64 }` struct + `verify_alive()` returning `unimplemented!()`; stub `WorkspaceError::PathEscape` variant.

**Files:** `src/shim/pidfile.rs` (new, stub), `tests/integration/stale_pid_recovery_test.rs`, `src/errors/mod.rs`.

**Execution posture:** test-first (red phase).

**Granularity:** 3 files, 1 stub function, 4 failing test scenarios.

**Dependency:** `029.002.002-T` and `029.002.003-T` depend on this task; cannot start until tests compile and fail. Test helpers creating temp `.engram/` workspaces MUST return `TempDir` to the caller (per `docs/compound/test-failures/tempdir-lifetime-in-contract-tests-2026-03-30.md`).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Note

- Harness command: `cargo test verify_alive_returns_false_when_pid_reused --lib && cargo test verify_alive_returns_true_for_live_process --lib && cargo test atomic_write_persists_in_pid_dir_not_temp_dir --lib && cargo test shim_recovers_from_stale_pid_file`

## Acceptance Criteria

<!-- AC:BEGIN -->
- [x] #1 All four test scenarios compile and FAIL with stub-driven failure mode (`unimplemented!()` panic or assertion)
- [x] #2 `PidFile { pid, start_time_unix }` struct exists as stub in `src/shim/pidfile.rs` with `serde` derives
- [x] #3 `WorkspaceError::PathEscape { attempted: PathBuf, root: PathBuf }` variant exists in `src/errors/mod.rs`
- [x] #4 `cargo build --tests` succeeds; no production wiring yet
<!-- AC:END -->
