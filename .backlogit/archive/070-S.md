---
archived_from: .backlogit/queue/070-S.md
artifact_type: shipment
commit: 6d16e113a93ad7c314038985b1fb96062045a06c
created_at: 2026-07-04T12:12:34.4223329-07:00
custom_fields:
    items:
        - 070-F
        - 070.001-T
        - 070.002-T
    predecessor_shipment: 069-S
    review_artifact: 070.001-R
    source_plan: docs/exec-plans/2026-07-04-tmdl-parser-correctness-fixes-plan.md
id: 070-S
status: archived
title: TMDL safe-parser correctness fixes (070-F)
updated_at: 2026-07-04T20:30:00Z
---

## Description

<!-- BEGIN:description -->
Queued shipment for feature 070-F: fix the two incrementally-fixable heuristic
bugs the 069-S evaluation gate surfaced in the safe TMDL parser
(`crates/powerbi-tmdl-parser/src/lib.rs`), as recorded in the eval finding
`docs/decisions/2026-07-04-tmdl-eval-gate-finding.md`. This is exactly the
"optional, bounded follow-on" the DECLINE finding proposed — no grammar, no new
dependency, crate stays `#![forbid(unsafe_code)]`. Two test-first tasks:
070.001-T (calculated-column expression capture — split the name on `=`, add an
additive `TmdlColumn.expression`; flips S-PTM-24 + downstream S-PTM-28) and
070.002-T (measure-DAX colon heuristic — refine `looks_like_tmdl_property` to a
property-shaped `key:`; flips S-PTM-25). The harness
`tests/unit/tmdl_differential_eval_test.rs` already pins the buggy behavior, so
TDD is half-done: each task flips its pinned assertion buggy→correct, then the
parser fix makes it green. Reviewed and accepted via 070.001-R. Plan:
docs/exec-plans/2026-07-04-tmdl-parser-correctness-fixes-plan.md. Awaiting Ship to
claim.
<!-- END:description -->

## Manifest

| Item | Title | Status | Origin |
|---|---|---|---|
| 070-F | TMDL safe-parser correctness fixes (calculated columns, measure-DAX colon) | queued | new umbrella |
| 070.001-T | Calculated-column expression capture (split name on '=', add TmdlColumn.expression) | queued | new (S-PTM-24 / S-PTM-28) |
| 070.002-T | Measure-DAX colon heuristic (refine looks_like_tmdl_property to property-shaped key) | queued | new (S-PTM-25) |

## Dependency Order

```text
070-F (umbrella)
   ├─contains─▶ 070.001-T (calc-column expression capture)
   └─contains─▶ 070.002-T (measure-DAX colon heuristic)
                    depends_on 070.001-T (serialization, shared test aggregate)
```

Execute 070.001-T → 070.002-T. The edge is a **serialization** dependency, not a
logical data dependency: both tasks edit `crates/powerbi-tmdl-parser/src/lib.rs`
and the shared `differential_gate_counts()` aggregate + module-doc delta table in
`tests/unit/tmdl_differential_eval_test.rs`. Sequencing avoids a merge conflict
and lets each task decrement `heuristic_bugs` by one (2 → 1 → 0). Feature parent
070-F is first in the manifest.

## Step 5.5 Scope Guard

Single-width parser correctness. **In scope:** the two heuristic fixes in
`crates/powerbi-tmdl-parser/src/lib.rs` + their pinned test assertions in
`tests/unit/tmdl_differential_eval_test.rs` (flip buggy→correct) + the aggregate
`differential_gate_counts` / module-doc delta-table reconciliation + the additive
`TmdlColumn.expression` field. **Out of scope (requires a new plan / new items):**
the four model-richness gaps (S-PTM-21 relationship qualifiers, S-PTM-23
hierarchy/level, S-PTM-26 calculationGroup, S-PTM-27 RLS role); any grammar /
tree-sitter work (069-F DECLINE stands); surfacing the column expression onto
`PowerBiColumn`; any schema / CozoDB relation / indexer / CLI change; any new
dependency; any `#![forbid(unsafe_code)]` relaxation. If a fix wants to grow into
model richness, **spawn a new feature/task** — do not expand these two.

## Follow-on (NOT in this shipment)

- **Model-richness gaps** (candidate future work): S-PTM-21 relationship
  qualifiers (crossFilteringBehavior / isActive / cardinality), S-PTM-23
  hierarchy/level, S-PTM-26 calculationGroup / calculationItem, S-PTM-27 RLS
  role / tablePermission — each needs a new model type; none is fixable by a
  heuristic tweak.
- **DAX tree-sitter** (stash `F7E89921`) stays parked/deferred — unsafe-myth
  correction already applied to its spike
  (`docs/decisions/2026-06-13-dax-tree-sitter-spike.md`); no consumer requires
  symbolic DAX yet.
- **Deferred backlog:** 064.004-T (Phase 1b reactive sync), 065.004-T remain
  queued and untouched.

## Ship Notes

- Test-first: flip the pinned assertions listed in each task spec, watch them
  fail, implement the parser fix, watch them pass; reconcile the aggregate
  `differential_gate_counts` (`heuristic_bugs` 2 → 0) + module-doc delta table.
- Bug 2 regression guard: confirm `dataType:` / `lineageTag:` / relationship
  `fromColumn:`/`toColumn:` property recognition is unaffected by the
  `looks_like_tmdl_property` refinement.
- Additive; back-compat; crate stays `#![forbid(unsafe_code)]`; no new
  dependency. Gates: `cargo fmt --check`,
  `cargo clippy --all-targets -- -D warnings -D clippy::pedantic`,
  `cargo test --all-targets`.
- S-PTM-28 corrects automatically from the parser name-split (adapter
  `src/services/powerbi_tmdl.rs:105` flows `name` through) — no adapter change.

## Blocked Returns

<!-- BEGIN:blocked-returns -->
None.
<!-- END:blocked-returns -->
