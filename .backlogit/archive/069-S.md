---
archived_from: .backlogit/queue/069-S.md
artifact_type: shipment
commit: 2c0eea35a58f7c95ed62811a078a8f0ab37fb8ec
created_at: 2026-07-04T02:06:18.7754401-07:00
custom_fields:
    items:
        - 069-F
        - 066.008-T
    predecessor_shipment: 068-S
    review_artifact: 069.001-R
    source_plan: docs/exec-plans/2026-07-04-tmdl-tree-sitter-eval-gate-plan.md
id: 069-S
status: archived
title: TMDL tree-sitter evaluation gate (069-F)
updated_at: 2026-07-04T18:55:00Z
---

## Description

<!-- BEGIN:description -->
Queued shipment for feature 069-F: run the TMDL tree-sitter **decision gate**.
A single refined task (066.008-T) builds a differential-evaluation harness that
quantifies where the current safe TMDL parser (`parse_tmdl_document`,
`crates/powerbi-tmdl-parser/src/lib.rs:251`) drops, truncates, or mis-scopes
real-world TMDL structure, then applies a baked-in decision rule: low error →
`decline`; material mis-parses → promote grammar follow-on tasks under 069-F.
No new dependency, no grammar — this is measurement, not a build. Reviewed and
accepted via 069.001-R. Plan:
docs/exec-plans/2026-07-04-tmdl-tree-sitter-eval-gate-plan.md. Awaiting Ship to
claim.
<!-- END:description -->

## Manifest

| Item | Title | Status | Origin |
|---|---|---|---|
| 069-F | TMDL tree-sitter grammar path — evaluation and first slice | queued | new umbrella |
| 066.008-T | TMDL safe-parser differential-evaluation harness (tree-sitter decision gate) | queued | re-scoped (was tree-sitter grammar eval) |

## Dependency Order

```text
069-F (umbrella)
   └─contains─▶ 066.008-T (differential-evaluation harness — only task)
                    depends_on 068.003-T (DONE — the safe parser being measured)
```

`066.008-T`'s `depends_on 068.003-T` is a satisfied cross-shipment provenance
edge (068-S shipped/archived); it does not block claiming 069-S. Feature parent
069-F is first in the manifest (added before its child task).

## Step 5.5 Scope Guard

Single-width decision gate. **In scope:** TMDL parser test fixtures + a
differential-evaluation harness + a recorded finding on 066.008-T.
**Out of scope (requires a new plan / new 069-F tasks):** any tree-sitter or
grammar dependency, vendored grammar crate, external scanner / `grammar.js`,
production `parse_tmdl_document` behavior changes beyond trivially-safe fixes,
DAX work (parked at stash `F7E89921`), and any schema / indexer relation / CLI
change. If the harness surfaces material mis-parses, **promote new 069-F tasks**
— do not expand 066.008-T.

## Follow-on (NOT in this shipment)

- If the gate recommends building: new 069-F tasks for grammar sourcing
  (vendor vs. generate), external indentation scanner, ABI pinning +
  build-fragility mitigation (`docs/compound/build-errors/tree-sitter-grammar-abi-tsx-dispatch-2026-04-15.md`),
  and parity/regression tests vs. the safe parser.
- **DAX tree-sitter** (stash `F7E89921`) stays parked/deferred — same unsafe-myth
  correction applied to its spike (`docs/decisions/2026-06-13-dax-tree-sitter-spike.md`),
  but no consumer requires symbolic DAX yet. Out of scope for this cycle.

## Ship Notes

- Test-first: continue the inline `S-PTM-2x` fixture pattern in
  `tests/unit/powerbi_extract_tmdl_test.rs`; **do not** reference the uncommitted
  `tmp/ILSOS-VehicleServices…` sample. Optionally one
  `tests/integration/powerbi_search_ingestion_test.rs` assertion if a miss
  affects ingestion.
- Measurement-only; additive; crate stays `#![forbid(unsafe_code)]`; gates:
  `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings -D clippy::pedantic`,
  `cargo test --all-targets`.
- The task DoD carries the decision rule; Ship (or the executing agent) records
  the correctness delta and routes per the rule.

## Blocked Returns

<!-- BEGIN:blocked-returns -->
None.
<!-- END:blocked-returns -->

