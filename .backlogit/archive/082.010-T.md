---
artifact_type: task
created_at: 2026-07-11T00:59:21.5579942-07:00
dependencies:
    - 082.003-T
    - 082.008-T
id: 082.010-T
labels:
    - width:schema
    - graph
    - migration
    - rollback
    - slice:1
parent_id: 082-F
priority: medium
references:
    - docs/exec-plans/2026-07-10-callgraph-cross-file-resolution-plan.md
status: done
title: 'ROLLBACK down-migration: retract resolved edges + drop/ignore resolution column'
updated_at: 2026-07-11T17:54:00.913155-07:00
---

ROLLBACK DOWN-MIGRATION (added by the re-harvest per Copilot #239 §7 rollback). Reverting rec1 is NOT purely additive once `calls_edge.resolution` (082.003-T) lands.

Files (<=3 src + 1 test): src/db/cozo_backend/schema.rs (down-migration path that drops/ignores the `resolution` attribute), src/db/cozo_queries.rs (retract_all_calls_resolved_singleton_edges + a single NAMED public entry point `rollback_calls_resolution` that orchestrates retract-then-drop in order, callable by the operator trigger). Test: tests/integration/calls_resolution_downmigration_test.rs.

Contract: the reindex/delete paths do NOT clear calls_edge rows (code_graph.rs:294-300, :1181-1205), so a plain reindex after reverting the code leaves prior `calls_resolved_singleton` edges behind, and the reverted old-schema writer (`{ from, to => created_at }`) may be incompatible with the migrated relation. This task defines and tests an explicit pre-revert cleanup / down-migration in a STRICT ORDER so every provenance query runs while its column still exists: (1) WHILE the `resolution` attribute is still present, retract ALL `calls_resolved_singleton` edges (direct edges preserved) and confirm zero remain via count_calls_edges_by_resolution; (2) ONLY THEN drop/ignore the `resolution` attribute so calls_edge reverts to `(from, to) => created_at` and the old-schema writer round-trips. This task delivers the reusable migration LOGIC exposed as the named `rollback_calls_resolution` entry point; the operator-invocable TRIGGER (maintenance CLI subcommand) is the separate task 082.013-T, which depends on this one — split off per Copilot #240 cycle-3 finding 2 so this task stays within its file cap (a CLI subcommand adds cli/commands/*.rs + mod.rs + engram.rs). Idempotent; safe to run before or during the reverting reindex.

Width: schema / migration / rollback. Scenarios (3, ordered so no count query runs after the column is dropped): (1) with the `resolution` attribute still present, `rollback_calls_resolution` retracts ALL `calls_resolved_singleton` edges (direct edges preserved) and count_calls_edges_by_resolution then reports zero singleton edges — asserted BEFORE the column is dropped; (2) after retraction, dropping/ignoring `resolution` reverts calls_edge to `(from, to) => created_at` and the reverted old-schema writer round-trips; (3) idempotent — rerunning `rollback_calls_resolution` (retract + drop) is a no-op with no error.

Depends on 082.003-T (storage), 082.008-T (post-pass edges exist to retract). Operator trigger: 082.013-T.