---
artifact_type: task
commit: 33a0a41e345cef8965b707346728d44fa5492daf
created_at: 2026-09-03T00:16:59.9715666Z
id: 142.002-T
parent_id: 142-F
priority: critical
status: done
title: Add strict DaemonMode parsing with managed default
updated_at: 2026-09-03T20:00:50.744239Z
---

Plan unit F02 - strict mode contract. Add strict `DaemonMode` parsing to `src/models/config.rs` so the daemon has exactly one shared mode resolver. The managed default applies only when the setting is absent; any present-but-invalid value is a hard parse error rather than a silent fallback to managed.

F02 is a graph root with no in-edges. It is the single source of truth every downstream mode decision consumes, so no other unit may re-derive or re-parse the mode.

Execution posture: test-first.

<!-- BEGIN:acceptance-criteria -->
- `DaemonMode` parses strictly: a present-but-unrecognized value is a typed error, never a fallback.
- The managed default is applied only when the setting is absent.
- Exactly one shared mode resolver exists; no duplicate parsing logic elsewhere.
- `tests/unit/plugin_config_test.rs` covers absent, valid managed, valid read-server, and invalid values.
- Round-trip parse/serialize is stable for both valid modes.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Size: S | Complexity: low | size_source: agent | size_ruleset_version: engram-stage-2h-rule-v1. Recorded as prose because this workspace task type defines no structured size/complexity field (backlogit rejects --size/--complexity for artifact type "task"). Size is effort/volume; complexity is difficulty/uncertainty; the two axes are independent.

Owned files: `src/models/config.rs`, `tests/unit/plugin_config_test.rs`.
`tests/unit/plugin_config_test.rs` already exists and is already registered as `unit_plugin_config`, so F02 is deliberately excluded from the F00 registration table and carries no `F00 -> F02` edge.
Downstream consumers of this resolver: F03 (immutable mode in state), F11 (direct-sync boundary), F20 (request entry).
<!-- END:implementation-notes -->

<!-- BEGIN:verification -->
1. `cargo test --test unit_plugin_config` is GREEN.
2. `cargo dev-test`
3. `cargo ci`
<!-- END:verification -->