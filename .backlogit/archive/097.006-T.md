---
artifact_type: task
created_at: 2026-07-30T16:52:00-07:00
custom_fields:
    harness_status: pending
dependencies:
    - 097.001-T
    - 097.002-T
    - 097.003-T
    - 097.004-T
id: 097.006-T
labels:
    - spark
    - data-lineage
    - hardening
    - lineage-model
    - freshness
    - rollout
    - X1
parent_id: 097-F
priority: medium
references:
    - docs/exec-plans/2026-07-30-spark-lineage-v1-hardening-plan.md
    - src/models/lineage.rs
status: done
title: 'X1: Bump CURRENT_EXTRACTOR_VERSION 1.0.0 -> 1.1.0 + validate re-extraction rollout (freshness propagation)'
updated_at: 2026-07-31T01:04:56.1236821Z
---

X1 (rollout) — bump the lineage extractor version so the tightened V2/V5/W2/W1 behavior reaches already-indexed notebooks, and validate the re-extraction path. Domain: lineage-model / freshness. Plan Unit X1. LANDS LAST.

PROBLEM. The U4b freshness token = `{CURRENT_EXTRACTOR_VERSION}:{config_fingerprint}` (src/models/lineage.rs: const at L38 = "1.0.0"; `lineage_freshness_token` L278) gates re-extraction of unchanged notebooks by content-hash + token. V2/V5/W2/W1 change extractor OUTPUT; without a version bump, already-indexed notebooks retain the old (less-precise / potentially false) lineage until their content changes (C4 stale-lineage trap). The extractor version must change exactly ONCE to reflect the complete new behavior.

CHANGE. Bump `CURRENT_EXTRACTOR_VERSION` from "1.0.0" to "1.1.0" (a single edit — precision-tightening minor bump). Add / assert a freshness test proving the token change forces re-extraction of a previously-stamped notebook (the C4 path), so the tightened extraction is applied on the next index.

FILE TARGETS. src/models/lineage.rs (the const) + a freshness re-extraction assertion (unit in lineage.rs::tests and/or reuse the tests/integration/notebook_lineage_freshness_test.rs patterns).

ACCEPTANCE CRITERIA (test-first):
- AC1 (single bump): `CURRENT_EXTRACTOR_VERSION == "1.1.0"`; `lineage_freshness_token` reflects the new version; exactly one edit to the const (no double bump).
- AC2 (re-extraction forced): a notebook stamped with the old freshness token is re-extracted after the version change even when its content hash is unchanged (C4 skip-predicate invalidated).
- AC3 (precision floor holds): after re-extraction, the lineage precision/recall metric (lineage_precision_recall_test.rs) holds the 0-false-edge precision floor with the V2/V5/W2/W1 fixes applied.

REAL DEPENDENCY. Blocked by V2 (097.001-T), V5 (097.002-T), W2 (097.003-T), W1 (097.004-T) — the single version bump must reflect all behavior changes and land after them to avoid a same-line double edit. (V4/097.005-T is a test and is NOT a dependency.)

ROLLBACK. Revert the const to "1.0.0" (restores the old token -> halts forced re-extraction) if the precision floor regresses or a re-extraction sweep fails. Owner: Ship (closure). Validation window: first full re-index after merge.

Harness-ready: RED freshness/re-extraction harness then GREEN via the bump. No schema/API change (internal freshness string only).
