---
archived_from: .backlogit/queue/029.003.001-T.md
artifact_type: task
commit: 091a164a405e42d55bc0345f35ce09f39e7d5500
created_at: 2026-04-21T11:30:00-07:00
id: 029.003.001-T
labels:
    - reliability
    - daemon
    - shipment-b1
    - harness-ready
parent_id: 029.003-C
priority: high
status: archived
title: Scaffold failing workspace-id drift harness (red phase)
updated_at: 2026-04-22T07:47:44.1291478-07:00
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
**Red-phase harness task.** Establishes failing test surface for Unit 3 BEFORE implementation. Implementation tasks `029.003.002-T` and `029.003.003-T` make these pass.

Scaffold:
1. Unit tests in `src/db/workspace.rs`: `workspace_id_stable_across_canonical_forms`, `first_bind_creates_workspace_id`, `ambiguous_bind_returns_typed_error`.
2. `tests/integration/workspace_id_drift_test.rs` — `two_canonical_paths_bind_single_daemon` end-to-end.
3. Stub `load_or_create_workspace_id(path) -> Result<Uuid, EngramError>` returning `unimplemented!()`; stub `WorkspaceError::AmbiguousBind { expected_id, found_id, path }` variant in `src/errors/mod.rs`.

**Files:** `src/db/workspace.rs`, `tests/integration/workspace_id_drift_test.rs`, `src/errors/mod.rs`.

**Execution posture:** test-first (red phase).

**Granularity:** 3 files, 1 stub function, 4 failing test scenarios.

**Dependency:** `029.003.002-T` and `029.003.003-T` depend on this task. Test helper that creates temp workspaces MUST hold `TempDir` for full test body. Detection of `ambiguous_bind` is **shim-side only** — no daemon protocol change in this Unit.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Note

- Harness command: `cargo test workspace_id_stable_across_canonical_forms --lib && cargo test first_bind_creates_workspace_id --lib && cargo test ambiguous_bind_returns_typed_error --lib && cargo test two_canonical_paths_bind_single_daemon`

## Acceptance Criteria

<!-- AC:BEGIN -->
- [x] #1 All four test scenarios compile and FAIL with the expected stub-driven failure mode
- [x] #2 `load_or_create_workspace_id` exists as stub in `src/db/workspace.rs` returning `Err(EngramError::Workspace(WorkspaceError::AmbiguousBind { .. }))` or `unimplemented!()`
- [x] #3 `WorkspaceError::AmbiguousBind { expected_id: Uuid, found_id: Uuid, path: PathBuf }` variant exists in `src/errors/mod.rs`
- [x] #4 `cargo build --tests` succeeds; no production wiring yet
<!-- AC:END -->
