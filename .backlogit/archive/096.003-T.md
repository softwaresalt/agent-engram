---
artifact_type: task
created_at: 2026-07-23T18:45:36.7548937-07:00
dependencies:
    - 096.001-T
id: 096.003-T
labels:
    - python
    - canonical-identity
    - code
parent_id: 096-F
priority: medium
references:
    - src/services/code_graph.rs
    - src/db/cozo_queries.rs
status: done
title: 'T3: Python canonical_path populator (module.name; "" for package roots)'
updated_at: 2026-07-26T08:35:10.4399777Z
---



<!-- BEGIN:acceptance-criteria -->
- A .py module-level def gets canonical_path == "mod.f".
- A def in __init__.py gets canonical_path == "" (package-root fail-closed).
- Q3/M5: a def under a src/ source-root or an implicit PEP 420 namespace package gets canonical_path == "" (T1 rejects any chain with an ancestor dir lacking __init__.py).
- Two same-file defs of f both persist the identical canonical path (sets up the duplicate the resolver later fails closed on; ties to FF7DE872 non-subsumption).
- C6-1 package-topology invalidation: ADDING p/__init__.py recomputes descendant defs' canonical paths from ""->"p.mod.f", and REMOVING it invalidates them back to "" -- BOTH transitions must occur even when the descendant file content is unchanged (past the sync content-hash skip at code_graph.rs:1252-1263).
- No cozo_queries.rs schema change. cargo test --test integration_code_graph green; clippy --all-targets clean; fmt clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Populate function_meta.canonical_path for Python module-level defs. Add a language dispatch to canonical_path_for_function (code_graph.rs:145-169) so Python defs get canonical_path = "<module>.<name>" via T1 ("" when python_module_path_for_file returns None). Build a lightweight Python per-file context at the two populator call sites (code_graph.rs:721 and :1446), parallel to rust_ctx; that context carries the regular-package predicate T1 needs -- a set of dirs containing __init__.py, derived ONCE from the already-indexed file set (Q3: NO CodeGraphConfig change, NO source-root config). Reuse upsert_function_with_canonical UNCHANGED (no DB/schema change). C6-1 (package-topology invalidation): the regular-package predicate depends on OTHER files (__init__.py presence in every ancestor dir), but sync CONTENT-HASH-SKIPS unchanged descendants (code_graph.rs:1252-1263), so adding or removing a p/__init__.py would leave STALE canonical_path values on descendant defs (false/missing edges). T3 must treat an add/remove of any __init__.py as a PACKAGE-TOPOLOGY CHANGE that reindexes/invalidates the affected descendants (recompute their canonical paths past the content-hash skip), for BOTH the add transition (namespace/src -> regular package) and the delete transition (regular package -> namespace). TDD RED->GREEN. Domain: code.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on T1 (096.001-T). Dispatch on Language::Python; Rust path unchanged. Build the regular-package predicate once per index pass from the indexed file set (which dirs contain __init__.py) and thread it into the per-file context (Q3: no PackageLayout, no source roots, no CodeGraphConfig change). C6-1: because the predicate is cross-file, an add/remove of __init__.py must force re-computation of descendant canonical paths even though those descendants' own content hashes are unchanged (the sync skip at 1252-1263 would otherwise keep stale values); pin BOTH add- and delete-transition tests. Files (<=2): src/services/code_graph.rs, tests/integration/code_graph_test.rs.
<!-- END:implementation-notes -->