---
artifact_type: task
created_at: 2026-07-22T18:20:00-07:00
dependencies:
    - 095.002-T
id: 095.012-T
parent_id: 095-F
priority: medium
status: done
title: 'U1a'': Lineage db write helpers + fail-closed scope-delete cascade'
updated_at: 2026-07-23T22:00:57.1865587Z
---

Add the lineage subgraph DB WRITE HELPERS + the fail-closed scope-delete / GC cascade, split out of U1a (095.002-T) per AR-04's granularity rule; the version-state read/write helpers were FURTHER split out to U1a" (095.014-T, H3). DEPENDS ON U1a for the four relations + the shared value types (no new relations or value types here). In src/db/cozo_queries.rs add batch-upsert helpers upsert_dataset_nodes / upsert_lineage_edges / upsert_lineage_edge_evidence (mirror upsert_powerbi_nodes/edges at cozo_queries.rs:5471-5520, :put). Add delete_lineage_by_scope(notebook_path) as a 4-STEP FAIL-CLOSED CASCADE: (1) delete ALL this notebook's evidence rows (every cell, matched by notebook_path); (2) GC any lineage_edge with zero remaining evidence; (3) GC any dataset_node no longer referenced by any surviving edge; (4) delete the notebook's lineage_index_state row for notebook_path (AR-22) so a whole-file deletion also clears freshness state (a direct :rm on lineage_index_state for notebook_path). WRITE ORDERING (AR-28): the node/edge/evidence upserts are three non-atomic upserts + this delete cascade (matches the existing PowerBI / content-record pattern; the indexer is single-threaded per notebook, so there are no intra-notebook races); intended ordering is evidence-and-nodes-before-edge (D1 edge-driven-node invariant) under a per-notebook scope-replace (delete-then-insert) critical section, with partial-failure recovery = full re-extraction of the notebook on the next run (the scope-delete makes re-extraction idempotent). This recovery guarantee holds BECAUSE the U4 write-path (095.015-T) issues the lineage_index_state stamp as its FINAL write: a pre-stamp failure leaves NO stamp row, so U4b's hash+version skip predicate cannot skip — and thus cannot freeze — the partial graph (I1). Tests (U1a'): node/edge/evidence round-trip via the writers + CANONICAL-IDENTITY (same dataset from two notebooks keeps ONE node with both evidence rows, no clobber); SHARED-EDGE + SAME-EDGE/TWO-CELLS deletion (same edge evidenced by two notebooks OR two cells with distinct chunk_index — scope-delete one leaves the edge/other evidence, both removes it — finding E1); WHOLE-FILE GC + INDEX-STATE deletion (scope-delete GCs orphaned edges/nodes while still-evidenced datasets survive AND removes the lineage_index_state row for notebook_path — AR-22). Granularity: 4 functions, 3 test scenarios — within the 2-hour gate. Files: src/db/cozo_queries.rs. <=1 file, db domain (width-isolated). Blocked by U1a (095.002-T). Version-state helpers: U1a" (095.014-T). U2/U2b/U3/U4 persist through THESE writers. Plan Unit 1a'.
