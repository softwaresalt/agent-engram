---
artifact_type: task
commit: 4c28054
created_at: 2026-07-13T10:51:27.5383809-07:00
dependencies:
    - 085.003-T
    - 085.005-T
id: 085.006-T
labels:
    - powerbi
    - dax
    - lint
    - mcp-tool
    - agent-native-parity
parent_id: 085-F
priority: high
references:
    - docs/exec-plans/2026-07-13-dax-intelligence-plan.md
    - src/tools/mod.rs
    - src/shim/tools_catalog.rs
status: done
title: P6 — DAX lint Tier 2 + lint_dax MCP tool (dispatch/catalog/manifest/contract lockstep)
updated_at: 2026-07-13T21:39:56.488924-07:00
---



<!-- BEGIN:acceptance-criteria -->
- Tier-2 rules produce correct findings: broken column/measure refs (not in model), unqualified column that resolves to a real column, qualified reference to a name that is actually a measure, and measure->measure cycles.
- Tier-2 broken-ref rules (`dax.broken_column_ref`, `dax.broken_measure_ref`) obtain their input by REPARSING the indexed model expressions (measure DAX + calculated-column DAX via P2) against a MODEL-SCOPE resolved schema — aggregated across ALL sibling `.tmdl` files of one model and keyed by `canonical_tmdl_model_path` (src/services/powerbi_tmdl.rs:85), reusing P3's aggregation, since TMDL ingestion parses each file independently (src/services/powerbi_indexer.rs:924-984) and a file-local schema would report valid cross-table refs as broken — at lint time; NOT from `powerbi_node`/`powerbi_edge`, which persist only resolved endpoints + node identity/path/hash and retain no DAX text or unresolved refs (src/db/cozo_backend/schema.rs:770-796). No CozoDB schema change; a fixture with a broken column ref, a broken measure ref, AND a VALID cross-table reference whose target table lives in a sibling `.tmdl` (asserted NOT broken) proves both rules fire without false positives.
- Model-scope INVALIDATION counterpart (PR #245 Copilot cycle-4, deferred; routed by operator 2026-07-13): the Tier-2 broken-ref reparse builds its MODEL-SCOPE resolved schema from the CURRENT indexed state of ALL sibling `.tmdl` files in the `canonical_tmdl_model_path` scope (reusing P3's aggregation), so after a sibling file changes/deletes a column, re-linting catches now-broken/stale refs in an UNCHANGED sibling — e.g. a `Sales.tmdl` measure referencing a column that `Date.tmdl` renamed/removed fires `dax.broken_column_ref` — instead of reporting them as still-valid. Fixture coverage: a sibling column rename/delete makes a peer file's measure ref fire `dax.broken_column_ref`, and a sibling column add clears a previously-broken ref, proving the reparse uses the up-to-date full-model schema regardless of which files the incremental indexer re-processed. (Persisted-edge staleness under incremental sync is handled separately by P3b `085.008-T`; P6 is robust-by-reparse and does not depend on it.)
- `dax.measure_cycle` detection is cycle-SAFE (visited-set / bounded depth) — terminates on self- and mutually-referential measure fixtures (no infinite loop).
- `lint_dax` MCP tool returns `{ conformant, findings[] }` over the bound workspace's indexed model(s), accepting an **optional `model_path`** selector — a TMDL model path canonicalized via `canonical_tmdl_model_path` (src/services/powerbi_tmdl.rs:85) to one model scope, NOT the shared `source_path`/`ContentSource.path` registry directory (src/models/registry.rs:63) which every Power BI file under a content source shares — (omitted = all indexed models; supplied = filter to exactly that one model; a path that is not an indexed model returns an error result). Multi-model coverage: two semantic models under one registered content source resolve to distinct model scopes. This lets P7's `engram lint-dax <model.tmdl>` target a single model.
- `TOOL_COUNT` bumped to 21; catalog + manifest + dispatch + should_record_metrics updated in lockstep; `tool_count_matches_dispatch` and the lint_dax schema/error-code contract test pass.
- clippy pedantic clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
DAX lint Tier 2 (semantic) + new `lint_dax` MCP tool. Add Tier-2 schema-aware rules over the INDEXED model: dax.broken_column_ref (Error), dax.broken_measure_ref (Error), dax.unqualified_column (Warning), dax.qualified_measure (Warning), dax.measure_cycle (Error). Tier-2 inputs come from REPARSING the indexed model expressions (measure + calculated-column DAX via P2) against a MODEL-SCOPE resolved schema (aggregated across all sibling `.tmdl` files of one model, keyed by `canonical_tmdl_model_path` src/services/powerbi_tmdl.rs:85, reusing P3's aggregation — TMDL ingestion parses each file independently at src/services/powerbi_indexer.rs:924-984) at lint time — the persisted graph (powerbi_node/powerbi_edge) retains no DAX text or unresolved refs (src/db/cozo_backend/schema.rs:770-796), so broken-ref detection re-derives them rather than reading dropped P3 edges; this needs no CozoDB schema change. Add a new `lint_dax` MCP tool returning `{ conformant, findings[] }` for the bound workspace (Tier 1 + Tier 2; Tier 2 needs the resolved schema, hence daemon-side per decision D1), with an optional `model_path` selector canonicalized via `canonical_tmdl_model_path` (NOT the shared `source_path` content-source directory; default = every indexed model; supplied = exactly one model; unindexed path = error result) so P7's CLI can target a single model. Register in dispatch + `should_record_metrics` (src/tools/mod.rs), bump `TOOL_COUNT` 20 -> 21 and add the catalog entry (src/shim/tools_catalog.rs) + manifest, and add an agent-native parity contract test. Width: lint service + MCP tool. Largest unit — split at documented seams if >2h.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Subtask split points (promote to siblings if >2h): P6.a Tier-2 semantic rules over the resolved schema (incl. cycle-safe measure->measure detection over P3 edges); P6.b lint_dax tool handler + dispatch + should_record_metrics + TOOL_COUNT/catalog/manifest bump; P6.c contract test (lint_dax schema/error-code + tool_count_matches_*). Test-first: the catalog/contract test is the first-authored FAILING harness (R4). CLI parity (`engram lint-dax`) is delivered by sibling task **P7 (`085.007-T`, depends on this task)** per the 2026-07-13 D4 reversal — **this task (P6) stays MCP-only** (tool + agent-native parity contract test); it does NOT add the CLI subcommand. The broader full-surface MCP↔CLI audit remains with stash `30F372C8`. Grounding: should_record_metrics (src/tools/mod.rs:36), impact_analysis dispatch (:324); TOOL_COUNT=20 (src/shim/tools_catalog.rs:21) asserted by tool_count_matches_dispatch (:381). Depends on P3 (resolved edges/schema) and P5 (VerifyFinding.severity + Tier-1 infra).
<!-- END:implementation-notes -->