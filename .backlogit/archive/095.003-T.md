---
artifact_type: task
created_at: 2026-07-22T14:59:49.0699874-07:00
dependencies:
    - 095.012-T
id: 095.003-T
parent_id: 095-F
priority: medium
status: done
title: 'U2a: PySpark method-chain extraction + endpoint/authority resolution (resolved endpoints)'
updated_at: 2026-07-23T22:51:17.7504486Z
---

Add the PURE extraction + resolution half of the Python lineage path as a new spark_lineage submodule reachable from src/services/parsing/python.rs (parse_python_source at python.rs:19; reuse is_method at python.rs:227,288-289), split out of U2 per H4's 2-hour granularity rule. This U2a half OWNS: (1) the Spark method-chain WHITELIST recognizer — spark.read.<fmt>(...), spark.read.load(...), spark.table(...), df.write.saveAsTable(...), df.write.save(...), df.write.mode(...).saveAsTable/save(...) (spark.sql(...) is DEFERRED OUT of v1 — REMOVED from the whitelist, documented deferred exclusion, equivalent CTAS/INSERT captured via %%sql cells / U3; createOrReplaceTempView(...) captured as content only, NO v1 edge — temp-view unrepresentable, Fork A); (2) ENDPOINT RESOLUTION reading string-literal args only — resolve a LineageEndpoint ONLY when the ref satisfies the Q3 predicate AND is bound to a trusted authority: table endpoint requires 3-part catalog.schema.table + trusted metastore authority; path endpoint requires absolute URI + storage authority; the WRITE RECEIVER = the base simple-name at the CHAIN ROOT so df.write.mode("overwrite").saveAsTable(...) resolves to df (Option<name> None when the chain root is not a simple name — AR-13); (3) AUTHORITY RESOLUTION against the U1b LineageAuthorityContext, embedding the resolved metastore/storage authority in the canonical dataset key so two metastores sharing catalog.schema.table do NOT collide (AR-01). Produce CALL-LEVEL resolved data: each recognized Spark read/write CALL resolved to a LineageEndpoint (the value type is DEFINED by U1a; U2a POPULATES LineageEndpoint) PLUS its receiver variable + source order + enclosing scope. U2a does NOT link reads to writes and does NOT build LineageEdgeCandidate — candidates are produced DOWNSTREAM by U2b's single-cell dataflow join. PURE — no persistence (NOT via ExtractedEdge / code_graph; U2c streams events over this resolved data, U2b joins events into candidates, U4 write-path persists). FAIL CLOSED (013-D, 0 false edges) — zero resolved endpoints for: non-literal / f-string / concatenated args, relative-path literals, 1-/2-part table names, config/widget-derived names, AND a 3-part literal with NO trusted authority (finding 10). The SEAM this task hands to U2c (095.013-T) = the call-level resolved LineageEndpoint + receiver/order/scope data (NOT LineageEdgeCandidate). Do NOT emit the assignment/scope event stream here (that is U2c/095.013-T); do NOT link read->write or persist (U2b/U4). Tests: each resolvable read/write form WITH an injected trusted authority resolves to the expected LineageEndpoint carrying role + chain-root receiver base-name (AR-13); the SAME 3-part literal with NO authority resolves to ZERO endpoints (fail-closed, AR-01); each non-resolvable arg form (f-string, concatenation, relative path, 1-/2-part name, config/widget name, spark.sql(any) deferred) resolves to ZERO endpoints (strong assertion == 0). Granularity: 4 functions, 3 test scenarios — within the 2-hour gate. Domain: python-parser (width-isolated). Blocked by U1a' (095.012-T). Seam consumer: U2c (095.013-T). Plan Unit 2a.
