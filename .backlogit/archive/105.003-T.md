---
artifact_type: task
created_at: 2026-07-30T03:18:22.1613326Z
custom_fields:
    source_stash_id: 7A317008
    source_stash_kind: task
    source_stash_path: stash.jsonl
    source_stash_priority: medium
    source_stash_text: 'Forced-index reconciliation ordering: evict indexed-minus-discovered file nodes BEFORE the cross-file singleton post-pass (reresolve_calls_edges_with_canonical_context) instead of in the certify block after it. Ref Copilot PR #299 thread PRRT_kwDORJEduc6Usxxl (code_graph.rs:1936). When an excluded-but-still-indexed file duplicates a callee name that has one live def elsewhere, the post-pass runs against the stale pre-eviction set, sees ambiguity, and withholds the cross-file singleton; eviction does not re-run resolution, so a recoverable direct edge stays missing. NOT a regression (pre-103-F the excluded file was never evicted so the post-pass saw the identical ambiguity) and fail-closed (missing edge, never a false edge). Fix: move the U2 eviction loop ahead of the post-pass (gated force||!any_hash_skipped) + add a duplicate-callee-then-excluded recall-RECOVERY test. Touches 082-F/094-F/096-F/101-F post-pass invariants; warrants its own unit + test.'
id: 105.003-T
labels:
    - code-graph
    - indexing
    - forced-index
    - tdd
links:
    - target_id: 015-D
      link_type: related_to
parent_id: 105-F
priority: medium
status: done
title: 'R3: Forced-index reconciliation ordering (evict indexed-minus-discovered BEFORE the singleton post-pass)'
updated_at: 2026-07-30T22:54:36.7341244Z
---

PR #299 Copilot thread PRRT_kwDORJEduc6Usxxl, src/services/code_graph.rs. The cross-file singleton post-pass reresolve_calls_edges_with_canonical_context is invoked at :1867; the indexed-minus-discovered file-node eviction loop (103-F) sits inside the certify block (force || !any_hash_skipped, :1914) at :1926-1948 -- AFTER the post-pass. When an excluded-but-still-indexed file duplicates a callee name that has exactly one live def elsewhere, the post-pass runs against the STALE pre-eviction set, sees ambiguity, and withholds the cross-file singleton; eviction does not re-run resolution, so a recoverable direct edge stays missing. NOT a regression (pre-103-F the excluded file was never evicted so the post-pass saw the identical ambiguity) and fail-closed (missing edge, never a false edge). Fix: move the force||!any_hash_skipped-gated eviction loop AHEAD of the post-pass invocation at :1867 + add a duplicate-callee-then-excluded recall-RECOVERY test. Preserve dangling-edge sweep + generation-marker certify order (082/094/096/101/103-F invariants).

<!-- BEGIN:acceptance-criteria -->
- AC1 (RED FIRST, MUST FAIL pre-fix): a duplicate-callee-then-excluded recall-RECOVERY test -- index a workspace where an excluded-but-still-indexed file duplicates a callee name that has EXACTLY ONE live def elsewhere; assert the cross-file singleton IS resolved after a forced index. Fails today (post-pass at :1867 sees pre-eviction ambiguity and withholds the singleton; eviction does not re-run resolution).
- AC2 (GREEN): move the force||!any_hash_skipped-gated indexed-minus-discovered eviction loop (currently :1926-1948) AHEAD of the reresolve_calls_edges_with_canonical_context invocation at :1867, so the post-pass resolves against the post-eviction set.
- AC3: fail-closed preserved -- NEVER emits a false cross-file edge; the dangling-edge sweep (retract_dangling_calls_edges) + code-graph generation-marker certify still run AFTER the post-pass (103-F order preserved).
- AC4: H5 negative branch -- a discovered/hash-skipped file stays in discovered_rel and is NOT evicted; the existing cross-file recall suite stays green (recovery adds recall, removes none). Preserve 082/094/096/101/103-F post-pass invariants.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Fix sites: src/services/code_graph.rs:1867 (post-pass invocation) vs :1914 certify block / :1926-1948 eviction loop. Independent width (code-graph reconciliation) -- NO execution-blocking dependency on R1/R2. Width: forced-index code-graph reconciliation only (~2h). RED->GREEN, TDD non-negotiable.
<!-- END:implementation-notes -->