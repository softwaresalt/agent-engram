---
archived_from: .backlogit/queue/141-S.md
archived_status: done
artifact_type: shipment
created_at: 2026-09-03T01:03:10.4238815Z
custom_fields:
    items:
        - 142.047-T
        - 142.048-T
        - 142.049-T
        - 142.050-T
        - 142.051-T
        - 142.052-T
        - 142.053-T
dependencies:
    - 134-S
    - 138-S
id: 141-S
priority: high
title: Error transport, response provenance, lifecycle policy and generation observability
updated_at: 2026-09-23T16:15:00Z
status: archived
---



<!-- BEGIN:description -->
AUDIT RATIONALE (manual safe-close): 141-S closes via targeted manual
safe-close performed under Ship's own Role-Boundary-permitted,
non-discretionary backlog mandate ("close shipments, archive completed
items") as a mandatory continuation of the merge of PR #407. The operator
gave explicit merge approval verbatim ("PR 407: Merge approved") at
2026-09-23T09:08:26.508-07:00, following an earlier round of explicit
authorization for shipment claim ("Stage explicitly returned
CLAIM_141_NEXT") and, separately, for exactly one additional CI rerun of
the `start-launcher-windows` check. All last-mile gates were re-verified
immediately before merge and again confirmed unchanged/current at merge
time:

- P-018 (Copilot review): `autoharness gate copilot-review 407` returned
  `SATISFIED` at HEAD `0bcabd0a001dffe9df293e5408777c76796d75d3`, 0
  unresolved Copilot-authored threads (4 threads total across the review
  cycle: 2 resolved by reference to pre-existing deferred stash entries
  `9B7EC1E4` and `6C5DF765`, 2 fixed in-scope via commits `743064fa` and
  `f6c49b49`/`d306e989`, independently re-reviewed `READY`).
- P-009 (merge-commit-only): confirmed via repository settings
  (`allow_squash_merge: false`, `allow_rebase_merge: false`,
  `allow_merge_commit: true`) — merge executed via `gh pr merge 407
  --merge`.
- P-016 (topology): `autoharness gate pipeline-topology --phase lifecycle
  --shipment 141-S` returned exit 0 / `topology gate pass` immediately
  before merge (single active shipment `141-S`, correct branch ownership,
  single implementation worktree, no spike/research worktree conflicts).
- CI: `build` pass (6m18s); `start-launcher-windows` pass (2m11s) — this
  check had failed on 2 separate HEADs on the pre-existing, out-of-scope
  flaky timing assertion `launcher_fails_open_to_copilot_within_one_prewarm_budget`
  (`tests/contract/start_launcher_test.rs`, untouched by this shipment's
  7 tasks) and repeatedly hung across 8 rerun attempts consistent with
  transient GitHub Actions Windows-runner infrastructure degradation; the
  operator explicitly authorized exactly one additional rerun, which
  passed cleanly (the job had, in fact, already completed successfully —
  the extended `in_progress` observation during monitoring was GitHub
  Actions API step-status display lag, not a real hang).
- §1.9 local review readiness: `READY_WITH_FOLLOWUPS` at reviewed HEAD
  `0bcabd0a`, `P0=0, P1=0`, full local build evidence recorded
  (`cargo check --all-targets`, `cargo clippy --all-targets -- -D
  warnings -D clippy::pedantic`, `cargo fmt --all -- --check`, `cargo
  dev-test` full suite — all successful except one known pre-existing,
  unrelated failure), follow-ups explicitly enumerated (see below).

PR #407 merged at reviewed/gated HEAD
`0bcabd0a001dffe9df293e5408777c76796d75d3`, merge commit
`f115835e260c089bf094d82fa565371077f5bac8`, merged at
2026-09-23T16:09:51Z, merge method: merge commit via `gh pr merge 407
--merge`. Merge commit reachability independently confirmed via `git
merge-base --is-ancestor f115835e260c089bf094d82fa565371077f5bac8
origin/main` (exit code 0) after `git fetch origin main`.

PR #407 implemented all seven manifest items (142.047-T through
142.053-T), each independently verified `status: done` and already
individually archived under `.backlogit/archive/` prior to this
shipment-record closure step (confirmed via pre-mode reconciliation, all
seven classified `pre-archived`, report at
`.backlogit/reconcile/141-S-pre-2026-09-23T161200Z.md`, recommendation
`PROCEED`; zero orphans — the only other queue file referencing `141-S`,
`142-S.md`, does so solely via a `dependencies:` entry, not a
`shipment_id` field, and is unrelated to manifest membership).

Manual safe-close (not the `backlogit shipment ship` cascade operation)
was required for two independent reasons already established against the
identical covering feature on predecessor shipments `133-S` through
`140-S`:

1. **P-015 protected-scope risk**: `142-F` is a shared-parent covering
   feature spanning a 59-unit roster (F00, F01-F55, F04a, F12a, F16a),
   with substantial remaining scope (52 of 59 units, including the
   `142-S` shipment currently `queued` and blocked by this very shipment)
   still outstanding beyond this shipment. `142-F` is not a manifest
   member of `141-S` and does not meet the P-015 fully-covered-root
   exception (the manifest is 7 task items out of the 59-unit roster, and
   the manifest contains no feature member at all, so the exception's
   "nothing beyond the qualifying root feature(s) and their children"
   precondition also fails). The cascade `shipment ship` path — which can
   force-release the covering feature and requeue/detach unshipped
   descendant siblings — is therefore prohibited for this shipment
   regardless of tool behavior.
2. **Established tool rejection, independently reconfirmed**: the generic
   fallback (`backlogit move 141-S --status shipped`) was attempted for
   this closure and confirmed rejected outright by the CLI (backlogit
   version 1.10.1-0.20260823032255-b07729386a31+dirty): `Error: shipment
   must be shipped via ShipShipment, not a direct status update` (exit
   code 9), identical to the rejection already established against
   `137-S`, `138-S`, and `140-S`. No partial state was produced by this
   rejected attempt: `141-S` status was unchanged (`active`) and no
   `.backlogit/archive/141-S.md` file existed prior to this manual
   authoring step. Given this established precedent and the P-015
   prohibition above, `backlogit shipment ship 141-S` (the only
   registered cascade transition path) was not attempted, per the
   documented non-termination symptom in
   `docs/compound/workflow-issues/backlogit-shipment-ship-non-terminating-large-covering-feature-2026-09-06.md`
   and its addenda for `137-S`/`138-S`/`140-S`.

Only the seven items explicitly enumerated in this shipment's manifest
were mutated (individually archived during routine task-completion
bookkeeping within PR #407 itself, ahead of this closure step); `142-F`
remains `active` and byte-for-byte untouched (SHA-256
`59263E8FFB779485E135A7AA41D9DAAC89B4A996B767D128D76A1AD2E70404C3`,
802 bytes — identical to the hash independently recorded against the same
file at the `138-S` and `140-S` closures, confirming no shipment closure
since then, including this one, has touched it); no other `142.*` item's
`parent_id`, status, or shipment membership was altered by this closure.
Dependency predecessors `134-S`/`138-S` and transitive predecessor
`140-S` were confirmed already archived/terminal before claim and were
not re-read or mutated during this closure. Successor shipment `142-S`
(currently `queued`, blocked by `141-S` via its own dependency edge) was
not claimed, executed, or mutated at any point in this session, per the
operator's explicit instruction. Stash entry `0011C2DC` and its parked
docs branch (`docs/stage-0011c2dc-content-record-schema`, local commit
`c3a4c23e`, not on `origin/main`) were not touched, merged, or altered.

`archived_status: done` (rather than `shipped`) is deliberate, not an
oversight: it factually records that this record did not transit the
`active -> shipped` `ShipShipment` cascade (the CLI hard-rejected the
direct-to-`shipped` path, see above), and it matches the identical,
already-established manual-safe-close precedent for the `archived_status:
done` / manual-archive-file format on `133-S` through `140-S`. This is
the eighth consecutive shipment under covering feature `142-F` to close
via this pattern.

**Successor eligibility clarification** (added in response to PR #408
Copilot review thread `PRRT_kwDORJEduc6lPjAe`): `archived_status: done`
does not literally match the string `"shipped"` quoted in
`backlogit.instructions.md`'s Shipment Sequencing Protocol prose ("every
`blocks`-type predecessor is `status == shipped`"). This is a real
textual inconsistency between that prose and the established
manual-safe-close convention, but it does **not** leave successor
shipment `142-S` functionally ineligible: `142-S`'s own predecessors
(`134-S`, `138-S`, `140-S`, all recorded identically as `status:
archived` / `archived_status: done`) were independently verified
satisfied by `autoharness gate pipeline-topology --phase pre_claim` when
this very shipment (`141-S`) was claimed, which treats `status: archived`
(regardless of the `archived_status` sub-value) as terminal/satisfied for
`blocks`-dependency purposes — proven functionally, not just asserted.
The documentation/prose-vs-convention mismatch is captured as a separate
deferred item for Stage's consideration (stash `3A963D34`), rather than
retroactively renaming this or any of the 8 prior archive records'
`archived_status` values, which would expand this closure's scope beyond
its own contract surface.

This closure carries forward three pre-existing P-021 deferred
scope-expansion stash entries captured during the review round for this
shipment (`6C5DF765` — `process_request` never wires
`admit_read`/`dispatch_with_read_context`, pre-existing, requires Stage
deliberation; `9B7EC1E4` — `run_read_server_startup` never installs a
production `GenerationActivator`, requires new `GenerationStore`
infrastructure design; `4628001C` — `get_workspace_status` unconditional
`connect_db` in `ReadServer` mode, pre-existing) plus one advisory
(non-blocking, no stash entry) noted in the PR body regarding
`shim::transport::translate_ipc_response`'s error-envelope shape — none
of these were fixed in this shipment's scope, consistent with P-021 C1.
Two further deferred entries were captured during PR #408's Copilot
review round on this closure PR itself: `5684685C` (validator-manifest
stale probe-command drift, advisory, non-blocking) and `3A963D34`
(`archived_status: done` vs. Shipment Sequencing Protocol prose
reconciliation, see Successor eligibility clarification above). Two
additional entries were captured during PR #408's second Copilot review
round (fix commit `db1d4925`, on the closure branch itself):
`21D0F63C` (correct malformed `feature`/`requires_deliberation` fields
on `3A963D34`/`5684685C` at next Stage triage — Ship cannot edit stash
entries post-capture per P-021 C5) and `D77BCBBC` (add automated
log-based monitoring for the IPC/MCP/CLI error-envelope fidelity
signal, currently manual-probe-only).

The pre-existing dirty-worktree files present at shipment claim
(`.backlogit/stash.jsonl` modification, untracked
`docs/memory/2026-09-20-ship-pr-406-merge-140-s-closure-repair.md`) were
preserved non-destructively throughout execution and landed in `main` as
part of PR #407's own merged history — neither was discarded, overwritten,
reset, or silently absorbed. A second memory artifact created after PR
creation documenting the CI-rerun outcome
(`docs/memory/2026-09-21-ship-141-s-pr407-authorized-rerun-passed.md`)
was deliberately left uncommitted on the feature branch (to avoid
re-advancing the already-reviewed/gated PR HEAD before merge) and is
instead committed as part of this shipment's post-merge closure branch,
not this archive step.

Cascade confirmation: `backlogit_ship_shipment` / `backlogit shipment
ship` was never invoked for `141-S` during this closure. This record was
produced entirely via manual authoring of this archive file plus deletion
of `.backlogit/queue/141-S.md`, followed by `backlogit sync` to rebuild
the index, consistent with the manual safe-close procedure established
for `133-S` through `140-S`.
<!-- END:description -->
