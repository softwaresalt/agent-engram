---
artifact_type: task
commit: 30c3911b
created_at: 2026-07-27T07:04:42.0838713Z
id: 099.003-T
labels:
    - follow-up
    - python-canonical
    - parity
parent_id: 099-F
priority: medium
status: done
title: Extend C6-1 package-topology invalidation to the index path (sync parity)
updated_at: 2026-07-30T07:59:26.8237341Z
---

rust-reviewer P0-1283 (deferred, non-blocking). The C6-1 package-topology invalidation was DoD-scoped to the `sync` path; the `index` path lacks it and relies on the marker gate (force || !python_hash_skipped, code_graph.rs:1758) to fail closed on currency, converging on the next sync. Bring the index path to parity so topology changes invalidate directly rather than deferring to the next sync.

<!-- BEGIN:acceptance-criteria -->
- Index path invalidates canonical edges on package-topology change with the same semantics as sync.
- Test proves an __init__.py add/remove invalidates affected Python canonical edges on `index` without requiring a subsequent sync.
- Marker-gate fail-closed behavior preserved.
- RED test first: the __init__.py add/remove-on-`index` invalidation test must be authored to fail against the current index path (which defers to the next sync) before parity is implemented.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Mirror the sync-path C6-1 invalidation into the index path. Reference: code_graph.rs sync backfill region ~2600-2665 vs index path ~1700-1770. Non-blocking: current behavior is fail-closed (stale currency refused), just not eager.
<!-- END:implementation-notes -->