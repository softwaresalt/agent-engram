---
artifact_type: task
created_at: 2026-07-10T19:21:57.87093-07:00
dependencies:
    - 082.002-T
id: 082.003-T
labels:
    - width:schema
    - graph
    - provenance
    - slice:1
parent_id: 082-F
priority: medium
references:
    - docs/exec-plans/2026-07-10-callgraph-cross-file-resolution-plan.md
status: done
title: 'PROVENANCE STORAGE (DB layer): calls_edge.resolution migration + count/enumerate query'
updated_at: 2026-07-11T17:22:52.9254279-07:00
---

PROVENANCE STORAGE — DB LAYER ONLY (re-split per Copilot #240 finding 1: the previous 082.003-T also required the CodeEdge model field + dehydration.rs + proptest_models.rs struct-literal changes, which pushed it to >=4 source files. The model/serialization half is now the separate task 082.011-T; this task is schema + DB read/write only).

Files (<=3: 2 src + 1 test): src/db/cozo_backend/schema.rs (extend `calls_edge` with a `resolution` attribute + forward migration), src/db/cozo_queries.rs (persist `resolution="direct"` in create_calls_edge :1206-1214 at the raw cozo-row level; add a resolution-accepting writer `create_calls_edge_with_resolution(from, to, resolution)` so downstream tasks can persist arbitrary provenance WITHOUT changing the 2-arg `create_calls_edge` signature — the ~30 existing 2-arg callers in code_graph.rs/hydration.rs/tests stay buildable; add `count_calls_edges_by_resolution` + an enumerate-`calls_resolved_singleton` read query that returns RAW (from, to, resolution) rows/counts — NOT hydrated `CodeEdge` structs, so this task does NOT need the model field and can land before 082.011-T). Test: tests/integration/calls_edge_resolution_storage_test.rs.

Contract: today calls_edge stores only `(from, to) => created_at` (schema.rs:319; cozo_queries.rs:1206-1214 `:put calls_edge { from, to => created_at }`). Add a `resolution: String` attribute with a forward migration that DEFAULTS pre-existing rows to the exact string `direct`. The 2-arg create_calls_edge persists `resolution="direct"` for direct in-file edges (signature unchanged); the new `create_calls_edge_with_resolution(from, to, resolution)` writer lets the post-pass (082.008-T) persist the EXACT canonical value `calls_resolved_singleton` and the rehydrate path (082.012-T) persist whatever provenance a JSONL edge carries. `calls_resolved_singleton` is the single source-of-truth string (never a shortened `resolved_singleton`) consumed by 082.004-T target-correctness checks and 082.010-T retraction. The read surface (`count_calls_edges_by_resolution` + enumerate) works on raw rows so 082.004-T can list/validate tagged edges without depending on the CodeEdge model field.

Width: schema (DB provenance storage). Scenarios (4): migration adds `resolution` and defaults existing rows to `direct`; a direct in-file edge persists `resolution="direct"`; count_calls_edges_by_resolution returns correct per-provenance counts; the enumerate query lists `calls_resolved_singleton` rows by provenance.

Depends on 082.002-T. Model/serialization propagation (`CodeEdge.resolution`) is the separate task 082.011-T, which depends on this one.