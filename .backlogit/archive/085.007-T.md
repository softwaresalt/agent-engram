---
archived_from: .backlogit/queue/085.007-T.md
archived_status: done
artifact_type: task
commit: f10caffb29cdb51a8e0384fd3e14b37523d1bd02
created_at: 2026-07-13T16:18:19.3729298-07:00
dependencies:
    - 085.006-T
id: 085.007-T
labels:
    - powerbi
    - dax
    - cli
    - mcp-parity
    - lint
parent_id: 085-F
priority: high
references:
    - docs/exec-plans/2026-07-13-dax-intelligence-plan.md
    - src/bin/engram.rs
    - src/cli/commands/verify.rs
    - src/shim/tools_catalog.rs
status: archived
title: P7 — engram lint-dax CLI subcommand + CLI↔MCP parity-guard coverage for lint_dax
updated_at: 2026-07-14T14:16:50.4195369-07:00
---



<!-- BEGIN:acceptance-criteria -->
- `engram lint-dax [<model.tmdl>]` invokes the daemon `lint_dax` MCP tool and renders its findings; the optional `<model.tmdl>` argument maps to the tool's `model_path` selector — canonicalized via `canonical_tmdl_model_path` (src/services/powerbi_tmdl.rs:85) to exactly one model scope, NOT the shared `source_path` content-source directory that all Power BI files under a registered source share — (omitted = every indexed model in the bound workspace); integration coverage includes two models under one registered source resolving to distinct scopes; subcommand help annotates the mirrored tool (`lint-dax=lint_dax`) per the existing CLI convention.
- Exit-code contract consistent with verify: 0 conformant, 1 findings, 2 error; a `<model.tmdl>` that is not an indexed model in the bound workspace exits 2 (error).
- A parity-guard contract test enforces that `lint_dax` HAS a CLI mapping AND that no NEW MCP-tool-without-CLI drift is introduced.
- The parity guard does NOT fail on the documented pre-existing gaps owned by stash 30F372C8 — it uses either a focused lint_dax assertion or a general guard with an explicit allowlist: {query_graph_neighborhood, create_task, update_task, query_changes, index_git_history}.
- Integration coverage exercises the subcommand end-to-end against a bound fixture workspace.
- clippy pedantic clean; no unwrap/expect.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Add an `engram lint-dax [<model.tmdl>]` CLI subcommand that mirrors the `lint_dax` MCP tool (from P6), plus a BOUNDED CLI<->MCP parity-guard test. The subcommand is daemon-backed (Tier-2 semantic lint needs the resolved schema, like `engram impact` mirrors `impact_analysis`), NOT local like `engram verify`. Add a `LintDax` variant to the `Command` enum in src/bin/engram.rs with `#[command(name = "lint-dax")]` and a doc comment annotating the mirrored MCP tool per the existing convention (`... (lint_dax).`), and implement it under src/cli/commands/ (new lint_dax.rs or extend verify.rs) to call the daemon `lint_dax` tool (passing the optional `<model.tmdl>` as the tool's `model_path` selector — canonicalized via `canonical_tmdl_model_path` to one model scope, NOT the shared `source_path` content-source directory; omitted = whole bound workspace; an unindexed path exits 2) and render `{ conformant, findings[] }` with an exit-code contract consistent with `engram verify` (0 conformant / 1 findings / 2 error). Operator reversed scope-cut D4 (2026-07-13): this is now IN SCOPE for 085-F. Width: CLI subcommand + parity test.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
BOUNDED SCOPE (do NOT absorb 30F372C8): P7 delivers only (a) the `lint-dax` subcommand and (b) a parity guard that pins lint_dax + prevents NEW drift via an allowlist of the 5 known-open gaps. The full-surface MCP<->CLI audit, the canonical mapping doc, and closing the other five gaps remain with 30F372C8. Prefer an explicit allowlist const so the guard is self-documenting and 30F372C8 can shrink it as gaps close. Test-first: author the parity contract test (tests/contract/) before wiring the subcommand. Pattern: `Impact` variant (daemon-backed, annotates `impact_analysis`) is the closest model; `Verify` shows the exit-code rendering. Grounding: Command enum + parity subcommands (src/bin/engram.rs:64+), Verify variant (:~128), src/cli/commands/*.rs, tool catalog TOOL_COUNT/tool_count_matches_dispatch (src/shim/tools_catalog.rs:21,381). Depends on P6 (085.006-T) — needs the lint_dax MCP tool to exist.
<!-- END:implementation-notes -->