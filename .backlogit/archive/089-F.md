---
artifact_type: feature
created_at: 2026-07-14T23:35:52.1527548-07:00
custom_fields:
    harness_status: pending
    source_stash_id: E1A9ED33
    source_stash_kind: feature
    source_stash_path: stash.jsonl
    source_stash_priority: medium
    source_stash_text: 'rec1 followup: persist and rehydrate staged_call rows through the JSONL dehydration path. Today staged_call rows are not exported, so after rehydration or a fresh upgrade the post-pass runs against empty staging and relies on preserved singleton edges. Carrying staged rows through JSONL would let the post-pass fully revalidate cross-file calls after a restart. This is Copilot finding 4 fuller suggestion.'
id: 089-F
links:
    - target_id: 088-F
      link_type: informs
priority: medium
status: done
title: Durable staged_call provenance through JSONL persist & rehydrate
updated_at: 2026-07-16T03:42:17.2393788-07:00
---

rec1 durability followup (Copilot finding 4 fuller suggestion). Today staged_call rows are not exported to JSONL, so after rehydration or a fresh upgrade the deferred cross-file post-pass (082.008-T) runs against empty staging and relies only on preserved singleton edges. Carry staged_call rows through the JSONL dehydration/rehydration path so the post-pass can fully revalidate cross-file calls after a daemon restart. Builds on 082.011-T/082.012-T (CodeEdge.resolution model + JSONL export/rehydrate for edges) but extends persistence to the staging relation itself. Source stash E1A9ED33. Grounding: src/db/cozo_backend/cozo_queries.rs (staged_call staging + post-pass), schema.rs (staged_call relation ~510,597), hydration.rs (JSONL dehydrate/rehydrate path).