---
artifact_type: task
created_at: 2026-07-11T01:20:50.8183317-07:00
dependencies:
    - 082.003-T
id: 082.011-T
labels:
    - width:models
    - graph
    - provenance
    - serialization
    - slice:1
parent_id: 082-F
priority: medium
references:
    - docs/exec-plans/2026-07-10-callgraph-cross-file-resolution-plan.md
status: done
title: 'MODEL/SERIALIZATION: CodeEdge.resolution field + struct-literal + proptest propagation'
updated_at: 2026-07-11T17:31:52.9807363-07:00
---

MODEL / SERIALIZATION PROPAGATION (split from 082.003-T per Copilot #240 finding 1 — the DB-layer `calls_edge.resolution` migration + count/enumerate query is 082.003-T; this task carries the `CodeEdge` model field + serialization/struct-literal propagation, a distinct model width).

Files (3 source + 1 test): src/models/code_edge.rs (add `resolution: Option<String>` to `CodeEdge` at :36, matching the existing `import_path`/`linked_by` `#[serde(skip_serializing_if = "Option::is_none")]` pattern plus `#[serde(default)]` for backward-compatible JSONL deserialization of pre-field edges), src/services/dehydration.rs (THREE edits, all in this one file: (a) add `resolution: Option<String>` to the INTERMEDIATE `EdgeLine` struct at :230 — the struct actually serialized to JSONL — with the same `#[serde(skip_serializing_if = "Option::is_none")]` pattern; (b) copy `edge.resolution.clone()` into the `serialize_edges_jsonl` CodeEdge→EdgeLine mapping at :390 (currently copies only import_path/linked_by, so provenance is DROPPED on export today); (c) set `resolution` on the two `CodeEdge` test literals at :595/:603), src/db/cozo_queries.rs (add `resolution: None` to the THREE pre-existing GENERIC edge-read `CodeEdge` constructors — get-edges-by-type readers, unrelated to calls provenance — that rustc flags once the field lands). Test (1): tests/unit/proptest_models.rs (update the `CodeEdge` prop_map literal at :323 to generate/set `resolution`).

Contract: `CodeEdge` (code_edge.rs:36, fields edge_type/from/to/import_path/linked_by/created_at) gains `resolution: Option<String>`, and the field is threaded through the JSONL EXPORT path so dehydrated/exported edges carry provenance (`direct` | `calls_resolved_singleton`) end-to-end. Export goes CodeEdge → `EdgeLine` (dehydration.rs:230) → JSONL via `serialize_edges_jsonl` (:382); per Copilot #240 cycle-3 finding 1, adding the field to `CodeEdge` alone is INSUFFICIENT because `EdgeLine` is the struct actually serialized and its mapping (:390) copies only import_path/linked_by — so this task also adds `resolution` to `EdgeLine` and its mapping. Value semantics mirror 082.003-T's DB column; `None` serializes away (skip_serializing_if) so existing consumers/fixtures/golden JSONL are unaffected. The READ-BACK / rehydrate half (ParsedEdge + calls upsert in hydration.rs, so provenance survives daemon restart) is the sibling task 082.012-T, which depends on this one. Depends on 082.003-T (DB column + canonical strings defined first).

Compiler follow-on (grounding note; single-token, no logic, no test): the three `resolution: None` additions on the pre-existing GENERIC edge-read `CodeEdge` constructors in src/db/cozo_queries.rs are OWNED BY THIS TASK (listed in the Files section above). They MUST land in this same change because rustc requires every `CodeEdge` construction site to set the new field the moment it is added — splitting the field definition from its construction-site updates would break the build (constitution: every commit must be buildable). All three are mechanical single-token additions carrying no logic. Counted as source file 3 of 3 (code_edge.rs + dehydration.rs + cozo_queries.rs), keeping this task within the ≤3-source-file cap. Flagged so Ship expects the extra constructor sites.

Width: models / serialization. Scenarios (3): `CodeEdge` round-trips `resolution` through serde for both `Some(..)` and `None`; `serialize_edges_jsonl` output (via `EdgeLine`) INCLUDES a `resolution` field only when `Some`, and omits it when `None`; the proptest `CodeEdge` strategy exercises `resolution` without a serialization panic.

Depends on 082.003-T.