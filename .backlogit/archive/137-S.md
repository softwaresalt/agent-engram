---
archived_from: .backlogit/queue/137-S.md
archived_status: done
artifact_type: shipment
created_at: 2026-09-03T01:03:05.2705841Z
custom_fields:
    items:
        - 142.015-T
        - 142.016-T
        - 142.020-T
        - 142.021-T
        - 142.022-T
        - 142.027-T
dependencies:
    - 135-S
    - 136-S
id: 137-S
priority: high
title: Candidate indexing, direct-sync boundary and supervisor crate separation
updated_at: 2026-09-08T23:10:00.0000000-07:00
status: archived
---



<!-- BEGIN:description -->
AUDIT RATIONALE (manual safe-close): 137-S closes via targeted manual
safe-close per operator-approved merge of PR #388 (`PR 388: Merge approved`,
approved/reviewed HEAD `653e973e201883790a9862e6ff54ea84efbb902d`, merge
commit `ef0135bf05aba5d7329a7306eb78bb9b18e02f69`, merged at
2026-09-09T06:05:39Z, merge method: merge commit via `gh pr merge 388
--merge`). PR #388 implemented all six manifest items (142.015-T, 142.016-T,
142.020-T, 142.021-T, 142.022-T, 142.027-T), each independently verified
`status: done` via `backlogit get` and already individually archived under
`.backlogit/archive/` prior to this shipment-record closure step (confirmed
via pre-mode reconciliation, all six classified `pre-archived`, report at
`.backlogit/reconcile/137-S-pre-20260908T230954Z.md`, recommendation
`PROCEED`).

Manual safe-close (not the `backlogit shipment ship` cascade operation) was
required for two independent, previously-established reasons documented
against the identical covering feature on predecessor shipments `134-S` and
`135-S`:

1. **P-015 protected-scope risk**: `142-F` is a shared-parent covering
   feature for later shipments (`138-S` onward), spanning a 59-unit roster
   (F00, F01-F55, F04a, F12a, F16a). `142-F` is not a manifest member of
   `137-S` and does not meet the P-015 fully-covered-root exception (the
   manifest is 6 task items out of the 59-unit roster, and the manifest
   contains no feature member at all, so the exception's "nothing beyond
   the qualifying root feature(s) and their children" precondition also
   fails). The cascade `shipment ship` path — which force-releases the
   covering feature and can requeue/detach unshipped descendant siblings —
   is therefore prohibited for this shipment regardless of tool behavior.
2. **Established tool non-termination defect**: `backlogit shipment ship`
   was already demonstrated (see
   `docs/compound/workflow-issues/backlogit-shipment-ship-non-terminating-large-covering-feature-2026-09-06.md`)
   to not return within a bounded observation window (up to ~5.5 minutes,
   steadily climbing CPU, zero WAL growth) for shipment `135-S`, which
   shares the identical `142-F` covering feature and the identical
   small-manifest-vs-large-roster shape as `137-S`. Given this established
   precedent against the same covering feature, `backlogit shipment ship
   137-S` was not attempted for this closure — repeating a demonstrated
   non-terminating operation would risk an unbounded session stall for no
   expected benefit, since P-015 already prohibits the cascade outcome even
   if it were to complete. The generic fallback (`backlogit move 137-S
   --status shipped`) was attempted and confirmed rejected outright by the
   CLI (backlogit v1.10.1-0.20260823032255-b07729386a31+dirty): `Error:
   shipment must be shipped via ShipShipment, not a direct status update`
   (exit code 9) — establishing that `shipment ship` is currently the sole
   registered CLI transition path for a shipment's `active -> shipped`
   status, and that path is unavailable here for the reasons above. No
   partial state was produced by this rejected attempt: `137-S` status was
   unchanged (`active`) and no `.backlogit/archive/137-S.md` file existed
   prior to this manual authoring step.

Only the six items explicitly enumerated in this shipment's manifest were
mutated (individually archived during routine task-completion bookkeeping
ahead of this closure step); `142-F` remains `active` and byte-for-byte
untouched (pre-closure SHA-256
`59263E8FFB779485E135A7AA41D9DAAC89B4A996B767D128D76A1AD2E70404C3`,
802 bytes, verified unchanged after this closure step); no other `142.*`
item's `parent_id`, status, or shipment membership was altered by this
closure. `138-S` (queued) and all later shipments covering the remaining
`142-F` roster were not read, claimed, or mutated.

`archived_status: done` (rather than `shipped`) is deliberate, not an
oversight: it factually records that this record did not transit the
`active -> shipped` `ShipShipment` cascade (the CLI hard-rejected the
direct-to-`shipped` path, see above), and it matches the identical,
already-established manual-safe-close precedent for the `archived_status:
done` / manual-archive-file format on `133-S`, `134-S`, and `135-S`
(`.backlogit/archive/133-S.md`, `.backlogit/archive/134-S.md`,
`.backlogit/archive/135-S.md`, all `archived_status: done`). Rewriting it to
`shipped` here alone would misrepresent the closure mechanism actually used
and break consistency with that precedent chain.

Cascade confirmation: `backlogit_ship_shipment` / `backlogit shipment ship`
was never invoked for `137-S` during this closure. This record was produced
entirely via manual authoring of this archive file plus deletion of
`.backlogit/queue/137-S.md`, followed by `backlogit sync` to rebuild the
index, consistent with the manual safe-close procedure established for
`134-S` and `135-S`.
<!-- END:description -->
