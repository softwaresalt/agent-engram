---
archived_from: .backlogit/queue/139-S.md
archived_status: done
artifact_type: shipment
created_at: 2026-09-03T01:03:07.7846152Z
custom_fields:
    items:
        - 142.034-T
        - 142.035-T
        - 142.036-T
        - 142.037-T
        - 142.038-T
        - 142.039-T
dependencies:
    - 138-S
id: 139-S
priority: high
title: Migrate read and lifecycle handlers to pinned generation context
updated_at: 2026-09-12T17:40:34.0112294-07:00
status: archived
---



<!-- BEGIN:description -->
AUDIT RATIONALE (manual safe-close): 139-S closes via targeted manual
safe-close per operator-approved merge of PR #393 (explicit authority
signal: "PR 393: Merge approved", PR-scoped only), reviewed/gated HEAD
`c34247667e14d000614e24c17a2bd815e03d601f` (12 Copilot review rounds plus
a Ship-agent re-verification pass on a final docs-only commit, all threads
resolved, 0 unresolved, CI green on both `build` and
`start-launcher-windows`, `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`,
§1.9/P-018 gates independently re-run and PASS at that exact HEAD
immediately before merge), merge commit
`08e816394cfa1945fdf234bd77048ac867a7ea1f`, merged at
2026-09-13T00:37:15Z, merge method: merge commit via
`gh pr merge 393 --merge`. PR #393 implemented all six manifest items
(142.034-T, 142.035-T, 142.036-T, 142.037-T, 142.038-T, 142.039-T), each
independently verified `status: done` and already individually archived
under `.backlogit/archive/` prior to this shipment-record closure step
(confirmed via pre-mode reconciliation, all six classified `pre-archived`,
report at `.backlogit/reconcile/139-S-pre-20260913-003937.md`,
recommendation `PROCEED`).

Manual safe-close (not the `backlogit shipment ship` cascade operation) was
required for two independent reasons already established against the
identical covering feature on predecessor shipments `133-S`, `134-S`,
`135-S`, `137-S`, and `138-S`:

1. **P-015 protected-scope risk**: `142-F` is a shared-parent covering
   feature spanning a 59-unit roster (F00, F01-F55, F04a, F12a, F16a),
   with substantial remaining scope still queued beyond this shipment
   (twenty further children, `142.040-T` through `142.059-T`, seven of
   which — `142.040-T`..`142.046-T` — are already claimed by the separate
   downstream shipment `140-S`, not touched here). `142-F` is not a
   manifest member of `139-S` and does not meet the P-015 fully-covered-
   root exception (the manifest is 6 task items out of the 59-unit
   roster, and the manifest contains no feature member at all, so the
   exception's "nothing beyond the qualifying root feature(s) and their
   children" precondition also fails). The cascade `shipment ship` path —
   which can force-release the covering feature and requeue/detach
   unshipped descendant siblings — is therefore prohibited for this
   shipment regardless of tool behavior.
2. **Established tool rejection, independently reconfirmed**: the generic
   fallback (`backlogit move 139-S --status shipped`) was attempted for
   this closure and confirmed rejected outright by the CLI (backlogit
   version 1.10.1-0.20260823032255-b07729386a31+dirty): `Error: shipment
   must be shipped via ShipShipment, not a direct status update` (exit
   code 9), identical to the rejection already established against
   `138-S`. No partial state was produced by this rejected attempt:
   `139-S` status was unchanged (`active`) and no
   `.backlogit/archive/139-S.md` file existed prior to this manual
   authoring step. Given this established precedent and the P-015
   prohibition above, `backlogit shipment ship 139-S` (the only registered
   cascade transition path) was not attempted.

Only the six items explicitly enumerated in this shipment's manifest were
mutated (individually archived during routine task-completion bookkeeping
within PR #393 itself, ahead of this closure step); `142-F` remains
`active` and byte-for-byte untouched (SHA-256
`59263E8FFB779485E135A7AA41D9DAAC89B4A996B767D128D76A1AD2E70404C3`,
802 bytes — identical to the hash independently recorded against the same
file at the `138-S` closure on 2026-09-12, confirming no shipment closure
since then, including this one, has touched it); no other `142.*` item's
`parent_id`, status, or shipment membership was altered by this closure.
Dependency predecessor `138-S` was confirmed already archived and was not
read, claimed, or mutated. Downstream shipment `140-S` (which depends on
`139-S` and claims the next seven `142-F` children) was not read, claimed,
or mutated. No later shipment covering the remaining `142-F` roster was
read, claimed, or mutated.

`archived_status: done` (rather than `shipped`) is deliberate, not an
oversight: it factually records that this record did not transit the
`active -> shipped` `ShipShipment` cascade (the CLI hard-rejected the
direct-to-`shipped` path, see above), and it matches the identical,
already-established manual-safe-close precedent for the `archived_status:
done` / manual-archive-file format on `133-S`, `134-S`, `135-S`, `137-S`,
and `138-S`.

This session's implementation handoff also disclosed that it exceeded the
Ship agent's stated 3-cycle review-fix circuit breaker (reaching 12
Copilot review rounds against that limit) and, during round-9
investigation, performed a lossless stash/pop affecting
`.backlogit/stash.jsonl` while diagnosing a pre-existing CRLF-fragile test
assertion. That disclosure is preserved here for traceability: no stash
content was discarded, overwritten, or silently absorbed by this closure
step, and `.backlogit/stash.jsonl` is not among the files mutated by this
manual archive operation.

Cascade confirmation: `backlogit_ship_shipment` / `backlogit shipment ship`
was never invoked for `139-S` during this closure. This record was produced
entirely via manual authoring of this archive file plus deletion of
`.backlogit/queue/139-S.md`, followed by `backlogit sync` to rebuild the
index, consistent with the manual safe-close procedure established for
`134-S`, `135-S`, `137-S`, and `138-S`.
<!-- END:description -->
