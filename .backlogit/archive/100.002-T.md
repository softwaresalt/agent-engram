---
archived_from: .backlogit/queue/100.002-T.md
archived_status: done
artifact_type: task
commit: 8a6c6e32507434ff80e7453b92ecf27d21992bc4
created_at: 2026-07-28T04:42:20.9134621Z
dependencies:
    - 100.001-T
id: 100.002-T
labels:
    - code-graph
    - fail-closed
    - green
parent_id: 100-F
priority: medium
status: archived
title: 'U2: Ambiguity-aware resolver + guarded direct-edge minting sites (GREEN)'
updated_at: 2026-07-28T15:59:13.055708Z
---



<!-- BEGIN:acceptance-criteria -->
- New ambiguity-aware helper resolves exactly-one match; returns None/typed-ambiguous on >1 same-file same-name candidate.
- Both minting sites (@1644 index, @2522 sync) use it symmetrically for callee AND caller; an ambiguous same-file call is NOT minted as a direct edge (staged or fail-closed dropped).
- find_function_id unchanged for caller attribution (@1617/@2497) and the resolve path (@2805).
- Same-file ambiguous drops increment an observability counter (cross_file_edges_dropped or a dedicated same_file_ambiguous_dropped).
- U1's Python + Rust wrong-target scenarios now pass; the unique-name recall control passes.
- fmt + clippy pedantic clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Implement the fail-closed fix that makes U1 (100.001-T) GREEN. Add an additive ambiguity-aware resolver (e.g. find_unique_function_id) that returns the id ONLY when exactly one same-file candidate matches the name, and signals ambiguity (None / typed reason) when >1 match. Wire it into the CALLEE (and CALLER, per deliberation open-question 1) resolution at BOTH direct-edge minting sites — index (~code_graph.rs:1644) and sync (~2522) — routing an ambiguous same-file call into the existing staging path (put_staged_call_with_provenance) so the cross-file post-pass handles it under its own fail-closed rules, else drop fail-closed. Leave find_function_id BYTE-IDENTICAL for its other 6 consumers. Language-agnostic (no last-wins). Width-isolated to code-graph. Plan Unit U2. Blocked by U1; blocks U3.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Additive helper only — do NOT change find_function_id global semantics (7 shared call sites, Rust+Python, index/sync/resolve). Reject Option B last-wins (unsound for Rust inline-mod-per-file). Mirror the cross-file singleton 'skip ambiguous to bound false edges' precedent (code_graph.rs:1764-1766). ~4 edits across the two symmetric sites; within the 2-hour gate.
<!-- END:implementation-notes -->