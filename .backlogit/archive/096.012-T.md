---
artifact_type: task
created_at: 2026-07-25T15:27:17.1026246-07:00
dependencies:
    - 096.001-T
id: 096.012-T
labels:
    - python
    - canonical-identity
    - test
    - code
parent_id: 096-F
priority: medium
references:
    - Cargo.toml
    - tests/unit/python_canonical_test.rs
status: done
title: 'T1-setup: python_canonical test-target registration + external acceptance harness'
updated_at: 2026-07-26T07:37:02.5283173Z
---



<!-- BEGIN:acceptance-criteria -->
- M3: Cargo.toml registers `[[test]] name="unit_python_canonical" path="tests/unit/python_canonical_test.rs"` so `cargo test --test unit_python_canonical` runs (repo registers every nested tests/unit/*.rs; this target did not previously exist).
- The external `tests/unit/python_canonical_test.rs` harness exercises the PUBLIC `python_module_path_for_file` (096.001-T) over the acceptance table: nested regular package p/q/r.py -> p.q.r; top-level mod.py -> mod; fail-closed -> None for __init__.py, a src/-style root (src/ lacks __init__.py), an implicit PEP 420 namespace package (ancestor dir without __init__.py), a non-identifier segment, and notes.txt (non-.py).
- Compiling-but-failing harness (TDD RED): the registered target compiles against 096.001-T's public fn and the acceptance assertions drive RED->GREEN consistent with the sibling tasks.
- Single skill DOMAIN: test-target / build-config registration ONLY; NO module-path algorithm here (owned by 096.001-T, which this CONSUMES).
- clippy --all-targets -- -D warnings -D clippy::pedantic clean; fmt --all --check clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Test-target registration + external acceptance harness SPLIT OUT of T1 (096.001-T) per C14-3 (2-hour rule: T1 bundled the pure module-path algorithm + its module scaffold + a NEW Cargo `[[test]]` target + a NEW external harness = 4 files / 2 domains). This task OWNS the Cargo.toml `[[test]] unit_python_canonical` registration and the `tests/unit/python_canonical_test.rs` external acceptance harness that CONSUMES the public `python_module_path_for_file` from 096.001-T. 096.001-T retains the module (mod.rs) + the pure fn (module_path.rs) with its inline same-crate unit tests (mod.rs and module_path.rs are compile-coupled, so they stay together; the Cargo `[[test]]` registration + external harness are the separable build/test-config concern). Depends_on 096.001-T (the harness compiles against T1's public fn). TDD RED->GREEN. Single skill domain: test / build-config.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Split from 096.001-T per C14-3 (2-hour rule + single skill domain). The pure algorithm + module scaffold stay in T1 because `python_canonical/mod.rs` and `python_canonical/module_path.rs` are compile-coupled, while the Cargo `[[test]]` registration + external harness are a separable build/test-config concern. Files (<=2): Cargo.toml (2-line `[[test]]` registration; unit_parsing exists ~231-237; unit_python_canonical did not), tests/unit/python_canonical_test.rs (external acceptance harness calling the public fn). Depends_on 096.001-T (096.012-T -> 096.001-T); no other deps. Keep <=4 scenarios (the module-path acceptance table folds into one parametrized fail-closed/resolve scenario). The same `unit_python_canonical` target additionally hosts the T2/T2b module-binding integration cases (registered here).
<!-- END:implementation-notes -->