---
artifact_type: task
created_at: 2026-07-15T17:34:33.7252415-07:00
custom_fields:
    source_stash_id: 32DAA85B
    source_stash_kind: bug
    source_stash_path: stash.jsonl
    source_stash_priority: medium
    source_stash_text: '086.004 follow-up (082-S adversarial review F4): writer-side workspace+config atomicity. get_workspace_status now reads atomically (086.004-T), but the set_workspace handler still publishes workspace then config in two separate awaits (state.rs set_workspace L253 + set_workspace_config L361; called sequentially in lifecycle.rs ~164-165), leaving a narrow new-workspace/old-config window observable even by an atomic reader. Add an atomic set_workspace_and_config on AppState holding BOTH write locks; migrate the bind flow to use it; extend the get_workspace_status atomicity test to exercise real A->B/B->A writer ordering (not routed through a neutral binding). Non-blocking. See docs/closure/2026-07-15-082-s-runtime-reliability-adversarial-review.md.'
id: 092.001-T
parent_id: 092-F
priority: medium
status: done
title: 'Atomic set_workspace_and_config: hold both write locks; migrate bind flow; non-vacuous A->B/B->A test'
updated_at: 2026-07-16T23:52:28.2949549-07:00
---

Writer-side workspace+config atomicity fix (082-S adversarial review F4). Add an atomic AppState::set_workspace_and_config that holds BOTH write locks and migrate the bind flow to it; extend the atomicity test to exercise real A->B/B->A writer ordering (not routed through a neutral binding). Non-blocking. Source stash 32DAA85B.

<!-- BEGIN:acceptance-criteria -->
TEST-FIRST:
1. Add a non-vacuous atomicity test driving REAL A->B and B->A writer ordering (bind ws A/config A, then ws B/config B). A concurrent atomic reader (snapshot_dispatch_context) sampled across the transition MUST NEVER observe a mismatched pair (workspace_i, config_j) with j != i. The test MUST fail against the current two-await writer (proves non-vacuity) and pass after the fix.
Then implement:
2. AppState::set_workspace_and_config acquires active_workspace.write() THEN workspace_config.write() (SAME order as snapshot_dispatch_context) — no lock-order inversion (D3-a).
3. The LimitReached capacity check runs first while holding both guards; on failure returns Err WITHOUT mutating either value (no partial publish) (D3-b).
4. lifecycle.rs bind flow migrated to the single set_workspace_and_config call; surrounding behavior preserved (query_stats reset, scan generation, background hydration spawn).
5. No .await on I/O while holding the write guards; bind-latency SLA (029-F WS-6) unchanged (D3-c).
6. No new deps; #![forbid(unsafe_code)] intact. Rollback = revert to the two-await sequence (no schema/format change).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Grounding @ origin/main df77584: src/server/state.rs (snapshot_dispatch_context L245 reader workspace->config; set_workspace L253; set_workspace_config L361); src/tools/lifecycle.rs bind flow (L159-160 two awaits). Single-width concurrency/atomicity change; touches ONLY state.rs + lifecycle.rs + the atomicity test. Independent of Option C (091-F) and of 083/084/085. Plan: docs/exec-plans/2026-07-15-092-F-writer-side-workspace-config-atomicity-plan.md. Adversarial+plan-review gate 092.001-R = PASS (HIGH P1 D3-a/b/d remediated in-plan). Source: stash 32DAA85B; docs/closure/2026-07-15-082-s-runtime-reliability-adversarial-review.md (F4).
<!-- END:implementation-notes -->