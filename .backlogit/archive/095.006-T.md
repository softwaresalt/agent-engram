---
artifact_type: task
created_at: 2026-07-22T15:00:31.66011-07:00
dependencies:
    - 095.004-T
    - 095.005-T
    - 095.011-T
id: 095.006-T
parent_id: 095-F
priority: medium
status: done
title: 'U4a: Notebook cell routing + magic stripping + raw_parse_source carrier'
updated_at: 2026-07-23T23:59:20.2927342Z
---

Wire notebook cell ROUTING + magic handling in src/services/notebook_extract.rs and src/models/notebook.rs (the raw_parse_source carrier field, AR-10), split out of U4 per H1's 2-hour granularity rule; the lineage WRITE PATH + freshness stamp were split out to U4 write-path (095.015-T, H1). Route python code cells to the U2b resolver and %%sql cells to the U3 extractor, preserving chunk_index (notebook_extract.rs:29-30) so evidence carries correct cell ordering (order is metadata only — NEVER an edge). The extractors MUST receive the RAW cell source, NOT the persisted retrieval-wrapped content: notebook_extract stores each code cell's content as format!("Language: {language}. {trimmed}") (notebook_extract.rs:49-55) — a synthetic 'Language: {lang}. ' prefix PLUS the original source which still carries the %%sql/%sql magic — and magic_language (notebook_extract.rs:111) only peeks. Add a NON-PERSISTED raw_parse_source: Option<String> carrier on the cell record (src/models/notebook.rs — NotebookCellRecord today carries only the wrapper-applied content; the pre-wrap trimmed is a local in extract_notebook at notebook_extract.rs:24), populated at extraction time as trim() with (a) the 'Language: {lang}. ' retrieval wrapper NEVER applied and (b) the leading cell magic stripped — the real magic constants are %%sql / %sql / %%scala / %%sparkr / %%python (there is NO %%spark magic; AR-14) — and hand THAT to the U2b/U3 extractors, never the retrieval content. FAIL CLOSED: raw_parse_source None (unrecoverable) => emit no lineage for that cell (AR-10). %sql LINE-MAGIC POLICY DECIDED (AR-11): %sql line-magic cells are EXCLUDED from v1 — ONLY the %%sql cell-magic routes to the U3 SQL extractor; hand this decision to U7. AUTHORITY (cycle-5 F1): the extractors' trusted authority_ctx is BUILT and THREADED by U1b (095.011-T; config surface + propagation) — with no configured authority the router still runs but emits NO table/path edge (fail-closed), so U4a DEPENDS ON U1b. U4a COLLECTS the LineageEdgeCandidates returned by U2b (Python path) and U3 (SQL path) and hands that Vec<LineageEdgeCandidate> to the U4 write-path (095.015-T) — U4a itself does NO persistence (that seam belongs to 095.015-T). Tests: a notebook with a %%sql cell + a PySpark cell routes both with the EXACT byte-for-byte parse-source asserted (no 'Language: {lang}. ' wrapper and no leading magic token) and chunk_index preserved; a %sql LINE-magic cell is EXCLUDED (NOT routed to U3; AR-11); a raw_parse_source that cannot be recovered emits no lineage for that cell (fail-closed, AR-10). Granularity: 4 functions, 3 test scenarios — within the 2-hour gate. Domain: notebook-path (integration). Blocked by U2b (095.004-T), U3 (095.005-T), U1b (095.011-T). Write-path consumer: U4 write-path (095.015-T). Plan Unit 4a.
