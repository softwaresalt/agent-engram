---
archived_from: .backlogit/queue/002.004-T.md
commit: a56a8ba
artifact_type: task
created_at: 2026-05-05T10:00:00-07:00
dependencies:
    - 002.001-T
    - 002.002-T
    - 002.003-T
id: 002.004-T
labels:
    - indexer
    - backlog-graph
parent_id: 002-F
priority: high
status: archived
title: Backlog indexer ΓÇö file walk and extraction
updated_at: 2026-05-06T11:28:00-07:00
---

## Description

Create `src/services/backlog_indexer.rs` ΓÇö walks a backlog content source
directory, parses markdown files via the frontmatter parser, and produces
`BacklogIndexResult` (nodes, edges, content records).

## Scope

- `src/services/backlog_indexer.rs` (new)
- `src/services/mod.rs` (add `pub mod backlog_indexer;`)

## Implementation

- `pub async fn index_backlog_source(source: &ContentSource, queries: &CodeGraphQueries) -> Result<BacklogIndexResult, EngramError>`
- Walk directory, collect `.md` files only (JSONL deferred to follow-up)
- For each file: read content, compute SHA-256 hash, compare with DB (skip if unchanged)
- Parse frontmatter; extract id, title, artifact_type, status, parent_id, dependencies, references, labels
- Produce BacklogNode + BacklogEdge structs
- Produce BacklogContentRecord for body text: upsert via `queries.upsert_backlog_content_record()` with content_type "backlog" (uses dedicated `backlog_content_record` relation, NOT generic `content_record`, to avoid key collisions on overlapping `docs/` paths)
- Log warnings for files with invalid frontmatter (skip them)

## Tests

- `tests/unit/backlog_indexer_test.rs` (4 scenarios):
  index valid files, skip invalid, handle missing fields, verify hash-based skip

## Acceptance Criteria

- Produces correct nodes/edges/records from sample backlog files
- Skips unchanged files (hash comparison)
- Handles missing/optional frontmatter fields gracefully
- Logs warnings for malformed files without panicking
- `cargo test` passes

## References

Plan: `docs/exec-plans/2026-05-05-backlog-markdown-hydration-plan.md` Unit 4
Posture: Test-first