---
artifact_type: shipment
created_at: 2026-07-10T19:23:28.1200479-07:00
custom_fields:
    items:
        - 082.001-T
        - 082.002-T
        - 082.003-T
        - 082.011-T
        - 082.012-T
        - 082.008-T
        - 082.009-T
        - 082.010-T
        - 082.013-T
        - 082.004-T
id: 078-S
status: done
title: 'SHIP-2: rec1-calledges cross-file & method-call resolution (082-F re-harvested: 001,002,003,011,012,008,009,010,013,004) — eval gate (SHIP-1 S1+S3) satisfied on main'
updated_at: 2026-07-11T19:59:17.2376491-07:00
---

Ships the Rust-first rec1-calledges tasks, re-harvested (Copilot reviews #239 &
#240, stash CC5D369E) into single-width, ≤4-scenario executable items:
082.001-T (method-call capture) → 082.002-T (staging capture) →
082.003-T (provenance storage, DB layer) → 082.011-T (CodeEdge.resolution model +
JSONL export) → 082.012-T (rehydrate provenance on restart) → 082.008-T (post-pass
resolution) → 082.009-T (staging lifecycle) → 082.010-T (rollback down-migration
logic) → 082.013-T (rollback CLI trigger) → 082.004-T (acceptance via 081-F eval).
Does NOT include or archive the parent feature 082-F: the deferred peer-language
fan-out tasks (082.005-T Python, 082.006-T TypeScript, 082.007-T Go) remain queued
under 082-F, so 082-F stays active after this shipment and is archived only once
every child completes in a later follow-on shipment. This avoids leaving queued
tasks orphaned under an archived parent.

RE-HARVEST COMPLETE (Copilot reviews #239, #240 cycles 2 & 3): the over-limit tasks
were split into single-width items — 082.002-T (staging capture) + 082.009-T
(staging lifecycle); 082.003-T (DB-layer provenance storage + resolution-accepting
writer) + 082.011-T (model + JSONL EXPORT path: CodeEdge/EdgeLine/serialize) +
082.012-T (JSONL REHYDRATE path: ParsedEdge + calls upsert, so provenance survives
daemon restart) + 082.008-T (post-pass unambiguous resolution); and the rollback was
split into 082.010-T (reusable down-migration logic exposing `rollback_calls_resolution`)
+ 082.013-T (operator-invocable maintenance CLI trigger). Provenance is now durable
end-to-end (DB → export → JSONL → rehydrate → DB) and the rollback is operational,
not dead code. Full staged_call lifecycle + resolution migration + read-query
contract propagated INTO the executable task bodies; 082.004-T reduced to 4 scenarios
with its rollback-adjacent counts asserted before column drop. Dependencies: storage
(003) precedes model+export (011) precedes rehydrate (012); 082.008-T depends on BOTH
003 and 011; rollback logic (010) precedes rollback trigger (013); 082.004-T and the
language fan-out (005/006/007) depend on the post-pass task (008). Tracked by stash
CC5D369E.

Now QUEUED. The eval-subsystem gate (SHIP-1 / 077-S / 081-F, including 081.001-T
(S1 contract/model) and 081.005-T (S3 graph metric)) is ALREADY SHIPPED and
archived on main, so this dependency is satisfied and no upstream work remains to
wait on — 078-S is claimable as soon as this re-harvest merges.

