---
artifact_type: task
created_at: 2026-07-10T19:21:49.7011424-07:00
dependencies:
    - 082.001-T
id: 082.002-T
labels:
    - width:indexing
    - graph
    - core
    - staging-capture
    - slice:1
parent_id: 082-F
priority: medium
references:
    - docs/exec-plans/2026-07-10-callgraph-cross-file-resolution-plan.md
status: done
title: 'STAGING CAPTURE: staged_call relation + record unresolved calls (index + sync)'
updated_at: 2026-07-11T17:09:11.0854598-07:00
---

STAGING CAPTURE (split from the original 082.002-T per Copilot #239 — this half is the staging relation + record-unresolved only; clear-before-reindex / deletion cleanup / stale-edge retraction are the separate single-width task 082.009-T).

Files (<=3 src + 1 test): src/db/cozo_backend/schema.rs (new `staged_call` relation), src/services/code_graph.rs (index path :466-475 and sync path :1070-1077), src/db/cozo_queries.rs (put_staged_call + list_staged_calls). Test: tests/integration/unresolved_calls_capture_test.rs.

Contract: define `staged_call { caller_id, callee_name, source_file => created_at }`, keyed so every staged row carries its source_file (required by the 082.009-T lifecycle). On BOTH the full-index path (code_graph.rs:466-475) and the incremental sync path (:1070-1077), when find_function_id(callee) misses, record a staged_call row (caller id + callee name + source file) instead of discarding it. An in-file resolvable call still creates a direct calls_edge immediately, exactly as today — this task does NOT change the calls_edge schema; its `resolution="direct"` provenance is introduced by the 082.003-T storage task (avoids a schema dependency inversion). Preserve cross_file_edges_dropped accounting (:496 / :1099). Blocklisted names are already filtered at extraction by CALL_BLOCKLIST (082.001-T) and must never be staged.

Width: core-indexing / staging-capture. Scenarios (4): cross-file callee is recorded in staged_call (not dropped); in-file callee still yields a direct calls_edge; sync path parity with index path (identical staged rows); blocklisted names never staged.

Depends on 082.001-T.