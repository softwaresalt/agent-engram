---
artifact_type: task
created_at: 2026-07-11T00:59:03.3424929-07:00
dependencies:
    - 082.002-T
    - 082.003-T
    - 082.008-T
id: 082.009-T
labels:
    - width:indexing
    - graph
    - core
    - lifecycle
    - slice:1
parent_id: 082-F
priority: medium
references:
    - docs/exec-plans/2026-07-10-callgraph-cross-file-resolution-plan.md
status: done
title: 'STAGING LIFECYCLE: clear-before-reindex, deletion cleanup, retract stale resolved edges'
updated_at: 2026-07-11T17:47:32.8104455-07:00
---

STAGING LIFECYCLE (split from the original 082.002-T per Copilot #239 — capture is 082.002-T; this task is clear / delete / retract).

Files (<=3 src + 1 test): src/services/code_graph.rs (clear staged_call before (re)index on index path ~:294-300 and sync path ~:880-885; deletion cleanup in handle_deleted_file :1181-1205; retract stale resolved edges BEFORE function metadata is deleted), src/db/cozo_queries.rs (clear_staged_calls_for_file + retract_resolved_calls_edges_for_file). Test: tests/integration/calls_staging_lifecycle_test.rs.

Contract: (1) CLEAR-BEFORE-REINDEX — both the full-index reindex path and the incremental sync path clear a file's prior staged_call rows before re-staging, so a changed or removed call never leaves a stale staged row that a later forced post-pass could resolve into a stale edge. (2) FILE-DELETION cleanup clears the deleted file's staged_call rows (handle_deleted_file). (3) RETRACT STALE RESOLVED EDGES — the reindex/delete paths (code_graph.rs:294-300, :1197-1205) delete function metadata + defines/references edges but NOT calls_edge, so a changed/deleted caller or callee would leave the prior `calls_resolved_singleton` edge dangling. This task retracts a file's prior `calls_resolved_singleton` edges (edges whose caller OR callee belongs to the file) WHILE THE OLD SYMBOL IDS STILL EXIST — i.e. before delete_functions_by_file (:294 / :1197) — then re-stages. Must test changed AND deleted callers and callees after a post-pass.

Width: core-indexing / staging-lifecycle. Scenarios (4): re-indexing a file whose call CHANGED retracts its stale `calls_resolved_singleton` edge (no dangling) and clears the old staged row; DELETING a file retracts its resolved edges and clears its staged rows; a changed/deleted CALLEE retracts the inbound `calls_resolved_singleton` edge (caller in another file); clear-before-reindex leaves no stale staged_call row for a later forced post-pass.

Depends on 082.002-T (capture), 082.003-T (resolution provenance), 082.008-T (post-pass produces the edges retracted/tested).