---
archived_from: .backlogit/queue/140-S.md
archived_status: done
artifact_type: shipment
created_at: 2026-09-03T01:03:09.0196708Z
custom_fields:
    items:
        - 142.040-T
        - 142.041-T
        - 142.042-T
        - 142.043-T
        - 142.044-T
        - 142.045-T
        - 142.046-T
dependencies:
    - 138-S
    - 139-S
id: 140-S
priority: high
title: Migrate services to pinned context and enforce read-path pinning
updated_at: 2026-09-18T23:57:00Z
status: archived
---



<!-- BEGIN:description -->
AUDIT RATIONALE (manual safe-close): 140-S closes via targeted manual
safe-close performed under Ship's own Role-Boundary-permitted,
non-discretionary backlog mandate ("close shipments, archive completed
items") as a mandatory continuation of the merge of PR #404. The
operator's dark-mode activation record for this session
(`DARK_MODE_ACTIVE`, scope: shipment 140-S / PR #404 only;
`merge_approval_pre_authorized=true` for PR #404) authorized the merge of
PR #404 and is not cited as, nor does it constitute, separate
authorization for this safe-close action beyond Ship's own backlog
mandate — see `docs/closure/2026-09-18-140-s-operational-closure.md` for
the operational-closure record. PR #404 merged at reviewed/gated HEAD
`18944fb1809926d3648fbbde7cc35be92f8deca5` (4 Copilot review rounds, all
10 threads resolved, 0 unresolved; CI green on both `build` and
`start-launcher-windows`; `mergeable: MERGEABLE`, `mergeStateStatus:
CLEAN`; §1.9/P-018 gates independently re-run and PASS at that exact HEAD
immediately before merge), merge commit
`eb1416cdb007c2f389f5856569f4c2ee7a982ddf`, merged at
2026-09-18T23:52:07Z, merge method: merge commit via `gh pr merge 404
--merge`. The final reviewed HEAD reflects a single corrective commit
(`18944fb1`) applied during this same session to fix stale readiness
evidence and path-contract documentation misstatements identified by a
readiness audit (registry.rs docstring, retrieval-eval memory doc,
operational-closure doc, shipment execution memory doc), plus this
shipment's own already-made P-021 deferred-scope-expansion stash
captures; no production behavior changed. PR #404 implemented all seven
manifest items (142.040-T through 142.046-T), each independently verified
`status: done` and already individually archived under
`.backlogit/archive/` prior to this shipment-record closure step
(confirmed via pre-mode reconciliation, all seven classified
`pre-archived`, report at
`.backlogit/reconcile/140-S-pre-20260918T235400Z.md`, recommendation
`PROCEED`).

Manual safe-close (not the `backlogit shipment ship` cascade operation) was
required for two independent reasons already established against the
identical covering feature on predecessor shipments `133-S`, `134-S`,
`135-S`, `137-S`, `138-S`, and `139-S`:

1. **P-015 protected-scope risk**: `142-F` is a shared-parent covering
   feature spanning a 59-unit roster (F00, F01-F55, F04a, F12a, F16a),
   with remaining scope still queued beyond this shipment. `142-F` is not
   a manifest member of `140-S` and does not meet the P-015
   fully-covered-root exception (the manifest is 7 task items out of the
   59-unit roster, and the manifest contains no feature member at all, so
   the exception's "nothing beyond the qualifying root feature(s) and
   their children" precondition also fails). The cascade `shipment ship`
   path — which can force-release the covering feature and
   requeue/detach unshipped descendant siblings — is therefore prohibited
   for this shipment regardless of tool behavior.
2. **Established tool rejection, independently reconfirmed**: the generic
   fallback (`backlogit move 140-S --status shipped`) was attempted for
   this closure and confirmed rejected outright by the CLI (backlogit
   version 1.10.1-0.20260823032255-b07729386a31+dirty): `Error: shipment
   must be shipped via ShipShipment, not a direct status update` (exit
   code 9), identical to the rejection already established against
   `138-S` and `139-S`. No partial state was produced by this rejected
   attempt: `140-S` status was unchanged (`active`) and no
   `.backlogit/archive/140-S.md` file existed prior to this manual
   authoring step. Given this established precedent and the P-015
   prohibition above, `backlogit shipment ship 140-S` (the only
   registered cascade transition path) was not attempted (per the
   documented non-termination symptom in
   `docs/compound/workflow-issues/backlogit-shipment-ship-non-terminating-large-covering-feature-2026-09-06.md`
   and its addenda for `137-S`/`138-S`).

Only the seven items explicitly enumerated in this shipment's manifest
were mutated (individually archived during routine task-completion
bookkeeping within PR #404 itself, ahead of this closure step); `142-F`
remains `active` and byte-for-byte untouched (SHA-256
`59263E8FFB779485E135A7AA41D9DAAC89B4A996B767D128D76A1AD2E70404C3`,
802 bytes — identical to the hash independently recorded against the same
file at the `138-S` and `139-S` closures, confirming no shipment closure
since then, including this one, has touched it); no other `142.*` item's
`parent_id`, status, or shipment membership was altered by this closure.
Dependency predecessors `138-S` and `139-S` were confirmed already
archived and were not read, claimed, or mutated. No later shipment
covering the remaining `142-F` roster was read, claimed, or mutated.
`141-S` was never claimed or processed at any point in this session, per
the operator's explicit dark-mode scope restriction to shipment 140-S /
PR #404 only.

`archived_status: done` (rather than `shipped`) is deliberate, not an
oversight: it factually records that this record did not transit the
`active -> shipped` `ShipShipment` cascade (the CLI hard-rejected the
direct-to-`shipped` path, see above), and it matches the identical,
already-established manual-safe-close precedent for the `archived_status:
done` / manual-archive-file format on `133-S`, `134-S`, `135-S`, `137-S`,
`138-S`, and `139-S`.

This session also captured one additional P-021 deferred-scope-expansion
stash entry (`95D6C74C`, task/low) for a Copilot finding raised against
the corrective HEAD (`18944fb1`) after the operator's one-commit
anti-tail-chasing directive for this readiness-audit cycle had already
been satisfied; it was replied-to and resolved via the defer-capture
procedure without a further commit, and is carried into this closure's
follow-up disposition. `.backlogit/stash.jsonl`'s working-tree state
(carrying this entry plus the six captured earlier in the session) was
preserved verbatim across a branch switch via `git stash`/`git stash pop`
(no content discarded, overwritten, or silently absorbed) and is
committed as part of this post-merge closure branch, not this archive
step.

Cascade confirmation: `backlogit_ship_shipment` / `backlogit shipment ship`
was never invoked for `140-S` during this closure. This record was
produced entirely via manual authoring of this archive file plus deletion
of `.backlogit/queue/140-S.md`, followed by `backlogit sync` to rebuild
the index, consistent with the manual safe-close procedure established
for `134-S`, `135-S`, `137-S`, `138-S`, and `139-S`.
<!-- END:description -->
