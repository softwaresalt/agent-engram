# Operational Closure — Shipment 012-S

**Date**: 2026-04-25
**Shipment**: 012-S — Repository Configuration and Policy Compliance Cleanup
**Chore**: 033-C
**PR**: https://github.com/softwaresalt/agent-engram/pull/28
**Branch**: `stage/012-S-repo-config-policy`
**Head commit**: `8624a28`

## Release Readiness

| Check | Status |
|-------|--------|
| CI/build (cozo-backend) | ✅ pass (56s) |
| CI/build (surreal-backend) | ✅ pass (7m48s) |
| Code review gate | ✅ clean (1 P1 found and fixed) |
| cargo check | ✅ pass |
| cargo clippy -D warnings -D clippy::pedantic | ✅ pass |
| No Rust source changed | ✅ confirmed |

## Scope

Pure config/docs chore — no Rust source code changes, no database migrations, no API changes, no runtime surfaces modified.

## Delivered Changes

### `.mcp.json.example` (new)

A committed template for `.mcp.json` (which is gitignored per `.gitignore`). Allows contributors to copy the template and fill in their machine-specific values without committing secrets or absolute paths.

- 7 MCP server entries: `engram`, `backlogit`, `context7`, `microsoft-docs`, `agent-intercom`, `tavily`, `github`
- Placeholder tokens: `<WORKSPACE_ROOT>`, `<ENGRAM_EXE_PATH>`, `<WORKSPACE_ID>`
- Tavily API key: `${TAVILY_API_KEY}` env var substitution (not hardcoded)

### `.github/policies/workflow-policies.md` P-010 (new policy)

Branch creation enforcement added to Ship pre-flight (Step 1):

> Ship MUST verify it is on a dedicated feature branch before creating any commit. Direct commits to `main` are forbidden.

Background: Policy formalizes the requirement after 007-S was committed to `main` without a feature branch.

### `.github/policies/workflow-policies.md` P-009 compliance note

Added compliance action note to P-009 documenting the requirement to disable `allow_rebase_merge` in GitHub Settings. Tracked as 033.001-T. Note uses "Compliance action required" (not "Verified") to accurately reflect pending state.

## Runtime Verification

This shipment does not modify runtime surfaces. Verification is limited to:

- [x] `.mcp.json.example` is tracked in git (`git ls-files .mcp.json.example` returns file)
- [x] `.mcp.json.example` contains no API keys (`tvly-`, `ghp_`, etc.)
- [x] `.mcp.json.example` contains no hardcoded absolute paths (`D:\\`, `/home/`)
- [x] `workflow-policies.md` renders valid markdown (no heading violations, P-010 entry appears)
- [x] Both CI checks pass confirming repo-wide quality gates hold

## Monitoring Plan

No monitoring required — config/docs change only.

## Pre-Deploy Audit

N/A — no deployment, no migration, no runtime change.

## Rollback Procedure

If these changes need to be reverted: `git revert {merge_sha}`. The `.mcp.json.example` would be removed; `workflow-policies.md` would revert to v1.4.0 without P-010. No data or runtime state is affected.

## Rollback Trigger

No production signal triggers. If `.mcp.json.example` causes contributor confusion, open a follow-up stash item.

## Post-Deploy Observation Window

None required — static config/docs only.

## Open Items and Follow-Up

### 033.001-T — PENDING OPERATOR ACTION

**Required**: Disable `allow_rebase_merge` in GitHub repository settings.

1. Navigate to https://github.com/softwaresalt/agent-engram/settings
2. General → Pull Requests → uncheck "Allow rebase merging" → Save
3. Verify: `gh api /repos/softwaresalt/agent-engram --jq '.allow_rebase_merge'` → `false`
4. Update 033.001-T status to `done`

Once completed, update the P-009 compliance note in `workflow-policies.md` from "action required" to "verified" — this can be done in a follow-up stash entry or inline during the next maintenance chore.

### Local `.mcp.json` verification

The local `.mcp.json` paths and API key were fixed as part of this chore (033.003-T), but since `.mcp.json` is gitignored, this fix is machine-local only. Other contributors need to update their own `.mcp.json` using `.mcp.json.example` as a guide.

## Artifacts

| Artifact | Path |
|----------|------|
| Session memory | `docs/memory/2026-04-25/ship-012-s-repo-config-memory.md` |
| Stage deliberation | `docs/decisions/2026-04-25-repo-config-policy-cleanup-deliberation.md` |
| Implementation plan | `docs/exec-plans/2026-04-25-repo-config-policy-cleanup-plan.md` |
| Closure record | `docs/closure/2026-04-25-012-S-closure.md` (this file) |
| PR | https://github.com/softwaresalt/agent-engram/pull/28 |
