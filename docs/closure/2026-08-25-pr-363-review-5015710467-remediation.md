---
title: PR 363 review 5015710467 planning remediation
type: review-remediation
doc_type: closure
source: PR 363 reviews 5015636140 and 5015710467
source_commit: a45ffb3035cf08698c33fc22445a58eb409842cb
date: 2026-08-25
status: published-threads-resolved
---

# PR 363 review 5015710467 planning remediation

## Scope

Planning/backlog artifacts and PR #363 review threads only. No application source, tests, Cargo manifest/lockfile, config, build, test suite, source linter, shipment claim/close, merge, amend, force push, or PR #362 mutation.

## Exact review and thread evidence

| Thread / comment | Path | Exact disposition |
|---|---|---|
| `PRRT_kwDORJEduc6b9aOQ` / `3850157249` | `.backlogit/queue/131.001-T.md` | The stale PR-body claim that no-default commands omit `cozo-backend` is withdrawn; every corrected command and the copy-ready body use the exact required feature set. The already-corrected archive-disposition blocker remains absent. |
| `PRRT_kwDORJEduc6b9aO1` / `3850157296` | `.backlogit/queue/131.011-T.md` | The stale `45-105` PR-body range is replaced by `45-115`; the sole 115-minute task remains below two hours. |
| `PRRT_kwDORJEduc6b9aPK` / `3850157327` | `.backlogit/queue/131.007-T.md` | In-process environment mutation is removed from the RED contract. Endpoint cases run in self-relaunched child test processes configured with safe `Command::env`/`env_remove`. |
| `PRRT_kwDORJEduc6b9k1r` / `3850221998` | `.backlogit/queue/131.005-T.md` | The provider-drop oracle is removed. A passing layer-retention baseline reflects SDK truth; RED fails on unavailable explicit lifecycle/flush control and missing source-owned export timeout. |

Review `5015710467` also suppressed one daemon-key finding. `U1 + U2 -> U3 -> U4` has exactly three edges: `U1 -> U3`, `U2 -> U3`, and `U3 -> U4`. The plan now matches queued dependencies; feature `132-F`, its tasks/review, and shipment `126-S` remain blocked.

## Pinned SDK ownership truth

Read-only source verification used the packages pinned by `Cargo.lock`:

- `opentelemetry_sdk` 0.26.0, checksum `d2c627d9f4c9cdc1f21a29ee4bfbd6028fcb8bcf2a857b43f3abdf72c9c862f3`.
- `tracing-opentelemetry` 0.26.0, checksum `5eabc56d23707ad55ba2a0750fc24767125d5a0f51993ba41ad2c441cc7b8dea`.
- SDK `trace/provider.rs:55-65` says dropping one provider clone does not stop processing; explicit shutdown or dropping every clone is required.
- `provider.rs:216-221` passes `self.clone()` into each tracer; `trace/tracer.rs:29-49` stores that provider clone.
- Pinned bridge `tracing-opentelemetry-0.26.0/src/layer.rs:37-44,575-588` stores the supplied tracer in `OpenTelemetryLayer`.

Therefore attaching/holding the returned layer keeps the tracer and provider clone alive. Dropping only the constructor-local provider variable does not create the intended failure and may still export spans. The missing contract is a separately accessible application lifecycle handle that can invoke and report force flush/shutdown; that handle is not claimed necessary for layer liveness.

## Exact safe RED designs

### 131.005-T provider lifecycle RED

Exact command:

```text
cargo test --no-default-features --features cozo-backend,otlp-export --lib server::observability::tests::otlp_provider_red -- --nocapture
```

The library and tests compile first. Scenario 1 uses a controlled local simple exporter and must already PASS by exporting exactly `otlp-layer-owned-baseline-span` after the constructor-local binding ends. Scenario 2 uses the U4 production-shaped seam and a controlled local batch exporter, emits `otlp-red-contract-span`, requests force flush, and expects a separately accessible application lifecycle handle, successful flush result, and exactly that exported span. Its RED is `LifecycleUnavailable`/no explicit handle; 131.006-T owns GREEN. Scenario 3 uses a never-ready local exporter, drop token, paused Tokio time, and an injected 25 ms limit. Its RED is the absent application `BatchConfigBuilder::with_max_export_timeout` override; 131.006-T owns GREEN. No collector, network, wall sleep, provider-drop oracle, or environment mutation exists.

### 131.007-T endpoint/attachment RED

Exact command:

```text
cargo test --no-default-features --features cozo-backend,otlp-export otlp_daemon_red -- --nocapture
```

The lib/bin test modules compile first. A parent test relaunches the current test executable with a private child marker. Child A receives `ENGRAM_OTLP_ENDPOINT=http://env.invalid` through `Command::env` plus `daemon --otlp-endpoint http://flag.invalid`; child B receives environment only; child C uses `Command::env_remove` for absence. Children only read inherited environment and call `Cli::try_parse_from`. The parent includes child status/stdout/stderr and distinguishes spawn failure. Before 131.008-T, the command fails at runtime on the unexpected flag or missing resolved value; after 131.008-T, flag, environment-only, and absence cases become GREEN. Recording-factory handoff remains owned by 131.008-T, and attachment/lifecycle-handle retention remains owned by 131.009-T. No `set_var`, `remove_var`, unsafe block, serial environment lock, in-process mutation, or cross-test race exists.

## Graph, widths, roster, and states

- OTLP task range: `131.001-T` through `131.013-T`.
- Exact shipment roster: `131-F` plus those thirteen tasks, fourteen items total.
- Exact OTLP graph: one linear chain with twelve task dependency edges.
- Estimates: 45-115 minutes; 131.005-T remains 100 minutes/one file and 131.007-T remains 105 minutes/two files.
- Target backlog index: 1,126 artifacts, zero parse failures.
- Review coverage: 76/76 PR files.
- Shipment state: `125-S` sole queued/unclaimed; no active shipment; `126-S` through `129-S` blocked.
- No task-count, dependency, roster, parent, status, or shipment-membership change was needed; only task contracts and titles required correction.

## Exact PR metadata replacement for Ship

Stage does not edit live PR title/body. Ship may apply this exact title:

```text
chore(stage): queue 13-task OTLP repair; keep four identity plans blocked
```

Copy-ready body:

```markdown
## Summary

Planning/backlog only. Shipment `125-S` remains the sole queued and unclaimed release unit: feature `131-F` plus tasks `131.001-T` through `131.013-T` (fourteen roster items, exactly twelve task dependency edges). All estimates are 45-115 minutes. The target backlog index contains 1,126 artifacts with zero parse failures. Review covered 76/76 changed files.

Shipments `126-S` through `129-S` remain blocked; none was claimed or made executable. No application source, tests, Cargo manifest/lockfile, config, build, merge, or PR #362 change is included.

## Safe OTLP RED contracts

The provider RED compiles first. A controlled local exporter proves the attached tracing layer retains a tracer that owns an OpenTelemetry SDK 0.26 provider clone, so dropping only the constructor-local provider binding does not stop export. The actual RED is the missing separately accessible application lifecycle/flush handle and source-owned per-export timeout; task `131.006-T` owns GREEN.

The endpoint RED also compiles first. Flag/environment/absence cases run only in child test processes configured before startup with `Command::env`/`env_remove`; child code only reads inherited state. There is no `set_var`, `remove_var`, unsafe block, global environment lock, or in-process race. Task `131.008-T` owns endpoint resolution/handoff GREEN and `131.009-T` owns attachment/lifecycle-handle retention GREEN.

`OTLP_EXPORT_TIMEOUT = 5s` bounds each exporter future only. `OTLP_CLEANUP_WAIT_TIMEOUT = 5s` bounds only daemon wait for a detached native cleanup worker. Timeout never claims synchronous SDK cancellation or completion.

## Graph and blocked provenance

The daemon-key fan-in `U1 + U2 -> U3 -> U4` has exactly three prerequisite edges (`U1 -> U3`, `U2 -> U3`, `U3 -> U4`). Its exact-create-and-retain primitive remains unproven, so feature `132-F`, review `132.001-R`, tasks `132.001-T` through `132.004-T`, and shipment `126-S` remain blocked. The other blocked identity shipments remain unchanged.

Archive dispositions corrected earlier remain `blocked_unverified_planning`; no successful executable harvest is claimed.

## Claim guard

Review never authorizes claim. Ship must not claim `125-S` until the exact final PR #363 head is merged and is an ancestor of `origin/main`, final-head review state is clean, no competing shipment is active, and the exact fourteen-item/twelve-edge roster remains intact.
```

## Hardening and standard review rerun

OTLP hardening was required and satisfied. Local constitution, Rust/API, architecture, scope, test-strategy, operations, learnings, and external-boundary security lenses return PASS with no unresolved P0/P1. Cross-model/intercom review dispatch was unavailable. The daemon-key plan remains FAIL/BLOCKED only on its unproven exact-create-and-retain primitive; correcting the edge count does not unblock it.

## Publication state

Substantive planning commit `45ab3946a0deba1aebcc65d5d3e615545145355e` was pushed normally to the PR branch. Reply comments `3850375727`, `3850375748`, `3850375735`, and `3850375759` were posted to the two metadata, unsafe-environment, and provider-lifecycle threads; all four threads are resolved. Suppressed remediation comment `5406808549` records the exact three-edge daemon-key graph and preserved blocked state. A GraphQL re-query returned zero unresolved threads. The PR head was `45ab3946a0deba1aebcc65d5d3e615545145355e` at verification. Stage did not edit the live title/body; Ship owns application of the exact replacement above.
