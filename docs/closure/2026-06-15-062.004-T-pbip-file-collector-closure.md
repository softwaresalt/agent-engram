---
title: "062.004-T PBIP project-definition file collector — Closure"
type: closure
date: 2026-06-15
feature: 062-F
shipment: 050-S
task: 062.004-T
pr: 175
merge_sha: 824b47ad4429f07ed383b6f7bdc8fd8bab87f1ee
branch: post-merge/062.004-T-closure
---

## Summary

Closed task `062.004-T` (Build PBIP project-definition file collector) against
PR #175 and merge commit `824b47ad4429f07ed383b6f7bdc8fd8bab87f1ee`. Adds the
recursive PBIP walker (`collect_pbip_files`) and the deletion-sweep helper
(`compute_deleted_paths`) plus a `physical_file_type` helper that uses
`std::fs::symlink_metadata` so the walker does not follow symbolic links. The
existing `index_pbip_source` dispatch now populates `total_files` on its
returned `PbipIndexResult`. Persistence into content records and graph edges
is still owned by 062.002-T.

This is a single-task closure within a multi-task shipment. Feature `062-F` and
shipment `050-S` remain `queued` so the remaining four tasks can ship through
the same shipment manifest.

## Tasks Completed

| Task | Title | Status |
|---|---|---|
| 062.004-T | Build PBIP project-definition file collector | archived |

## Shipment Reconciliation

* Archived task `062.004-T` with merge metadata from PR #175
* Feature `062-F` stays `queued` (shipment manifest still owns four more tasks)
* Shipment `050-S` stays `queued` (does not archive until all manifest items complete)
* Remaining manifest items continue queued: `062.002-T`, `062.005-T`, `062.006-T`, `062.007-T`

## Quality Gates

| Gate | Result |
|---|---|
| PR merge strategy | Merge commit confirmed (`824b47ad4429f07ed383b6f7bdc8fd8bab87f1ee`) — admin override used because branch policy required reviewer approval and operator approved in chat |
| `cargo fmt --all -- --check` | Clean on branch HEAD |
| `cargo clippy --all-targets ... -- -D warnings -D clippy::pedantic` | Clean on branch HEAD (one `clippy::doc_markdown` finding fixed before the second push) |
| `cargo dev-test ...` | 157 passed, 0 failed |
| `cargo test --test integration_pbip_search_ingestion ...` | 9 passed, 0 failed (includes the symlink-skip regression added by review-fix) |
| `cargo test --test integration_pbip_source_dispatch ...` | 5 passed, 0 failed (no regression of 062.001-T) |
| `cargo test --test unit_pbip_extract_pbism --test unit_pbip_extract_tmdl ...` | 10 passed, 0 failed (no regression of 062.003-T) |
| CI workflow `build` (run 27526975084, initial commit) | SUCCESS in 3m 15s |
| CI workflow `build` (run 27529450840, review-fix commit) | SUCCESS in 2m 58s |

## Review Disposition

| Comment | Path | Disposition |
|---|---|---|
| 3411308755 | `src/services/pbip_indexer.rs:82` | Fixed in `1f453e4` — `collect_recursive` now uses a `physical_file_type` helper backed by `std::fs::symlink_metadata` so symlinked files and directories are skipped before deciding whether to recurse; `index_pbip_source` adopts the same physical check on the source root. Added regression test `collect_pbip_files_skips_symlinked_paths` (S-PFC-09) |

Thread resolved via `resolveReviewThread` GraphQL mutation.

## Invariants to Preserve

* `collect_pbip_files` stays case-insensitive in extension matching for `.pbip`/`.pbir`/`.pbism`/`.json`/`.tmdl` and continues to return sorted results
* `collect_pbip_files` never follows symlinks — the `physical_file_type` + `is_symlink()` skip is workspace-containment, not a performance optimization
* `compute_deleted_paths` continues to reject `absolute`, `..` traversal, Windows drive prefix, and forward-slash root path entries with `warn!` log
* `read_dir` failures inside the recursive walker continue to log `warn!` with the offending path and error so the failure mode is observable
* `index_pbip_source` continues to populate `total_files` on its result (consumed by 062.002-T persistence)

## Pre-Deploy Audit

| Check | Status |
|---|---|
| Feature flags | N/A |
| Data migration | None |
| Cross-service dependency | None |
| Rollback procedure | `git revert --no-edit -m 1 824b47ad4429f07ed383b6f7bdc8fd8bab87f1ee` |
| Monitoring plan | Manual — once 062.002-T persists records, exercise a PBIP source with a symlinked subdirectory and confirm linked entries are not indexed |

## Deployment or Rollout Path

Post-merge backlog closure only. The change is collector + sweep helpers with
no runtime side effects yet; observable behavior arrives when `062.002-T`
wires the collector into the persistence path.

## Post-Deploy Checks

* Confirm `.backlogit/queue/062.004-T.md` no longer exists
* Confirm `.backlogit/archive/062.004-T.md` exists with `commit: 824b47ad…`
* Confirm `.backlogit/queue/050-S.md` still lists `062.004-T` in its manifest
* Confirm `src/services/pbip_indexer.rs` on `main` exposes `collect_pbip_files`, `compute_deleted_paths`, and `physical_file_type`
* Confirm `cargo test --test integration_pbip_search_ingestion` passes 9/9 on `main`

## Risky Action Record

* **ProposedAction**: merge PR #175 with admin override after operator approval
* **ActionRisk**: low
* **ActionResult**: applied (`merge_sha: 824b47ad…`)
* **Why**: GitHub branch policy required reviewer approval; the operator explicitly approved the merge in chat, and the Copilot review was already performed and addressed before merge.

## Healthy Signals

* `git log --oneline -1 main` shows the merge commit `824b47a`
* `cargo dev-test` passes on `main` post-merge
* Collector and sweep helpers are present on `main` and exported through `crate::services::pbip_indexer`

## Failure Signals

* `cargo test --test integration_pbip_search_ingestion` failing on `collect_pbip_files_skips_symlinked_paths` (would mean the symlink containment regressed)
* `cargo test --test integration_pbip_search_ingestion` failing on `compute_deleted_paths_rejects_workspace_escape` (would mean the workspace-escape filter regressed; specifically the Windows forward-slash root case)
* Field traces showing "no PBIP files indexed" without an adjacent `warn!` log when the underlying cause is a `read_dir` failure (would mean the diagnostic regressed)

## Known Carryover

The `crate::services::powerbi_indexer::workspace_relative_path` function and
`collect_recursive` in the same module share the same Windows forward-slash
root and symlink-following gaps that this task closed for the pbip path. They
were not changed by this PR to keep scope focused. A small follow-up chore
could align both modules; tracked informally rather than as a backlog item
since the legacy path is stable today.

## Follow-on Backlog

* `062.002-T` Emit PBIP content records and graph edges (next; now has both the collector and the semantic-model extractor)
* `062.006-T` Extract pbip and pbir linkage entities
* `062.007-T` Extract PBIP page and visual entities
* `062.005-T` Document pbip boundary and verification flow
* When all four remaining tasks ship and merge, archive shipment `050-S` and feature `062-F`
