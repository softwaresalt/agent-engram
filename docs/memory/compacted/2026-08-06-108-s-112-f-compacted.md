---
title: "108-S / 112-F compacted shipment memory"
date: 2026-08-06
shipment_id: "108-S"
feature_id: "112-F"
status: compacted
source_memories:
  - "docs/archive/memory/2026-08-05-108-S-ship-continuity.md"
  - "docs/archive/memory/2026-08-06-108-S-post-merge-closure-memory.md"
---

# 108-S / 112-F Compacted Shipment Memory

Shipment `108-S` characterized the Windows cold CLI request/frame path with a
hard two-run cap. It added a focused ignored harness, debug-only fixed
workspace-local capture, and a terminal response-frame event without changing
wire bytes, production timeout behavior, startup/shutdown ordering, or
persistence.

The RED and post-seam runs both completed successfully from the client's
perspective, retained request ID `62046B37-cold-1` and correlation ID
`62046B37`, and proved exact PID death and named-pipe closure. Attempt two
reached the new frame seam, but pretty tracing prevented a JSON-decodable frame
record. The final JSON subscriber remediation has deterministic non-live
coverage only because the `2/2` live cap was exhausted. The durable runtime
classification remains `BLOCKED`.

PR #323 merged exact approved HEAD
`88668b85fc42baf186f2e1666d59cc04ccea2896` through the normal merge-commit
path. Merge commit `8e46559d1ed9a85cecd14e55e41c95bc6e473d50` is reachable
from `origin/main` with exactly two parents. No force, bypass, auto-merge,
squash, rebase, or branch deletion was used.

The manifest-scoped safe-close archived shipment `108-S` with the merge SHA.
Feature `112-F` and tasks `112.001-T` through `112.003-T` were already archived
as `done`; pre/post reconciliation found no missing members, orphans, unrelated
mutations, or archive deletions.

Preserved work:

- `9D943A6F` — fresh reviewed live validation of the final JSON capture
- `12418607` — unrelated S072 fixture stabilization
- `017-D` — unrelated `lz4_flex` advisory deliberation

The decided plan is
`docs/exec-plans/2026-08-05-cold-cli-request-frame-correlation-decided-plan.md`.
No next Stage cycle was started.
