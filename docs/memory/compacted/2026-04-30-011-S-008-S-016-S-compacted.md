---
compacted_from:
  - docs/memory/2026-04-30/011-S-ship-checkpoint.md
  - docs/memory/2026-04-30/ship-011-S-daemon-reliability-memory.md
  - docs/memory/2026-04-29/008-S-post-merge-closure-session-memory.md
  - docs/memory/2026-04-29/016-S-post-merge-closure-final-memory.md
date: 2026-04-30
shipments: [011-S, 008-S, 016-S]
status: complete — all shipments shipped and archived
---

## Compacted Memory — 011-S (+ 008-S / 016-S post-merge)

### 011-S Daemon Reliability Program

**Outcome**: All 5 manifest items archived. PR #51 merged at `6f8762a`. Post-merge closure complete.

**Deliverables**:

- `tests/integration/concurrent_sessions_test.rs` — 4 concurrent IPC tests (s_cs1–s_cs4)
- `Cargo.toml` — `[[test]]` registration for `integration_concurrent_sessions`
- `docs/architecture.md` — Concurrency Model section; schema version `3.0.0` → `4.0.0` (3 locations); dispatch bypass clarification

**Key decisions**:
1. `tokio::sync::Barrier(2)` for deterministic dispatch in s_cs3/s_cs4 (vs `yield_now`)
2. s_cs4 accepts dual outcome — 7003 OR dual success depending on indexing speed; follow-up stash `02E87E6E` captures improvement
3. 003-F closed as resolved — schema 4.0.0 `.engram/code-graph/{branch}/` separation is intentional design; co-location would place tracked files inside gitignored path
4. `IpcResponse.id` is `Value` (not `Option<Value>`); engram error codes in `IpcError.data["engram_code"]`, not `code` field
5. `cargo lint` alias fails with mutually exclusive features; use `cargo clippy -- -D warnings -D clippy::pedantic` directly
6. `.expect("msg {i}")` doesn't interpolate — must use `.unwrap_or_else(|| panic!("msg {i}"))`

**Copilot reviews**: 2 rounds × (11 + 3) = 14 comments, all addressed and resolved.

**Archive**: All 6 items in `.backlogit/archive/` (001-F, 001.009-T, 001.010-T, 003-F, 003.001-T, 011-S).

**Follow-up stash**: `02E87E6E` — Improve s_cs4 deterministic 7003 coverage.

### 008-S and 016-S Post-Merge

Both shipments fully archived in prior sessions. Closure artifacts in `docs/closure/`. No open follow-ups.
