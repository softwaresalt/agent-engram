---
title: "134-S / PR #379 — compacted session memory"
date: 2026-09-04
shipment_id: "134-S"
feature_id: "142-F"
pr_number: 379
compacted_from:
  - "docs/memory/2026-09-04-ship-134-s-pr-379-ready-for-operator-merge-decision.md"
  - "docs/memory/2026-09-04-ship-134-s-pr-379-final-readiness-9th-finding-resolved.md"
  - "docs/memory/2026-09-04-ship-134-s-pr-379-final-report.md"
archived_originals: "docs/archive/memory/"
note: "docs/memory/2026-09-04-ship-134-s-pr-379-merge-and-closure.md is the current session's own live checkpoint (not yet stale) and is preserved un-compacted, per the compact-context constraint to preserve the most recent checkpoint."
---

# 134-S / PR #379 — compacted session memory

## Outcome

Shipment `134-S` (feature `142-F`): IPC seam extraction (`142.008-T` + 4
subtasks), `AppState` mode constructor migration (`142.009-T`), shim restart
mode propagation (`142.010-T`), error envelope (`142.003-T`), and tool
descriptor schema (`142.005-T` + 3 subtasks) — 12/12 manifest items `done`.

PR #379 merged via merge commit `760b44752a0f00704bd1a6f88fb78f91bd4e997d`
at exact operator/session-specified HEAD
`7562c29152b6f53a7551b330a1de1adaebf97084`, on explicit but narrowly-scoped
operator approval ("Keep working autonomously until the task is truly
finished" — approval for PR #379's merge only, not for manual
shipment-record archival).

## Decisions and rationale

* **Task-only manifest, no covering-feature member**: `134-S`'s manifest
  contains 12 task/subtask items, never `142-F` itself. Per the P-015
  verified-fully-covered-root exception, this means the cascade close path
  (`backlogit shipment ship`) is never applicable here — only manual
  safe-close is valid, mirroring the `133-S` precedent.
* **`DOCTOR_SMOKE` descriptor classification (finding 9, stash `F95653D1`)**:
  Copilot flagged `DOCTOR_SMOKE` as declared `Read`/`read_server_available:
  true` while its implementation calls `Control`-classified operations.
  Investigated and confirmed this is the explicit plan-mandated acceptance
  criteria (not an oversight), already pinned by
  `doctor_smoke_is_a_read_server_readiness_workflow`. The real gap (F20's
  future mode-gated dispatch wiring) is out of `134-S`'s manifest scope
  (F19/`142.005-T` is descriptor-schema only) — deferred, not fixed.
* **`_shutdown` availability flip-then-revert (findings 10–12)**: initially
  flipped to `read_server_available: false` to match plan intent, then
  reverted to `true` after Copilot correctly showed `admit()` doesn't
  consult `AppState` and `process_request` executes `_shutdown`
  unconditionally regardless of mode — the flip would have made the
  registry actively misleading vs. runtime reality. Doc comments now name
  the gap explicitly, pointing to stash `1918AFD2` (F20 scope).
* **Release-build regression discovered post-merge (stash `6C9AA7D3`,
  this session)**: `cargo build --release` fails
  (`-D unused-imports`, `Duration` in `src/daemon/startup_activation.rs`,
  used only inside a `#[cfg(debug_assertions)]` test-only startup-delay
  hook). Confirmed genuine regression (file is new to `134-S`), not
  pre-existing; `cargo check`/`cargo build` (dev) and the full targeted
  test suite (36/36) are unaffected. `release.yml`'s actual release build
  step would fail identically — this currently blocks cutting a real
  release from `main`. Not fixed this session (out of scope for a
  non-destructive, evidence-only closure branch); flagged high priority
  for prompt Stage-planned remediation.

## Gate evidence (final, at merged HEAD `7562c291`/merge `760b4475`)

* Local review readiness: `READY`, P0=0/P1=0.
* P-018 Copilot gate: `SATISFIED`, 0 unresolved threads (15 total across 7
  rounds, independently verified via GraphQL).
* CI: `build` and `start-launcher-windows` both SUCCESS.
* Mergeability: `CLEAN`/`MERGEABLE`. Merge strategy: merge-commit-only
  (P-009, squash/rebase disabled repo-wide).
* Pipeline-topology gate: passed at every checkpoint (pre-claim, post-claim,
  lifecycle ×3, post-merge lifecycle).
* Post-merge runtime verification: `PASS WITH FOLLOW-UP` — dev build/check
  green, 36/36 targeted tests green, release build regression (above) is
  the sole follow-up.

## Stash follow-ups (all P-021 C2, none blocked the PR)

| Stash | Priority | Summary |
|---|---|---|
| `4EE241DC` | — | Pre-existing Windows-only archive-verifier flake, unrelated |
| `E12542FF` | — | Pre-existing `--all-features`/otlp-export build break, unrelated |
| `1918AFD2` | — | No IPC surface enforces `read_server_available` yet; F20 scope |
| `F95653D1` | — | `DOCTOR_SMOKE` descriptor/implementation mismatch; F20 scope |
| `AA5698E3` | — | Stale ship-owned checkpoints / legacy-schema hygiene, operational |
| `6C9AA7D3` | high | `cargo build --release` regression (new, this shipment) — see above |

## Explicitly withheld this session (by design)

* Manual shipment-record archival for `134-S` (12-item commit attribution +
  status transition + archive) — reserved for separate, explicit operator
  approval. Full 15-step mutation set specified in
  `docs/closure/134-S-2026-09-04-post-merge-closure.md`.
* Source-code fix for the release-build regression — out of scope for the
  evidence-only closure branch/PR.
* Merge of the post-merge closure PR — left for operator review.

## Closure artifacts (durable, not compacted)

* `docs/closure/134-S-2026-09-04-runtime-verification.md`
* `docs/closure/134-S-2026-09-04-post-merge-closure.md`

## Next steps

1. Fix stash `6C9AA7D3` (small, in-scope completion of `142.008-T`) via its
   own build-fix cycle.
2. Obtain separate operator approval and execute the 15-step manual
   safe-close for `134-S`.
3. Re-run the `pipeline-topology --phase pre_claim` gate for `135-S` once
   both are resolved.
4. Review/merge the `post-merge/134-s-...` closure PR.
