# 142.027-T (F15) — Exclude supervisor from agent installation

**Shipment**: 137-S | **Status**: done | **Branch**: feat/137-s-candidate-indexing-direct-sync-boundary-and-supervisor-crate-separation

## What was done

- `src/installer/mod.rs`: added `pub const INSTALLED_BINARIES: &[&str] = &[crate::APP_NAME];` — a minimal, honest regression-guard contract. `src/installer/mod.rs` has no actual binary-copying logic (its functions manage `.engram/` data files and generate AI-assistant hook/config text, e.g. `.github/copilot-instructions.md`, `.mcp.json`); binary distribution is handled entirely by `scripts/install.ps1`/`scripts/install.sh`, which were already confirmed agent-only with zero `engram-indexer` references. The constant documents/enforces that if binary-install logic is ever added to this module, it may only ever include the agent binary.
- `tests/contract/supervisor_install_exclusion_test.rs`: asserts `INSTALLED_BINARIES == &[engram::APP_NAME]`, that it excludes `"engram-indexer"`, and that the module source text contains no `"engram-indexer"` reference at all.

## Verification

- `cargo test --test contract_supervisor_install_exclusion`: 2/2 GREEN.
- `cargo clippy --all-targets -- -D warnings -D clippy::pedantic`: clean.
- `cargo fmt --all -- --check`: clean.

## Review

Code-review: READY. Reviewer confirmed this is a correctly-scoped, honest minimal contract (not decorative) given the module has no real binary-install logic to guard beyond the constant itself, and correctly leaves `scripts/install.ps1`/`install.sh` untouched (outside this task's owned-files list).
