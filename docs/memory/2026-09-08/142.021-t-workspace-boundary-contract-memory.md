# 142.021-T (F13) — Assert supervisor workspace boundary contract

**Shipment**: 137-S | **Status**: done | **Branch**: feat/137-s-candidate-indexing-direct-sync-boundary-and-supervisor-crate-separation

## What was done

- `tests/contract/supervisor_workspace_boundary_test.rs` (written during harness generation) already asserts the full F13 contract using `cargo metadata --no-deps` (JSON), not brittle string/TOML parsing:
  - `engram-indexer` is a distinct workspace member.
  - Root `engram` package declares no `engram-indexer` binary.
  - `engram-indexer` declares its own `[[bin]]` target.
  - `engram-indexer` exposes a library target (true once 142.020-T/F12 landed `src/lib.rs`).
- Zero production code changes required — the structural boundary was already correct (F12a set workspace membership; F12 added the lib target). This task's job was purely the contract test, which was already in place and passing.
- Root `Cargo.toml` confirmed untouched by this task (per plan amendment: F13 owns only the contract test file, not workspace membership — that's F12a's job).

## Verification

- `cargo test --test contract_supervisor_workspace_boundary`: 4/4 GREEN.
- `cargo clippy --all-targets -- -D warnings -D clippy::pedantic`: clean.
- `cargo fmt --all -- --check`: clean.

## Review

Code-review: READY, one non-blocking P3 advisory (a redundant `assert_ne!` on package IDs that adds no coverage beyond an existing lookup guarantee — not worth a follow-up, noted here for completeness only).
