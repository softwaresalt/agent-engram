---
title: 142.015-T sealed index target memory
date: 2026-09-08
tag: 142.015-T
agent: build-feature
status: done
---

# 142.015-T sealed index target memory

## Completed work

* Implemented `index_sealed_target` in `src/services/code_graph.rs`
* Added sealed-target scope resolution for `IndexTargetKind::LegacyDirect` and `IndexTargetKind::Candidate`
* Reused the existing indexing pipeline through a private file-selection helper instead of accepting raw paths

## Files modified

* `src/services/code_graph.rs`

## Decisions

* Legacy direct targets index from the sealed file's parent directory but retain only the sealed file in discovery
* Candidate targets use the sealed directory itself as the discovery root
* No ad hoc path validation was added; the implementation trusts `GenerationStore`-minted `IndexTarget` values

## Failed approach

* Initial signature change to `index_workspace_impl` required updating two internal test helpers in the same module

## Verification

* `cargo test --test integration_candidate_indexing_service`
* `cargo check --all-targets`
* `cargo clippy --all-targets -- -D warnings -D clippy::pedantic`
* `cargo fmt --all -- --check` reported unrelated pre-existing diffs outside this task; `rustfmt --edition 2024 --check src\services\code_graph.rs` passed for the touched file

## Review-fix cycle 2

* Restricted the raw-path indexing wrappers to crate-private visibility in non-debug builds while preserving existing dev/test harness access
* Disabled deletion-authoritative sibling eviction for single-file sealed `LegacyDirect` indexing scope
* Added coverage for candidate-build active-generation immutability and for pre-populated legacy-direct sibling preservation
* Verified `cargo check --all-targets`, `cargo test --test integration_candidate_indexing_service`, `cargo test --lib services::code_graph`, `cargo test --test integration_prewarm_progress`, `cargo test --lib full_index`, `cargo test --lib queued_backfill_python_runs_gated_sync_on_drain`, `cargo clippy --all-targets -- -D warnings -D clippy::pedantic`, and `cargo fmt --all -- --check`
* `cargo test --test integration_direct_sync_mode` still fails on the separate 142.016-T direct-sync read-server refusal gap and was left out of scope

## Next steps

* Ship/orchestrator can batch this with the remaining F10-F15 task outputs and run broader suite validation later
