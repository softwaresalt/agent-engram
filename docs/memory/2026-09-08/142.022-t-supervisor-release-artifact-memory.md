# 142.022-T (F14) — Publish supervisor as a distinct release artifact

**Shipment**: 137-S | **Status**: done | **Branch**: feat/137-s-candidate-indexing-direct-sync-boundary-and-supervisor-crate-separation

## What was done

`.github/workflows/release.yml` `build` matrix job now additionally:
- Builds `engram-indexer` via `cargo build -p engram-indexer` / `cross build -p engram-indexer` (mirroring the existing `--bin engram` steps, same gating conditions).
- Stages it into a SEPARATE `staging-indexer-${{ matrix.target }}` directory (never the existing `staging-${{ matrix.target }}` used by the agent archive).
- Archives it as `engram-indexer-${TAG}-${{ matrix.target }}.tar.gz` (Unix) / `engram-indexer-$tag-${{ matrix.target }}.zip` (Windows).
- Uploads it via a separate `actions/upload-artifact` step named `engram-indexer-${{ matrix.target }}`, using the same pinned action SHA already used for the agent artifact.

The existing `engram` agent build/archive/upload steps are untouched (purely additive diff, 46 insertions, 0 deletions). The `release` job needs no changes — `merge-multiple: true` + `files: artifacts/*` automatically picks up the new distinct artifacts.

## Verification

- `cargo test --test contract_supervisor_release_artifact`: 3/3 GREEN.
- YAML validated well-formed via PyYAML.
- `cargo fmt --all -- --check`: clean (no `.rs` files touched).

## Review

Code-review: READY, no findings.
