# 142.020-T (F12) — Supervisor crate foundation and boundary harness

**Shipment**: 137-S | **Status**: done | **Branch**: feat/137-s-candidate-indexing-direct-sync-boundary-and-supervisor-crate-separation

## What was done

- `crates/engram-indexer/src/lib.rs` (new): `#![forbid(unsafe_code)]`, `run_for_target`/`run_for_target_with_options` accept only a sealed `IndexTarget` and delegate directly to `engram::services::code_graph::index_sealed_target` — no reimplemented indexing logic.
- `crates/engram-indexer/src/main.rs`: minimal env-driven entrypoint; seals a target via `GenerationStore::seal_legacy_direct` and calls the library facade. No raw path handling of its own; no panicking env/IO unwraps.
- `crates/engram-indexer/tests/supervisor_boundary_test.rs` (new, crate-local, auto-discovered — NOT registered in root `Cargo.toml`): seals a `LegacyDirect` target and exercises `run_for_target`.
- `crates/engram-indexer/Cargo.toml`: added `tokio = { features = ["macros", "rt"] }` (current-thread flavor only, minimal feature set).

## Verification

- `cargo test -p engram-indexer`: 1/1 integration test GREEN.
- `cargo clippy --all-targets -- -D warnings -D clippy::pedantic`: clean.
- `cargo fmt --all -- --check`: clean.
- `cargo check --all-targets` (whole workspace): clean.
- Root `Cargo.toml` confirmed to have zero `supervisor_boundary_test` registrations (crate-local auto-discovery preserved).

## Review

Single code-review pass: READY (no findings). No review-fix cycle needed for this task.

## Dependency note for downstream tasks

142.021-T (F13, workspace boundary contract test) and 142.027-T (F15, installer exclusion) both depend on this crate existing with a real `lib.rs`/dependency — both already had contract tests written during harness generation that assert against this state and were already passing pre-emptively (structural boundary already true from F12a). No further action needed there from F12's side.
